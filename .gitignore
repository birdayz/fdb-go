# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace files are intentionally committed — the cmd/frl multi-module
# setup relies on go.work, so every contributor and CI sees the same workspace.


# IDE files
.idea/
.vscode/
*.swp
*.swo
*~

# Secrets and credentials
.env
.env.*
*.pem
*.key
*.p12
*.pfx
credentials.json
service-account*.json

# OS files
.DS_Store
fdb-record-layer

# SIFT-1M benchmark data
pkg/recordlayer/testdata/*.fvecs
pkg/recordlayer/testdata/*.ivecs

# Bazel symlinks
bazel-bin
bazel-out
bazel-testlogs
bazel-fdb-record-layer-go
bazel-client-qualit
.bazel-bep.jsonl
test-report.html
.tools/
pkg/fdbgo/wire/types/v5/
binding-stress-out/
binding-stress-report.txt
bench-raw.txt
bench-results.txt
.claude/worktrees/
.claude/scheduled_tasks.lock
test-report.html
