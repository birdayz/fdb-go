#cloud-config

package_update: true
packages:
  - docker.io
  - build-essential
  - curl
  - git
  - jq

write_files:
  - path: /etc/docker/daemon.json
    content: |
      {"default-address-pools": [{"base": "172.17.0.0/12", "size": 24}, {"base": "192.168.0.0/16", "size": 24}]}

  # Watchdog: restart the runner if it hasn't picked up a job in 30 minutes.
  # The runner logs "Running job:" when it starts work and "Listening for Jobs"
  # when idle. If the last log line is older than 30 min and GitHub has queued
  # jobs, the listener is stuck on a dead connection.
  - path: /usr/local/bin/runner-watchdog.sh
    permissions: "0755"
    content: |
      #!/bin/bash
      set -euo pipefail
      UNIT="actions.runner.birdayz-fdb-record-layer-go.gh-runner-fdb.service"
      REPO="birdayz/fdb-record-layer-go"
      # Check if the service is active.
      if ! systemctl is-active --quiet "$UNIT"; then
        echo "runner not active, restarting"
        systemctl restart "$UNIT"
        exit 0
      fi
      # Get timestamp of last log line from the runner.
      LAST=$(journalctl -u "$UNIT" -n 1 --output=short-unix --no-pager 2>/dev/null | awk '{print int($1)}')
      NOW=$(date +%s)
      if [ -z "$LAST" ] || [ "$LAST" -eq 0 ]; then
        echo "no log output found, restarting"
        systemctl restart "$UNIT"
        exit 0
      fi
      AGE=$(( NOW - LAST ))
      # Only restart if silent AND there are queued/in_progress runs.
      # If nothing is queued, silence is normal — the runner is just idle.
      if [ "$AGE" -gt 1800 ]; then
        QUEUED=$(curl -fsSL -H "Accept: application/vnd.github+json" \
          "https://api.github.com/repos/$REPO/actions/runs?status=queued&per_page=1" 2>/dev/null \
          | jq '.total_count // 0')
        if [ "$QUEUED" -gt 0 ]; then
          echo "runner silent for ${AGE}s with $QUEUED queued runs, restarting"
          systemctl restart "$UNIT"
        fi
      fi

  - path: /etc/systemd/system/runner-watchdog.service
    content: |
      [Unit]
      Description=GitHub Actions Runner Watchdog

      [Service]
      Type=oneshot
      ExecStart=/usr/local/bin/runner-watchdog.sh

  - path: /etc/systemd/system/runner-watchdog.timer
    content: |
      [Unit]
      Description=Check GitHub Actions Runner health every 10 minutes

      [Timer]
      OnBootSec=5min
      OnUnitActiveSec=10min

      [Install]
      WantedBy=timers.target

runcmd:
  # Docker
  - systemctl enable --now docker
  - systemctl restart docker

  # FDB client
  - curl -fsSLO "https://github.com/apple/foundationdb/releases/download/${fdb_version}/foundationdb-clients_${fdb_version}-1_amd64.deb"
  - dpkg -i "foundationdb-clients_${fdb_version}-1_amd64.deb"
  - rm -f "foundationdb-clients_${fdb_version}-1_amd64.deb"

  # Bazelisk (pinned)
  - curl -fsSL -o /usr/local/bin/bazelisk https://github.com/bazelbuild/bazelisk/releases/download/v1.28.1/bazelisk-linux-amd64
  - chmod +x /usr/local/bin/bazelisk
  - ln -sf /usr/local/bin/bazelisk /usr/local/bin/bazel

  # Just (task runner, pinned)
  - curl -fsSL https://just.systems/install.sh | bash -s -- --to /usr/local/bin --tag 1.48.1

  # MinIO client (for uploading test reports to Hetzner Object Storage)
  - curl -fsSL https://dl.min.io/client/mc/release/linux-amd64/mc -o /usr/local/bin/mc
  - chmod +x /usr/local/bin/mc

  # Runner user
  - useradd -m -s /bin/bash runner
  - usermod -aG docker runner

  # GitHub Actions runner
  - |
    RUNNER_VERSION=$(curl -fsSL https://api.github.com/repos/actions/runner/releases/latest | jq -r '.tag_name' | tr -d v)
    RUNNER_DIR="/home/runner/actions-runner"
    mkdir -p "$RUNNER_DIR"
    cd "$RUNNER_DIR"
    curl -fsSL -o runner.tar.gz "https://github.com/actions/runner/releases/download/v$${RUNNER_VERSION}/actions-runner-linux-x64-$${RUNNER_VERSION}.tar.gz"
    tar xzf runner.tar.gz
    rm runner.tar.gz
    chown -R runner:runner "$RUNNER_DIR"
    su - runner -c "cd $RUNNER_DIR && ./config.sh --unattended --url https://github.com/${github_repo} --token ${github_runner_token} --name gh-runner-fdb --labels ${runner_labels} --replace"
    cd "$RUNNER_DIR"
    ./svc.sh install runner
    ./svc.sh start

  # Enable runner watchdog timer
  - systemctl daemon-reload
  - systemctl enable --now runner-watchdog.timer
