# dayshift-85

- **Date:** 2026-05-10
- **Actual start:** ~10:00 CEST
- **Actual end:** ~13:30 CEST
- **PR:** #171

## What was done

### Review follow-up (PR #170 items)

1. **SelectMergeRule** — normalizes nested Select/Filter combinations (`Select(ForEach(Filter(scan, preds)), ...)` → `Select(ForEach(scan), ..., preds)`). ExpressionRule (EXPLORE phase) since Go's PLANNING doesn't support multi-round implementation. 7 tests.
2. **findRecordTypes documentation** — clarified intentional limitation to logical expressions during PLANNING (PropDistinctRecords is the primary path).

### SQL conformance

3. **Schema-qualified table names (#99)** — `ResolveQualifiedTableName` validates qualifier matches current schema (case-insensitive), errors with `ErrCodeUndefinedDatabase` / `ErrCodeInternalError`. Wired into SELECT (naive + Cascades), INSERT, UPDATE, DELETE, JOIN paths. 9 unit + 6 FDB integration tests.

### M-2 completion (PartialMatch/Compensation infrastructure)

4. **PredicateCompensationMap** — real identity-keyed map replacing entry-count stub. Methods: Entries, ApplyCompensations, Amend.
5. **PredicateCompensationFunc** — extended with Amend + ApplyCompensationForPredicate. OfPredicateCompensation factory.
6. **ResultCompensationFunction** — extended with Amend, ApplyCompensationForResult, ResultCompensationOfValue factory.
7. **ForMatchCompensation.Apply** — wraps expression with residual predicate filters via TranslationMap.
8. **ForMatchCompensation.Intersect** — combines compensations for index intersections.
9. **QueryPlanConstraint** — full port from placeholder (wraps QueryPredicate, IsTautology/IsConstrained).
10. **ValueEquivalence** — EmptyValueEquivalence + AliasMapValueEquivalence with ConstrainedBoolean. AliasMap.ContainsMapping added.
11. **Full MaxMatchMap.compute** — replaced simplified positional matcher with the full recursive algorithm: incrementalValueMatcher (lazy candidate tracking), cross-product children enumeration, memoization, branch-and-bound pruning via maxDepthBound. EqualsWithoutChildren + WithChildren infrastructure added to values package.

### M-3: PushReferencedFields (column pruning)

12. All 4 rules: Filter, Select, Distinct, Unique. ReferencedFields constraint type + ReferencedFieldsConstraintKey.

### M-4: PlanPartition matcher abstraction

13. FilterPlanPartitions, SelectMinCostPartition, WhereDistinct/WhereStored/WhereOrdered convenience filters.

### DecorrelateValuesRule (scalar subquery foundation)

14. Detects "values box" quantifiers (ForEach over constant-result Select), inlines via TranslationMap, removes the quantifier. Handles sideways correlation rejection, correlated result rejection, multiple values boxes.

## Stats

- **19 commits**, +4,500 / -250 LOC
- **37+ files changed**
- **46 test targets pass**
- **~85 new tests** across compensation, matching, partition, values, schema-qualified names
- **120s fuzz** across 2 targets, 0 panics

## What to work on next

1. **Scalar subquery wiring** — the translator needs to produce SelectExpressions with values boxes when it encounters `(SELECT MAX(v) FROM t)` patterns. DecorrelateValuesRule + all M-1–M-4 infrastructure is ready. ~1 shift.

2. **Correlated subquery infrastructure** — correlation binding at execution time for correlated scalar subqueries. All foundations (TranslationMap, AliasMap, ValueEquivalence, MaxMatchMap) are now in place. ~1-2 shifts.

3. **Phase 5 items** — DATE/TIMESTAMP types (#26), DDL action types (#29), driver adapter gaps (#33).

### Flaky test root-cause fix

15. **PlanPropertiesMap non-determinism** — root cause of flaky `TestFDB_CascadesStreamingAggFromIndex`. `PlanPropertiesMap.All()` returned a Go map with random iteration order. When two plans (StreamingAgg vs HashAgg+InMemorySort) had equal cost, the winner depended on map iteration order. Fix: `PlanPropertiesMap` now tracks insertion order; `toPartitionsFromMap` iterates deterministically. 5/5 full-suite passes after fix.

### Review fixes (PR #171 review)

16. SelectMergeRule: multi-quantifier child alias translation via TranslationMap. New test.
17. Compensation.Intersect: documented stub with TODO.
18. DecorrelateValuesRule: removed dead aliasMap variable.
19. referenced_fields.go: removed broken FieldValuesFromPredicates export.
20. NewPlanPartition: deterministic via addExpression.

## Known issues

- MaxMatchMap.compute omits the Simplification variant-expansion step (MaxMatchMapSimplificationRuleSet) — requires a separate rule engine. The base recursive matching works correctly without it.
