# swingshift-83

- **Date:** 2026-05-09
- **Actual start:** 15:19 CEST
- **Actual end:** (in progress, wind-down at 21:30)
- **PRs:** #158 (merged), #159 (merged), #160 (merged), #161 (merged), #162 (merged)

## What was done

### D-3: Distinct elimination → PLANNING phase — DONE

Moved `DistinctOnUniqueElimRule` (EXPLORE) and `ImplementDistinctRule` (BatchA EXPLORE) to `ImplementDistinctFinalRule` (PLANNING phase), matching Java's `ImplementDistinctRule` (ImplementationCascadesRule) 1:1.

Key implementation details:
- `FireImplementationRuleWithContext` added to thread `PlanContext` through PLANNING-phase rules
- PK column coverage check moved from logical walk to PLANNING-phase rule
- Fixed early-return bug in wrapping path (wraps ALL FinalMembers, not just first)
- Same bug found and fixed in `ImplementProjectionFinalRule`
- Dead code files deleted: `rule_sort_over_ordered_elim.go`, `rule_distinct_on_unique_elim.go`, `rule_implement_distinct.go`

### M-1 foundation: AliasMap + RebaseValue

Ported Java's `AliasMap` (bidirectional CorrelationIdentifier mapping, immutable, builder pattern). Added `ForwardMap()` bridge to `values.RebaseValue()`.

Extended `RebaseValue` to handle 13 Value types: QuantifiedObjectValue, ArithmeticValue, CastValue, PromoteValue, ScalarFunctionValue, RecordConstructorValue, NotValue, AggregateValue, FieldValue, ConstantValue, NullValue, BooleanValue, ParameterValue.

Added `FuzzRebaseValue_NoPanic` fuzz target (exercises all 13 types).

### RebasePredicate (PR #162)

Ported `predicates.RebasePredicate` — alias rebasing for query predicates. Handles 7 predicate types: ComparisonPredicate, AndPredicate, OrPredicate, NotPredicate, ValuePredicate, ExistsPredicate, ConstantPredicate. 9 tests.

**M-1 infrastructure is now COMPLETE:** AliasMap (8 tests) + RebaseValue (13 types, 17 tests + fuzz) + RebasePredicate (7 types, 9 tests). Ready for DecorrelateValuesRule.

### Error code alignment (6 fixes, 12+ code paths)

| Error | Before | After | Java source |
|---|---|---|---|
| Unknown qualifier | 22000/0AF00 | 42703 | SemanticAnalyzer.resolveIdentifier |
| Undefined column in WHERE | 0AF00 (planner catch-all) | 42703 | ColumnNotFoundError |
| IN-list NULL | 22000 | 42809 | WRONG_OBJECT_TYPE |
| BETWEEN type mismatch | 22000 | 42804 | ExceptionUtil.translateErrorCode |
| Comparison type mismatch | 22000 | 42804 | COMPARISON_OF_INCOMPATIBLE_TYPES |
| IN-list type mismatch | 22000 | 42804 | Same as comparison |

Fixed across 8 code paths: eval_predicate.go, eval_map.go, eval_predicate_map.go, scalar_functions.go, cascades_generator.go, plan_visitor.go, logical_predicate.go, in_list_pushdown.go.

### SQL_CONFORMANCE.md corrections

Discovered 3 features incorrectly marked as missing:
- **Scalar subqueries**: work (uncorrelated) — infrastructure was already wired end-to-end
- **LEFT/RIGHT OUTER JOIN**: work via NLJ with JoinLeftOuter
- Summary updated: subqueries 50%→70%, joins 70%→85%

### Conformance tests

- 113 yamsql scenarios (+11 new: unknown_qualifier, java_alignment_probes, nested_aggregate_rejection, cte_error_codes, aggregate_edge_cases, null_operator_alignment, type_mismatch_alignment, dml_error_codes, join_error_codes, cascades_plan_shapes, recursive_cte_aggregate)
- 31 cross-engine probes added to SeedRunCorpus (error codes, result-set, scalar subquery, recursive CTE, LEFT OUTER JOIN)

### Documentation

- CASCADES_DIVERGENCE.md: D-3 MOSTLY DONE, M-1 partially ported, priority list updated
- SQL_CONFORMANCE.md: full rewrite with error code table (18 entries)
- TODO.md: current priorities updated

## Current state

- 46 test targets all pass
- 113/113 yamsql conformance (up from 102)
- ~1650 cross-engine corpus entries
- AliasMap + RebaseValue (13 types) infrastructure ready for DecorrelateValuesRule

## Known issues / tech debt

- D-3 gap: Go checks PK column coverage at logical level; Java uses physical DistinctRecordsProperty. Documented in CASCADES_DIVERGENCE.md.
- `computeDistinctRecords` for `RecordQueryProjectionPlan` propagates from child (record-level), but SQL DISTINCT needs value-level uniqueness. Fixing requires distinguishing pass-through vs reshaping projections.
- Shift-tag comments in corpus.go (existing cleanup fodder, ~20 entries).
- `scalar_functions.go` GREATEST/LEAST error code comment says 42804 but the code uses `ErrCodeDatatypeMismatch` which IS 42804 — consistent but the old "22000" wording in the comment block header (line 154) was updated.

## What to work on next

1. **Correlated subqueries** — the ONE remaining user-visible Java gap. Needs `DecorrelateValuesRule` + correlation infrastructure. AliasMap + RebaseValue foundation now in place. ~2-3 shifts.
2. **#26 DATE/TIMESTAMP types** — Phase 5 first item. Parser grammar supports them; need api.NewDateType, proto mapping, DDL cases, value coercion. ~1 shift.
3. **D-7 multi-aggregate** — needs PartialMatch infrastructure (M-2). ~1 shift.
4. **D-8 CardinalityProperty split** — internal architecture. ~0.5 shift.
5. **D-2 PushOrdering** — 2-3 shifts, major architectural change.
