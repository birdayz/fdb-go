# nightshift-87

- **Date:** 2026-05-11
- **Actual start:** ~00:15 CEST
- **Actual end:** ~04:15 CEST
- **PR:** #173

## What was done

### Phase 5 type system — complete (#26, #27, #28)

**#26 DATE/TIMESTAMP type hierarchy (Go extension)**
- `CodeDate`/`CodeTimestamp` in Code enum, JDBC mappings (91/93)
- `DateType`/`TimestampType` primitive structs with singleton pattern
- Grammar: DATE | TIMESTAMP in `primitiveType` rule, regenerated parser
- DDL: `parseColumnType` handles new terminals
- Proto storage: DATE/TIMESTAMP columns stored as STRING (ISO 8601) — self-describing, lexicographically sortable
- `datatypeToProtoFieldType` maps CodeDate/CodeTimestamp → TYPE_STRING
- `ConvertToProtoValue` accepts time.Time for StringKind fields (formats ISO)
- SQL type name constants and bidirectional conversions

**Comparison infrastructure (both paths)**
- `CompareValues` handles time.Time vs time.Time and time.Time vs string (with ParseTimestamp)
- `valuesComparable` extended for temporal cross-type comparison
- Cascades `cmpAny` handles time.Time and cross-type string comparison
- Fixed ordering: string branch checks time.Time before returning false

**CAST evaluation (both paths)**
- `CastValue` (functions/cast.go): STRING→DATE, STRING→TIMESTAMP, time.Time→DATE, int64→TIMESTAMP — returns ISO strings
- Cascades `CastValue.Evaluate`: TypeCodeDate/TypeCodeTimestamp — returns ISO strings
- `primitiveTypeToValueType` maps DATE/TIMESTAMP for CAST target resolution
- `explainTypeName` renders DATE/TIMESTAMP (not UNKNOWN) in plan explain

**Type inference and metadata**
- `inferSpecificFunctionJDBCType` returns DATE/TIMESTAMP for datetime functions
- `inferScalarFunctionJDBCType` reports BIGINT for date-part functions
- `convertedDataTypeToJDBC` handles DATE/TIMESTAMP
- `jdbcTypeMax` promotion lattice: DATE < TIMESTAMP, temporal incompatible with numeric
- `valueTypeName` checks Value.Type() for computed expressions (CastValue, ScalarFunctionValue)
- `ColumnTypeScanType` returns time.Time for DATE/TIMESTAMP columns
- CURRENT_TIMESTAMP/CURRENT_DATE/CURRENT_TIME/LOCALTIME return proper NullableTimestamp/NullableDate types

**Date-part functions on stored columns**
- `evalScalarFunctionCall` parses string→time.Time fallback for YEAR/MONTH/DAY etc.
- Works on both stored columns (string from proto) and CURRENT_TIMESTAMP (time.Time)

**time.Time parameter binding**
- `substituteParams` accepts time.Time, formats via FormatTimestamp

**#27 ARRAY column DDL (1:1 Java port)**
- Extract `isRepeated` from `colDef.ARRAY()` token
- Wrap element type: `ArrayType.from(element.withNullable(false), nullable)`
- Proto mapping: CodeArray → element's field type with LABEL_REPEATED
- `datatypeToLabel` returns REPEATED for array types
- NOT NULL on non-ARRAY columns: Go follows SQL standard (Java restricts to ARRAY only per TODO #50)
- Tested all element types: STRING, BIGINT, INTEGER, DOUBLE, FLOAT, BOOLEAN, BYTES, UUID, DATE ARRAY

**#28 Date-part cleanup decision: KEEP**

### Infrastructure improvements

- **Plandiff DML harness**: Go runner now detects INSERT/UPDATE/DELETE and uses ExecContext, returns ROWS_AFFECTED column. Unblocks DML conformance entries.
- **#89 resolved**: Type mismatch correctly handled by Cascades path (cmpAny → isNumericStringMismatch → TypeMismatchError → SQLSTATE 42804). Dead non-Cascades path no longer relevant.

## Stats

- **43 commits**, +4655 / -2774 LOC (net +1881)
- **37 files changed**
- **46 test targets pass** (all uncached)
- **43 new yamsql scenarios** (datetime_column_types.yaml: 43, array_column_type.yaml: 5)
- **12 FDB integration tests** (columns, comparison, insert, cast, edge cases, index scan, params, array)
- **2 fuzz targets** (ParseTimestamp, FormatParseRoundTrip) — 60s extended run clean
- **2 benchmarks** (TimestampInsert, TimestampRangeScan)
- **1691 total yamsql scenarios** (was 1647)

## What to work on next

1. **#29 DDL action types** — standalone CREATE TABLE/INDEX (Go extension, ~2 shifts). Grammar changes + DDL routing + constant action implementations. Java doesn't have standalone DDL (everything in schema templates), so this is a UX improvement that would let users skip the template indirection.

2. **#33 driver adapter gaps** — custom scanner-valuer for complex types (Struct/Array/Versionstamp/Continuation). All standard interfaces done. Low priority ~1 shift.

3. **#34 perf benchmarks** — temporal benchmarks added this shift. Full Go-vs-Java comparison needs Java conformance server. Next step: add benchmarks for JOIN, aggregate, index scan patterns.

4. **Plan cache key normalization** — currently keyed by SQL text hash. Different literal values produce different cache entries. Java normalizes via AST. This is a deliberate design trade-off (RFC-024) but could be revisited for performance.

5. **DATE/TIMESTAMP column metadata on read-back** — known limitation: after schema template serialization, proto descriptor doesn't carry SQL-level type info. INFORMATION_SCHEMA.COLUMNS reports STRING instead of DATE/TIMESTAMP. Fix requires extending schema template storage with column-type annotations.

## Known issues

- DATE/TIMESTAMP columns report "STRING" in metadata after schema template round-trip (proto_types.go documented)
- `time.Time` parameters always format as full timestamp ("2024-07-04 00:00:00"), not date-only ("2024-07-04") — users wanting date-only should pass string params
- FormatTimestamp truncates to seconds precision (sub-second nanoseconds dropped)
