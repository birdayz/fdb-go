# Dayshift-14 Handover

**Date:** 2026-04-14, started ~07:10 CEST, ended ~14:00 CEST
**PRs:** #52 (merged), #53 (closed), #54 (merged), + direct master commits

## What was done

### 1. CI test cache invalidation fix (main task)

**Root cause:** `bench-ci` used `bazelisk test` with `--nocache_test_results` and different `--test_timeout`/`--test_arg` flags. Bazel stores one action cache entry per test target — the bench step's execution overwrote the test step's cached result. Every CI run re-executed `recordlayer_test` (~45s) and `types_test` (~0.1s).

**Fix:** All bench recipes (`bench`, `bench-one`, `bench-ci`, `bench-compare`) now use `bazelisk run` (build binary + execute directly). This bypasses the test cache entirely.

**Verification:** 3 consecutive CI runs confirmed all 16 test targets are `(cached)`. Test step: 50s → 4.7s.

### 2. CI cleanup

- Removed redundant `--test_timeout=900` override from `ci.yml` and `nightly-coverage.yml`. `.bazelrc` already sets proper per-category timeouts.
- Removed `2>/dev/null` from `bazelisk info bazel-bin` calls (surface errors instead of silently failing).
- Moved `bazelisk build` outside `tee` block in `bench-ci` (cleaner bench-raw.txt).
- Added `timeout` wrapper to `bench-compare` recipe.

### 3. Bench-report false positive reduction

- Raised regression threshold from 5% to 10% (added `-threshold` flag).
- Only flags timing regressions when allocs/bytes also changed. Timing-only deltas on shared CI = VM noise, not code regression.

### 4. store_version.go test coverage

18 new specs for `packVersion`/`unpackVersion` roundtrip, `LoadRecordVersion`, and `saveRecordVersion`:
- 3 roundtrip variants (local=0, non-zero, max)
- Tuple format verification (Versionstamp type)
- 3 error handling tests (nil, invalid bytes, wrong type)
- LoadRecordVersion: non-existent record, after commit, snapshot, local cache, distinct local versions
- saveRecordVersion: complete version direct write + sizeInfo tracking, nil sizeInfo safety

### 5. FDBMetaDataStore wire compatibility fix (PR #54)

**Bug:** Go stored metadata at `subspace.Pack(null)`, Java stores at `subspace.Pack(null, 0)` (unsplit suffix via SplitHelper). Different keys — cross-language metadata reading was broken.

**Fix:** Rewritten to use `saveWithSplit`/`loadWithSplit`. Also fixed stale split chunks bug (pass `existingSize` to `saveWithSplit`). Added wire-format verification test.

### 6. Bench variance investigation (PR #53)

3 consecutive CI runs measured 10-20% CV for timing on the Hetzner runner. Alloc counts stable (±1). Conclusion: current allocs-only heuristic is the right approach. Closed without changes.

### 7. Wire format audit

Audited all `tx.Set`/`tx.Get` calls in `pkg/recordlayer/` against Java source. Confirmed record counts, index states, store headers, and version keys all correctly use plain Set/Get (matching Java). MetaDataStore was the only place missing SplitHelper.

### 8. Verification

- **Binding stress:** 50 seeds × 1000 ops running (20+20 from earlier = 0 failures)
- **Directory stress:** 20/20 pass
- **Fuzz:** 400M+ total executions across 6 targets, 0 crashes
- **CI:** All test targets cached on consecutive runs

## Current state

- **15 Bazel test targets pass** (all cached)
- **2696 Ginkgo specs + 433 conformance specs + 53 chaos tests**
- **CI:** Green, test step 4.7s (fully cached)
- **Coverage:** ~80%
- **Open PRs:** None

## What to work on next

### MetaDataStore conformance test (tracked in TODO.md)
- "Go writes, Java reads" verified working
- "Java writes, Go reads" blocked by tenant prefix issue between CGo and pure Go FDB clients — needs investigation

### Coverage gaps
- `metrics.go` 57% — parse functions (need wire-format test vectors)
- `coordinator.go` 62% — parsing helpers

### LOW (from TODO.md)
- Pool frame read buffers
- LRU eviction for location cache
