# Nightshift-12 Handover

**Date:** 2026-04-13, started ~21:42, ended ~23:35 CEST
**PR:** #48

## What was done

### 1. RYW SnapshotCache (C++ divergence #5 — FIXED)

Sorted interval map (`ryw_snapshot_cache.go`) caches server reads within a transaction. Repeated `getRange`/`get` calls hit cache instead of re-fetching from FDB. Not invalidated by local writes/clears — those merge at read time, matching C++ SnapshotCache + WriteMap architecture.

- 22 unit tests + 3 integration tests against real FDB
- 2 benchmarks: GetKey 55ns/0 allocs, GetRange(1000) 44us/11 allocs
- 14M fuzz executions clean (FuzzRYWCache)
- 10/10 binding stress seeds pass
- Only 3 C++ behavioral divergences remain (all design choices)

### 2. Benchmark CI pipeline (RFC 018 — Implemented)

- `cmd/bench-report`: Go tool that compares two benchmark files, outputs markdown table, posts/updates PR comment via `gh api` with marker comment (`<!-- bench-report -->`) for edit-in-place
- `just bench-ci`: runs 172 benchmarks across 4 test targets (~90s locally, ~15min on CI)
- CI workflow: master pushes upload baseline to S3, PRs get benchstat-style comparison comment
- `just bench-report old.txt new.txt`: local comparison convenience recipe

### 3. ConcurrentWritesDuringDD multi-shard test

Last remaining multi-shard test gap. 8 goroutines × 25 ops write 8KB values across 51 existing shards. Point read + range scan cross-check verifies no data loss.

### 4. Shift infrastructure improvements

- Renamed all 13 shift files to date-prefix format (`YYYY-MM-DD-{name}.md`)
- Converted vollkonti from `.claude/commands/` to `.claude/skills/vollkonti/SKILL.md`
- Runner watchdog: systemd timer checks every 10min, restarts runner if silent >30min AND GitHub has queued jobs
- Fixed stuck runner (zombie listener after 3 days uptime)

## Current state

- **All 14 Bazel test targets pass** (CI green)
- **172 benchmarks** captured via `just bench-ci`
- **Binding stress:** 10/10 seeds pass
- **Fuzz:** 14M executions clean
- **C++ divergences:** 3 remaining (auto-reset, QueueModel key, FLAG_FIRST_IN_BATCH) — all design choices

## What to work on next

### MEDIUM
- **Pool frame read buffers** — `ReadFrame` allocates per response. Blocked by zero-copy design.

### LOW
- **`net.Buffers` (writev)** — scatter-gather I/O for frame writes
- **LRU eviction for location cache** — currently random
- **Pre-allocate prefixed keys** — commit path optimization
