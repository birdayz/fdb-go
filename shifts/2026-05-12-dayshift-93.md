# dayshift-93

- **Date:** 2026-05-12
- **Actual start:** ~11:42 CEST
- **Actual end:** ~13:35 CEST (writing handover)
- **PR:** #179

## What was done

### DecorrelateValuesRule: 3 real bugs fixed

Ported 3 missing Java negative tests that exposed real Go bugs:

1. **DoNotUseValuesBoxWithPredicates** — values box with predicates (even tautologies like TRUE) was incorrectly accepted as a values box. Fixed: reject when `len(childSel.GetPredicates()) > 0`.

2. **DoNotTreatRangeTwoAsValues** — values box over `range(2)` (cardinality 2, not 1) was incorrectly accepted. Fixed: added `isRangeOneQuantifier()` that evaluates RangeValue constant arguments and verifies cardinality == 1.

3. **DoNotTreatRangeWithConstantObjectValueAsValueBox** — values box over `range(ConstantObjectValue)` was incorrectly accepted because the COV doesn't correlate to the child quantifier. Fixed: `isRangeOneQuantifier` requires all-constant range arguments.

Test infrastructure updated: `makeValuesBox`/`makeRangeOneQ` now use `TableFunctionExpression(range(0,1,1))` instead of `FullUnorderedScanExpression`, matching Java's actual `range(1)` source.

**25/29 Java DecorrelateValuesRule tests ported** (was 22/29).

### NormalizePredicatesRule existential guard investigation

Investigated removing the existential quantifier guard. Root cause: removing it causes **planner non-convergence** (MaxTasks cap hit at 10k). The NLJ rule successfully fires on normalized expressions and yields physical plans, but cascading rule interactions (NormalizePredicatesRule → PredicatePushDownRule → re-normalization) create an infinite exploration loop. The NLJ plans ARE yielded correctly but the planner never converges to extract them.

Fixed: requires rule deduplication infrastructure (tracking which normalizations have been applied). Updated DIVERGENCES.md with precise root cause. Separate improvement: NormalizePredicatesRule now uses `NewSelectExpressionWithAliases` to preserve source aliases through CNF normalization.

### TODO.md cleanup

Trimmed from 335 → 42 lines. Removed verbose completed-item history, kept only active work + remaining unchecked items.

### New yamsql scenarios (29 scenarios across 5 files)

- `computed_column_names.yaml` (7 scenarios) — positional `_N` names for unnamed expressions
- `type_promotion.yaml` (9 scenarios) — implicit type promotion across INTEGER/BIGINT/DOUBLE
- `update_computed_multi.yaml` (5 scenarios) — multi-column UPDATE with self-referencing SET
- `join_exists_self.yaml` (4 scenarios) — self-join with correlated EXISTS/NOT EXISTS
- `cte_aggregate.yaml` (4 scenarios) — CTE materialization + GROUP BY/COUNT/HAVING/AVG

### SQL stress test harness

Built `stress_test.go` with batched INSERT, query timing, scalability probes.
Skipped in `just test` (-test.short); run with `just stress`.

**10K rows (1K customers) — baseline:**
| Operation | Time | Rows |
|---|---|---|
| PK lookup | 250ms | 1 |
| Index scan (eq) | 257ms | 5 |
| Index range >9000 | 254ms | 1014 |
| COUNT(*) full scan | 240ms | 1 |
| GROUP BY status | 866ms | 4 |
| ORDER BY PK | 276ms | 10K |
| IN-list (5 values) | 261ms | 41 |
| **JOIN 10×1K** | **37.6s** | 10 |
| **EXISTS subquery** | **11.8s** | 928 |

**100K rows (10K customers) — critical bugs:**
| Operation | Time | Rows | Expected |
|---|---|---|---|
| PK lookup | 6s | 1 | O(1), got O(N) |
| GROUP BY status | 4.1s | **2** | **4 (truncated!)** |
| ORDER BY PK | 3.7s | **47K** | **100K (truncated!)** |
| JOIN 10×10K | 4s | **0** | **10 (failed!)** |

**Critical bugs found:**
1. **Silent FDB timeout truncation** — GROUP BY returns 2/4 rows, ORDER BY returns 47K/100K rows. No error raised. Partial results returned silently.
2. **PK lookups scale O(N)** — 250ms at 10K, 6s at 100K. Scan infrastructure doesn't use index seeks.
3. **NLJ is O(N×M)** — no correlated index probes. Full inner table scan per outer row.

### Verification

- 46/46 test targets pass
- Binding stress: 5/5 seeds, 1000 ops, 0 failures
- Fuzz: translator (30s) + planner determinism (30s) clean
- 1754 total yamsql scenarios (was 1725)

## Stats

- **7 commits**, ~800 LOC changed
- **46 test targets pass**
- **3 real bugs fixed** in DecorrelateValuesRule
- **3 critical scalability bugs surfaced** via stress test

## What to work on next

1. **CRITICAL: Silent FDB timeout truncation** — queries return partial results without errors when the 5s transaction limit is hit. This is a data correctness issue. The executor needs to either paginate across transactions or raise an error when the scan is incomplete.

2. **CRITICAL: PK lookups scale O(N)** — the scan executor does a prefix range scan instead of a point lookup. Needs investigation in `scanComparisonsToTupleRange` and the executor's scan path.

3. **HIGH: NLJ performance** — O(N×M) full inner scan per outer row. Needs correlated index probes (Java's FlatMapPlan with correlation bindings).

4. **DecorrelateValuesRule test gap** — 25/29, 4 remaining need push-down-into-child infrastructure.

5. **NormalizePredicatesRule convergence** — needs rule deduplication infrastructure. Documented in DIVERGENCES.md.
