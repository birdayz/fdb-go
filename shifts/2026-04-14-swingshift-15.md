# Swingshift-15 Handover

**Date:** 2026-04-14, started ~14:02 CEST, ended ~22:00 CEST
**PR:** #56

## What was done

### 1. Bug fixes (5 found)

| # | Bug | Severity | How found |
|---|---|---|---|
| 1 | **Data race: Watch vs postCommitReset** — Watch() goroutine races with Commit()→postCommitReset() on readConflicts/writeConflicts slice headers | HIGH | Race detector |
| 2 | **getRange `more` flag across shard boundaries** — Returns `false` when limit met exactly across shards; should be `true` | MEDIUM | Code review vs C++ |
| 3 | **Wire reader panic on malformed responses** — 9 Reader methods (ReadBytes, ReadVectorInt32, etc.) missing upper bounds check on vtable offset | HIGH | Fuzz (FuzzParseGetKeyValuesReply) |
| 4 | **OOM from crafted wire count fields** — ParseKeyValueRefStringVector + ReadVectorCount used raw uint32 count in make(), enabling 206GB allocation | HIGH | Code review after #3 |
| 5 | **Binding stress container leak** — Docker containers never cleaned up on process exit/crash, 63 orphans found | LOW | User observation |

### 2. Code removal

- Query execution engine removed (query_plan.go, query_builder.go, tests, design doc). Premature — hardening first.

### 3. Tests added (~150 new test cases)

| Category | Tests | File(s) |
|---|---|---|
| RYW adversarial | 44 | ryw_adversarial_test.go |
| RYW fuzz expansion | 12 atomic types | ryw_fuzz_test.go |
| Retry/OnError | 30 | retry_adversarial_test.go |
| Concurrent stress (client) | 5 | concurrent_stress_test.go |
| Boundary/off-by-one | 7 | boundary_test.go |
| Isolation | 3 | isolation_test.go |
| Limits | 6 | limits_test.go |
| Wire parser fuzz | 8 targets | parse_fuzz_test.go |
| Wire Reader fuzz | 2 targets | reader_fuzz_test.go |
| Directory partitions | 4 | directory_test.go |
| Range iterator | 5 | range_iterator_test.go |
| MustGet/panic recovery | 3 | fdb_test.go |
| Store stress (Ginkgo) | 3 | store_stress_test.go |
| Index edge cases (Ginkgo) | 5 | index_edge_cases_test.go |

### 4. C++ completeness audits

5-subsystem audit (readpath, commitpath, transaction, RYW, GRV+database) — all pass. 2 off-by-one audits (client + record layer) — no bugs.

## Current state

- **15 Bazel test targets pass** (all cached)
- **21 fuzz targets**, 600M+ total executions, 0 failures
- **Race detector**: all targets clean
- **Binding stress**: 329 API + 100 directory seeds = 0 failures (after Docker cleanup)
- **CI**: green
- **Coverage**: 4549 tests, 80.3%

## What to work on next

### HIGH — continue hardening
- [ ] More index edge case tests (RANK with duplicate scores, PERMUTED_MIN/MAX boundary values, TEXT with empty documents, BITMAP with all-zero/all-one bitmaps)
- [ ] Fuzz the record layer's protobuf deserialization (deserializeAndDiscover, deserializeRecord)
- [ ] Multi-shard test specifically for the `more` flag fix (write test that verifies `more=true` when limit met at shard boundary)
- [ ] Investigate FuzzNewReader with nested struct reading — could there be panic paths in ReadNestedReader → NewReader recursion?

### MEDIUM — infrastructure
- [ ] Fix binding stress to use testcontainers instead of raw Docker commands (proper lifecycle, cleanup on crash, Ryuk reaper)
- [ ] Add `govulncheck` to CI (currently 2 Docker-only vulns, non-blocking)

### LOW
- [ ] Coverage gaps: metrics.go (57%), coordinator.go (62%), time_window_leaderboard_maintainer.go (65%)
- [ ] Pool frame read buffers (performance)
- [ ] LRU eviction for location cache (performance)
