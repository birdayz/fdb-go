# Swingshift-11 Handover

**Date:** 2026-04-13, started ~14:40, ended ~TBD CEST
**PR:** #44

## Objective

Test coverage improvement (77.7% → 79.1%) + multi-shard watch test (HIGH priority from dayshift-10 handover).

## What was done

### 1. Multi-shard watch tests (HIGH priority from handover)

4 new tests in `pkg/fdbgo/client/multishard_test.go` exercising Watch across 51 shards:
- **WatchBasic**: set/modify/fire cycle across shards
- **WatchMultipleShards**: 5 concurrent watches on different shards, all fire
- **WatchDuringHeavyWrites**: watch fires under 200-write shard load
- **WatchClearRange**: cross-shard ClearRange triggers watch

Validates the `wrong_shard_server` retry logic in `Watch()` works in real multi-shard clusters.

### 2. Test coverage: 77.7% → 79.1% (+423 lines, +238 Ginkgo specs)

| Area | Tests | What |
|---|---|---|
| MetaDataEvolutionValidator | 56 | Builder methods, record type removal/rename, SinceVersion, FormerIndex versions, message descriptor field validation (synthetic protos), cardinalityString |
| Aggregate functions | 81 | Constructors, canEvaluateAggregate dispatch, isGroupPrefix matching, RANK aggregates, tuple comparison, splitEqualRangeForRank |
| Index scan | ~45 | TupleRange constructors/Prepend/Contains, IndexEntry PK extraction, continuation wrap/unwrap round-trip, indexCursor state |
| Bunched map | ~55 | joinBytes/tupleEqual/positionListsEqual helpers, instrumentation, TextSubspaceSplitter, iterator state machines |
| Store API | 16 | AsBuilder, CopyBuilder, IsVersionChanged, SetFormatVersion, IndexStateSubspace, GetAllIndexStates, universal indexes, error type formatting |
| Metadata proto | 6 | Round-trip all index types, SinceVersion, explicit type keys, multi-type indexes |
| Runner | 3 | Retry backoff timing, max attempts, context cancellation during retry |
| Tuple fast | 25 | tupleSkip all type codes, splitKeySuffix edge cases |
| Tuple ordering | 2 | BigInt (0x0B, 0x1D) tupleElementEndPos |
| IndexState | 8 | IsScannable/IsWriteOnly/IsDisabled predicates, String, indexStateFromCode |
| RecordType | 6 | GetIndexes, GetMultiTypeIndexes, GetAllIndexes, HasExplicitRecordTypeKey |

### 3. Validation

- **Binding stress (API)**: 82 seeds × 1000 ops = 0 failures
- **Binding stress (directory)**: 19/20 pass (1 failure = stale binary from concurrent build, not a bug)
- **Fuzz testing**: 95M+ executions across 10 targets = 0 crashes
- **Race detector**: Full client test suite passes with `-race`

## Current state

- **Branch:** `swingshift-11`, PR #44
- **All 13 Bazel test targets pass**
- **Ginkgo specs:** 2555 (was 2317)
- **Conformance specs:** 430
- **Line coverage:** 79.1% overall, 82.8% client, 81.3% record layer

## Known issues

- **Claude Code review workflow broken**: `oven-sh/setup-bun` fails with "Unable to locate executable file: /home/runner/.bun/bin/bun" on the self-hosted runner. The bun binary isn't being installed/cached properly. This is an infrastructure issue, not code-related. Tried 3 times, same failure. May need runner cleanup or cache invalidation.
- **CI queue congestion**: Self-hosted runner gets overloaded when binding stress (Docker containers) runs concurrently with CI. Jobs queue for 10+ minutes.

## What to work on next

### HIGH
- **Fix Claude Code review workflow**: Debug the bun installation on the self-hosted runner. May need to update `oven-sh/setup-bun` action version or switch to a different bun install method.

### MEDIUM
- **RYW SnapshotCache** — C++ caches server reads for reuse within a transaction. Architectural change (segment tree vs map).
- **Speculative second request (secondDelay)** — C++ hedge request for p99. QueueModel already tracks latency.
- **Pool frame read buffers** — Blocked by zero-copy design.

### LOW
- **Coverage: push toward 80%** — Need ~280 more covered lines. Remaining gaps are mostly integration code (online indexer, time window leaderboard, index maintainers) that need FDB.
- **`net.Buffers` (writev)** — Scatter-gather I/O. Low impact.
- **LRU eviction for location cache** — Currently random. Works well enough.
