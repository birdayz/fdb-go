# Swingshift-11 Handover

**Date:** 2026-04-13, started ~14:40, ended ~22:00 CEST
**PRs:** #44, #45, #46, #47 + direct master commits (78 total)

## Objective

Test coverage improvement (77.7% → 80.0%), multi-shard watch tests, client test gaps from C++ audit, CI infrastructure fixes.

## What was done

### 1. Multi-shard watch tests (HIGH priority from dayshift-10)

4 new tests in `multishard_test.go` exercising Watch across 51 shards.

### 2. Coverage push: 77.7% → 80.0% (+726 lines)

| Area | Tests | What |
|---|---|---|
| MetaDataEvolutionValidator | 56 | Builder methods, type removal/rename, FormerIndex versions, synthetic proto validation |
| Aggregate functions | 81 | Constructors, dispatch, isGroupPrefix, RANK, tuple comparison |
| Index scan | ~45 | TupleRange, IndexEntry PK extraction, continuations |
| Bunched map | ~55 | Helpers, instrumentation, iterator state machines |
| Store API | 16 | AsBuilder, CopyBuilder, IsVersionChanged, universal indexes |
| Metadata proto | 6 | Round-trip all index types |
| Metadata coverage | ~40 | Builder getters, version bumps, CommonPrimaryKey, replacement chains |
| Helpers | ~15 | toInt64, keyExpressionHasNullField, calculateDelay |
| Various small | ~30 | HNSWStats, subspace, wirelog, key, vec_math, hnsw_vector, cursor, topology, rpc |

### 3. Client test gaps from C++ audit (HIGH priority)

12 new behavioral integration tests against real FDB:

**Snapshot isolation (5 tests):**
- GetAfterClear, GetRangeAfterClearRange, GetRangeDoesNotConflict
- GetAfterAtomicAdd, ConflictAsymmetry

**Transaction retry (4 tests):**
- OnError resets RYW cache, new read version after OnError
- Conflict detection across full retry cycle, Transact automatic retry

**Watch edge cases (3 tests):**
- Timeout via context deadline, atomic mutation triggers watch, cancellation

### 4. Fuzz target: FuzzRYWCache

Model-based fuzz target for the RYW merge logic. Generates random Set/Clear/ClearRange sequences, verifies Get/GetRange against a map model. 5M+ executions clean. Found and fixed a model bug during development (ClearRange boundary off-by-one in the test, not in production).

### 5. COUNT_NOT_NULL chaos tests

3 new chaos tests — the last index type without chaos coverage. All 19 index types now have chaos tests.

### 6. CI infrastructure

- **Concurrency groups**: CI workflow auto-cancels stale runs on same branch
- **Claude Code bun fix**: `~/.bun/bin/bun` cache went cold → fixed by downloading bun zip and extracting with python3 zipfile (no unzip/sudo needed)
- **Removed Claude Code concurrency group** (reviews need to run to completion)

## Current state

- **All 14 Bazel test targets pass**
- **Coverage:** 80.0% overall (was 77.7%)
- **Ginkgo specs:** ~2670 (was 2317)
- **Chaos tests:** 53 (was 50)
- **Fuzz targets:** 11 (was 10)
- **Binding stress:** 172 API seeds + 19 directory seeds = 0 failures
- **Race detector:** Clean (all new tests)
- **RYW fuzz:** 10M+ executions (Set/Clear/ClearRange/AtomicAdd, forward + reverse range) = 0 failures

### 7. Speculative second request (COMPLETE)

Full implementation of C++ secondDelay hedge for p99 latency improvement:
- `secondDelay()`: max(10ms, 2×latency) matching C++ BACKUP_REQUEST_DELAY
- `chooseTopTwo()`: picks best + second-best server by QueueModel
- `sendFrameWithHedge()`: race two servers with hedge delay timer
- Wired into ALL three read paths: sendGetValue, sendGetKey, sendGetRange
- 6 hedge unit tests (primary fast, secondary wins, send failure, single server, cancellation, conn error)
- 2 loadbalance tests (secondDelay + chooseTopTwo)
- Divergence #9 marked as FIXED

## What to work on next

### MEDIUM (from TODO.md)
- **RYW SnapshotCache** — C++ caches server reads for reuse within a transaction (divergence #5). Would need segment-tree or interval-map.
- **Pool frame read buffers** — Blocked by zero-copy design.

### LOW
- **Multi-shard concurrent writes during DD** — last remaining multi-shard test
