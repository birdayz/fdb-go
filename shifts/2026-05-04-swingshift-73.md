# swingshift-73

- **Date:** 2026-05-04
- **Actual start:** ~17:07 CEST
- **End:** 22:00 CEST (wind-down from 21:30)
- **PRs:** #147

## What was done

### #65 — Non-recursive CTE translation for Cascades planner

Complete CTE-to-Cascades pipeline: non-recursive CTEs inline their body into the Cascades expression tree, with full predicate resolution and safety checks.

**Translator refactoring:**
- Refactored `cascades_translator.go` from free functions to a `cascadesTranslator` struct with CTE scope (`map[string]LogicalOperator`)
- Non-recursive CTEs inline: body operator stored in scope, scan references expand the body
- Shadowing-safe: temporarily removes CTE from scope during body expansion to prevent infinite recursion when CTE name equals table name
- Recursive CTEs bail to naive (not yet ported)

**CTE predicate resolution (body-derived column schemas):**
- Pre-scan CTE definitions in `buildLogicalPlanForQueryWithCatalog` to extract column schemas before building the main query
- `buildCTEColumnSource`: derives `ScopeSource` from CTE body's SELECT projection + underlying table metadata
- Chained CTE support: process in declaration order so CTE B can resolve columns from CTE A
- CTE+JOIN scope resolution: `buildWherePredicateForJoinsWithCTEScopes` handles mixed real-table + CTE sources
- CTE scopes threaded through UNION branches in the main query

**JOIN ON predicate upgrade:**
- Walk logical plan tree after building to find `LogicalJoin` nodes
- Upgrade text-only ON predicates to real `QueryPredicates` using full join scope
- Fixed multi-join index reversal: collect joins first, match in reverse order (builder chains outermost-last)

**Safety checks preventing silent filter drops:**
- Text-only filter in CTE scope → bail to naive (prevents Cascades from dropping WHERE on CTE reference)
- HAVING clause → bail to naive (HAVING text not translated to predicate)
- LEFT/RIGHT outer joins → bail to naive (SelectExpression has INNER semantics only)

**Column type derivation improvements:**
- Aggregate plans: COUNT→BIGINT, AVG→DOUBLE, SUM/MIN/MAX→operand type
- JOIN plans: combined outer + inner columns
- UNION plans: first branch columns via `findUnionPlan` walker
- Projection-level aggregate pattern detection (`aggregateTypeName`)

**Testing:**
- 37 Cascades FDB tests (up from 18): simple CTE, body filter, outer WHERE, chained, SELECT*, aggregate on body, CTE+JOIN, CTE+UNION body, chained SELECT*, CTE DISTINCT, CTE GROUP BY, CTE projection alias, complex CTE stack, UNION ALL, explicit JOIN ON, 3-way join, multi-filter, computed projection, CTE in UNION branches
- 22 translator unit tests (up from 16): CTE inline, CTE with filter, chained CTE, CTE shadowing table name, outer text filter bail, recursive CTE nil, multi-ref CTE, HAVING bail
- 6 predicate resolution unit tests: CTE outer WHERE, chained CTE, SELECT *
- 2 translation benchmarks: CTE inline ~1μs, scan ~200ns

## Current state

- **47 test targets, all pass** (same as shift start — no regressions)
- 37 Cascades FDB integration tests (doubled from 18 at shift start)
- CI green on all pushes
- Fuzz targets clean (TopologicalSort, EligibleSet, ConvertAndPlan, MemoConsistency)

## Known issues

- Recursive CTEs fall back to naive (translator returns nil)
- HAVING predicates fall back to naive (text-only, not resolved)
- LEFT/RIGHT outer joins fall back to naive (INNER semantics only in SelectExpression)
- CTE column aliases (`WITH t(a, b) AS ...`) not handled in schema derivation
- Aliased projection columns show as UNKNOWN type in `deriveColumnsFromPlan`
- CTE bodies with aggregates or computed expressions decline in `buildCTEColumnSource`
- ORDER BY without matching index falls back to naive (Java rejects; #25)

## What to work on next

1. **#25** ORDER BY fallback removal — align with Java's rejection behavior. Requires test updates (many CTE/JOIN tests use ORDER BY that naive handles but Java rejects)
2. **#65 remaining** — recursive CTE→RecursiveUnion (TempTableInsert/Scan + RecursiveUnionExpression), HAVING predicate resolution
3. **#72 full port** — Value.pullUp/pushDown for ordering translation through value hierarchies
4. **Phase 5** — #26 DATE/TIMESTAMP, #31 plan cache integration, #33 driver adapter gaps
