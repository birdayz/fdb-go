# Dayshift-10 Handover

**Date:** 2026-04-13, started ~07:00, ended ~14:00 CEST
**PR:** #43

## Objective

FDB client test coverage improvement (72.4% → 80%+), connection monitor implementation, C++ conformance fixes.

## What was done

### 1. Client test coverage (72.4% → 82.8%)

9 new test files, ~145 test cases:

| File | Cases | What |
|---|---|---|
| `atomic_test.go` | 73 | All 13 RYW atomic mutation types vs C++ Atomic.h contracts |
| `atomic_roundtrip_test.go` | 4 | Client-side atomics vs real FDB server (ADD, BYTE_MAX, COMPARE_AND_CLEAR, overflow) |
| `endpoint_test.go` | 16 | IPv4/IPv6 parsing, token extraction, getAdjustedEndpoint arithmetic |
| `snapshot_test.go` | 7 | Snapshot API: RYW, conflict-freedom, range scans, SnapshotRYWDisable |
| `transaction_options_test.go` | 12 | GetPipelined, grvFlags, SetNextWriteNoConflict, versionstamp |
| `database_test.go` | 10 | DB defaults propagation, ReadTransact, conflict ranges, batch priority |
| `readpath_test.go` | 7 | Key selector short-circuits, range scans, reverse, limit |
| `connmon_test.go` | 2 | Connection monitor: bytesReceived tracking, idle connection survival |
| `commitpath_test.go` | 9 | intersectConflictRanges: overlap, disjoint, adjacent, fallback |
| `ping_test.go` | 2 | PingRequest wire format round-trip |

### 2. Connection monitor (new feature, C++ conformant)

Port of C++ FlowTransport.actor.cpp connectionMonitor() (lines 641-721):
- Outer loop: 750ms check, idle skip, 750ms jittered delay, send PING
- Inner retry loop: 2s timeout per round, retry if ANY bytes arrived, kill only on true silence
- PING reply registered via PrepareReply — matches C++ `pingRequest.reply.getFuture()`
- Idle connections (no pending requests) untouched
- Zero benchmark overhead

Key insight: C++ uses `timeouts` counter only for logging, NOT for kill decisions. The kill condition is solely `bytesReceived unchanged`. Our inner retry loop matches this exactly.

### 3. C++ conformance fixes

- **commitDummyTransaction** (`intersectConflictRanges`): now matches C++ `intersects()` at NativeAPI.actor.cpp:6744. Picks conflict key from write∩read overlap instead of arbitrary `writeConflicts[0].Begin`.
- **proxyTagThrottledDuration**: resolved as reply-only field. C++ CommitProxyInterface.h:318 says "Not serialized, because this field does not need to be sent to master." Added `GetTagThrottledDuration()` public API.
- **Divergences**: 5/10 fixed (was 4/10 entering shift).

### 4. Testcontainer multi-node support

Major expansion of the FDB testcontainer module:

- **WithProcessCount(n)** — runs n fdbserver processes on ports 4500..4500+n-1 in one container. Init with single redundancy, start processes, reconfigure.
- **RunCluster(ctx, n, opts...)** — creates n Docker containers on shared network. First = coordinator, rest = storage replicas. Full cluster lifecycle management.
- **WithKnob(name, value)** — server-side knob injection via entrypoint sed. Validated (alphanumeric names, no shell metacharacters). Verified knobs appear in `ps aux` / `/proc/PID/cmdline`.
- **Multi-shard test** — seeds 1MB across 100 keys with max_shard_bytes=50000, polls for shard splits. Currently single-shard (DD needs more time on local dev), but validates full GetRange path.
- 6 new testcontainer tests (process count, knob injection, cluster lifecycle, validation).
- **Multi-shard test suite** — 11 sub-tests across 51 shards: GetRange, GetRangeReverse, paged GetRange, GetKey selector, AtomicAdd, GetEstimatedRangeSize, SnapshotRead, ClearRange, ConflictDetection, ConcurrentReadsWrites, GetRangeSplitPoints. All gomega assertions. C++ workload references (AtomicOps, PhysicalShardMove, RandomMoveKeys, Watches, GetEstimatedRangeSize).

### 5. Other infrastructure

- **Claude review workflow** switched to `[self-hosted, linux, x64, hetzner]` — GH free tier exhausted.
- **vollkonti skill** updated: "get back to work" kick timer at shift start.

## Current state

- **Branch:** `dayshift-10`, PR #43 (reviewer LGTM after 2 rounds)
- **All 13 Bazel test targets pass**
- **CI:** Green on all pushes after connection monitor rewrite
- **Binding stress:** 80+ seeds × 1000 ops = 0 failures (this shift)
- **Fuzz testing:** 883M+ total executions across all 10 targets, 0 crashes
- **Race detector:** Full client test suite passes with `-race`
- **Line coverage:** 77.7% overall, 82.8% (client), 78.3% (record layer)

## Known issues

- `TestMultipleWatchesSameKey` was flaky under `bazel coverage` with the initial 2s-only timeout. Fixed by matching C++ retry loop (inner loop retries as long as any bytes arrive).
- Docker resource limits on local machine prevent large binding stress runs (>30 seeds). CI runs full suite.

## What to work on next

### HIGH
- **Multi-shard watch test** — Watch a key during active shard moves. Needs careful Watch API usage (the facade returns a future, client blocks). Port C++ Watches.actor.cpp pattern.

### MEDIUM
- **RYW SnapshotCache** — C++ caches server reads for reuse within a transaction. Architectural change (segment tree vs map).
- **Speculative second request (secondDelay)** — C++ hedge request for p99. QueueModel already tracks latency.
- **Pool frame read buffers** — Blocked by zero-copy design.

### LOW
- **`net.Buffers` (writev)** — Scatter-gather I/O. Low impact.
- **LRU eviction for location cache** — Currently random. Works well enough.
