# swingshift-47 — 2026-04-24

**Shift:** Swing (14:00–22:00 CEST).
**Continues:** dayshift-46 (merged `d7ce2705`).
**PR:** [#109](https://github.com/birdayz/fdb-record-layer-go/pull/109).
**Status at draft:** 52+ commits, CI green on every push (via pre-commit + re-runs on gazelle updates), 2 review rounds approved, round 3 requested.

---

## Theme

Phase 3 SemanticAnalyzer seed + Phase 4.0 Value-catalogue continuation. Delivered a complete scaffolding layer the future parse-tree driver can consume.

---

## What landed (chronological)

### Semantic package seed — `pkg/relational/core/query/semantic/`

Owns identifier normalization, catalog lookup, scope resolution, function catalogue, parse-tree bridges. Neither depends on cascades.

- **Identifier** (`identifier.go`) — case-folded value-type; map-keyable by `==`; `WasQuoted` flag. `NormalizeString` exposed for call sites that don't want the wrapper.
- **QualifiedName** (`qualified_name.go`) — dot-separated; per-segment quoting; `ParseQualifiedName`, `FromSegments`, `EqualsIgnoreQuoting`, `PrefixedWith`, `LeafIdentifier`.
- **Parse bridges** (`parse_bridge.go`) — `FromFullIdContext` / `FromUidContext` read ANTLR's `GetText()` and normalize.
- **Catalog / Table / Column** (`catalog.go`) — interfaces + `InMemoryCatalog` + `StaticTable` for tests. Catalog has `LookupTable`, `TableExists`, `AllTableNames`.
- **Analyzer** (`analyzer.go`) — `ResolveTable`, `ResolveTableRef` (parse-tree form), `ResolveColumn`, `ResolveColumnRef` (bare-or-qualified dispatch), `ExpandStar`, `ExpandScopeStar`, `ExpandQualifiedStar`. Typed errors: `TableNotFoundError`, `ColumnNotFoundError`.
- **Scope** (`scope.go`) — FROM-clause tracking + correlated subquery parent chain. `ResolveColumn` with ambiguity detection, `ResolveQualifiedColumn`, `AllSourcesRecursive`. Typed errors: `AmbiguousColumnError` (carries conflicting `Sources []Identifier`), `SourceNotFoundError` (carries `Available []Identifier`), `DuplicateAliasError`.
- **Scope builder** (`scope_build.go`) — walks ANTLR `IFromClauseContext`, produces Scope. Rejects JOIN / subquery-in-FROM with `UnsupportedFromShapeError`. Handles AS-optional implicit alias (`FROM users u`).
- **Function catalogue** (`functions.go`) — `FunctionSpec` with `FunctionKind` (explicit values for serialization stability), `MinArgs`/`MaxArgs`, `AllowsStar`, `AllowsDistinct`. `RegisterDefaults` seeds COUNT/SUM/MIN/MAX/AVG. `ValidateArity` with star-case doc contract.
- **Integration test** — full parse → resolve → scope → column path.
- **Microbenchmarks** — hot-path baselines.

### rlcatalog adapter — `pkg/relational/core/query/semantic/rlcatalog/`

Separate subpackage so the core semantic package stays free of `recordlayer` dependency. Adapts `*recordlayer.RecordMetaData` to `semantic.Catalog`.

- **Wrap(md)** builds a pre-folded name→RecordType map at construction (O(1) lookup; review feedback).
- **recordTypeTable** — per-table column cache via `sync.Once` (`LookupColumn` 97 ns → 14 ns).
- **isNullable** — uses `Cardinality()`, not `HasDefault()` (review bug fix: `HasDefault` only true for proto2 explicit defaults, missed proto3 singulars).
- **NewAnalyzer(md, caseSensitive)** convenience — `md → Analyzer` one-shot.
- Benchmarks.

### Phase 4.0 cascades Value catalogue — `pkg/recordlayer/query/plan/cascades/`

Closes the queued dayshift-46 Value follow-ups (all four):

- **AggregateValue** — COUNT / COUNT(*) / SUM / MIN / MAX / AVG. Evaluate panics (aggregates are multi-row — rule/plan layer routes to the aggregator).
- **QuantifiedObjectValue** — row reference by `CorrelationIdentifier`. Implements `Correlated`. Doc warns about the single-source compat shim.
- **PromoteValue** — analyzer-inserted implicit coercion. Evaluate delegates to child (runtime no-op; plan-time inspection point).
- **RecordConstructorValue** — labelled-field struct. Duplicate names panic at construction.

Plus rule-author helpers: **WalkValue**, **ValueSize**, **ContainsAggregate**, **IsConstantValue**, **EvaluateConstant** (panic-recovery for foldable sub-expressions).

### Expression resolver — `pkg/relational/core/query/expr/`

New subpackage bridging semantic + cascades. Neither base package depends on the other; `expr` sits above both.

- **Resolver** bound to an Analyzer + Scope.
- **ResolveIdentifier** (bare or qualified) → cascades.FieldValue with ValueType inferred from Column type string.
- **ResolveConstant** — Go-native literals → ConstantValue / NullValue / BooleanValue. Float literals get a specific error pointing to the Phase 4.0 Type hierarchy port.
- **ResolveArithmetic** / **ResolveComparison** / **ResolveAnd** / **ResolveOr** / **ResolveNot**.
- **ResolveIn**, **ResolveCast**, **ResolveIsNull** / **ResolveIsNotNull**, **ResolveLike**, **ResolveStartsWith**.
- **ResolveFunctionCall** — `AggregateValue` dispatch via FunctionCatalog. Star handling, arity validation, typed errors.
- **WalkExpression / WalkPredicate** — the parse-tree driver! Converts an ANTLR IExpressionContext into a cascades.Value / QueryPredicate. Supports:
  - Bare column / qualified column → FieldValue.
  - Integer / string literal → ConstantValue (with escaped-quote handling).
  - Binary comparison (`a = b`) → ComparisonPredicate; operators `= <> != < <= > >= IS [NOT] DISTINCT FROM` dispatched from the ComparisonOperator context's terminal tokens.
  - LogicalExpression AND/OR → And/OrPredicate with left-deep chain flattening.
  - NotExpression → NotPredicate.
  - Parenthesised expressions (grammar-level 1-element RecordConstructor unwrapped).
  - IS [NOT] NULL via grammar-level IsExpression predicate.
  - BETWEEN via desugar to `AND(>=, <=)`.
  - Arithmetic operators (`+`, `-`, `*`, `/`) → ArithmeticValue. MOD / DIV not wired (cascades.ArithmeticOp doesn't expose them).
  - LIKE (no ESCAPE) → ComparisonLike. NOT LIKE wraps in Not.
  - IN-list (explicit values) → ComparisonIn. NOT IN wraps in Not.
  - Aggregate function calls (`COUNT(*)` / `COUNT(col)` / `SUM/MIN/MAX/AVG(col)`) → AggregateValue.
  - Everything else (IN subquery / parameter / column, LIKE ESCAPE, XOR, scalar function calls, CAST, IS TRUE/FALSE) returns `UnsupportedExpressionShapeError` so callers fall back to the existing logical-builder path.
- **Integration test** — expr → cascades.Simplify pipeline. `(5 = 5) AND (id > 0)` folds to `ID > 0`; parsed `WHERE id = 1 AND 5 = 5` walks and simplifies to `ID = 1`.
- Microbenchmarks.

### Review rounds

- **Round 1** (post-request at 15:45) — nits caught: `isNullable` proto2 bug (correctness), `isQuoted` empty-string edge case, `scope_build` implicit alias, `AmbiguousColumnError` naming sources, `AllowsDistinct` flag, `QuantifiedObjectValue` doc shim, rlcatalog O(N) → O(1) (pre-empted).
- **Round 2** (at 15:57) — verdict: "ready to merge". Two minor cleanups: LookupColumn allocation hoist, `Matches` redundancy noted as convenience field.
- **Round 3** (at 16:25) — addressed: AllTableNames original-casing, float-literal specific error, FieldValue key-normalisation doc, bench b.Fatal on AddSource.
- **Round 4** (at 20:42) — three items: bytes hex-literal Explain rendering (actionable), LogicalCTE row in TestChildren_Arity, explicit `{TRUE, NULL}` / `{FALSE, NULL}` XOR rows. All addressed in `431ff1ea`; final re-check requested.

### Post-round-3 polish (this shift, session 2)

Landed between rounds 3 and 4 — coverage, documentation, walker extensions:

- **XOR walker** — desugared to `(a OR b) AND NOT (a AND b)` (Kleene-exact; NULL propagates through both sides). Replaces the prior `UnsupportedExpressionShapeError`. Test pins the structural shape + the 7-row truth table including NULL, plus the canonical Explain form.
- **IS [NOT] TRUE/FALSE 2VL fix** — prior walker rendered `x IS TRUE` as bare `x = TRUE` which is Kleene-3VL (NULL = TRUE → UNKNOWN). Standard SQL is 2VL (NULL IS TRUE → FALSE). Fixed with `(x IS NOT NULL) AND (x = TRUE)` desugar. Divergence is invisible at WHERE top level but breaks under NOT — new test pins `NOT (NULL IS TRUE) = TRUE`.
- **FieldValue key-normalisation contract** documented on the cascades type (flagged by review as a future landmine — SQL resolver emits UPPER keys, row producers must match).
- **ComparisonPredicate.Explain** now routes RHS through `formatCompareOperand`: strings quoted, NULL as `NULL`, bool as `TRUE`/`FALSE`, IN-lists as `(e1, e2, e3)`. Prior `%v` fallback rendered `NAME = bob` and `active = true` — visually ambiguous and non-SQL.
- **rlcatalog protoKindToSQL full-mapping test** — walks every proto Kind on TypedRecord + covers RECORD/repeated on Order. Any drift in the Kind → SQL-type dispatch string (e.g. accidentally `BOOLEAN` vs `BOOL`) now surfaces at test-time instead of biting the downstream planner.
- **Logical operator Children() arity + pointer-identity sweep** — `pkg/relational/core/query/logical` coverage 84.8% → 97.3%. Table-driven test covering every operator's Children() including nil-input branches.
- **Fullstack predicate integration test** — end-to-end coverage for BETWEEN (hit/boundary/miss), IN/NOT IN, LIKE `%` and `_` wildcards, XOR + NULL propagation, plus BETWEEN∧IS NOT NULL combinations.
- **NOT-paren-AND regression pin** — stale comment claimed this needed "separate walker dispatch"; actually works via the existing NotExpression → WalkPredicate recursion. Test pins both structure and 3-row truth table.
- **ValuePredicate.Explain** fix (landed earlier but worth calling out) — was returning Value.Name() which is the KIND ("field") instead of per-instance text; now uses ExplainValue.
- **rlcatalog Indexes() cover** — previously 0% coverage on this method.
- **Bench error-check fix** — `_ = scope.AddSource(...)` silent-fail replaced with `b.Fatal` wrap so construction failures can't masquerade as zero-cost hot paths.
- **BYTES column type fix** — `sqlTypeToCascadesValueType` mapped `"BYTES"` → `TypeInt` (a lie — would route BYTES comparisons through int promotion which always fails). Now `TypeUnknown` alongside FLOAT/RECORD. Full INT/STRING/ENUM/BOOL/FLOAT/BYTES/RECORD mapping pinned by new test.
- **bytes comparator** — cmpAny grows a `[]byte` branch (lexicographic via `bytes.Compare`), matching SQL BINARY. Mixed bytes/string degrades to UNKNOWN. Unblocks WHERE clauses on BYTES columns (UUID lookups, digest equality, etc.). Covers IN-list membership too.
- **Explain bytes as SQL hex literal** — `formatCompareOperand` now renders `[]byte` as `X'0102'` (BINARY/VARBINARY style); previous `%v` fallback produced Go's `[1 2]` which reads as a failed array rather than a bytes value.

---

## Shipped by the numbers

- PR #109: 52+ commits (ready-to-merge after round 2, plus ~15 more additions since).
- 2 review rounds approved + round 3 in flight.
- 35 bazel test targets green (was 33 at shift start).
- New packages:
  - `pkg/relational/core/query/semantic/` — 11 source files, ~95 test functions incl. benchmarks.
  - `pkg/relational/core/query/semantic/rlcatalog/` — RecordMetaData adapter.
  - `pkg/relational/core/query/expr/` — parse-tree → cascades bridge, full builder surface.
- Cascades package: 10 concrete Values → 10 (started at 6 pre-dayshift-46-compaction; swingshift added 4 more), plus helper surface for rule authors.
- Net LOC: ~3500 added.

---

## Current state

**Ready-to-consume:** A parse-tree driver that wants to build a Value tree from an ANTLR ExpressionContext now has:
- `semantic.Analyzer` to resolve identifiers.
- `semantic.Scope` (+ `BuildScopeFromFromClause`) to track FROM sources.
- `expr.Resolver` to build Values for every expression shape (columns, constants, arithmetic, comparisons, AND/OR/NOT, IN, CAST, IS NULL, LIKE, STARTS_WITH, aggregate calls).
- `rlcatalog.Wrap / NewAnalyzer` to plug into a live `RecordMetaData`.

The pieces compose: integration test in `expr_test.go` shows resolver → `cascades.Simplify` pipeline folding a tautology end-to-end.

**Parse-tree walker: landed + comprehensive.** WalkExpression (Values) + WalkPredicate (QueryPredicates) handle most WHERE/SELECT expression shapes in the grammar. See handler list in the package doc above. The integration point into the logical-builder is the natural next deliverable.

**Full-stack pipeline test** at `expr/fullstack_test.go` demonstrates the target composition: `parse → BuildScopeFromFromClause → WalkPredicate → Simplify → Eval`. Runs a realistic `WHERE id >= 18 AND (name IS NOT NULL OR active) AND 5 = 5` end-to-end, confirms the tautology folds, evaluates against 4 row shapes.

---

## Next-shift follow-ups

### HIGH — parse-tree walker (partially done)

- [x] `pkg/relational/core/query/expr/walk.go` seed — shipped! Covers bare columns, qualified columns, integer/string literals, binary comparisons, logical AND/OR (with flattening), escaped-quote string handling.
- [x] Extend walker: BETWEEN ✓, IN-list ✓, LIKE (no ESCAPE) ✓, IS [NOT] NULL ✓, IS [NOT] TRUE/FALSE ✓, NOT ✓, XOR ✓ (desugared; Kleene-exact), arithmetic +/-/*// ✓, parens ✓ (incl. `NOT (a AND b)`), aggregate function calls ✓ (COUNT/SUM/MIN/MAX/AVG, star form). Remaining: scalar function calls, LIKE+ESCAPE, IN-subquery/parameter, CAST, STARTS_WITH (requires grammar support), NULL literal (requires grammar NullConstantContext dispatch), MOD / integer DIV (waits on cascades.ArithmeticOp extension).
- [ ] Wire the walker into the logical-builder so `WHERE` predicates and `SELECT` expressions become cascades Value trees instead of ANTLR nodes carried verbatim. The builder can start with `WalkPredicate` for WHERE and fall back to its existing path when the walker returns `UnsupportedExpressionShapeError`.

### HIGH — Comparison RHS takes Value

- [ ] Extend `cascades.Comparison.Operand` from `any` to `cascades.Value`. Unblocks non-constant RHS (`a = b`, `a < b + 1`) in expr.ResolveComparison.
- [ ] Update rule_simplify rules to handle the new shape.

### MEDIUM — Phase 4.0 Type hierarchy

- [ ] Replace `cascades.ValueType` enum with a structured `Type` / `TypeRepository` / `Typed`. Enables struct-valued Columns and real Record / List types.
- [ ] Thread Type through Column / FunctionSpec so type inference works end-to-end.

### MEDIUM — parameter-bound Comparison

- [ ] Support `x = ?` at plan time by letting Comparison carry a Placeholder on its RHS. Plan cache needs this for query-by-value reuse.

### LOW — inherited

- [ ] `LogicalOperator → Plan` translator (dayshift-46 carryover).
- [ ] Mutual recursion in CTEs.
- [ ] Continuation resumption of partial recursive results.

---

## Shift-end checklist (to complete)

- [x] All local tests pass (`just test` 35/35 every commit).
- [x] CI green on each push.
- [x] Review round 1 addressed (scope, ambiguity, rlcatalog O(N), Nullable bug, isQuoted, functions, eval-context shim).
- [x] Review round 2 addressed (FunctionKind explicit values, AmbiguousColumnError doc, LookupColumn hoist).
- [x] Review round 3 addressed (AllTableNames casing, float literal error, FieldValue row-key contract, bench b.Fatal).
- [x] Post-round-3 polish shipped (XOR walker, IS TRUE/FALSE 2VL fix, Explain formatting, rlcatalog Kind→SQL mapping, logical Children() coverage, fullstack BETWEEN/IN/LIKE pipeline, NOT-paren-AND pin).
- [x] Round 4 review requested.
- [ ] Round 4 addressed + merge.
- [x] Handover updated with post-round-3 section.
- [ ] Merge PR at shift-end (22:00 CEST) after CI re-green.
