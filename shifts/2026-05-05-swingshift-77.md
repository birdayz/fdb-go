# swingshift-77

- **Date:** 2026-05-05
- **Actual start:** ~15:41 CEST
- **End:** ~21:30 CEST (overtime)
- **PRs:** #151

## What was done

### HN Launch Blockers (all 5 done)

- **#93** Self-join + CTE+JOIN alias threading through NLJ pipeline
- **#94** Reverse index scan for ORDER BY DESC on secondary indexes
- **#95** Streaming aggregation ordering for GROUP BY + ORDER BY
- **#96** README: SQL engine docs, Getting Started, corrected feature lists
- **#97** FuzzTranslateToCascades for translator panic coverage

### RFC-001: In-Memory Sort (Go Extension)

Physical sort operator for ORDER BY without supporting index. Cascades still optimizes scan/filter/join/agg; only the final sort is in-memory. Strict separation: `GoExtensionImplementationRules()` factory, clearly labeled files, separate executor dispatch section.

### Unified Semantic Scope (Java-aligned architecture)

Replaced 5 independent scope constructions with one `buildSelectScope()` per SELECT. All identifier resolution — projections, ORDER BY, WHERE, GROUP BY — goes through the semantic scope. Same architecture as Java's `QueryVisitor` holding a `SemanticAnalyzer`.

- `buildLogicalPlanForSelectWithCTECatalog` returns `(LogicalOperator, error)` — semantic errors propagate
- `buildLogicalPlanForQueryWithCatalog` returns `(LogicalOperator, error)` — parse-tree errors propagate
- Ambiguous columns (42702), undefined columns (42703) detected at build time
- ORDER BY resolved via expression walker (Java's `ExpressionVisitor.visitOrderByExpression`)
- WHERE ambiguity detected before predicate builder falls back to text

### Other Fixes

- **#91** Function rejection aligned to ErrCodeUndefinedFunction (42883)
- **OrderedPrimaryScanRule** — ORDER BY PK DESC via reverse primary scan
- **CTE column alias resolution** — WHERE + projection through aliased columns
- **UNION ALL column-name alignment** — second+ branches remap to first branch's names
- **Qualified-star source validation** — `SELECT wrong.* FROM a` errors with 42F01
- **Parser fix** — `GetText()` → `canonicalTextOf()` for 7 sites (whitespace preservation)
- **ANTLR tree walker** — replaced text-matching function detection with parse-tree walk
- **INFORMATION_SCHEMA** — replaced `strings.Contains(sql)` with parse-tree check
- **CLAUDE.md** — added NO TEXT MATCHING rule
- **Skip elimination** — 38 conditional t.Skipf → t.Fatalf, zero non-Docker skips in sqldriver/cascades

## Current state

- **51 commits**, +2300/-570 across 49 files
- **46 test targets** all pass
- **1579/1579** SQL corpus entries execute without error
- **51/111** yamsql scenarios pass (was 40 start-of-shift, 11 flipped)
- **0** non-Docker skips in sqldriver/cascades tests
- CI green on all pushes

### Yamsql breakdown (60 failing)

- ~120 queries: Cascades planner gaps (subqueries, qualified star expansion, recursive CTE)
- ~50 queries: wrong error code or missing validation
- ~60 queries: row mismatches (GROUP BY expression, HAVING complex shapes)

Gap table at top of TODO.md with cross-references.

## What to work on next

1. **Yamsql conformance** (TODO top section) — the remaining 60 scenarios. Highest ROI:
   - Qualified star expansion (`SELECT a.*` in joins) — ~15 queries
   - GROUP BY expression support (`GROUP BY amt / 100`) — ~10 queries
   - Fix remaining error-code mismatches
2. **Strict/extended test mode** — toggle Java-conformant vs Go-extension behavior in yamsql runner
3. **Expression GROUP BY** — upgrade GROUP BY expressions through the resolver
4. **Scalar subqueries** — port `DecorrelateValuesRule` for `(SELECT MAX(v) FROM t)`
5. **LICENSE file** — README references it but doesn't exist. Java uses Apache 2.0.
