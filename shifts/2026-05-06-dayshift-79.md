# dayshift-79

- **Date:** 2026-05-06
- **Actual start:** ~11:30 CEST
- **End:** ~22:00 CEST (extended day+swing+overtime)
- **PRs:** #153

## What was done

### Yamsql conformance: 63/98 → ~75/98 (+12 scenarios, 64% → 76%)

33 commits across 22 files, ~+1400 LOC. Principal-engineer review + subagent delegation model.

### Expression walker extensions
- **Byte literal support** — `x'CAFE'` and `b64'...'` in WHERE predicates via `walkBytesConstant`
- **Datetime function values** — CURRENT_TIMESTAMP, CURRENT_DATE, CURRENT_TIME, LOCALTIME via `walkSimpleFunctionCall`
- **CURRENT_USER** — returns empty string (Java alignment)
- **ORDER BY aggregate expression** — Value resolution for sort keys with `__orderby_aggexpr_` sentinel

### Cascades planner features
- **ImplementDistinctFinalRule** — PLANNING-phase DISTINCT implementation enabling DISTINCT + ORDER BY composition
- **ImplementInMemorySortRule** — MaxTasks 2k→10k for IN 5+ and CTE chains
- **GROUP BY expression values** — `GroupKeyValues` on LogicalAggregate, `translateAggregate` uses resolved Values
- **Recursive CTE translation** — full pipeline: `translateRecursiveCTE` with seed/recursive partitioning, TempTableScan/Insert wiring, column name normalization

### Validation fixes (Java-aligned)
- GROUP BY 42803 for non-aggregated columns
- GROUP BY expression resolution skip (don't validate expression text as column)
- GROUP BY error code priority (42703 before 42803)
- UNION non-ALL rejection (0AF00 before column count check)
- UNION ORDER BY column validation against left branch
- UNION column type compatibility (42F65)
- UNION ORDER BY lifting through Project wrapper
- Wrong qualifier validation in JOIN ON / WHERE (42703)
- CTE column count mismatch (42F10)
- IN column ref error code (0A000)
- Ambiguous column in cross-join GROUP BY/aggregates

### Structural fixes
- **Qualified star expansion** — `SELECT a.*` for JOINs via `expandProjQualifier`
- **Qualified column refs in JOIN projections** — keep `EMP.NAME` for JOINs, bare `NAME` for single-table
- **NLJ multi-level join** — skip re-qualification for already-qualified keys (prevents `EMP.EMP.NAME`)
- **CTE alias propagation** — executor dual-key storage (original + alias) + deriveColumnsFromProjection alias Name
- **CTE cross-join collision detection** — skip alias rewrite when maps would create overlapping target columns
- **Index scan column derivation** — findIndexPlan + findLeafDescriptor for SELECT * over secondary index
- **GROUP BY column leak** — post-agg LogicalProject strips group keys not in SELECT list
- **Recursive CTE column source** — buildCTEColumnSource handles SetQueryContext (UNION ALL) bodies

### Documentation
- **SQL_CONFORMANCE.md** — comprehensive feature matrix: Java 4.11.1.0 vs Go vs ANSI SQL

## Current state

- **39 commits**, ~+1900 LOC across 25+ files
- **46 test targets** all pass
- **~77/98** yamsql scenarios pass (79%), was 63 (64%) start-of-shift, +14 flipped
- LEFT/RIGHT OUTER JOIN via Cascades (JoinType through full pipeline)
- EXISTS/NOT EXISTS subquery infrastructure (uncorrelated; correlated in progress)
- Recursive CTE full pipeline: translation + planning (cycle detection) + execution (depth guard)
- CTE cross-join collision detection for overlapping column names

### Yamsql breakdown (~23 still failing)

- ~11 scenarios: subqueries (EXISTS, scalar, correlated, IN subquery) — needs DecorrelateValuesRule
- ~3 scenarios: OUTER JOIN — needs ImplementOuterJoinRule
- ~4 scenarios: derived table column scope — alias propagation in nested/renamed derived tables
- ~2 scenarios: GROUP BY edge cases (CASE expression sort key, alias resolution)
- ~2 scenarios: UNION positional column mapping
- ~1 scenario: recursive CTE stack overflow (Memo cycle in exploration)
- ~2 scenarios: ORDER BY elimination (equated column cost model), covering index (EXISTS)

## What to work on next

1. **Recursive CTE stack overflow** — the Memo exploration cycles on RecursiveUnionExpression quantifiers. Needs cycle detection or exploration depth guard. Agent was investigating at shift end.
2. **Subquery support** — EXISTS, scalar subqueries, correlated subqueries. Needs `DecorrelateValuesRule` + `SelectExpression` + correlation binding infrastructure. Biggest remaining gap (~11 scenarios).
3. **OUTER JOIN** — needs `ImplementOuterJoinRule` in the Cascades planner. The NLJ executor already handles left/right outer join types.
4. **Derived table column scope** — alias propagation from inner subquery projections to outer query scope. Multiple patterns: nested derived tables, renamed derived tables, GROUP BY on derived table columns.
5. **GROUP BY expression sort key alignment** — datum key (ExplainValue) vs sort key (SQL text) use different representations. Needs unified naming.
