# nightshift-50 — 2026-04-25 / 26

**Shift:** Night (22:00–06:00 CEST). Started 22:16, wound down ~05:46.
**Continues:** dayshift-49 (merged `f3b7b1ab`).
**PR:** [#115](https://github.com/birdayz/fdb-record-layer-go/pull/115)
— 37 commits ahead of master, 6 review rounds approved before the
user asked to skip further reviews until end-of-shift wind-down.
39/39 bazel test targets green on every commit; race-detector clean
on the cascades + plandiff trees.

---

## Theme

Two big CRITICAL items from TODO.md, plus a Phase 4.0 Type-system
seed that unblocks the next round of cascades work, plus matcher-
catalogue catch-up:

1. **RFC-025 Phase 1 execution** — split flat `cascades/` into
   `values/` + `predicates/` + `matching/` sub-packages.
2. **RFC-022 §4.-1 Phase 3** — catalog-aware Go mode for the plan-
   equivalence harness.
3. **Phase 4.0 Type seed** — `Type` interface + concrete impls
   (PrimitiveType / RecordType / ArrayType / EnumType) +
   TypeRepository + ValueRichType bridge.
4. **Phase 4.2 matcher catalogue** — five new matchers + one
   interface, mostly closing the RFC-025-aligned matcher gap.
5. **Bug audit** — three real correctness bugs fixed by inspection.
6. **Derived-table WHERE** — basic shape now routes through the
   catalog-aware builder.

---

## What landed (chronological)

### 1. cascades: split values / predicates / matching (RFC-025 Phase 1) — `2cd260f6`

Mechanical move out of the flat `cascades/` package into:

- `cascades/values/` — values.go, value_not.go, simplifier_value.go,
  folder.go, **correlation.go**, coercion.go (new — extracted
  ToFloat64 / ToInt64 / LiteralValue from comparisons.go to break
  the layering inversion the move surfaced).
- `cascades/predicates/` — predicates.go, comparisons.go,
  simplifier_predicate_values.go.
- `cascades/matching/` — matcher.go, combinators.go, list_matcher.go,
  all_elements_matcher.go.

Root `cascades/` retains the rule infra (rule.go, rule_simplify.go,
rule_demorgan.go, rule_value_predicate_fold.go, simplifier.go) until
RFC-025's Phase 2 fires when Batch A rules start landing.

`correlation.go` moved into `values/` rather than staying in root —
RFC §"Don't split" footnote allowed this since `values` is the only
intra-package consumer; keeping it in root would have created a
`values → cascades → values` cycle once root cascades imports the
sub-packages for rules.

Two test files extracted into root cascades to avoid cycles:
`comparison_simplify_test.go` (FireRule on
ComparisonConstantSimplifyRule) and `predicate_matchers_test.go`
(uses unexported root-cascades predicate-matcher constructors).

11 external caller files updated (`embedded/`, `expr/`, `logical/`)
— a Python rewriter mapped each `cascades.<X>` qualifier to
`values.<X>` / `predicates.<X>` / `matching.<X>` based on the
symbol's new home; root-cascades imports kept where rule infra is
still referenced.

### 2. plandiff: catalog-aware Go mode (RFC-022 §4.-1 Phase 3) — `a08b334e`, `005af173`, `02e3b613`

`embedded.NewExplainOnlyGeneratorWithSchema(schemaDDL)` parses a
CREATE SCHEMA TEMPLATE body into an in-memory
`RecordLayerSchemaTemplate`, wraps it in an `api.Schema`, seeds a
synthetic `Session.SchemaCache`, and returns a Generator whose
connection routes WHERE / DELETE / UPDATE shapes through
`buildLogicalPlanFor*WithCatalog`. Predicates render as real
`cascades.predicates.QueryPredicate` trees in Explain output
instead of source-text fallback.

Auto-wraps bare bodies (`CREATE TABLE Item (...)` →
`CREATE SCHEMA TEMPLATE auto_template CREATE TABLE Item (...)`) the
way `conformance/sql_plan_steps.java#planSql` does on the Java
side. plandiff's `goEngine` picks up the catalog-aware constructor
when `Query.SchemaTemplate` is non-empty.

SeedCorpus extended with six `catalog_*` entries:
`catalog_select_eq_filter`, `catalog_select_arith_filter`,
`catalog_delete_filter`, `catalog_update_filter`,
`catalog_derived_table_where`, `catalog_and_where`. Hash bumped
twice: 631267…→07f065…fb88f655 (35→39 queries) →
473f93…b896 (39→41 queries).

### 3. embedded: derived-table WHERE in catalog-aware builder — `1f88d605`

`buildWherePredicate` previously declined on `sq.derivedQuery != nil`.
Now `buildWherePredicateForDerived` extracts the inner query's
selectQuery via the existing `extractFromQueryTerm` parser, walks
`projCols` / `projAliases`, looks up each bare-column projection in
the inner table's catalog entry, and synthesises a
`semantic.StaticTable` whose columns inherit the inner-table types.
The synthesised `ScopeSource` lets the walker resolve `x.val` against
the inner BIGINT column without the derived rows having to
materialise.

`buildDerivedTableSource` declines on shapes the seed Type system
can't reason about — derived-of-derived, joins, SELECT *,
aggregates, computed projections, qualified-star — and the caller
falls back to the text builder. Phase 4.0 Type hierarchy port
unlocks computed projections; the decline gate documents why.

### 4. cascades/values: Phase 4.0 Type hierarchy seed — `d4f26925`, `6fa1edfe`, `6bec402b`, `2fba3975`, `0b2f8c35`, `27f5b1d9`

Mirrors the bare-minimum surface of Java's
`com.apple.foundationdb.record.query.plan.cascades.typing.Type`.
Lives in `cascades/values/type.go` (~1000 LOC + ~1000 LOC of tests)
— above RFC-025's 300-LOC threshold for a `typing/` split, but
deferred until the next port (RelationType, plan-serialisation
hooks, the conversion lattice) actually needs the move.

Contents:

- `TypeCode` enum — Java's well-known codes (NULL, BOOLEAN, INT,
  LONG, FLOAT, DOUBLE, STRING, BYTES, VERSION, ENUM, RECORD, ARRAY,
  RELATION, ANY, NONE, UUID) plus UNKNOWN. `IsPrimitive` /
  `IsNumeric` helpers.
- `Type` interface — Code + IsNullable + Equals (structural) + String.
- `PrimitiveType` impl + canonical singletons (`NotNullInt`,
  `NullableString`, `NullType`, `UnknownType`, etc.).
  `NewPrimitiveType` panics on structured codes.
- `RecordType` (Field + Ordinal-based) — anonymous-field-allowed,
  duplicate-named-field panic, defensive-copy constructor,
  LookupField + GetField.
- `ArrayType` — ElementType (nullable supported), nested ARRAY-of-
  RECORD, nil-element-as-not-yet-inferred.
- `EnumType` — Name + Number values, dup-name AND dup-number
  panic, both lookups, declared-order-aware Equals.
- `WithNullability(t, nullable)` — flips a Type's nullability;
  returns canonical singletons for primitives, fresh copies for
  structured types.
- `Typed` interface (RichType()) — forward-compat hook for
  things-with-a-type, parallel to existing Value.Type() ValueType.
- `FromValueType(vt, nullable)` / `ToValueType(t)` — legacy bridge.
  `TypeInt` ↔ LONG (BIGINT default); `TypeFloat` ↔ DOUBLE; the
  reverse collapses LONG/INT and DOUBLE/FLOAT.
- `TypeRepository` — name → Type registry. Per-query / per-statement
  instance, not goroutine-safe. `TypeRegistrationError` structured
  error.
- `ValueRichType(v)` free function — bridges every Value impl to the
  rich Type without forcing the Value interface itself to gain a
  RichType() method (avoids a breaking change).

### 5. cascades/matching: Phase 4.2 matcher catalogue — `bec23171`, `9b93ae77`, `a3bea697`, `82a29849`, `c869c9a6`, `8eab9221`, `9502a2ff`

Five new matchers + one interface, substantially closing the
RFC-025-aligned matcher gap from Java's
`matching/structure/` package:

- **`SomeElementsMatcher`** (Java's `MultiMatcher.SomeMatcher`) —
  matches when at least one element binds the downstream. Empty
  input fails (asymmetric vs AllElementsMatcher's vacuous-true).
- **`AtLeastElementsMatcher`** (Java's `MultiMatcher.AtLeastMatcher`)
  — matches when at least N elements bind. minMatches=0 always
  matches; minMatches>0 + fewer matches → nil. Negative panics.
- **`EmptyCollectionMatcher`** (Java's `CollectionMatcher.empty`) —
  matches an empty []any input. id-counter pattern for distinct
  identity.
- **`TypedMatcher[T, U]`** (Java's
  `TypedMatcherWithExtractAndDownstream<T>`) — generic: type-asserts
  to T, applies extractor T → U, runs downstream on U. nil-extract /
  nil-downstream panic at construction.
- **`NegationMatcher`** (Java's `NotMatcher`) — inverts a downstream:
  succeeds when downstream fails, fails when it matches. Naming
  disambiguated from the pre-existing `predicateMatcher[*NotPredicate]`
  (which matches when the input IS a NotPredicate).
- **`SatisfyingMatcher[T]`** (Java's `PrimitiveMatchers.satisfies`)
  — generic: type-asserts to T, runs caller-supplied predicate(T).
  Use case: rule pattern constraints that don't fit a dedicated
  matcher.
- **`CollectionMatcher` interface** — closed-set marker interface
  satisfied by the four list-matchers (AllElements / Some /
  AtLeast / List / Empty) for type-safe combinator composition.

### 6. Bug fixes (all by code-inspection audit)

- **`CAST(int64 AS STRING)` on negatives** (`1540acf0`) — used
  `uitoa(uint64(i))` which reinterprets the sign bit; `CAST(-5 AS
  STRING)` produced "18446744073709551611". Fixed with
  `strconv.FormatInt`. Regression test pins -5 / 0 / MIN_INT64 /
  MAX_INT64.
- **`intToDec(MinInt64)` plan-cache-key collision** (`cadbcd4c`) —
  hand-rolled formatter used `n = -n` which overflows for MinInt64
  and produced just `"-"`. Since intToDec drives ExplainValue
  (the plan-cache key seam), wrong rendering would silently hash-
  collide distinct queries. Same fix pattern; same regression
  pinning.
- **`buildSchemaTemplateFromDDL` nil-deref** (`e1920e8d`) —
  short-circuit `len(stmts.AllStatement())` after `stmts == nil`
  check would panic. Split the guards.
  `TestGoEngine_CatalogAware_StatementlessDDL` (whitespace-only,
  semicolon-only) is the regression sentinel.
- **map-path IN-list expression evaluation** (`c499cec1`) — TODO
  in eval_predicate_map.go silently dropped arithmetic / function-
  call list elements (only PredicatedExpression's atom was
  evaluated). Switched to `evalExprOnMap` to match the proto path.

### 7. CAST gap-fills (`ce0e2852`)

`CAST(BOOL AS STRING)` returned nil at the cascades fold path while
runtime `functions.CastValue` returned "true"/"false" — fold-vs-
runtime divergence on a constant input. Added the BOOL → STRING
arm (matches Java's CastValue.BOOLEAN_TO_STRING lowercase) and the
BOOL → FLOAT arm (mirrors the runtime's CAST(b AS INT) AS FLOAT
chain in one step).

### 8. Test depth additions

- `comparisons_null_table_test.go` (`fda46343`, `d1afcf73`) —
  parameterised NULL-propagation table for binary Comparisons. 28
  sub-tests pinning SQL 3VL per-operator: 6 binary ops × 3 null
  configs (left/right/both); IS DISTINCT FROM and IS NOT DISTINCT
  FROM full 5-case symmetric tables; LIKE/STARTS_WITH null-degrade;
  IS [NOT] NULL unary resolution.
- Type seed tests — every TypeCode rendering, primitive/structured
  + numeric splits, PrimitiveType.Equals across all (code, nullable)
  pairs, RecordType / ArrayType / EnumType shape + Equals + lookup,
  WithNullability primitive-singleton round-trips, FromValueType /
  ToValueType bridges, TypeRepository validation arms,
  ValueRichType per-Value-impl mapping.
- catalog_aware_test.go — derived-table WHERE pin (FoldsWhere +
  DeclinesComplex), StatementlessDDL regression test, the
  CatalogAwarePathDiffersFromTextOnly assertion.
- All 5 new matchers ship with shape + edge-case + distinct-identity
  + interface-conformance tests.

### 9. Doc cleanups

- 9 stale `cascades.<X>` doc-comment references swept after Phase 1
  split (`7c063b5a`).
- RFC-025 status bumped to "Phase 1 executed" (`fb791457`).
- TODO.md refreshed across multiple items as work landed.
- correlation.go's parenthetical fold (`9e44c1b4`).
- Java-class-path doc-comment revert in correlation.go (`e34a3516`)
  — round-4 nit caught my sed sweep over-correcting Java's path
  (it's at root cascades, not cascades.values).

---

## Review rounds

6 rounds, all approved. Real findings:
- **Round 2**: Phase 3 nil-deref in buildSchemaTemplateFromDDL.
  Fixed in e1920e8d with regression test.
- **Round 3**: minor IS NOT DISTINCT FROM symmetry gap. Closed in
  d1afcf73.
- **Round 4**: nit — over-corrected Java class path. Reverted in
  e34a3516.
- **Round 5**: nit — doubled parenthetical. Folded in 9e44c1b4.
- **Round 6**: clean.

User then said "no more reviews in PR before end-of-shift wind down.
just straight start of shift => work until 30min before end". The
remaining commits are all work past the round-6 LGTM that landed
without further review.

---

## Shipped by the numbers

* 37 commits on PR #115 (including the start-shift empty commit).
* 39/39 bazel test targets green on every commit.
* Race-detector clean on cascades + embedded + plandiff.
* 4 real bugs fixed with regression tests.
* 6 new corpus entries pinning catalog-aware Go path shapes.
* New API surface:
  - `cascades/values/coercion.go` exports `ToInt64`, `ToFloat64`,
    `LiteralValue`.
  - `cascades/values/type.go` exports the entire Phase 4.0 Type
    seed (Type, TypeCode, PrimitiveType + 14 singletons, RecordType,
    ArrayType, EnumType, TypeRepository, WithNullability,
    ValueRichType, FromValueType / ToValueType).
  - `cascades/matching/` adds `SomeElementsMatcher`,
    `AtLeastElementsMatcher`, `EmptyCollectionMatcher`,
    `TypedMatcher[T, U]`, `NegationMatcher`,
    `SatisfyingMatcher[T]`, `CollectionMatcher` interface.
  - `embedded.NewExplainOnlyGeneratorWithSchema(schemaDDL)`.
  - `embedded.buildWherePredicateForDerived`,
    `embedded.buildDerivedTableSource`.

---

## Current state

* **Cascades package split done.** `values/` (Value tier),
  `predicates/` (QueryPredicate + Comparison), `matching/`
  (BindingMatcher catalogue) are the canonical homes; root
  `cascades/` is rule infra only. RFC-025's Phase 2 (rules/) fires
  when Batch A rules start landing.
* **Plan-equivalence harness Go side has catalog-aware mode.** Both
  engines now consume `Query.SchemaTemplate`. Six corpus entries
  exercise it. The harness is one step closer to producing real
  Go-vs-Java tree diffs once Phase B SqlSteps lands the Java
  `runSql` step.
* **Phase 4.0 Type hierarchy seed.** Type / PrimitiveType /
  RecordType / ArrayType / EnumType / TypeRepository / WithNullability
  / Typed / ValueRichType / FromValueType-ToValueType bridges all
  in place. Ready for callers who want rich types; existing
  ValueType callers keep working unchanged.
* **Phase 4.2 matcher catalogue substantially closed.** The five
  new matchers cover the bulk of Java's MultiMatcher / NotMatcher
  / TypedMatcherWithExtractAndDownstream / PrimitiveMatchers /
  CollectionMatcher.empty surface. Remaining (gated):
  OptionalIfPresentMatcher (Go-idiom mismatch),
  PartialMatchMatchers + graph/ matchers (Reference / DAG
  infra needed).
* **Derived-table WHERE basic shape ships.** Bare-column inner
  projections route through the catalog-aware path; computed
  projections still decline (Type-hierarchy gated).

---

## Next-shift follow-ups

### CRITICAL (TODO.md)

- 4.-1 Phase 4 (gated on cost model + cache key spec).
- Phase B SqlSteps — needs Java-side `runSql` step in
  `conformance/sql_plan_steps.java` taking (cluster, schemaTemplate,
  sql) and returning rows as JSON. Multi-shift.
- Catalog wire format Go↔Java round-trip (subset of Phase B).
- SQL semantic equivalence (depends on Phase B).

### HIGH

- **Phase 4.0 follow-on**: RelationType, UuidType structured impls;
  plan-serialisation hooks; full conversion / coercion lattice.
  Once Type seed exceeds ~1500 LOC the typing/ split per
  RFC-025 becomes mandatory.
- **Phase 4.1 RelationalExpression** — multi-shift; the natural
  next building block after Phase 4.0 lands.
- **Catalog NOT-NULL flag through to ValueRichType** — currently
  FieldValue's RichType is conservatively nullable. Threading the
  catalog's Column.Nullable through would unlock NULL-elimination
  rules.
- **Type-aware ArithmeticValue.Type()** — currently always TypeInt
  in legacy enum and NullableLong in RichType. Once two operands'
  types differ, infer the promoted result.

### MEDIUM

- **GROUP BY (a+b) AS alias** — multi-touch; the rewrite path
  only handles bare-column group-by entries. Wiring expressions
  needs alias-aware resolution.
- **Wire SimplifyValue into ORDER BY / aggregate / INSERT VALUES**
  — gated on cascades.Value runtime evaluation seam in embedded
  (currently embedded uses parse-tree evalExpr).
- **ParameterBinder caller plumbing** — gated on the same
  cascades.Value runtime eval seam.
- **OptionalIfPresentMatcher / PartialMatchMatchers / graph/
  matchers** — last few items in the matcher-catalogue gap.
  Optional needs a Go-idiom decision (`*T` vs `(T, bool)`); the
  others gated on Reference / DAG infra (Phase 4.3+).

### LOW / inherited

- Mutual recursion in CTEs, continuation resumption of partial
  recursive results.
- ANTLR DoS parse-time limit (TODO advises against unilateral
  Go-only fix).
- Arithmetic over floats (gated on Phase 4.0 follow-on for
  proper type inference).

---

## Shift-end checklist

- [x] CRITICAL items advanced: RFC-025 Phase 1, RFC-022 §4.-1
  Phase 3.
- [x] All local tests pass (`just test` 39/39 every commit).
- [x] CI green on each push.
- [x] Race detector clean on changed packages.
- [x] 6 review rounds approved before reviews paused per user
  direction.
- [x] TODO.md updated (Phase 1 + Phase 3 marked done; Phase 4.0 +
  4.2 progress noted; misc HIGH/MEDIUM refresh).
- [x] RFC-025 status bumped to "Phase 1 executed".
- [x] Handover written.
- [ ] Merge PR at shift-end (~05:46 wind-down) after final CI green.
