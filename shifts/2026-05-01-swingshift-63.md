# Swingshift-63 — 2026-05-01

**Started**: 14:30 CEST. **Ended**: 15:00 CEST (early — user-closed). **Duration**: ~30 min. **PR**: #131.

## What this shift was

TODO restructure session, not a coding shift. User asked for a phased execution plan to "finish the relational subsystem", then asked to delete the existing 657-line TODO.md and replace with the 37-item ordered list to grind through with no distractions.

## What was done

1. **TODO.md collapsed 657 → 62 lines** into 37 items across 6 sequential phases. Items inside a phase run in parallel unless gated. `git show 32bbfb9c` for the rewrite; `TODO_old.md` retains the pre-rewrite 657-line content for reference.
2. **CLAUDE.md updated** to reference phase ordering instead of the old `## CRITICAL > ## HIGH > ## MEDIUM > ## LOW` buckets. Pick the lowest-numbered unchecked item whose gates are satisfied.
3. **Branch hygiene**: created `swingshift-63` from master, opened draft PR #131. CI green on shift-start + TODO restructure + TODO_old archive commits.

## What to pick up next shift

**Start with TODO.md #1** (SELECT DISTINCT plain projection cleanup). All Phase 1 items (#1-9) are gate-free and parallel-safe — pick any unchecked one.

### #1 starting context (audit done this shift)

Goal: align Go to reject plain `SELECT DISTINCT col1, col2 FROM t` the way Java's fdb-relational does, since plain DISTINCT in non-trivial cases is a Cascades-rule gap upstream (only DISTINCT-aggregate was aligned nightshift-61).

**Files that consume `sq.distinct`** (the parser-set flag for plain SELECT DISTINCT):
- `pkg/relational/core/embedded/select_parser.go:86` — field declaration on `selectQuery`
- `pkg/relational/core/embedded/select_parser.go:819,903` — set from `simpleTable.DISTINCT() != nil`
- `pkg/relational/core/embedded/select_query_full.go:1098,1173,1270` — execution-time DISTINCT branch + ORDER BY exemption gate
- `pkg/relational/core/embedded/join.go:468` — JOIN-path DISTINCT branch
- `pkg/relational/core/embedded/cte_scan.go:181` — CTE-path DISTINCT branch

**There is no separate `select_distinct.go` file** — DISTINCT is a flag inlined into the existing executor paths. Removal is straightforward: reject at parse time when `simpleTable.DISTINCT() != nil`, and delete the three downstream branches + the ORDER BY exemption.

**Java's actual rejection** — verify via the conformance harness with a probe like `SELECT DISTINCT col1 FROM t` against fdb-relational. nightshift-61 listed plain DISTINCT as a "BIG one from CLAUDE.md gotchas" needing alignment; the exact Java error string is what to align Go's rejection message to. Likely shape: `UnableToPlanException` or `UnsupportedOperationException` with a "DISTINCT not supported" / "no rule fires" wording.

**Affected test surface to rewrite** (sized 1 shift in roadmap):
- Roughly 15 yamsql files using plain `SELECT DISTINCT` outside the aggregate path (grep `pkg/relational/conformance/yamsql/testdata/*.yaml` for `DISTINCT`).
- Possibly some sqldriver tests and cross-engine corpus entries (grep `pkg/relational/sqldriver/embedded_fdb_test.go` and `pkg/relational/conformance/plandiff/corpus.go`).

Each affected test either: (a) rewritten to use `GROUP BY` for the dedup semantics, OR (b) converted to a rejection assertion (`error_code: "0AF00"` / `0A000` / whatever Java emits).

**Pin via cross-engine corpus** — once aligned, add a `select_distinct_rejected` corpus entry that asserts `Go.api.Error.Message == Java.JavaError.Message` byte-equal (use `ExpectErrorMessage`, not `ExpectErrorContains`).

**ORDER BY exemption removes mechanically** — the `!sq.distinct` clause at `select_query_full.go:1270` was added because DISTINCT is a Go-only feature with its own pipeline (per nightshift-60 handover line 71). Once DISTINCT is rejected at parse time, the exemption is dead code.

## Phase 1 alternatives if #1 hits a snag

All gate-free, equally pickable:
- **#2** scalar STRING family (UPPER/LOWER/LENGTH/SUBSTRING/TRIM/CONCAT/`||`/REPLACE/LEFT/RIGHT/POSITION/REVERSE) — biggest mechanical removal but cheapest per-function ratio. ~25 yamsql files.
- **#3** scalar ARITHMETIC (ABS/SQRT/POWER) + DATETIME (CURRENT_TIMESTAMP/NOW). ~8 files.
- **#7** WHERE bare-paren — smallest at ~3 files. Half-shift.

The phase-ordering in TODO.md is canonical, but Phase 1 items have no inter-dependencies, so picking by file-count or risk profile is fine.

## Open questions for whoever picks this up

1. **Java's exact rejection message for plain SELECT DISTINCT** — verify before committing the Go alignment. Probe: `SELECT DISTINCT id FROM t` and `SELECT DISTINCT id, val FROM t WHERE val > 0` against fdb-relational. The handover's "BIG one" framing comes from nightshift-61 line 111; the actual error wording isn't yet captured.
2. **DISTINCT-via-GROUP BY rewrite mechanics** — when a yamsql test is rewritten from `SELECT DISTINCT a, b FROM t` to `SELECT a, b FROM t GROUP BY a, b`, the row order may change. ORDER BY may need to be added to keep determinism in the cross-engine harness. Already handled by ORDER BY rejection structural fix nightshift-60, but worth verifying as you rewrite each file.

## Shift hygiene

- PR #131 is **draft** — once the next shift picks up #1 and has working changes to add, ready it for review with `gh pr ready`.
- The TODO restructure can stand on its own (no code changes), so if the user wants the restructure landed independently of the Phase 1 work, they can squash-merge #131 as-is and the next shift starts a fresh branch.
- `TODO_old.md` is committed as an archive — drop it whenever the team feels the historical detail is no longer needed. Suggested removal: after Phase 1 lands.
