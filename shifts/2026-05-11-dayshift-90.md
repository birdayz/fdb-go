# dayshift-90

- **Date:** 2026-05-11
- **Actual start:** ~11:46 CEST
- **Actual end:** ~12:15 CEST (writing handover)
- **PR:** #176

## What was done

### Accurate ColumnTypeNullable metadata (#33 driver quality)

Fixed `ColumnTypeNullable` to report actual column nullability from proto field cardinality instead of hardcoded `return true, true`. NOT NULL columns (proto REQUIRED) now correctly report `nullable=false`.

Nullable derivation added to all column paths:
- **Scan path** (`deriveColumnsFromPlan` — direct record scan)
- **Projection path** (`deriveColumnsFromProjection` — SELECT with expressions)
- **GROUP BY path** (`buildAggColumns` — group keys from proto field)
- **Aggregate path** (`buildAggColumns` — COUNT/SUM/etc. always nullable)
- **JOIN path** — inherits from underlying source derivations (no change needed)
- **Computed expressions** — default to nullable (conservative, correct)

## Stats

- **2 commits**, +33 / -2 LOC
- **2 files changed**
- **46 test targets pass**

## What to work on next

1. **#33 driver adapter gaps** — NamedValueChecker done (dayshift-89), ColumnTypeNullable done (dayshift-90), ColumnTypeLength for DATE/TIMESTAMP done (dayshift-89). Remaining: custom Scanner/Valuer for Struct/Array/Versionstamp/Continuation. Low priority.

2. **#34 perf benchmarks** — Go-side complete. Needs Java conformance server.

3. **#36 catalog wire format reverse** — needs Java conformance server.

4. **Performance** — JOIN 38K allocs could benefit from streaming NLJ.
