# nightshift-68

**Date:** 2026-05-02  
**Actual hours:** 22:00 – 05:30 CEST  
**PR:** #136  

## What was done

### Phase 2 — Cascades core machinery (completed this shift)

1. **Memo implementation** — cross-Reference equivalence-class sharing with self-reference guard and MemoizeExpression. FuzzPlanner_MemoConsistency verified.
2. **Per-rule task granularity (TODO #11)** — TransformReferenceTask + SaturationCheckTask replace monolithic ApplyRulesTask; determinism fix (leafRefs slice + ordered candidates); FuzzPlanner_Determinism verified 5.8M execs.
3. **B5 Batch A index rules (TODO #12)** — MatchCandidate, ValueIndexScanMatchCandidate, RecordQueryIndexPlan, physicalIndexScanWrapper, ImplementIndexScanRule, InComparisonToExplodeRule, PlanContextBuilder. 20+ tests + 3.8M fuzz execs.

### Phase 3 — Cascades rules (continued)

4. **LIMIT/OFFSET support** — LogicalLimitExpression, LimitMergeRule, PushLimitThroughProjectionRule, NoOpLimitElimRule, ZeroLimitRule, ImplementLimitRule, PushLimitThroughUnionRule. Fuzz targets verified.
5. **JOIN support** — SelectExpression (INNER JOIN) conversion, ImplementNestedLoopJoinRule, RecordQueryNestedLoopJoinPlan.
6. **Aggregation pipeline** — GroupByExpression, ImplementStreamingAggregationRule, ImplementHashAggregationRule, StreamingAggFromIndexRule, AggregateDataAccessRule, PushFilterThroughGroupByRule, DistinctOverGroupByElimRule.
7. **ImplementProjectionRule + RecordQueryProjectionPlan** — new this mini-session.
8. **ImplementValuesRule + RecordQueryValuesPlan** — new this mini-session.
9. **PushLimitThroughUnionRule** — with divergence guard (allHaveLimit check).

### Phase 4 — PlanGenerator complete (TODO #20, DONE)

10. **Text predicate parser** — full coverage: comparison ops (=, !=, <>, <, >, <=, >=), AND/OR with correct precedence, BETWEEN, IN/NOT IN, LIKE/NOT LIKE with ESCAPE, IS [NOT] NULL, IS [NOT] DISTINCT FROM, STARTS_WITH(), NOT prefix, parenthesized expressions, dotted-ref column refs.
11. **Arithmetic expression parsing** — recursive descent with correct precedence (+- low, */% high), parenthesized sub-expressions, left-associativity via rightmost-operator-first.
12. **Function-call text→Value** — UPPER(x), COALESCE(a,b), nested (UPPER(LOWER(x))), mixed with arithmetic (ABS(x-1)), zero-arg (NOW()), malformed-input safety.
13. **LogicalValuesExpression** — leaf source for SQL VALUES (a,b,c), wired through converter.
14. **UTF-8 safety** — indexFoldASCII byte-level approach across all keyword-matching functions. Fuzz-verified.

### Testing & quality

- 108 commits on this branch
- 44/44 test targets pass, all green
- 8 fuzz targets: FuzzPlanner_Determinism, FuzzPlanner_MemoConsistency, FuzzPlanner_Limit_NoPanic, FuzzPlanner_LimitOverUnion_NoPanic, FuzzPlanner_ProjectionPipeline_NoPanic, FuzzPredicateTextParser, FuzzConvert, FuzzConvertAndPlan
- Combined fuzz: 30M+ executions across all targets, zero failures
- E2e pipeline tests: complex query (Limit+Sort+Project+Filter+Scan), INSERT+VALUES, sort-elim-through-projection, projection merge

## Current state

- **Tests:** 44/44 pass
- **CI:** green
- **Rule count:** 40 logical (DefaultExpressionRules) + 18 physical (BatchAExpressionRules) + 3 DML = 61 total
- **Physical plan types:** Scan, IndexScan, Filter, Sort, Distinct, TypeFilter, Union, Intersection, StreamingAgg, HashAgg, NestedLoopJoin, Limit, Insert, Delete, Update, Values, Projection (17 types)
- **TODO #10, #11, #12, #20:** all DONE

## Known issues / tech debt

- Sort-elimination through projection doesn't fire when index scan is behind filter+projection (needs deeper rule integration — the index rule fires on the scan, but the ordering doesn't propagate up through the filter wrapper's HintOrdering → projection's HintOrdering chain correctly in all topologies)
- No `ImplementProjectionRule` test in the cascades package itself (tested via e2e in plangen only)
- `ExplainValue` used for equality comparison in LogicalValuesExpression and RecordQueryValuesPlan (adequate but not structurally rigorous)

## What to work on next

1. **#21 C2 QueryExecutor** — SINGLE HIGHEST-LEVERAGE ITEM. All gates satisfied (#11, #12, #20 done). Execute `RecordQueryPlan` against `FDBRecordStore`, return `RecordCursor`. This is the integration that makes the entire Cascades optimizer actually useful at runtime. Start with a 1-shift prototype spike: scan plan → cursor, filter plan → filtered cursor, then expand.
2. **#15 B5 Batch B2** — ImplementRecursiveDfsJoinRule (needs CTE infrastructure). Gate: #14. Lower priority than #21.
3. **Sort-elim-through-projection** — the HintOrdering plumbing is in place but the rule interaction needs debugging. Minor; fix opportunistically.
4. **Extend corpus** — #8 is done but opportunistic additions welcome.
