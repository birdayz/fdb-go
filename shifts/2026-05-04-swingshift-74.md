# swingshift-74

- **Date:** 2026-05-04
- **Actual start:** ~21:03 CEST
- **End:** 22:00 CEST (wind-down from 21:30)
- **PRs:** #148

## What was done

### #25 — ORDER BY fallback removal

Cascades planner failure now returns `"Cascades planner could not plan query"` error instead of silently falling back to the naive generator. Matches Java's `UnableToPlanException`.

**Changed:**
- `cascades_generator.go`: planner failure (nil bestExpr or error), plan extraction failure (no planExtractor interface, nil physical plan) all return `api.Error{Code: ErrCodeUnsupportedQuery}` with the Java-aligned message
- Translation returning nil (recursive CTEs, HAVING, outer joins) still falls back to naive — correct safety valve for genuinely unsupported structures
- `TestFDB_CascadesOrderByNoIndex`: renamed from `TestFDB_CascadesOrderBy`, now asserts the error message instead of checking sorted results
- `TestFDB_CascadesOrderByWithIndex`: unchanged, still verifies ORDER BY works when an index satisfies the ordering

## Current state

- **47 test targets, all pass** (same as shift start)
- CI green on all pushes
- #25 closed in TODO.md

## Known issues

- CTE+ORDER BY and JOIN+ORDER BY queries still route through naive fallback — the Cascades translator translates them successfully, but the logical plan builder or translation chain returns nil before reaching the planner for these shapes. Needs investigation: either the `buildLogicalPlanForQueryWithCatalog` isn't producing a valid logical plan for these compound shapes, or the translator's CTE scope interaction with ORDER BY causes a nil at some stage. The non-CTE `SELECT name FROM Item ORDER BY name ASC` correctly errors.
- All known issues from swingshift-73 still apply (recursive CTEs, HAVING, LEFT/RIGHT outer joins, CTE column aliases)

## What to work on next

1. **#65 remaining** — investigate why CTE+ORDER BY falls back to naive (trace the nil-return path); recursive CTE→RecursiveUnion; HAVING predicate resolution
2. **#72 full port** — Value.pullUp/pushDown for ordering translation through value hierarchies
3. **Phase 5** — #26 DATE/TIMESTAMP, #31 plan cache integration, #33 driver adapter gaps
