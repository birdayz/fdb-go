# swingshift-70

- **Date:** 2026-05-03
- **Actual start:** ~13:38 CEST
- **End:** 21:30 CEST (wind-down from 21:30)
- **PRs:** #139 (merged), #140 (merged), #141 (merged), #142 (merged)

## What was done

### TODO #17 — DONE (all 6 ImplementationRules)
- ExpressionProperty framework (singleton keys, PropertyMap, 30+ plan type computation)
- PlanPartition with property maps (ToPlanPartitions, RollUpPlanPartitions)
- PlanPropertiesMap per-Reference storage, wired into PLANNING phase
- 6 ImplementationRules ported with proper Java-aligned ordering:
  - ImplementSimpleSelectRule (filter + map + Existential→FirstOrDefault + nullOnEmpty→DefaultOnEmpty + tautology filtering + alias rebasing)
  - ImplementDistinctUnionRule (ordering merge + PK compatibility + comparison key enumeration + constraint pushing)
  - ImplementInJoinRule (ordering-aware source selection via ComparisonRange→Comparison→GetCorrelatedTo)
  - ImplementInUnionRule (ordering-aware binding adjustment)
  - ImplementUniqueRule (absorbs when inner is distinct)
  - ImplementUnorderedUnionRule (simple concatenation)
- DefaultImplementationRules() factory
- Two-pass PLANNING phase (top-down constraints + bottom-up implementation)
- Plan extraction fix (prefer FinalMembers after PLANNING)

### TODO #23 — DONE (continuation propagation)
- ResultSet.Continuation() propagates cursor continuation bytes
- Wire format inherited from key-value cursor (conformance-tested)

### TODO #65 — Major progress (Cascades query routing)
- TranslateToCascades: LogicalOperator → RelationalExpression (7 operator types)
- cascadesGenerator + cascadesPlan: full Execute path
- QueryEngine enum + SetQueryEngine + DSN ?engine=cascades
- **Optimizer PROVEN to choose IndexScan** (TestPlanChoice structural assertion)
- Real index metadata wired from catalog via GetMatchCandidates
- Explain() shows physical plan from Cascades planner
- **11 FDB integration tests: scan, eq filter (int+string), inequality, not-equal, multi-predicate AND, OR, projection, DISTINCT, COUNT(*), SUM**
- metadataPlanContext from catalog (PK columns)
- Column metadata derivation from proto descriptor
- cascadesRows wrapping ResultSet as driver.Rows
- **7 FDB integration tests proving end-to-end: scan, equality filter (int + string), inequality filter, multi-predicate AND, projection, COUNT(*) ALL PASS**

### Infrastructure
- RichOrdering with Satisfies, Merge, Concat, Enumerate, DirectionalParts
- RequestedOrdering + ProvidedSortOrder + OrderingBinding
- PlannerConstraint system for ordering constraint propagation
- CrossProduct[T], InSource hierarchy, SetPlan helpers
- Comparison.GetCorrelatedTo, RichOrderingHinter on index scans
- Value.RebaseValue for alias remapping
- IsTautology/IsContradiction predicates
- PlanHash for cache key (#31)
- DATE/TIMESTAMP type codes + singletons + promotion (#26)
- 9 new plan types + 9 physical wrappers
- Executor: all new plan types executable
- PlanContext.GetPrimaryKeyColumns + PrimaryScanRule PK wiring
- NamedPhysicalQuantifier, ForEachNullOnEmptyQuantifier
- 200+ new tests, 200k+ fuzz executions, 3 FDB integration tests

## Current state

- **46 test targets, all pass** (75 build targets total)
- CI green on all pushes
- Cascades optimizer proven against real FDB for 11 query shapes
- **Optimizer intelligence proven** (6 plan choice tests):
  - Single/multi-column index selection
  - Partial prefix matching
  - Unique index point lookups
  - Correct rejection when no index matches
  - Correct selection among multiple candidate indexes

## Known issues

- InJoin/InUnion executor stubs (delegate to inner, don't iterate IN-values)
- MergeSortUnion executor delegates to concat (no merge-sort cursor)
- toPartitionsFromMap first-wins for ordering/PK (documented, low-risk)
- Parser rejects LIMIT/ORDER BY per Java alignment (not Cascades failures)

### Divergence audit (end-of-shift)
- 3 subagent audit comparing Go ImplementationRules + executor against Java 4.11.1.0
- 12 concrete items filed as TODO #66-#77 (PR #142, merged)
- Executor subagent confirmed: InJoin fully non-functional, MergeSortUnion produces unordered results, PredicatesFilter missing async/correlation binding

## What to work on next

1. **#67** PartiallyOrderedSet infrastructure — gates #66, #68, #70, #71 (the critical path for full Cascades alignment)
2. **#65 completion** — test more shapes through Cascades (JOIN, aggregate), ORDER BY routing
3. **#76** Executor stubs — InJoin iteration, MergeSortUnion merge cursor (can unblock once #67 ordering infra exists)
4. **#25** ORDER BY fallback removal — blocked on #65 fully routing ORDER BY queries
5. **#26** DATE/TIMESTAMP completion — type codes landed, needs DDL parser + executor value handling
6. **#31** Plan cache — PlanHash landed, needs SQL text → cache key integration + cache API
