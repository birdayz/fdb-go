# swingshift-35 Handover

**Date:** 2026-04-19
**Shift start:** ~14:00 CEST
**Shift end:** ~22:00 CEST (handover drafted ~16:00, extended ~18:00, finalising at end-of-shift)
**PR:** #84 — `swingshift-35: SQL correctness — NULL tri-state logic, aggregate NULL handling`
**Commit count:** 41+ on the branch (5 rounds of LGTM)

---

## What was done

The dayshift-34 handover left a 5-agent QA audit listing HIGH + MEDIUM + architecture gaps. This shift cleared **every HIGH bug**, **every bounded MEDIUM item**, **one architecture item**, added SQL-layer fuzz coverage + typed error-path assertions, and caught three additional Java-conformance bugs via a mid-shift audit (ORDER BY NULL ordering, GROUP BY NULL hash collision, COUNT(DISTINCT) type-tag). The only intentionally-open MEDIUM is the 8 datatype panics — discovered to be Java-conformant during that audit (Java also throws unchecked RuntimeExceptions for the same conditions).

### HIGH — correctness / Java conformance

**1. NOT of UNKNOWN returns UNKNOWN** (`6853cee5` → `35bea829`)

`NotExpression` was `!v` on a bool that had already collapsed NULL→false at the leaves, so `NOT (x = NULL)` returned TRUE and every row matched. Same pattern in `NOT LIKE NULL`, `NOT IN (NULL)`, `NOT BETWEEN NULL`, `NOT (UNKNOWN AND TRUE)`, `NOT (UNKNOWN OR FALSE)`.

Fix: introduced Kleene three-valued `triBool` (triTrue/triFalse/triNull) and `evalExprPredicateTri` / `evalHavingTri` / `evalPredicateOnMapExprTri` + leaf Tri variants for comparison, IN, LIKE, BETWEEN. The existing bool-returning functions become thin wrappers that collapse UNKNOWN→false at the WHERE/HAVING/ON filter boundary — matching SQL semantics (UNKNOWN filters the row out, same outcome as FALSE, but NOT of UNKNOWN stays UNKNOWN internally). AND/OR short-circuit on the dominant concrete value (FALSE for AND, TRUE for OR) but combine through `triAnd`/`triOr` otherwise.

**2. Mixed-type equality via stringification** (`6853cee5`)

`valuesEqual` and `compareValues` fell through to `fmt.Sprintf("%v", ...)` for cross-type pairs, so `'5' = 5`, `n IN ('5', 6)` (n=int), and similar returned TRUE. Leaked into IN, NULLIF, subquery IN.

Both helpers now: preserve int64/float64 numeric coercion (same SQL "number" family), compare strings to strings, bools to bools, []byte to []byte, and return false (valuesEqual) / non-zero by type-name (compareValues) when concrete types are genuinely different.

**3. Arithmetic unification** (`ad249d55`)

`applyMathOp` (proto path) and `applyArithmeticOp` (map/JOIN/CTE path) had diverged:
- Div/0: proto errored (SQL standard), map silently returned NULL.
- `%` operator: map supported, proto missing.
- int64 preservation: proto yes, map always promoted to float64.

Collapsed both to one canonical `applyMathOp`; `applyArithmeticOp` now a one-line delegate so they cannot drift again.

**4. Aggregate NULL semantics** (`c370213e`)

Five sub-bugs, all in the aggregate accumulator loops (proto path in `execSelectByIndex`, map path in `aggregateMapRows`, fixed symmetrically):

- `COUNT(col)` previously counted NULLs — counter was bumped unconditionally. Now only increments on non-null. `COUNT(*)` still counts every row (special-cased by `aggArg == ""`).
- `SUM` of empty-or-all-NULL returned 0. SQL spec is NULL — emit nil when no non-null contribution was observed (tracked via `counts[i]`, which doubles as "non-null seen" after the COUNT fix).
- `SUM`/`AVG` silently treated non-numeric values as 0 (`toFloat64` `_` on ok). Now returns `ErrCodeInvalidParameter` with the offending type.
- Ungrouped aggregate over empty input emitted zero rows. SQL spec is one row (COUNT=0, SUM/MIN/MAX/AVG=NULL). Added a synthetic empty group.
- `COUNT(*)` inside `GROUP BY` was implicitly broken by the NULL-skip fix (empty aggArg means column lookup yields nil). Special-cased ahead of the NULL-skip check.

**5. Catalog subspace Java compatibility** (`04d03398`)

`DefaultCatalogSubspace` packed two string tuple elements (`"__SYS", "CATALOG"`), but Java's `RelationalKeyspaceProvider` declares:

```
KeySpaceDirectory(SYS,     KeyType.NULL)            -> NULL
  KeySpaceDirectory(SYS,   KeyType.NULL)            -> NULL
    KeySpaceDirectory(CATALOG, KeyType.LONG, 0L)    -> int64 0
```

So the actual catalog prefix Java writes is tuple `(NULL, NULL, 0L)`, which packs to `0x00 0x00 0x14`. Our previous two-string prefix meant a Go-written catalog was at a completely different subspace than a Java-written one — **blocking for the cross-language goal**.

Fix: `subspace.Sub(nil, nil, int64(0))` produces the exact prefix Java uses. `TestDefaultCatalogSubspaceBytes` pins `0x000014` so any future drift is caught at unit-test time rather than by a cross-language conformance run.

### MEDIUM

**6. CAST(NULL AS type), ABS overflow, integer-arg validation** (`1707446b`)

- `CAST(NULL AS <type>)` returned "unsupported CAST" error instead of NULL. Early-return `nil, nil` in `castValue`.
- `ABS(math.MinInt64)` silently wrapped back to MinInt64 (two's-complement negation is undefined for the smallest int64). Return `ErrCodeInvalidParameter` matching MySQL/Postgres behaviour.
- `LEFT` / `RIGHT` / `SUBSTRING` length/position args used a blind `int64` type assertion. Any float, string, bool, or NULL silently became 0, truncating to empty string. Replaced with `toIntegerArg` helper: accepts int64, accepts whole-valued float64 as a convenience, errors on fractional floats and non-numeric types.

**7. fmt.Errorf → api.Error sweep** (`d8defe3f`)

~30 production `fmt.Errorf(...)` sites across metadata, catalog, embedded, keyspace, and ddl dropped the SQLSTATE ErrorCode, so `errors.As(&apiErr)` silently missed them. Users couldn't map returned errors to SQLSTATE — violated the project error-handling rule.

Added `api.WrapErrorf(cause, code, format, args...)` mirroring the `fmt.Errorf("ctx: %w", err)` idiom but preserving an explicit code. Every production `fmt.Errorf` swept to `api.WrapErrorf` or `api.NewErrorf` / `api.NewError`. Codes chosen by call-site semantic: InvalidSchemaTemplate, InvalidParameter, UnsupportedOperation, InternalError.

**8. ResetSession clears per-request state** (`fcf64daa`)

Previously ResetSession only restored `schema`. Other per-request state (activeTx, ctes, schemaCache) leaked into the next pooled checkout:

- `activeTx`: if a query panicked mid-transaction, next checkout inherited an open (stale) FDB context.
- `ctes`: usually self-clearing via defer, but panics/errors could leave the map populated → phantom CTE names.
- `schemaCache`: entries survived across checkouts even after the underlying schema evolved → stale descriptor.

ResetSession now rolls back active tx, nils ctes, resets the schemaCache map, and restores `defaultSchema`.

**9. MetaDataEvolutionValidator wired into CreateTemplate** (`bef810c0`)

Validator already existed in recordlayer (exercised by 23 unit tests) but `CreateTemplate` never invoked it — a Go writer could save a schema-template evolution that Java's validator would reject. On `CreateTemplate`, if a prior version exists and the new SQL-layer version strictly exceeds it, run `MetaDataEvolutionValidator.Validate(prior, new)` and wrap any failure as `ErrCodeInvalidSchemaTemplate`. Uses `SetAllowNoVersionChange(true)` because SQL-layer versions are independent from RecordMetaData versions.

**10. CTE scope stack** (`c92301ed`)

`c.ctes` was a single flat map. Two bugs:
- Nested `WITH C AS (...)` inside a subquery/derived table overwrote the outer map with `make(map[...])`, losing every outer CTE. Defer then wiped on return.
- Derived-table aliases materialised inside inner queries stayed visible in outer scope until the outermost SELECT unwound.

Added `pushCTEScope()` returning a pop closure. Each entry point that introduces CTE names (`execSelect` with WITH; `execSelectQuery` with derived-table FROM) does `defer c.pushCTEScope()()`. New scope inherits the outer map so inner queries can still reference outer CTEs, but pops back to the exact outer map on exit.

### Review feedback

**11. NULL-in-IN-list correctness hole** (`4570981c`, reviewer-caught)

SQL §8.4: `x IN (1, NULL)` and `x NOT IN (1, NULL)` yield UNKNOWN when no element matches, because the expansion expands into an AND/OR chain with `x = NULL` as UNKNOWN. Both proto and map paths now track `hadNullElement` during the list scan, skip NULL entries from equality, and after the loop emit `triNull` if any NULL was seen and nothing matched.

**12. evalHavingTri NOT error guard** (`4570981c`, reviewer-caught)

When inner expression errored, zero-value `v` was `triFalse` and `v.Not()` returned `triTrue`, burying the error. Fixed to return `triFalse, err` on error, matching `evalExprPredicateTri`'s pattern.

**13. SUM `|| aggDistinct` guard removed** (`4570981c`, reviewer-caught)

Papered over the DISTINCT SUM path not accumulating into `sums[i]`. Guard removed, TODO added marking the deeper gap.

### Architecture

**14. Transaction.Unwrap() substitutability** (`e855b188`)

Catalog impls asserted concrete `*FDBTransaction` / `*InMemoryTransaction` — a future remote/gRPC or middleware Transaction impl would be rejected at runtime. Added `Unwrap() any` to the `api.Transaction` interface mirroring Java's `<T> T unwrap(Class<T>)`. All five concrete-type assertions (unwrapFDB, checkOpenTxn, create/drop schema FDB store) now go through `txn.Unwrap()` so a decorator forwarding Unwrap continues to satisfy them. MockTransaction regenerated via `go generate`.

### Testing

**15. FuzzParse** (`15cfd8f2`) — first SQL-layer fuzz target (record-layer has 24; relational had zero). Drives the public `Parse()` with arbitrary byte sequences and asserts: no panic, success returns `(root, nil)`, failure returns `(nil, *api.Error with ErrCodeSyntaxError)`. 21-entry seed corpus mixes well-formed SQL with ANTLR pathological cases (unterminated string / comment, leading NUL, control bytes, invalid UTF-8, 100-deep nesting). 20s continuous run: 3.6M executions on 24 workers, no crashes.

**16. TestFDB_ErrorPathSQLSTATE** (`57198867` + later expansions) — error-path coverage was ~0.2% (2/862 error asserts). Now table-driven with **14 error paths** (syntax, unknown table/column, div/0, mod/0, ABS overflow, SUBSTRING fractional length, duplicate DATABASE, DROP non-existent DB/schema, unknown template, INSERT omitting NOT NULL PK, INSERT NULL into NOT NULL, UPDATE SET col = NULL on NOT NULL). Every case verifies `errors.As` extracts an `*api.Error` with the Java-matching SQLSTATE.

Along the way: "table X not found" → `ErrCodeUndefinedTable` (42F01) at 5 sites, "column X not found" → `ErrCodeUndefinedColumn` (42703) at 7 sites — both had been returning `ErrCodeInvalidParameter` (22023), wrong per Java's `ErrorCode.java`.

### Later additions (post-LGTM #4, Java-conformance audit)

**17. ORDER BY NULL ordering** (`3b87574d`) — user prompt "always look in light of strict Java conformance" triggered a sweep. `compareValues` returned NULL > non-NULL, so ASC put NULLs last — the opposite of Java's default (`ASC NULLS FIRST`, `DESC NULLS LAST` per `ParseHelpers.isNullsLast`). Flipped the NULL direction; all MIN/MAX/BETWEEN/comparison callers filter NULL before entering compareValues so the flip is transparent. New test `TestFDB_OrderByNullOrdering` pins both directions.

**18. GROUP BY NULL-vs-"<nil>" collision** (`b059485e`) — `groupByKey` used `fmt.Sprintf("%v", nil)` which renders as `"<nil>"`, colliding with the literal string. Length-prefixed type-tagged encoding mirrors `rowKey`'s pattern. Test `TestFDB_GroupByNullVsNilString` pins one NULL row + two '<nil>' rows → two groups (pre-fix: one).

**19. COUNT(DISTINCT) type-tagged key** (`ee249664`) — same `%v` collision in distinct-set construction. Type-tag (`%T\x00%v`) keeps int 5 and string "5" apart, matching `valuesEqual`'s mixed-type-equality rule.

**20. MetaDataEvolutionValidator revert** (`0bc6fd3f`) — caught via the Java-conformance audit. Initial wiring added validation to `CreateTemplate` that Java's `RecordLayerStoreSchemaTemplateCatalog.createTemplate()` does NOT do — Go was rejecting evolutions Java accepts. Reverted. TODO.md notes the gap exists in both codebases; upstream discussion warranted before unilateral Go-side enforcement.

**21. NOT NULL enforcement (INSERT/UPDATE/INSERT…SELECT)** (`1f389611`, `e9959ba9`, `38410fec`) — INSERT with explicit NULL, INSERT omitting a NOT NULL column, UPDATE SET col=NULL, and INSERT…SELECT with a NULL from the source all now return `ErrCodeNotNullViolation` (23502) matching Java. Previously surfaced as proto missing-required-field serialization error.

**22. Subquery IN + NULL row (SQL §8.4)** (`aba441f0`) — `matchSubqueryIN` silently skipped NULL values in subquery result rows; `x NOT IN (SELECT n FROM t)` with a NULL row returned TRUE instead of UNKNOWN. Widened to return `triBool` with a `hadNull` flag. Test `TestFDB_SubqueryInNullRow` covers both IN and NOT IN.

**23. DISTINCT aggregate accumulation** (`b206e210`) — SUM/AVG/MIN/MAX with DISTINCT only incremented `counts[i]` in the dedup-set-miss branch; the per-function accumulators never saw the stream. Now dispatches on aggFunc symmetrically with the non-DISTINCT path. Test `TestFDB_DistinctAggregates` pins the expected values on a multi-duplicate + NULL input.

**24. Additional corpus** — FuzzParse 4-min run surfaced an ANTLR exponential-time adversarial input (3.4KB deep-parens, 8.7s parse) — pinned as regression; same grammar as Java so the issue exists there too, TODO entry for upstream. Parser corpus entry `a1c9802306691af3`.

---

## Current state

- **PR #84**: 20 commits, 3 rounds of "Good to merge" from reviewer (most recent on the ResetSession + EvolutionValidator + CTE scope + fuzz batch). Final review requested after the Unwrap / UndefinedTable fix commits; pending at time of writing.
- **All 25 Bazel test targets pass.** CI is green end-to-end (codegen gate, build/lint/test, race detector, govulncheck).
- **Test count**: `pkg/relational/sqldriver:sqldriver_test` has 135 `TestFDB_*` tests (+11 new in this shift). Plus new unit test cases (`TestValuesEqual` mixed-type, `TestEmbeddedConnection_ResetSessionClearsPerRequestState`, `TestDefaultCatalogSubspaceBytes`, `TestTriBool` exhaustive Kleene truth table) and new fuzz targets (`FuzzParse`, `FuzzDeserializeTemplate`). Integration `TestFDB_ErrorPathSQLSTATE` now table-drives 14 error paths.

## TODO.md updates

Every fixed item in `TODO.md` § "Next-shift priority list" is now marked `[x]` with a brief description of what landed and in which commit. Unchanged `[ ]` items are the remaining work (see Next priorities below).

---

## Known issues / tech debt

**Surfaced this shift (added to TODO.md):**

- **DISTINCT SUM never accumulates.** The `|| ac.aggDistinct` guard I added initially papered over the fact that `aggregateMapRows`/proto DISTINCT path increments `counts[i]` but never writes into `sums[i]`. Guard removed in review; SUM of all-NULL DISTINCT groups now correctly emits NULL, but SUM(DISTINCT col) on a non-empty group returns 0 (float64 zero value) instead of the distinct sum. Needs a separate distinct-value-sum pass.
- **Subquery IN + NULL rows.** `matchSubqueryIN` silently skips NULL values in subquery result rows. `x NOT IN (SELECT n FROM t)` with a NULL row should return UNKNOWN when no concrete match found. Same SQL §8.4 rule as the NULL-in-list-literal fix but for subqueries. Pre-existing, surfaced by the reviewer.

**Pre-existing, still open:**

- 8 panics in `api/datatype_*.go` (NullType.WithNullable(false), UnknownType.Resolve, StructType.FieldByIndex OOB, etc.). Converting to returned errors requires widening the `WithNullable`/`Resolve` signatures, which ripples through every DataType implementation and every caller. Defer to next shift.
- The SQL grammar doesn't accept parenthesised expressions inside `WHERE` (parses as RecordConstructorExpressionAtom). A few test cases I wrote had to rely on NOT's precedence instead of explicit parens. Not a correctness issue for this PR but a usability gap — tracked informally.
- SQL grammar doesn't accept NULL as a LIKE pattern literal. Semantic path is covered in the evaluator, but SQL users can't write `WHERE s LIKE NULL` directly.

---

## Next priorities (for nightshift-36)

Ordered by impact. The first item is what the user explicitly asked this shift to leave for night shift.

1. **Java↔Go SQL conformance harness** *(highest-ROI test gap; user green-lit for night shift)*. `conformance/sql/` directory with `.sql` + `.json` expected-output files; drive both Go (`sql.Open("fdbsql", ...)`) and Java (`EmbeddedRelationalConnection` via the Bazel-built conformance server) against the same inputs; diff result sets. Seed with the existing yamsql corpus (1587 statements already parse — just execute them and diff). Also run write-in-Go / read-in-Java round-trips and reverse to catch wire-format drift — would have caught the catalog subspace bug on its own. Now that the catalog subspace + NOT tri-state + aggregate NULL + mixed-type equality fixes are in, the harness will measure real semantic drift rather than known divergences. Opt-in target (`just conformance-sql`), gated behind `@manual` to stay out of default `bazelisk test //...`.
2. **DISTINCT SUM path** — accumulate distinct non-null values into `sums[i]` inside the DISTINCT branch, guarded by a per-agg-col distinct-set.
3. **Subquery IN + NULL rows** — thread a `hadNull` flag through `matchSubqueryIN`, same shape as the list-literal fix.
4. **8 panics in `api/datatype_*.go`** — widen `WithNullable` / `Resolve` signatures to return errors; audit all callers. Breaking interface change, needs its own shift.
5. **More fuzz targets**: `FuzzEvalExpr` (via a parser frontend), `FuzzContinuationToken`, `FuzzSchemaTemplateProto`.
6. **Split `connection.go`** (5498 lines → ~12 files). Mechanical, delegation-friendly.
7. **Break up `evalScalarFunctionCallCore`** (576-line switch) by family (`evalStringFns`, `evalMathFns`, …).
8. **Planner / Plan seam** before any query-planner work starts — so Cascades has somewhere to plug in.
9. **Typed enums for `joinType` / `aggFunc`** (currently magic strings).
10. **Expand error-path test coverage** beyond the 8 cases landed this shift.

---

## Shift working notes

- **Review cadence**: four rounds of @claude review, each under 5 minutes. First found two real bugs (evalHavingTri NOT error guard + NULL-in-IN-list). Second/third/fourth were LGTMs with minor polish suggestions. Very productive feedback loop.
- **Java-conformance audit mid-shift**: user's explicit reminder ("always look at this in light of strict Java conformance") prompted a spot-check of recent changes. Found one real divergence (MetaDataEvolutionValidator wiring in CreateTemplate — Java doesn't validate at that layer; reverted) and one tighter conformance fix (ErrCodeUndefinedColumn 42703 for "column not found" — Java uses it, Go was returning ErrCodeInvalidParameter 22023 at 7 sites). Verified: tri-state NOT matches Java's `NotComponent.java:60` (`if (v == null) return null`); Unwrap() any is a legitimate Go-idiom analogue of Java's `<T> T unwrap(Class<T>)`; datatype panics match Java's unchecked RuntimeException path (Java *also* panics via `RelationalException.toUncheckedWrappedException()`) — so CLAUDE.md's "no panic" rule yields to Java-conformance here. Takeaway: when CLAUDE.md's general-case rule conflicts with Java behaviour, Java wins.
- **One large task at a time** held throughout. No parallel subagents on implementation work.
- **Grammar quirks**: parenthesised boolean expressions (`NOT (x = NULL)`) parse as RecordConstructorExpressionAtom and fail. Worked around by relying on SQL precedence (`NOT x = NULL`). Logged for grammar work. `NOT LIKE NULL` is rejected at parse time — semantic path tested via unit logic.
- **gofumpt** is enforced via nogo; `gofumpt -w <file>` after editing saves a CI trip.
- **Mock regeneration** via `go generate ./pkg/relational/api/...` automatically picks up new interface methods.
- **`just gazelle`** is required after any file add/remove; the BUILD.bazel sync is mechanical but must be committed.
