# swingshift-33 Handover

**Date:** 2026-04-19  
**Shift start:** ~22:00 CEST (continuation of prior session)  
**Shift end:** ~08:30 CEST  
**PR:** #82 — `feat(relational/sql): swingshift-33 SQL engine features`

---

## What was done

### Reviewer feedback addressed (commit 027278d6)
All 5 items from PR #82 review:
1. `filterSysRows` → now uses `evalPredicateOnMapExpr` (full predicate tree for INFORMATION_SCHEMA WHERE)
2. `execInsertSelect` — documented TOCTOU window (SELECT + INSERT are separate transactions)
3. `evalExpr` inline comment — fixed "no predicate modifier" inaccuracy
4. `execSelect` stale comment — updated to reflect JOIN/GROUP BY/HAVING support
5. `MOD` type promotion — `MOD(7, 3.0)` now returns float64; int+int stays int64; div-by-zero errors

### RIGHT JOIN correctness fix (commit 7de181fc)
- Previous implementation used fragile key-value equality heuristic to detect unmatched right rows — broke when both tables had a column named `id`
- Fixed to track matched right rows via `matchedRight[]` boolean slice (parallel to rightRows)
- `TestFDB_RightJoin`: orphan order with no matching customer → NULL name ✓

### COUNT(DISTINCT col) (commit e52056e4)
- `aggSelectCol.aggDistinct bool` field + `extractAggFunc` returns 5-tuple including `distinct`
- Distinct-set tracking per group (`map[string]struct{}` initialized only for DISTINCT aggs)
- Works with and without GROUP BY
- `TestFDB_CountDistinct`: ungrouped (3 distinct customers) + grouped by region ✓

### GREATEST/LEAST scalar functions (commit 1c6a6cf4)
- `GREATEST(a,b,c)` / `LEAST(a,b,c)`: NULL-skipping multi-arg min/max
- `TestFDB_GreatestLeast` ✓

### JOIN + GROUP BY / aggregates (commit 44c9dbe4)
- `execSelectJoin` now supports GROUP BY with COUNT/SUM/MIN/MAX/AVG, COUNT(DISTINCT), HAVING
- Uses map-based in-memory grouping on filtered map rows
- `TestFDB_JoinGroupBy`: Customer INNER JOIN Order GROUP BY name → COUNT(*) + SUM(amount) ✓

### Reviewer nit (commit a295f70f)
- `evalExpr` docstring updated (was "UPDATE SET expression")
- Inline comment accuracy fix (predicate modifier branch description)

---

## Current state

- **PR #82**: LGTM from reviewer, CI running
- **Test counts**: `TestFDB_*` tests in `pkg/relational/sqldriver/embedded_fdb_test.go` now cover 40+ SQL features
- **CI**: Green on last full run; latest push waiting for CI

## Known issues / tech debt

- `evalInPredicate` for `NULL NOT IN (...)` returns true (should be NULL/unknown per SQL standard — 3-valued logic)  
- `evalExprAtomOnMap` in JOIN path doesn't handle all the same cases as `evalExprAtom` for proto-based queries — the two evaluators are diverging
- INFORMATION_SCHEMA WHERE test coverage only verifies simple equality; compound AND/OR/NOT works via `evalPredicateOnMapExpr` but not integration-tested
- `execSelectJoin` uses nested-loop O(n²) — fine for small tables, not for production query planning
- No subquery support (IN (SELECT ...), EXISTS, correlated)
- No CTE (WITH ...) support

## Next priorities

1. **Address any remaining CI issues** — green before merge
2. **Merge PR #82** once CI passes
3. **Subquery support** (`WHERE col IN (SELECT ...)`) — high impact, complex implementation
4. **Unify proto + map expression evaluators** — evalExprAtom vs evalExprAtomOnMap are diverging; consider a unified evaluator with a `Value` interface
5. **Integration test coverage for filterSysRows AND/OR** — reviewer noted as follow-up
6. **CTE (WITH clause)** — useful for complex queries
