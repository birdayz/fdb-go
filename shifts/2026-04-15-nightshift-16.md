# Nightshift-16 Handover

**Date:** 2026-04-15, started ~23:37 CEST (2026-04-14), ended ~TBD
**PR:** #57

## What was done

### 1. Index edge case tests (13 new tests)

| Index Type | Tests | What they cover |
|---|---|---|
| RANK | 4 | Duplicate scores (10 records same price), empty store scan, out-of-bounds rank, delete-reduces-rank |
| PERMUTED_MIN/MAX | 3 | Min tracking per group + update, delete-of-min record, max with ties |
| TEXT | 3 | Empty string → no tokens, single-char tokens, update changes token set |
| BITMAP_VALUE | 3 | Single position set, delete clears bit, large position (5000) |

### 2. Fuzz targets (2 new + 1 enhanced)

- **FuzzDeserializeAndDiscover** — union wire format record type discovery via hand-rolled protowire parsing. 65M+ executions, 0 panics.
- **FuzzDeserializeRecord** — targeted union field extraction via protowire. 20M+ executions, 0 panics.
- **FuzzNewReader** enhanced — chained ReadNestedReader→ReadNestedReader (2 levels) + ReadVectorElementReader access. 33M+ executions, 0 panics.

### 3. Multi-shard `more` flag regression test

Validates swingshift-15 fix: GetRange returns `more=true` when limit met across shard boundaries. Tests 7 limit values (1,3,7,10,13,25,49) against 50 keys across multiple shards. Paged scan with misaligned page size (7) verifies no keys missed.

### 4. Time window leaderboard coverage gaps

- `EvaluateTimeWindowRankAndEntry`: correct Tuple{rank, score...} return, nil when record outside window
- BY_RANK scan on grouped index: errors when TupleRange missing group prefix
- `asInt64` type converter: int64/int32/int conversions + unsupported types

### 5. DeleteRecordsWhere with split records

New test verifying 250KB split order (3 chunks) is fully cleared while leaving other record types intact.

### 6. Bug fix from review

- Fixed goroutine-safety bug: TestAsInt64 subtests used parent test's gomega instance from parallel goroutines. Moved `g := NewGomegaWithT(t)` inside each subtest.
- Documented why TestMultiShard sub-tests run sequentially (shared container state).

## Verification

| Category | Result |
|---|---|
| Full fuzz campaign (23 targets × 60s) | 22/23 pass, 1 Docker timeout (re-verified clean). 700M+ total executions. |
| API binding stress (30 seeds × 1000 ops) | 30/30 pass, 0 FDB deaths |
| Directory binding stress (50 seeds × 500 ops) | In progress, 23/50 pass so far, 0 failures |
| Race detector (4 test targets) | 4/4 clean |
| CI | Green |
| Review | Approved, no remaining issues |

## Current state

- **15 Bazel test targets pass** (all cached)
- **23 fuzz targets**, 700M+ total executions, 0 failures
- **Race detector**: all targets clean
- **Binding stress**: API 30/30, directory in progress
- **CI**: green

## What to work on next

### HIGH — continue hardening
- [ ] Run extended binding stress (100+ seeds, both API and directory) to verify robustness
- [ ] Profile memory allocations during record scan — are there unnecessary copies?
- [ ] Investigate whether `AutoContinuingCursor` handles transaction timeout mid-page correctly

### MEDIUM — infrastructure
- [ ] Fix binding stress to use testcontainers instead of raw Docker commands (proper lifecycle, cleanup on crash, Ryuk reaper)
- [ ] Add `govulncheck` to CI

### LOW
- [ ] Coverage gaps: metrics.go (57%), coordinator.go (62%) — mostly error branches, not coverage padding
- [ ] Pool frame read buffers (performance)
- [ ] LRU eviction for location cache (performance)
