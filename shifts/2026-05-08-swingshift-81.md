# swingshift-81

- **Date:** 2026-05-08 → 2026-05-09
- **PRs:** #155

## What was done

### Yamsql conformance: 87/98 → 98/98 (88.8% → 100%)

39+ commits, ~5000+ LOC net change across 40+ files.

### Java-alignment refactors (TODO items 1-3)

1. **Merged buildOuterPlanOnDerived into buildSelectShell** (-156 LOC). One function for both derived and non-derived aggregate/sort/projection logic. Eliminates "fixed in one but not the other" bug class.

2. **Eliminated sortOnly/hidden/sentinel mechanism.** Replaced `sortOnly bool` + `hidden bool` with `visible bool` on aggSelectCol. Deleted `__orderby_aggexpr_N__` sentinels. ORDER BY aggregate expressions resolve through the Value-based sort path.

3. **PlanVisitor incremental builder.** Cascades path walks ANTLR directly via PlanVisitor — no extractFromSimpleTable, no selectQuery creation, no _postBuild call. classifySelectElements returns selectClassification for aggregate classification. Catalog-aware upgrades inlined into VisitSimpleTable.

### RFC-002: RequestedOrdering (complete)

All 6 phases done:
- Data structures (pre-existing)
- Constraint propagation (ImplementSortRule + ImplementInMemorySortRule push)
- PushOrderingThroughGroupByRule (new — moves Sort below GroupBy)
- Sort elimination via SortOverOrderedElimRule (pre-existing)
- sortOnly deletion (done this shift)
- Physical plan ordering via HintOrdering (pre-existing)

### RFC-003: Incremental Plan Builder (Phases 1-4 + inline)

- Phase 1: PlanVisitor skeleton + wiring
- Phase 2: 6-step incremental structure (FROM/WHERE/GroupBy/OrderBy/Limit/Distinct)
- Phase 3: visitFrom/Where/Limit/Distinct read ANTLR directly
- Phase 4: classifySelectElements replaces extractFromSimpleTable
- Inline: _postBuild upgrades inlined into VisitSimpleTable

### Conformance fixes

| Fix | Scenarios flipped |
|-----|-------------------|
| Derived table GROUP BY computed expressions | group_by_proj_expr, derived_table_group_by |
| Stale sq.projCols elimination | (structural, prevented regressions) |
| GROUP BY expression key lookup (ExplainValue parens) | group_by_multi |
| 42803 validation for ungrouped outExpr columns | derived_table_group_by |
| GROUP BY alias AS z resolution | group_by_validation |
| MIN/MAX on non-numeric columns (0A000) | scalar_subquery_types |
| CTE scalar subquery scope propagation | scalar_subquery_advanced |
| PK tiebreaker in in-memory sort | order_by_elimination |
| Cross-join derived table qualified column overwrite | derived_table |
| SELECT * cross-join column expansion | ambiguous_column |
| Correlated scalar subquery error code (42703) | correlated_subquery_probes |
| Recursive CTE column alias rename | recursive_cte |
| Recursive CTE UNION DISTINCT dedup | recursive_cte |
| Recursive CTE non-self-ref error code (0A000) | recursive_cte |
| Correlated EXISTS in cross-join (flat SelectExpression) | correlated_subquery_probes |
| Nested EXISTS scope threading | correlated_subquery_probes |

### Cascades planner enhancements

- PushOrderingThroughGroupByRule — enables sort elimination through GROUP BY via index ordering
- ImplementSortRule/ImplementInMemorySortRule push RequestedOrdering to children
- UNION DISTINCT support in recursive CTEs with dedup
- 3-quantifier SelectExpression for join + EXISTS patterns
- NLJ rule extension for join + existential decomposition

## Current state

- 98/98 yamsql conformance (100%)
- 46 test targets all pass
- PlanVisitor is the Cascades path's primary plan builder
- Proto/naive path unchanged (uses extractFromSimpleTable + _postBuild)
- toSelectQuery bridge remains (upgrade functions take *selectQuery)

### Additional Cascades planner work (post-conformance)

- **toSelectQuery bridge eliminated** — selectQuery embeds selectClassification via struct embedding
- **Value.pullUp/pushDown** (TODO #72) — PullUpValue/PushDownValue for arbitrary value hierarchies, 27 tests
- **PushOrderingThroughProjectionRule** — sort keys translate through projection mappings
- **PushOrderingThroughFilterRule** — filter preserves order
- **PushOrderingThroughDistinctRule** — distinct preserves order
- **PushOrderingThrough{Unique,Union,Delete,Insert,Update,TempTableInsert}** — complete Java PushRequestedOrdering* rule set
- **50 expression rules total** (was 40 at shift start)
- **E2E sort elimination through filter** verified

## What to work on next

1. **Cross-engine conformance testing** — run the yamsql scenarios against the actual Java server to catch Go-only divergences not covered by the Go-only harness.
2. **Plan cache** (TODO #31-32) — PlanHash integration with query parser, cache lookup/store API.
3. **Type hierarchy** (TODO #26-28) — DATE/TIMESTAMP types.
4. **Coverage gaps** — fuzz the new PlanVisitor code paths, especially the correlated EXISTS and recursive CTE changes.
