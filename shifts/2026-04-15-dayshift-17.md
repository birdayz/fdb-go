# Dayshift-17 Handover

**Date:** 2026-04-15, started ~10:01 CEST, ended ~12:00 CEST
**PR:** #60

## What was done

### 1. PR #59 review + merge (batch insert optimizations)

Full code review of 2,772-line PR across 21 files. Findings:

| Issue | Severity | Fix |
|---|---|---|
| Data race on `FieldKeyExpression.cachedFD` | Critical | `atomic.Pointer[fieldDescCache]` — race detector confirmed clean |
| `GetSliceOrPanic` in `loadSplitOnly` | Bug | → `GetSliceWithError` |
| `InsertBatch` silently corrupts mixed types | Bug | Per-record type validation with error |
| Compile-time assertion comment misleading | Safety | Rewritten to document inspection-based invariant |
| `EvaluateFlat` unnecessary `evaluateRepeated` | Minor | Return error immediately |

### 2. Correctness tests (35 new specs)

- **22 batch insert tests** (Ginkgo): InsertBatch, SaveRecordBatch, SaveRecordBatchInsertOnly — load-back, counts, VALUE/COUNT/SUM indexes, edge cases, equivalence test
- **13 tuple packing tests** (Go testing): All `Pack*Into` variants verified byte-identical to allocating versions, shared buffer growth safety
- **1 fuzz target**: `FuzzPackIntoEquivalence` — 32.5M executions, 0 failures

### 3. Standard path optimizations

- `saveRecordInternal`: `evaluateKeyFlat` instead of `Evaluate` → SaveRecord 102→100 allocs
- `loadWithSplit`: `PackConcatWithPrefix` instead of `appendToTuple+Pack` → LoadRecord 85→84, DeleteRecord 95→94
- `InsertBatch` docstring fixed: "silent corruption" → "rejected with error"

### 4. Verification

| Category | Result |
|---|---|
| Race detector (4 targets) | Clean — data race fix confirmed |
| Chaos tests (220) | All pass |
| Binding stress (10 seeds × 500 ops) | 10/10 pass, 0 FDB deaths |
| Fuzz (32.5M executions) | 0 failures |
| All 16 test targets | Pass |
| Benchmarks | Alloc reductions confirmed |

## Current state

- **16 Bazel test targets pass** (was 15, added tuple_test)
- **2768 Ginkgo specs** (was 2746, +22 batch insert)
- **24 fuzz targets** (was 23, +1 tuple packing)
- **Standard path**: SaveRecord 100 allocs, LoadRecord 84, StoreOpen 62
- **Batch path**: InsertBatch 461 allocs/50 records (vs 2733 sequential)
- **CI**: green
- **Race detector**: clean on all 4 targets

## Key decisions

- **Batch insert is Go-only API** — Java has no equivalent. Wire-compatible (same FDB mutations). Equivalence test proves identical output to N×SaveRecord.
- **`sync.Map` for `maintainerCache`** — attempted `map[string]` optimization, reverted after concurrent map panic in chaos tests. `sync.Map` overhead (~ns) negligible vs FDB round trips.
- **hyperpb-go not needed** — codebase already uses vtprotobuf (`MarshalVT`/`UnmarshalVT`/`SizeVT`) on all hot paths. Standard `proto.Marshal` is fallback only.

## What to work on next

### HIGH
- [ ] Further optimize standard SaveRecord path — remaining allocs are structural (transport/RYW/conflicts). Could pool read conflict buffers (same pattern as write conflicts in PR #59).
- [ ] Close PR #59 (superseded by PR #60 which contains it)
- [ ] Run extended binding stress (100 seeds) after merge

### MEDIUM
- [ ] AutoContinuingCursor transaction timeout handling (from nightshift-16 handover)
- [ ] Fix binding stress to use testcontainers
- [ ] Add `govulncheck` to CI

### LOW
- [ ] Coverage gaps: metrics.go (57%), coordinator.go (62%)
- [ ] Pool frame read buffers in transport (performance)
- [ ] LRU eviction for location cache (performance)
