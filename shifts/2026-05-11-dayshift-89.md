# dayshift-89

- **Date:** 2026-05-11
- **Actual start:** ~05:20 CEST
- **Actual end:** ~09:55 CEST (writing handover)
- **PR:** #175

## What was done

### Java ExceptionUtil error mapping (1:1 mechanical port)

Complete port of Java's `ExceptionUtil.recordCoreToRelationalException`:
- `*wire.FDBError{Code: 1031}` → 53F00 (TRANSACTION_TIMEOUT)
- `*wire.FDBError{Code: 1020}` → 40001 (SERIALIZATION_FAILURE)
- `*wire.FDBError{Code: 1007}` → 40001 (SERIALIZATION_FAILURE)
- `*wire.FDBError{Code: 2017}` → 25F01 (TRANSACTION_INACTIVE)
- `*MetaDataError` → 42000 (SYNTAX_OR_ACCESS_VIOLATION)
- `*RecordAlreadyExistsError` → 23505 (UNIQUE_CONSTRAINT_VIOLATION)
- `*RecordDeserializationError` → XXF01 (DESERIALIZATION_FAILURE)

Primary detection via typed `*wire.FDBError` (numeric codes), string fallback for wrapped errors. Translation covers both Plan() and Execute() error paths. 12 unit tests pin all mappings.

### SQL benchmarks (#34 progress)

3 new benchmarks with allocation data:
- `BenchmarkFDB_JoinQuery` — NLJ, 5 result rows: 4.65ms, 38K allocs
- `BenchmarkFDB_AggregateGroupBy` — 5 groups: 8.05ms, 9K allocs
- `BenchmarkFDB_IndexScanRange` — ~50 rows: 2.18ms, 12K allocs

Key finding: Cascades planner overhead is negligible (<0.05ms) vs FDB I/O (~1.1ms). Plan cache value is in parsing avoidance, not planning time.

### Yamsql conformance hardening

5 new yamsql files:
- `uuid_column.yaml` — UUID DDL, INSERT, WHERE, NULL
- `float_column.yaml` — FLOAT DDL, comparison, NULL, CAST
- `integer_range.yaml` — INT32 range enforcement (22003), CAST overflow (22F3H), NULL CAST
- `unique_index_violation.yaml` — PK constraint (23505), data integrity after failure
- `order_by_index.yaml` — sort elimination ASC/DESC on PK + secondary index

### Driver adapter improvements (#33 progress)

- `NamedValueChecker` — accepts `fmt.Stringer` types (including `uuid.UUID`) as SQL parameters
- `ColumnTypeLength` — reports 10 for DATE, 19 for TIMESTAMP (ISO 8601 format lengths)

### Java conformance audit

- ExceptionUtil: all mappings verified 1:1
- DDL factory: confirmed Go matches Java's MetadataOperationsFactory 1:1 (no standalone DDL in Java)
- #29 clarified: CreateTableAction etc. do NOT exist in Java 4.11.1.0 (Go extension, not conformance)
- Comparison types: 100% match for SQL operations
- FDB transaction retry: matches C++/Java semantics
- Continuation tokens: wire-compatible (magic number, raw/proto formats)
- Record version storage: suffix -1 format correct

### Verification

- Binding stress: 20/20 seeds, 10,000 ops, 0 failures, 0 FDB deaths
- Extended fuzz: planner determinism (60s), translator (60s), time format (30s×2) — all clean
- Full uncached test suite: 46/46 targets pass
- Plandiff corpus: all entries pass
- Yamsql: 1725 scenarios across 122 files

## Stats

- **26 commits**, +545 / -10 LOC
- **12 files changed**
- **46 test targets pass**
- **1725 total yamsql scenarios** (was 1699 at shift start)

## What to work on next

1. **#33 driver adapter gaps** — remaining: custom Scanner/Valuer for Struct/Array/Versionstamp/Continuation columns. NamedValueChecker done this shift. ~0.5 shift remaining.

2. **#34 perf benchmarks** — Go-side benchmarks complete. Next: Java conformance server for cross-engine comparison. Baseline: PK=1.15ms, IndexScan=2.18ms, JOIN=4.69ms.

3. **#36 catalog wire format reverse** — Go writes → Java reads verification. Needs Java conformance server. The write-side format is correct (verified: continuation tokens, record versions, split records, key encoding).

4. **#29 DDL action types** — confirmed NOT in Java 4.11.1.0. Go-only extension for standalone DDL UX. Low priority for conformance.

5. **Performance optimization** — JOIN allocations are high (38K/2.5MB per query due to NLJ materialization). Matches Java architecture but could benefit from streaming NLJ for large outer tables.

## Known issues

- DATE/TIMESTAMP columns report "STRING" in INFORMATION_SCHEMA.COLUMNS after schema template serialization (proto descriptor doesn't carry SQL-level type info)
- PrecisionScale returns (0,0,false) for all types (JDBC standard — only DECIMAL/NUMERIC report precision)
- Plan cache doesn't normalize literal values (different literals → different cache entries)
