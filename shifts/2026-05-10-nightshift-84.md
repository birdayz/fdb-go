# nightshift-84

- **Date:** 2026-05-09 → 2026-05-10
- **Actual start:** ~22:00 CEST (May 9)
- **Actual end:** ~09:00 CEST (May 10)
- **PRs:** #170 (22 commits, not yet merged)

## What was done

### Cascades divergences: 6 closed this shift

| ID | Divergence | What was done |
|---|---|---|
| **D-2** | PushOrdering rules | All 10 rules converted from EXPLORE-phase structural rewrites to PLANNING-phase constraint propagation. 4 batches: Sort/Distinct/Unique/Delete (transparent), Insert/Update/TempTableInsert (transparent), Filter (transparent), Projection (PushDownThroughValue translation), GroupBy (synthesize orderings from grouping keys), Union (push to all branches). Expression partition fix for sort elimination. |
| **D-3 gap** | Distinct elimination | ImplementDistinctFinalRule now checks physical-level DistinctRecordsProperty per FinalMember. computeDistinctRecords for ProjectionPlan returns false; MapPlan propagates only for identity mappings. |
| **D-7** | Multi-aggregate matching | AggregateDataAccessRule handles multi-aggregate GROUP BY via RecordQueryMultiIntersectionOnValuesPlan. MatchesSingleAggregateOf method on AggregateIndexMatchCandidate. Physical wrapper with min-child cost model. |
| **D-8** | CardinalityProperty | Ported Java's Cardinality/Cardinalities types with min/max bounds. computeCardinalities for 25+ plan types. Intersect/Union/Weaken merge helpers. |
| **D-11** | ConstantObjectValue promotion | promoteConstant implements numeric widening (int32→int64, int64→float64, etc.) matching Java's PromoteValue. |
| **D-3 gap** | (included in D-3 above) | Physical DistinctRecordsProperty per FinalMember |

**Score: 11 of 12 divergences now resolved.** Only D-4 (cost model, intentional) and D-5 (InComparison architecture, needs AbstractDataAccessRule) remain.

### M-2 PartialMatch/Compensation foundation — complete

**15 new types:**
- TranslationMap + RegularTranslationMap (fluent When/Then builder)
- LeafValue interface (QuantifiedObjectValue implements)
- BiMap with structural equality (ExplainValue-based, not pointer identity)
- GroupByMappings (matched groupings/aggregates/unmatched aggregates)
- MatchedOrderingPart + MatchedSortOrder
- MatchInfo + RegularMatchInfo + AdjustedMatchInfo + AdjustedBuilder
- Compensation + NoCompensation + ImpossibleCompensation + ForMatchCompensation
- PartialMatchImpl (6 core fields, simple getters)
- MatchPartition + SingleMatchedAccess
- MaxMatchMap (TranslateQueryValueMaybe/PullUpMaybe/AdjustMaybe)
- Traversal (candidate expression tree walker)

**3 matching rules wired into planner:**
- MatchLeafRule → MatchIntermediateRule → AdjustMatches
- MatchingRules() in default_rules.go
- Fires during EXPLORE via CascadesGenerator

**Value infrastructure:**
- Replace + ReplaceLeavesMaybe — pre-order tree substitution with copy-on-write
- withChildren for all ~33 non-leaf Value types

### Other
- Review fixes: promoteConstant int→int64 bug, BiMap nil panic, doc/filter consistency
- 3 sqldriver test hermeticity fixes (nonexistent cluster file)
- SQL_CONFORMANCE.md updated (115 yamsql, 11 divergences resolved)

## Stats

- **31 commits**, +19,014 / -2,290 LOC
- **125 files changed**
- **46 test targets pass**
- **~300 new tests** across 30+ test files

## What to work on next

1. **D-5 InComparison architecture** — the LAST open divergence. Needs:
   - Rewrite InComparisonToExplodeRule to produce SelectExpression + ExplodeExpression (instead of Union-of-filters)
   - Port AbstractDataAccessRule (~1000+ lines in Java) — the core data-access planning rule that consumes M-2 infrastructure
   - Port PredicateMultiMap (currently placeholder) — needed by AbstractDataAccessRule
   - Both must land atomically (new rule shape needs the new planner to handle it)
   - ~2-3 shifts

2. **Correlated subqueries** — last user-visible Java gap. Needs DecorrelateValuesRule + SelectExpression. AliasMap + TranslationMap + MaxMatchMap foundations ready. ~2-3 shifts.

3. **Phase 5 items** — DATE/TIMESTAMP types, DDL action types, driver adapter gaps.

## Known issues

- PredicateMultiMap is still a placeholder (entries count only, no real predicate mapping)
- MaxMatchMap.compute() uses seed structural equality, not the full recursive variant-generation algorithm
- ValueEquivalence not ported (semantic equality beyond structural)
- Expression partition fix in GroupBy/Union conversion may need validation against edge cases
