# swingshift-67

**Date:** 2026-05-02  
**Actual hours:** 14:00 – 22:00 CEST  
**PR:** #135  

## What was done

### Corpus expansion (TODO #8): 1221 → 1620 entries (+399)
- 7 commits, each with full 44/44 test suite green
- Exceeded 1587 yamsql parity target by 33 entries
- Categories covered: scalar subqueries, NULL/3VL, correlated EXISTS, DML+subquery, IS DISTINCT FROM, chained JOINs, nested derived tables, mixed-type, arithmetic, predicates, DML, CAST, aggregates, GROUP BY, LIKE, BETWEEN, CTE, ORDER BY, DOUBLE, DISTINCT, JOIN, STRING, BOOLEAN, composite PK, projection, empty table, COALESCE, CASE WHEN, HAVING, aggregate expressions, wide tables, INSERT/DELETE/UPDATE verification, IN lists

### Go bug fixes (3)
1. **Simple CASE expression** (`CASE expr WHEN val THEN … END`): implemented `CaseExpressionFunctionCallContext` handler in `scalar_functions.go` + type inference in `select_helpers.go`. Java's handler is a no-op (visitChildren, always falls through to ELSE). Go now correctly evaluates simple CASE. TODO #40 closed.
2. **integerLiteralJDBCType**: new function returns "INTEGER" for int32-range values, "BIGINT" for larger. Matches Java's int/long distinction in column metadata.
3. **GREATEST/LEAST type inference**: added to `inferScalarFunctionJDBCType` alongside COALESCE.

### Divergences documented
- Java can't plan DISTINCT (6 entries, TODO #42)
- Java can't plan GROUP BY (TODO #39)
- Java can't plan ORDER BY referencing columns from multiple joined tables
- Java's CTE outer SELECT ORDER BY misclassified as subquery
- Java's aggregate-wrapping expressions (CASE over COUNT, COALESCE over SUM/MIN) throw IllegalStateException
- Java skips implicit group on empty table when HAVING is present
- Java's simple CASE returns wrong rows (always ELSE)
- COUNT(DISTINCT): both reject but messages differ (Java NPE vs Go clean rejection)
- Nested unsupported functions: evaluation order differs (CONCAT vs UPPER)

## Current state

- **Corpus:** 1620 entries (target was 1587)
- **Tests:** 44/44 pass, all green
- **CI:** green
- **TODO #8:** complete — exceeded target
- **TODO #40:** closed (simple CASE implemented)

## Known issues / tech debt

- `TestFDB_SimpleCaseRejected` renamed to `TestFDB_SimpleCaseWorks` with correct assertions
- Java's simple CASE is still broken upstream (visitChildren no-op) — pinned as DivergenceJavaWrongRowsGoCorrect
- Some agent worktrees left in `.claude/worktrees/` — can be cleaned up

## What to work on next

1. **TODO #8 is done.** Corpus exceeds target. Future entries can be added opportunistically.
2. **Phase 2 — Cascades core machinery** is the next major milestone:
   - **#10** B3 full Memo (cross-Reference equivalence-class merging, cost-driven extraction) — gates everything
   - **#11** B6 planner driver (per-rule task granularity)
   - **#12** B5 Batch A: index rules
3. The 1620-entry corpus is now the regression safety net for the Cascades rewrite.
