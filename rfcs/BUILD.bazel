# The client prod-readiness audit is a status REDIRECT (B6): TestProductionStatusAuthority
# checks that it names road-to-prod.md as the authority in its first lines, so it has to be
# in the guard's runfiles. Exported individually rather than as a glob — the guards read
# these exact RFCs, and staging all of rfcs/ would make every RFC edit invalidate their cache.
exports_files([
    "250-flatmap-preserves-inner-row-limit-stops.md",
    "prod-readiness-go-client.md",
])
