Review the revised RFC-257 WS-J DESIGN (v2), and the two LANDED fixes it records (sections 3.1 and 3.2), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE b6aaf7546ad0ae91305679c9454860ab18ca010e; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 1455e2450393c5a507d808a2c7fa9e41bf2244b5104fe5ffeedaa1e09a49cf4a (631 lines). Previous: v1 TREE eb426a53f814802d5c9ff1f4fca67ee199f4c1cd (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v1/*.txt).
Delta since v1 touching WS-J: (1) PRODUCTION: literal-carrier fix (pkg/relational/core/query/ddl/generator.go literalKeyCarrier; pkg/relational/core/query/expr/walk.go INT-typed bitmap entry size) and Java getNullableLong operand widening in the long-arithmetic key evaluator (pkg/recordlayer/long_arithmetic_function.go nullableLong, javaDoubleToLong, KeyExpressionInvalidResultError), with unit pins (ddl literal_key_carrier_test.go, embedded AsSelect goldens, recordlayer TestArithmeticNumberWidening / WrongType, coverage_unit_test.go) and a rewritten sqldriver FDB test TestFDB_ArithmeticIndex_NonIntegerOperandTruncatesLikeJava (it previously pinned a disproven Java ClassCastException claim); (2) TEST-ONLY oracle v2 in conformance/ws_j_index_fidelity_conformance_test.go: harvest census (rfcs/257-java-upgrade-audit/ws-j-oracle/harvest.py, corpus JSON regenerated: 81 templates, 352 statements, 360 occurrences), structural derived and per-index isolation runs (466), unsplittable pin, class by canonical MetaData, canonicalization in place with anonymous-run ordering, whole-canonical-MetaData Java digest with template-name and correlation-alias placeholders, pins moved to conformance/testdata/rfc257/wsj_java_pins.json, an options-order dimension, 22 new shapes, the WSJQ query half pinned (18 probes), the Go-stored spec (driver-stored invisible 42F55; library-stored equal to Java-stored), and a spec measuring long arithmetic key functions over DOUBLE/FLOAT operands; (3) evidence: ws-j-oracle/evidence-v2.txt, evidence-f2.txt, evidence-mutations.txt, README; (4) TODO.md block for the Go SQL driver keyspace gap and catalog/fdb_store_catalog.go comment. NOT WS-J, same tree, own gates: RFC-257 WS-D v10 (heartbeat key-parse production fix) and WS-E v3 (test-only oracle round and harness de-duplication in conformance/sql_plan_steps.java); out of scope except where they touch WS-J claims.
Run records on this tree's WS-J bytes: two uncached runs of the five WS-J specs, 5/5 each (/var/tmp/fdb-upgrade-recovery/wsj-v2-ev-5.log, -6.log; hashes in evidence-v2.txt); both conformance targets green earlier with the same WS-J production bytes (rfc257-wse3.log: rfc257_oracle_test 23/23, conformance_test 1405/1524 with 119 skipped inherited from master); the unit targets touched by (1) green under Bazel (ddl_test, embedded, recordlayer_test, sqldriver focused test, docscheck); the last full `just test` (just-test-v10b.log, 94/94) predates only the test-only WS-J oracle-v2 edits and the options-order dimension.
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v1 verdicts, the FULL v2 design and its diff from v1, the oracle source, corpus/pins and evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions, not only whether requested words appear. Distinguish MEASURED target facts from SOURCE claims.
Preserved boundaries: the owner-approved Go read/raw nullable-array, scalar and sorting extensions; the Go-only IN-subquery read extension; LIMIT (approved Go extension); RFC-209 companions; the rebind validator; C++ 7.3.77; format max 15/default 14; Lucene separate.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the DDL front end, IndexSpec merge and quantifier resolution as structural ports, metadata assembly order, lane resolution at construction, planner use of stored key expressions, and the evolution policy for stored templates.
