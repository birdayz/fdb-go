Reconfirm the revised RFC-257 WS-E DESIGN (v9), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 1cf2b55d3598fca1645cd2f684b6c7b8d7529937; design rfcs/257-java-upgrade-audit/ws-e-design.md SHA256 d76887b8ee7e04e559d699ce19bb2e614792ff3f4ee3de0ec10a9e35eec86e14 (3163 lines). Previous: v8 TREE 612fd91e5f07c78c54789633e87ada47899d75c3 (three NAKs, rfcs/257-java-upgrade-audit/ws-e-design-review-v8/*.txt); v1-v7 in ws-e-design-review-v1/-v7. The v8 design is at /var/tmp/fdb-upgrade-recovery/ws-e-design.v8.md (also `git show 612fd91e5f07c78c54789633e87ada47899d75c3:rfcs/257-java-upgrade-audit/ws-e-design.md`); diff it against `git show TREE:rfcs/257-java-upgrade-audit/ws-e-design.md`.
Delta since v8 touching WS-E (the design's Status paragraph lists it): (1) 5.4(c): the boundary prune scoped by PROVENANCE (a source member and the ported rule's yields over it), the enumeration of REWRITING producers of predicate variants, NormalizePredicatesRule moved to PLANNING only, the prune at both crossings (AdvancePlannerStage and AdvanceStagePreservingMembers), rescues limited to primary-key and UNIQUE conjuncts (measured), the rule registered inside WS-F's decorrelate-then-simplify conditional chain as two instances of one type, and the (j) tests; (2) 5.4(a): the census population widened to predicate Eval(nil) (logical_predicate.go:8072); (3) 4.1(b)-(c): the nested_ins and two-IN-under-an-ordering shapes kept as the target plans them (measured, round v9) rather than the prototype's FlatMap, the dedup's equality as the comparison's `=` with the signed-zero and NaN rows measured, the indexed NaN equality mapped to the two NaN key ranges (a failure found by the round), the IN-union plan folding its source, the explode rule's two instances under one name; (4) 4.3(1)-(2): compilePromotion's temporal arms, canonicalization in the write converters by column type, one parser with a stated domain, the TIMESTAMP-to-DATE edge assignment-only, the lane admitted by the comparand's role and for IN only with uncorrelated items, a NULL comparand, GREATEST/LEAST after the change, error timing over empty input, the saturation wording; and the measured fact that the target has no DATE type; (5) the oracle: Describe "WS-E target oracle v9" (27 pins) in conformance/ws_e_probe_conformance_test.go; (6) code: TestGoSQLRunner_CanceledRunStillTearsDown asserts context.Canceled (pkg/relational/conformance/plandiff/go_runner_test.go), and the stale store-open comment (pkg/recordlayer/store_state_cache.go); (7) section 8's order (after WS-F D3) and its list of rows that stay different.
Records (results in /var/tmp/fdb-upgrade-recovery/): wse9-oracle/ (capture.log, pins.txt, pinned1.log), wse10-all1.log, wse10-all2.log, wse10-teardown.log, wse10-td-final.log, wse10-docscheck-oracle.log, wse10-evidence.sh/.out, wse10-src.sha, wse10-M9.log; earlier: the v8 records named in ws-e-design-review-v8/SCOPE.md and its prompts (the prototypes /home/birdy/projects/fdb-nofold-probe and /home/birdy/projects/fdb-explode-probe and their wse8-* logs).
NOT WS-E, same tree, own gates: WS-C section 7 addendum (revision 6, NAK, being revised), WS-J design v10 (NAK, being revised), WS-D v13 (NAK), WS-F v2 (NAK).
Tree objects: run `git show TREE:path`, `git grep ... TREE -- pathspec` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v8 verdicts and check that EVERY finding is resolved, the FULL v9 design and its diff from v8, the oracle and its evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions (the provenance class against every rule that yields predicate variants and every place a yield's provenance could be lost or copied, both crossings, the conditional chain and its root-operator check, the nested IN-join and two-source IN-union derivation, the dedup equality against the scan binder's zero widening and the NaN ranges, the canonicalization site against every writer, the one parser's domain, the lane's role rule), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the independent storage/wire correctness review lens: bytes a bound value stores (time.Time by instant, slices, the assignment lattice for every source, UTF-8 at bind and at save), index keys, isolation and read-conflict semantics (the measured read scope, always-snapshot sites, the index-state conflict range), DRY_RUN and snapshot admission, connection options and pool reset, continuation ownership, and write divergences.
