**ACK — completed WS-B final correction confirmation.**

Reviewed Git **tree `48b50548b11a53bd1452a627689c440535a3d093`**, against previous reviewed tree **`dd0956b11e901609edcd5f19d36ef64f04482b8a`**. I completed the production/test delta review, read all four retained NAKs and the approved design, traced surrounding callers, and checked tagged Java **`fdacd162a9c8acfadc49082b89185c823ab8ae4a`**. **No new actionable findings remain in this correction delta.**

All four previous findings are resolved:

1. **Reload/uniqueness-cleanup deadlock: resolved.** [Reload, store.go:1896](/home/birdy/projects/fdb-go/pkg/recordlayer/store.go:1896) now acquires `stateMu → registry → view`, compatible with maintenance’s `stateMu.RLock → registry` path. Both `loadStoreState` callers construct unpublished handles, so their registry-protected initial assignment does not recreate the operational cycle. State publication and snapshots do not acquire the registry while holding a view lock. The real-FDB regression establishes two violations, one remaining violation during deletion, and a queued reload writer. Its cancellation-before-materialization unwind avoids stranding the reproduced cycle. The retained reversal mutation compiled and failed the intended assertion.

2. **Clear-only transactions leaving warmed caches stale: resolved.** [Classification, index_state.go:320](/home/birdy/projects/fdb-go/pkg/recordlayer/index_state.go:320) and [invalidation before clearing:380](/home/birdy/projects/fdb-go/pkg/recordlayer/index_state.go:380) remove dependence on a subsequent Open. Known data classification excludes header/state overlap. Unopened record classification requires whole-range containment and a successfully decoded persisted header with a valid minimum format; byte matches alone cannot exempt a range. Missing, malformed or failed header reads retain conservative invalidation. Empty ranges do not invalidate. The thirteen classification cases and separate clear-only transaction cover the reported gap, including committed stamp changes, maintenance, scans and uncached reopening.

3. **Opened noncacheable deletion invalidating globally: resolved.** [DeleteStore, store_api.go:165](/home/birdy/projects/fdb-go/pkg/recordlayer/store_api.go:165) preserves its serializable, header-derived decision through the internal clear. Missing/empty/valid-noncacheable headers avoid the bump; cacheable/malformed headers invalidate. Unknown fields and unsupported format values retain the approved deletion behavior. Read errors return before mutation. Open→delete and Open→delete→recreate negative controls now pass, with positive controls retained. Shared-view clearing, exact tuple-range boundaries, queued-version removal and local-version cleanup remain intact.

4. **Exported snapshots reading stale local maps: resolved.** [GetRecordStoreState, store.go:1835](/home/birdy/projects/fdb-go/pkg/recordlayer/store.go:1835) and [GetAllIndexStatesMap, store_api.go:225](/home/birdy/projects/fdb-go/pkg/recordlayer/store_api.go:225) initialize lazily and copy index states from the common authority under its mutex. Returned maps are independent copies. Two-handle reload assertions and clear assertions exercise agreement with operational getters. Reload retains established conflicts and returns on read failure before replacing the shared projection.

The earlier initialization, former-index cleanup and mutation/publication corrections remain intact. Removing deferred clear history simplifies the lifecycle. Conservative context invalidation is an explicit policy extension; it changes neither generic FDB transaction behavior nor wire encoding. The approved Java deletion-callback correction remains preserved.

**Scoped SPFresh verdict: ACK.** I reread RFC094 and the relevant paper sections. Corrected common state handling removes the reported suppression of vector maintenance and inconsistent scan eligibility; corrected locking removes the mixed-index deadlock. These address the transactional adaptation’s freshness/concurrency obligations in [RFC094 §2.1](/home/birdy/projects/fdb-go/rfcs/094-spfresh-vector-index.md:107), relevant to [SPFresh §§3.2–3.3 and 4.2.2](/home/birdy/projects/fdb-go/.claude/skills/spfresh-reviewer/spfresh-paper.pdf). SPFresh/RaBitQ source, generation fences, data-read isolation, encodings and budgets are unchanged. This delta introduces no change to posting balance, closure replication, pruning or LIRE; the prior algorithm assessment against [SPANN §§3.2.1–3.2.3](/home/birdy/projects/fdb-go/.claude/skills/spfresh-reviewer/spann-paper.pdf) stands.

Evidence reconciles: **6,848/6,848 hashes** match frozen source tree **`ba06f34ff8998f61040a491e4f3a3e8b8de7ccf9`**, and **36/36 evidence hashes** match. Source-to-reviewed-tree changes are documentation/evidence only. Retained evidence supports **93/93 uncached targets**, **14/14 scoped race targets**, recordlayer **3426/3427**, conformance **1391/1510**, genuine red reproductions and four compiled mutation kills. Existing exclusions remain unchanged. The booking run reports **93 passed, one executed and 92 cached**. This is not full PR relational race coverage or actual CI.

The latest residual **does not independently block this milestone**: orders **1.0159× (+1.59%)**, customers **1.0407× (+4.07%)**, versus verified merge-base **`e48f5b4965543cd4d99b5578356059e12d969c7c`**. All four runs have matching 24 RUN/PASS populations, 26 identical row-count outputs and zero reported retries. Two sequential samples per side establish neither parity nor causation.

WS-C–K remain outside this verdict; queue/state4/format15 remain disabled. No edits, tests/builds, Git mutations, GitHub requests, publication or delegation occurred. HEAD remains **`71ccd8cf8b3fd0dbafe283e91171818e36af555e`**. This review’s launch log records **`gpt-6-astra` / `xhigh`**.