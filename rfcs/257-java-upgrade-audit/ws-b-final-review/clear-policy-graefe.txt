**ACK — completed WS-B correction confirmation, virtual Graefe architecture/Java lifecycle lens.**

Reviewed Git **tree `48b50548b11a53bd1452a627689c440535a3d093`**, against previous reviewed tree `dd0956b11e901609edcd5f19d36ef64f04482b8a`. I read all four retained NAKs, the approved design, complete production/test delta, surrounding callers, tagged Java `fdacd162a9c8acfadc49082b89185c823ab8ae4a`, and retained evidence. **No new actionable findings in this correction delta.**

All four previous findings are resolved:

1. **Reload deadlock — resolved.** [ReloadRecordStoreState](/home/birdy/projects/fdb-go/pkg/recordlayer/store.go:1890) takes `stateMu → registry → view`, consistent with maintenance-triggered uniqueness cleanup. State writes and snapshots take `stateMu → view` without reacquiring the registry. Both builder callers construct unpublished handles, so their registry-held initialization does not recreate the operational inversion. Reload preserves conflict registrations and publishes only after successful reads. The deterministic regression establishes the remaining real violation and queued writer; its failing path cancels the real transaction before range materialization, allowing both goroutines to unwind.

2. **Clear-only cache staleness — resolved.** [Range classification and invalidation](/home/birdy/projects/fdb-go/pkg/recordlayer/index_state.go:320) now act before the clear, independently of registration or a later Open. Known-data exclusions first check header/state overlap across registered views. Unopened record-range exclusions require whole-range containment and a successfully decoded persisted header with a valid minimum format. Missing, malformed or unreadable candidates retain conservative invalidation; empty ranges do not invalidate. The thirteen classification cases and expanded [warm-cache regression](/home/birdy/projects/fdb-go/pkg/recordlayer/index_state_test.go:2826) cover the reported gap, including a separate committed clear-only transaction, stamp changes, maintenance and cold committed scans.

3. **Noncacheable deletion over-invalidation — resolved.** [DeleteStore](/home/birdy/projects/fdb-go/pkg/recordlayer/store_api.go:165) retains its serializable, header-only decision and passes that already-applied policy through cleanup. Missing/valid-noncacheable headers avoid the stamp write; cacheable/malformed headers invalidate. Empty protobufs, unknown fields and unsupported formats retain the approved deletion semantics. Header-read failures return before mutations. Shared-view clearing, exact tuple-range boundaries, queued-version/local-version removal and retirement-callback cancellation remain intact. Open→delete and Open→delete→recreate negative controls complement the retained positive controls.

4. **Stale exported snapshots — resolved.** [GetAllIndexStatesMap and its shared helper](/home/birdy/projects/fdb-go/pkg/recordlayer/store_api.go:225), also used by [GetRecordStoreState](/home/birdy/projects/fdb-go/pkg/recordlayer/store.go:1835), initialize lazily and copy the common index-state authority under its mutex. Returned maps do not alias that authority. Two-handle reload and clear assertions now cover agreement with operational getters.

The conservative context-clear policy is an explicit coherence extension. It preserves the approved Java header-aware deletion contract without changing generic FDB transaction behavior or wire encoding. Earlier initialization, reconciliation and publication corrections remain intact, as does the intentional Java deletion-callback correction.

Evidence reconciles: **6,848/6,848 frozen hashes** match source tree `ba06f34ff8998f61040a491e4f3a3e8b8de7ccf9`; **36/36 evidence hashes** match; working files match the reviewed tree. Source-to-review differences contain documentation/evidence only. Retained logs substantiate **93/93 uncached targets**, **14/14 scoped race targets**, recordlayer **3426/3427**, conformance **1391/1510**, four original reproductions and four compiled mutation kills. Post-booking testing reports 93 passes with one executed and 92 cached. These results do not constitute the complete PR relational race set or actual CI.

The latest performance residual **does not independently block this milestone**: orders **1.0159× (+1.59%)**, customers **1.0407× (+4.07%)**, against verified merge-base `e48f5b4965543cd4d99b5578356059e12d969c7c`. All four sequential runs have 24 matching RUN/PASS cases, 26 identical row-count outputs and zero reported retries. Two samples per side establish neither parity nor causation.

**Scoped SPFresh verdict: ACK.** I reread RFC094’s freshness, query and transactional lifecycle sections, [SPANN §§3.2.1–3.2.3](/home/birdy/projects/fdb-go/.claude/skills/spfresh-reviewer/spann-paper.pdf), and [SPFresh §§3.2, 4.2.1–4.2.2](/home/birdy/projects/fdb-go/.claude/skills/spfresh-reviewer/spfresh-paper.pdf). The prior category-(b) blockers in common maintenance/search admission are resolved. SPFresh/RaBitQ algorithms, generation fences, snapshot data reads, posting balance, closure replication, pruning, LIRE, encoding and budgets are unchanged. This confirms the lifecycle correction’s compatibility with [RFC094 §2.1](/home/birdy/projects/fdb-go/rfcs/094-spfresh-vector-index.md:107); it makes no new recall or algorithm-performance claim.

WS-C–K remain outside this verdict; queue/state4/format15 remain disabled. I performed no edits, tests/builds, Git mutations, GitHub requests, publication or delegation. HEAD remains `71ccd8cf8b3fd0dbafe283e91171818e36af555e`. The [review launch log](/var/tmp/fdb-upgrade-recovery/ws-b/clear-policy-graefe.log:5) records **gpt-6-astra/xhigh**.