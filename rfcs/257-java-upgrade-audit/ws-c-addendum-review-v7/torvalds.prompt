Review REVISION 7 of an ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together. Repo /home/birdy/projects/fdb-go. Candidate TREE aa29308afd7637d7e98a42ef1d108335013ead8c; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 0ff22d05c138b986f4bf3e1291765023223db87bb58349c46f746ec58c61e6c9, 1326 lines); 7.1 to 7.7 are revisions 1 to 7, each stating what changed and why. Revision 6 was gated at tree c789832131f2e6206ee2ebad9b92a3ae463d6f6d and got three NAKs with low findings: read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v6/{graefe,torvalds,storage}.txt (and v1-v5's) and check that EVERY finding is resolved. Revision 7 also folds in the WS-J v10 findings that are the same comparison's inputs (ws-j-design-review-v10/*.txt: the former-index key, SetSubspaceKey(nil), RecordCoreArgumentError's rendering, the test binding).
What changed: `git diff c789832131f2e6206ee2ebad9b92a3ae463d6f6d aa29308afd7637d7e98a42ef1d108335013ead8c -- <path>` (both are tree objects; the same diff carries WS-J design v11's changes, gated separately, and WS-E's review records). WS-C revision 7 files: pkg/recordlayer/subspace_key_identity.go (NEW: subspaceKeyIdentity, subspaceKeysEqual, tupleEquivalentValue) and subspace_key_identity_test.go (NEW); pkg/recordlayer/index.go (equalsJava: raw type, subspaceKeysEqual, the predicate by identity or row-number window; SetSubspaceKey normalizing and refusing nil; subspaceKeyErr); pkg/recordlayer/key_expression.go (keyExpressionEquals: identity, Dimensions, function instanceof); pkg/recordlayer/metadata.go (Build's subspace-key checks as MetaDataValidator's, the former-against-former check, GetIndexFromSubspaceKey, normalizeSubspaceKey deleted, addIndexCommon returning the key refusal); pkg/recordlayer/metadata_evolution_validator.go (validateCurrentAndFormerIndexes, indexesBySubspaceKey, formerIndexesBySubspaceKey, validateFormerIndex, validateFormerIndexFromIndex, validateIndex; subspaceKeyString and validateFormerIndexes deleted); pkg/recordlayer/metadata_proto.go (formerIndexFromProto and formerIndexToProto as Java's; the options read before the root, which is WS-J's; AmbiguousLegacyUnionError.HeldBy/Stored, WS-J's); pkg/recordlayer/rank_scan.go (RecordCoreArgumentError.HasSubspaceKey and Error); pkg/recordlayer/online_indexer.go (SetTargetIndexes copies; the nil-target guard after the source-index checks; the IndexBuildOutcomeBuilt doc); the tests corrected to Java (metadata_evolution_validator_test.go, metadata_builder_test.go, metadata_test.go, bug_bounty3_metadata_test.go, metadata_coverage_test.go, metadata_proto_fidelity_test.go, metadata_proto_test.go) and the new specs in online_indexer_test.go and indexing_merger_test.go (retitle); conformance/subspace_key_identity_conformance_test.go (NEW) and conformance/metadata_proto_conformance.java (steps buildMetaDataVerdict, validateMetaDataEvolutionFlags, formerIndexFromProtoVerdict); CHANGELOG.md, DIVERGENCES.md (the merge bullet and closing list), ws-c-design.md (7.5/7.6 corrected in place, 7.7).
Evidence: /var/tmp/fdb-upgrade-recovery/wsc-v7/ (focus1.log, the unmutated focus; mut-m45..m60 *.log with perl, marker count and applied diff, *.full.log; run-mutations.sh, mutate.sh, run-mutations.out; the .good copies; jvm-subspace2.log, the JVM specs on the candidate's code; jvm-subspace-base.keep.log and jvm-former-base.log, the same specs against revision 6's tree in a worktree; wsjixk.log; gotest-recordlayer*.log) and /var/tmp/fdb-upgrade-recovery/ev11/ (src.sha over ../ev11-files.txt, src2.sha over ../ev11c-files.txt, wsc-focus.log, jvm-md.log, justtest.log, justtest-executed.txt, justtest-tree*.txt, final-targets.log, final-recordlayer-test.log; drivers ../ev11a.sh, ../ev11b.sh, ../ev11c.sh). rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v11.txt summarises the combined run.
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/: metadata/Index.java, metadata/FormerIndex.java, metadata/TupleTypeUtil.java, metadata/MetaDataValidator.java, metadata/MetaDataEvolutionValidator.java, metadata/IndexPredicate.java, metadata/expressions/*KeyExpression.java, RecordMetaData.java, provider/foundationdb/OnlineIndexer.java, IndexingBase.java, IndexingHeartbeat.java.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Check every v6 finding's resolution against Java and the code: equalsJava field by field against Index.equals (including the normalization's arms against toTupleEquivalentValue, the strict BigInteger bounds, Float/Double equality, list normalization, the predicate classes); keyExpressionEquals against every KeyExpression class's equals; every place Go compares subspace keys (is any left on another rule?); the evolution validator against Java's validateCurrentAndFormerIndexes and its three helpers, check order and messages; the builder's validator against MetaDataValidator; the loader and writer of former indexes; SetSubspaceKey's normalization against Java's setter and every Go consumer of SubspaceTupleKey; the merge's scoped lock-out; the mutation logs and the JVM differentials, including the runs against revision 6's tree. Distinguish measured facts from source hypotheses.

Apply the virtual Linus Torvalds code quality lens: logic holes, incomplete conversions (every use of the old mutual flag, every caller and test that relied on the old refusals and errors), dead code, test vacuity, and maintainability.
