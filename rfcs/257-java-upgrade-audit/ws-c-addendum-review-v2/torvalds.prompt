Review REVISION 2 of an ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together. Repo /home/birdy/projects/fdb-go. Candidate TREE e7f2e4d540c5ae85d402160d2ef21fba7a0ee221; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 57a3347303862f7c880a499b36def6ea3284db613321e047507a0d30cfdf7dbf, 643 lines); section 7.1 is revision 1 (corrected), section 7.2 is revision 2 and states what changed and why. Revision 1 was gated at tree 9265beef3e5c9dd9ec86caaade526c8137552976 and got three NAKs: read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v1/{graefe,torvalds,storage}.txt and check that EVERY finding is resolved, not only the ones 7.2 mentions.
What changed: both trees are tree objects, so `git diff 9265beef3e5c9dd9ec86caaade526c8137552976 e7f2e4d540c5ae85d402160d2ef21fba7a0ee221 -- <path>` shows revision 2 exactly (untracked files included). Files: pkg/recordlayer/online_indexer.go (IndexingPolicy IfMismatchPrevious/ForbidRecordScan, GetIfMismatchPrevious, withIfWriteOnly, fallbackToRecordsScan/enforcedStampOverwrite, buildsMutually/buildsByIndex, validateSourceAndTargetIndexes moved from the builder to session time, BuildIndex loop + indexingCatcher + savedSourceIndex + buildIndexAttempt, setIndexingTypeOrThrowForIndex's forced overwrite, markReadable's queuedInSession, buildIndexingStamp, buildRangeByIndex's validation errors), pkg/recordlayer/online_indexer_queue.go (IndexingValidationError fields, UnexpectedReadableError, checkOpenHeartbeats/checkHeartbeatAdmission, prepareIndexingState's admission after the action and without the early queue check, expectedIndexStatesOrThrow in validateBuildSession), pkg/recordlayer/indexing_mutual.go (mutualBuildAlreadyReadable removed), pkg/recordlayer/errors.go (PartlyBuiltError Saved/Expected), cmd/frl/internal/cmd/index_write.go (skip/publish message), pkg/relational/core/fleet/build.go (comment); tests in online_indexer_test.go (Describe "index state desired action" and its nested "build catcher", the BY_INDEX source-refusal specs, the rewritten "resumes multi-target build from partial progress" and "rejects a stamp mismatch" specs), online_indexer_session_test.go (the rewritten admission matrix), index_queued_dispatch_test.go (three specs), bug_bounty3_indexer_test.go (BUG6 text), cmd/frl/internal/cmd/write_integration_test.go; CHANGELOG.md (two OnlineIndexer entries under [Unreleased]), TODO.md (the "RFC-257 WS-C addendum" block).
Evidence (/var/tmp/fdb-upgrade-recovery/wsc-v2/): justtest.log; mut-*.log (16 mutations, each with its full log mut-*.full.log); census-{skip,markreadable,catch}/ (summary.txt and per-target logs); loop40.log; evidence-md5.txt; evidence-tree.txt.
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/: OnlineIndexer.java:133-316 (indexingLauncher, indexingCatcher, getIndexer), :855-885 (builder validation), :1006-1100 and :1240-1360 (IndexingPolicy); IndexingBase.java:184-261 (handleStateAndDoBuildIndexAsync), :357-440 (markIndexReadable), :444-530 (enforceStampOverwrite, setIndexingTypeOrThrow), :1169-1184, :1240-1390 (exceptions); IndexingByIndex.java:60-260; IndexingThrottle.java:330-440 (expectedIndexStatesOrThrow).
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Check the port against Java arm by arm (every catcher arm, the attempt limit, the method choice after a fallback, the forced overwrite, what is copied vs written through, the per-transaction state classes, validation messages and timing), the heartbeat admission rules and their declared divergence, every caller of BuildIndex and of the changed errors (what else relied on the builder refusing a source index, on IndexingValidationError mid-build, on PartlyBuiltError being returned), the test population and its vacuity (does each new spec fail without the code it pins; the mutation and census logs), and the CHANGELOG claims. Distinguish measured facts from source hypotheses.
Preserved boundaries: approved Go extensions; C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes, incomplete conversions (every use of the old mutual flag, every caller and test that relied on the old refusals and errors), dead code, test vacuity, and maintainability.
