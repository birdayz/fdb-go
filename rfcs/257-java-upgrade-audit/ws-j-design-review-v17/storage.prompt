Review the revised RFC-257 WS-J DESIGN (v17), and the fixes it records as LANDED, not the rest of the implementation. Repo /home/birdy/projects/fdb-go, branch upgrade/java-4.14.2.0, published as PR #786 (the owner's one PR for the whole upgrade). Candidate COMMIT d1ccff8c6; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 3e82c1059f9556b3f4cd5a67d5bc8f029d9ae1a578cd5498b015af5281260bde (2907 lines at d1ccff8c6). Previous: v16 at f53cd6fc6 (Graefe, Torvalds and storage NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v16/{graefe,torvalds,storage}.txt); v1-v15 in ws-j-design-review-v1/-v15. `git diff f53cd6fc6 d1ccff8c6 -- rfcs/257-java-upgrade-audit/ws-j-design.md` shows v17 (the tree diff also carries WS-C revisions 15 and 16 and WS-J step 2, gated separately).
The owner's rulings (rfcs/257-java-4.14.2.0-upgrade.md, "Verification and review gates" item 9): data written by pre-release Go builds (v0.1.0 included) is not supported, nothing is built to migrate, detect or refuse it; Go answers every query the target answers; evidence is red→green and Java-versus-Go, mutation only 0-3 per milestone where values could move together; one PR, pushed as it lands, CI and ACKs per workstream. Delta since v16 (the Status paragraph and the new section 4e list it; stale sites are marked [v17 → 4e]): the ruling applied to the carry rule (WIDENED and both literal-carrier arms withdrawn, section 4's tests re-based on templates Java or this build wrote, the merge unit restated), landed versus owed with evidence, Java's deleteTemplate(name, version) and the v′ <= latest refusal step 3 moves into CreateTemplate, the evidence v16 claimed, smaller corrections, and step 3 restated.
WS-J code since v16: 5d95cd27c (step 2, one DDL front end, `buildSchemaTemplate` in pkg/relational/core/embedded/ddl.go; test TestFDB_ExecutedTemplateIsTheToolingPathsTemplate) and 8e515423b (the v17 code half: pkg/recordlayer/metadata_evolution_validator.go and index_carry_class.go with the widening withdrawn, long_arithmetic_function.go, cmd/frl/internal/cmd/meta.go; key_expression_proto.go, errors.go, metadata_proto.go, metadata_extension_options.go for the 3.6 debt; pkg/relational/core/catalog/{fdb_store_catalog,fdb_template_catalog,template_catalog,store_catalog,template_bindings,template_restore}.go; tests index_carry_class_test.go, metadata_evolution_validator_test.go, metadata_proto_test.go, metadata_proto_fidelity_test.go, metadata_validate_records_test.go, fdb_catalog_test.go, template_restore_fdb_test.go, template_version_guard_fdb_test.go, template_version_guard_test.go, cmd/frl meta_test.go; conformance extension_options_conformance_test.go, key_expression_absent_child_conformance_test.go, metadata_proto_conformance.java (buildPartialMetaDataAnyVerdict), metadata_proto_conformance_test.go, ws_j_index_fidelity_conformance_test.go; nogo_config.json; CHANGELOG.md, DIVERGENCES.md, TODO.md). Evidence: /var/tmp/fdb-upgrade-recovery/evidence/wsj-step1-red/ (step 1's guard tests on 9ba005539's tree; TREE note), and the pre-commit hooks of 8e515423b and d1ccff8c6 (all 94 targets passing; the self-hosted CI runners are offline, the owner has been told).
Java tag 4.14.2.0 in fdb-record-layer/; the pre-upgrade Go builder and loader are at commit e48f5b496. Relational catalog Java: fdb-record-layer/fdb-relational-core/src/main/java/com/apple/foundationdb/relational/recordlayer/catalog/{RecordLayerStoreCatalog,RecordLayerStoreSchemaTemplateCatalog}.java; key expressions fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/metadata/expressions/; RecordMetaDataBuilder.java; MetaDataEvolutionValidator.java.
Tree objects: run `git show COMMIT:path` or `git diff A B -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read commit objects, not the working tree, which moves on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline; wire compatibility with Java-written stores is the hard line.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact commit and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Read the three v16 verdicts and check that EVERY finding is resolved, answered or made moot (and say which), the v17 delta, the landed code and tests, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (what the ruling removes and what it leaves the design owing to Java-written stores; the carry rule step 3 owes; the deserialization port; the catalog's guard, delete refusal, restore and concurrency), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.

Apply the independent storage/wire correctness review lens: what each catalog route reads and writes (stored templates, schema rows, TEMPLATES_VALUE_INDEX and its state, restores, the carried bytes step 3 will write), the transaction and conflict behaviour of CreateTemplate, DeleteTemplateVersion, the restore (commit_unknown_result included) and a concurrent bind or DROP SCHEMA, what a Java-side deleteTemplate leaves and how Go handles it, and wire compatibility of every stored template and records file with the target.
