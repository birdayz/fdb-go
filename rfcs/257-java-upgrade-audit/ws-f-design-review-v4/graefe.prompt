Reconfirm the revised RFC-257 WS-F DESIGN v4 (planner scheduling, explicit index-entry readers, planner properties: audit items cascades W6-W15) and the test-only oracle it rests on. Repo /home/birdy/projects/fdb-go. Candidate TREE 9aee2d3d3c3347375454d8cc44f91af4e486f291; design rfcs/257-java-upgrade-audit/ws-f-design.md SHA256 15bcea3dd51a6440219b3233a3e8a02cce29054b2c15131e44cb1e146f09116d (1954 lines). Previous: v3 TREE 8d721bd13b28b59be91f59a35b71acbd15d7131c (three NAKs, rfcs/257-java-upgrade-audit/ws-f-design-review-v3/*.txt); v1-v2 in -v1, -v2. The v3 design text is also at /var/tmp/fdb-upgrade-recovery/ws-f-design.v3.md. Diff the design with: git diff 8d721bd13b28b59be91f59a35b71acbd15d7131c 9aee2d3d3c3347375454d8cc44f91af4e486f291 -- rfcs/257-java-upgrade-audit/ws-f-design.md (the trees also differ by WS-C/WS-D/WS-E/WS-J work that has its own gates).
Delta since v3: the design's Status paragraph lists what v4 answers, item by item. (1) TEST-ONLY oracle v4: conformance/ws_f_probe_conformance_test.go (155 probes; the verdict-arm spec now also drives the access-path arms, the ratchet over explicit tables via wsfRatchetGaps, the ROOT-PAIRS reader's refusals, and the winner/EXPLAIN plan-shape comparison), new conformance/ws_f_access_path_test.go (wsfAccessPath/wsfPathClass, the SAME-PATH/SAME-CLASS/DIFF-PATH verdicts; every EXPLAIN row is accepted by access path), new conformance/ws_f_record_layer_go_test.go (Go's record-layer counterparts of the w8_rl rows, SAME-RL), conformance/sql_plan_steps.java (ROOT-PAIRS memo order and sequential winner; REWRITING-RESULT renders predicates), conformance/BUILD.bazel, pkg/relational/conformance/plandiff/go_runner.go and runsql.go (the sequence runner records each execution's plan-cache event; a prepare failure is a fixture error). Pins regenerated from /var/tmp/fdb-upgrade-recovery/wsf4/cap4.log. Evidence rfcs/257-java-upgrade-audit/ws-f-oracle/evidence-run.txt with /var/tmp/fdb-upgrade-recovery/wsf4/ (oracle.sh, tree.sh, run5/run6 .log/.lines/.txt/.uptime/.tree-*, aux.log; mut/ with run.sh, mutate.sh, run.out, good/, thirteen mut-*.log and *.full.log). The WS-F runs are bound to code tree 9de3649b169087eb73332010a90b0c4378c46c2c (the working copy minus rfcs/257-java-upgrade-audit/); the gate tree adds conformance/fromless_select_java_probe_test.go (a WS-C flake fix), comment-only lines in three Go files and documents, and the whole rfc257_oracle_test ran on candidate tree 512ae24895a17ec693b76bfb6d6d31cd63a8ee32 (wsc-v10/ev2/oracle.log, 61 of 61). (2) Design v4: the crossing decision (zero or several finals a checked error, AdvanceStagePreservingMembers deleted with the default flip, census members named), the memoizer and its extraction check, the pastRecordTypeHorizon mark, F-8 replacing the rule-internal pre-selections, section 12 by outermost decoder, the in-union product size, the IN rule matching a select, nested one-source in-unions, the sparse ? cache behaviour, the in-join covering-emission cause, the quoted-PK case fold, the declared covering-rank divergence covering final plans, the record-core error arm in translateFDBError (section 7), owner items (section 14).
Tree objects: run `git show TREE:path` and `git diff OLD NEW -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); tag 4.12.11.0 is available for diffs (git -C /home/birdy/projects/fdb-go/fdb-record-layer diff 4.12.11.0 4.14.2.0 -- path). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.
Read ALL three v3 verdicts, the FULL v4 design and its diff from v3, the oracle source, target steps and evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions, not only whether requested words appear. Distinguish MEASURED target facts from SOURCE claims and HYPOTHESES. WS-E v11's Graefe verdict relied on this design for the RFC-182 second crossing (REWRITING winner only crosses, unified_tasks.go): say whether v4 states and closes it.
Preserved boundaries: the owner-approved Go read/raw nullable-array, scalar and sorting extensions; LIMIT (approved Go extension); the materialized outer-join NLJ (RFC-152); RFC-209 companions; the rebind validator; C++ 7.3.77; format max 15/default 14; SPFresh/LIRE independent of GuardiANN; Lucene separate. (There is no Go IN-subquery read extension at this base; v3's prompt named one in error. Both engines refuse IN-subquery, 0AF00.)

Apply the virtual Goetz Graefe architecture lens: Cascades fidelity of the W6 port (progress, staleness, conditional chains, pruned-input scheduling, disentangled finalization, the physical REWRITING prune and every crossing, the memoizer and its extraction check, the plannability exit rule), the W7 reader/plan/property model, the W8 cost-model rung and relational planner configuration, the IN-list decisions (the horizon mark, the IN rule matching a select, nested in-unions, product size), F-8's per-partition yields against the rule-internal pre-selections, the kept rank against the measured cyclic relation and what the declared divergence covers, the W9 sargability and implication rules, and whether any decision is a Go-only substitute for a Java mechanism.
