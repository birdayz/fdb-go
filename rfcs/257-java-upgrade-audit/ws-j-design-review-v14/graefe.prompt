Review the revised RFC-257 WS-J DESIGN (v14), and the fixes it records as LANDED, not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE e23ae6f5589da822c878fb9ab436db9a09095ef2; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 3308f31a9674a12f79514a357d59694900289454f235ef12e1733fb7dab47254 (3178 lines). Previous: v13 TREE d6ad3e9c2c5bc3a32c42cf0399c97afade52e098 (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v13/*.txt); v1-v12 in ws-j-design-review-v1/-v12. The v13 design is at /var/tmp/fdb-upgrade-recovery/ws-j-design.v13.md; git diff d6ad3e9c2c5bc3a32c42cf0399c97afade52e098 e23ae6f5589da822c878fb9ab436db9a09095ef2 -- rfcs/257-java-upgrade-audit/ws-j-design.md shows v14 (the tree diff also carries WS-C revision 10, WS-F v4, WS-D v15 and WS-E notes, gated separately).
Delta since v13 (the design's Status paragraph lists it, each change marked v14 at its site): the framing shapes of 4c: LegacyFramingError WITHDRAWN, the ordinary load reads a union both builds choose as the target does (type-first, implicit record-type key = smallest union field, RecordType.java:171-179), because a refusal would lock Go out of Java-written stores; shape (g) redefined against the pre-upgrade last-field key; a store-open probe (LegacyRecordTypeKeyError) for the one form a limit-1 read can detect (primary key beginning with recordType()); the edited-file route takes the records' writer (PreUpgradeGo/Target) and compares record-type keys at (iii); the migration writes the explicit key toward the pre-upgrade choice; the residual declared in 9 (y) as an owner item. The restore: check (iv) runs the relational validator (enum order, VECTOR options); no-rename via SetDisallowTypeRenames; the literal-carrier arm symmetric for the restore only. R2's index at last-modified stored + 1; checkReservedNumbers on all three writers over every reservation, drop refused, declared. Section 3.6: key-expression deserialization completed as Java's (Field name/fan type, invalid fan type, function name/arity at load, DeserializationException extends RecordCoreException, MetaDataProtoDeserializationException wrapping; oracle measures both layers). 9 (x) inverted history has no exit; R2's remedy is a later ordinary save; 9 (z) CreateTemplate conflicts. Lock helpers renamed by lock behaviour. Stale text swept (section 8 step 3, 9 (w) cross-reference, a line citation, CHANGELOG plan, RepairSchema table row). DIVERGENCES.md: PENDING entries for (w), (x), (y), reservation entry corrected. TODO.md: a v14 block appended. Mutation record: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v14.txt (m97/m98/m103 from WS-C 7.9's run, and the HeldBy array-element mutation at /var/tmp/fdb-upgrade-recovery/wsj-held-mut/). The WS-J JVM oracle ran inside rfc257_oracle_test on tree 512ae24895a17ec693b76bfb6d6d31cd63a8ee32 (61 of 61, /var/tmp/fdb-upgrade-recovery/wsc-v10/ev2/oracle.log); e23ae6f5589da822c878fb9ab436db9a09095ef2 differs from 512ae248 in no WS-J code (git diff --stat).
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); the pre-upgrade Go builder and loader are at commit e48f5b496.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which has moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline; wire compatibility with Java-written stores is the hard line. (There is no Go IN-subquery read extension at this base; v13's prompt named one in error.)
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Read ALL three v13 verdicts and check that EVERY finding is resolved, the FULL v14 design and its diff from v13, the landed code and tests, the oracle and its evidence, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (the withdrawal of the load refusal against wire compatibility and against pre-upgrade-Go-only stores, the store-open probe's soundness and false positives, the route's writer argument, the key comparison, the restore's four checks against every way two histories can agree in keys and differ in meaning, the symmetric carrier arm, reservations on every writer, R2's interval, the deserialization port), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the DDL front end, IndexSpec and QuantifierValues on the translated graph, value-index expansion and the non-integer match policy, metadata assembly order, lane resolution at construction, the key-expression deserialization and its class hierarchy, and the evolution/carry policy for stored templates; and whether the framing decision is Java's reading and the residual correctly scoped.
