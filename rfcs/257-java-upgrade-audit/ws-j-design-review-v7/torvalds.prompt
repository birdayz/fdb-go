Review the revised RFC-257 WS-J DESIGN (v7), and the fixes it records as LANDED (sections 3.1, 3.2, 4b, 4c's validation, load order and shape-(d) refusal, the section-0 instrument), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 1718c9c7437d3038bdadd6defd996d7ca6824997; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 d2b84437093a214f195cb3bd03d46b9ae7305b2617bd38f5b63439a6e77f936a (1799 lines). Previous: v6 TREE 10a2023678daaa361c9d5cad4826ae1609c51324 (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v6/*.txt); v1-v5 in ws-j-design-review-v1/-v5. `git diff 10a2023678daaa361c9d5cad4826ae1609c51324 1718c9c7437d3038bdadd6defd996d7ca6824997 -- <path>` shows v7 exactly (both are tree objects); the v6 design is also at /var/tmp/fdb-upgrade-recovery/ws-j-design.v6-final.md.
Delta since v6 touching WS-J: (1) PRODUCTION: shape (d)'s refusal moved from the proto loader into RecordMetaDataBuilder.Build, last, run whenever the union was FOUND rather than named (a new builder flag unionNamed set by setRecords), so SetRecords over a (d) descriptor is refused as loaded metadata is and SetRecordsWithUnionName naming the found union builds it (pkg/recordlayer/metadata.go); the exemption is narrowed from "a field's type anywhere" to "reachable from the found union through message-typed fields" (reachableThroughFields, replacing messageUsedAsFieldType, metadata_proto.go). (2) TESTS: TestStoredMetaDataWithAnAmbiguousLegacyUnionIsRefused runs every case through the loader AND SetRecords (and SetRecordsWithUnionName for refused cases), with the Holder case flipped to refused and three new shapes (a record type's field, an envelope, a self-holding union); new TestAmbiguousLegacyUnionRefusalComesAfterTheTargetsFaults; the JVM records spec gains two shapes (31); the spec "WS-J a table or struct named UnionDescriptor" now loads the target's template through Go's catalog library and creates and queries the same DDL through the Go SQL driver, taking both outcomes before asserting; the F2b Describe gains a spec of `d + 0`/`f + 0` indexes whose entries are the saturated operand, byte-compared across engines (entries helper extracted to wsjIndexEntriesByID). (3) DESIGN v7 (its Status paragraph lists the changes): 4c's (d) condition and scope, migration arm for a referenced UnionDescriptor, output check naming the union; section 4's EQUIVALENT comparison (Java's normalization, unknown fields and extensions carried not compared through a stored-proto carrier, proto equality) and what WIDENED/CHANGED drop; 3.2's lane check on the build path (both CreateTemplate implementations) with a write-path table and the whole ArithmeticValue lane table; section 2's restore header check (header <= md, batched pre-reads) and the fresh-template guard over the whole name; 3.3's LIMIT/OFFSET over every limit clause in tree order; test 5's fixture and discriminating reads; 3.5's coverage wording; section 9 (r), (t), (u); stale counts. (4) CHANGELOG (code-built metadata), DIVERGENCES.md (the (d) entry rewritten, a new synthetic-type entry, the restore check), TODO.md (the first merge-unit block corrected in place, the v6 block amended), ws-j-oracle/README.md (items 9, 10, 17), evidence-mutations.txt (the v6 note corrected, the v7 block), evidence-v7.txt.
Evidence: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v7.txt and evidence-mutations.txt (v7 block). Run records under /var/tmp/fdb-upgrade-recovery/: wsj7-ev1.log, wsj7-ev2.log, wsj7-vr-jvm.log, wsj7-justtest.log and wsj7-justtest-*.log, wsj7-evidence.sh/.out, wsj7-src.sha, wsj7-files.txt; wsj7/ (unit mutation logs wm1-wm4, wm1-vr.log, wm2-ud.log, f2b.log, f2b-mut-signflip.log, and the .good copies); wsj7-vr.log and wsj7-ud.log (the first focused runs).
NOT WS-J, same tree, own gates: WS-C section 7 addendum (revision 2 NAK, under revision), WS-D v13 (NAK, under revision), WS-E v7 (NAK, under revision), WS-F v2 (NAK).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v6 verdicts and check that EVERY finding is resolved, the FULL v7 design and its diff from v6, the landed code and tests, the oracle and its evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions (the Build-time (d) check against every builder and loader path and every way a file can hold a message named UnionDescriptor, the reachability rule, the migration arms and output check, the EQUIVALENT carrier, the write-path table and lane table, the restore check and its batching argument, the whole-name guard, the LIMIT walk, test 5, the merge unit), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes in the landed fixes (option order, carrier arm, downgrade, INT refusal), test vacuity (class ratchet, companion canonicalization, discriminating reads, pins), incomplete conversions, evidence that does not bind the bytes, duplicate mechanisms, maintainability.
