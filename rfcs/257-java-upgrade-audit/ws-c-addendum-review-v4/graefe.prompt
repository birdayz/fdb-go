Review REVISION 4 of an ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together. Repo /home/birdy/projects/fdb-go. Candidate TREE 936a4aba27681bdd9a5e2938021f54d7b803e2a1; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 4249af27134885d20973c635fb920084f25000211419c08fcb04410d46c00786, 943 lines); 7.1 is revision 1, 7.2 revision 2, 7.3 revision 3, 7.4 revision 4, each stating what changed and why. Revision 3 was gated at tree 72fe965617516b0b9d9ebef01d28a19e5ef21007 and got three NAKs: read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v3/{graefe,torvalds,storage}.txt (and v1's and v2's in ws-c-addendum-review-v1/ and -v2/) and check that EVERY finding is resolved, not only the ones 7.4 mentions.
What changed: both trees are tree objects, so `git diff 72fe965617516b0b9d9ebef01d28a19e5ef21007 936a4aba27681bdd9a5e2938021f54d7b803e2a1 -- <path>` shows revision 4 exactly for these files (the same diff also carries WS-J design v8's changes to metadata*.go, the catalog and conformance specs, gated separately). WS-C files: pkg/recordlayer/online_indexer.go (BuildIndex's call outcome; IndexBuildOutcome docs; newHeartbeat through newIndexingHeartbeatWithID; the attempt's heartbeat info and the source index resolved by name; Build's validateIndexSetting order; IndexerID on the errors), pkg/recordlayer/online_indexer_queue.go (cleanupHeartbeatAttempt reads before clearing; checkOpenHeartbeats' Java claim; the follow-up comment; IndexingValidationError.IndexerID), pkg/recordlayer/indexing_heartbeat.go (newIndexingHeartbeatWithID), pkg/recordlayer/errors.go (PartlyBuiltError.IndexerID), cmd/frl/internal/cmd/index_write.go (formatPartlyBuilt), pkg/relational/core/fleet/build.go (buildPending; partlyBuiltAdvice; total after the error check); tests in online_indexer_test.go (the blocked-stamp spec's attempt count and IndexerID; the outcome reset; "reports a call whose earlier attempt built before the peers published as built"; "stamps and scans the metadata's source index..."; the unscannable-source state; the arm table's unknown action; the builder's duplicate-target and order cases; ForbidRecordScan IndexerID), online_indexer_session_test.go (the barrier's holdDispatch mode; the late-clear spec), cmd/frl/internal/cmd/index_test.go, pkg/relational/core/fleet/build_test.go (TestBuildPendingReportsWhatTheTenantBuilt, TestPartlyBuiltAdviceOffersOnlyWhatTheFleetCanDo); CHANGELOG.md (the OnlineIndexer entries), DIVERGENCES.md (the section "OnlineIndexer session start and build catcher: where Go differs"), ws-c-design.md (7.2's Java claim, 7.3's retry cost and preparation wording, 7.4), ws-c-cleanup/README.md.
Evidence: /var/tmp/fdb-upgrade-recovery/wsc-v4/ (mut-m23..m33 *.log with the perl expression, marker count and applied diff, and *.full.log; mut-m30.log is a go test run; run-mutations.sh, mutate.sh; focus1.log, the unmutated focus; late1.log and m-late.log, the first runs of the late-clear spec) and /var/tmp/fdb-upgrade-recovery/ev8/ (wsc-focus.log, wsc-loop40.log, justtest.log, justtest-tree.txt, justtest-tree-after.txt, src.sha over 66 files listed in ../ev8-files.txt; ev8-run1/ is a discarded first run that failed to build the fleet test). ws-j-oracle/evidence-v8.txt lists the hashes.
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/: OnlineIndexer.java:133-316, :855-885, :1006-1100, :1240-1360; IndexingCommon.java (indexerId); IndexingBase.java:120-135, :170-180, :184-261, :357-620, :955-1015, :1160-1184, :1285-1300; IndexingByIndex.java:60-260; IndexingHeartbeat.java; IndexingThrottle.java:330-440; FDBRecordStore.java:2610-2700, :3940-3990, :4841-4990, :6005-6020.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Check every v3 finding's resolution against Java and the code, the late-clear fix (does the read make every late clear conditional; what the barrier's holdDispatch mode models and whether the spec can pass vacuously), the call-level outcome and its callers, the source resolution, the INDEXER_ID sites, the builder order, the declared divergences (are they all declared, and are they true), the test population and its vacuity (the mutation logs carry the applied diff), and the CHANGELOG. Distinguish measured facts from source hypotheses.
Preserved boundaries: approved Go extensions; C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the build session as a whole (launcher/catcher, session start, per-transaction state checks, publication), phase boundaries, and whether any Go-only behaviour remains undeclared.
