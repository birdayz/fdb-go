Review the revised RFC-257 WS-J DESIGN (v15), and the fixes it records as LANDED, not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 0e057cc76c3121918e75b71d2b51a37fed21faf9; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 48db2141a5c55e892e625be62a7688daa62439efb6310553c6b8f4e741d5722c (3376 lines). Previous: v14 TREE e23ae6f5589da822c878fb9ab436db9a09095ef2 (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v14/*.txt); v1-v13 in ws-j-design-review-v1/-v13. The v14 design is at /var/tmp/fdb-upgrade-recovery/ws-j-design.v14.md; git diff e23ae6f5589da822c878fb9ab436db9a09095ef2 0e057cc76c3121918e75b71d2b51a37fed21faf9 -- rfcs/257-java-upgrade-audit/ws-j-design.md shows v15 (the tree diff also carries WS-D v16 text, WS-E v12 and its oracle, DIVERGENCES/TODO bookings and the umbrella RFC's owner-decision gate, item 9, gated separately).
Delta since v14 (the design's Status paragraph lists it): a new section 4d whose decisions supersede the v14 sentences marked [v15 → 4d]. One framing criterion for shapes (d), (f), (g): read as the target reads wherever the target loads, which flips the landed ordinary-load refusal of (d) (moved to the legacy routes and CreateTemplate), with the owner's confirmation made a merge gate (umbrella RFC item 9); the implicit key stated for every pre-upgrade framing; the explicit key named RecordType.explicit_key and SetRecords reading the record_type_key option as the target's in-code path does; the store-open probe made sound (every key type-prefixed, no type keyed at the number, snapshot read, conditional remedy, index roots and the count key, an operator detector); checkReservedNumbers(stored, new, union, writer) pairing as-framed; 3.6's limits (function names still loaded, arity checked for Go-registered functions via RegisterFunctionWithArity, parse-level required fields, absent nested children, count-key wrapping, NullStandin ported as a wire fix); smaller corrections to (f), (x), (z), the lock helper and the mutation's binding (/var/tmp/fdb-upgrade-recovery/wsj-held-mut/binding.txt). DIVERGENCES.md and TODO.md carry the matching bookings.
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); the pre-upgrade Go builder and loader are at commit e48f5b496.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which has moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline; wire compatibility with Java-written stores is the hard line. (There is no Go IN-subquery read extension at this base; v13's prompt named one in error.)
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Read ALL three v14 verdicts and check that EVERY finding is resolved, the FULL v15 design and its diff from v14, the landed code and tests, the oracle and its evidence, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (the one framing criterion and the flipped (d) refusal against wire compatibility and against pre-upgrade-Go-only stores, what an upgrade does to each pre-upgrade framing and its implicit key, SetRecords and explicit_key against the target, the store-open probe's soundness, false positives and footprint, the as-framed reservation pairing on every writer, the deserialization limits and the NullStandin port), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.

Apply the virtual Linus Torvalds code quality lens: logic holes, incomplete conversions, dead code, test vacuity, lock discipline and helper naming, and whether each declared refusal is reachable and pinned (including the store-open probe and its skip condition).
