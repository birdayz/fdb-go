Review the revised RFC-257 WS-J DESIGN (v11), and the fixes it records as LANDED (sections 3.1, 3.2, 4b, 4c's validation, load order and shape-(d) refusal including the catalog's store and load refusals, the loader's value_expression, added_version, subspace-key and option-order readings, the section-0 instrument), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE aa29308afd7637d7e98a42ef1d108335013ead8c; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 33a8fc1e35d537978db7e69ad40a140870911b3829df9d6a108e6bc7abdbf608 (2532 lines). Previous: v10 TREE c789832131f2e6206ee2ebad9b92a3ae463d6f6d (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v10/*.txt); v1-v9 in ws-j-design-review-v1/-v9. `git diff c789832131f2e6206ee2ebad9b92a3ae463d6f6d aa29308afd7637d7e98a42ef1d108335013ead8c -- <path>` shows v11 (both are tree objects; the diff also carries the WS-C addendum's revision 7, gated separately).
Delta since v10 touching WS-J: (1) CODE AND TESTS: the loader reads an index's options with its type, before the root and the subspace key (pkg/recordlayer/metadata_proto.go; TestStoredIndexSubspaceKeyIsReadAsJavaReadsIt's fourth shape and the WSJIXK spec's fourth shape); AmbiguousLegacyUnionError gains HeldBy and Stored, and LegacyUnionTemplateError(cause, stored) marks the loader's own error in place, only for the SQL family, with a load-side remedy (pkg/recordlayer/metadata_proto.go legacyUnionHolder, pkg/relational/core/metadata/builder.go, pkg/relational/core/catalog/fdb_template_catalog.go; TestLegacyUnionTemplateErrorRemedies, NEW; the WSJTT spec's assertions per path); the subspace-key specs read Go's side from marshalled bytes and require the class per shape; TestReusedIndexMessageKeepsAnEmptySubspaceKey (NEW). The former-index key reading, SetSubspaceKey(nil) and RecordCoreArgumentError's rendering are in WS-C revision 7 (ws-c-design.md 7.7) and measured there. (2) DESIGN v11 (its Status paragraph lists the changes): the edited-file route's own entry SaveEditedLegacyMetaData, its stored-side load as written (loadStoredAsWritten: the union by name, the legacy name-first framing, validateRecords and (d) suspended, the rest run), R1 and R2 as the only relaxations, the migration validated by its re-application only, its tests (4c); CreateTemplate as the one write entry carrying a stored name itself, CreateTemplateFromProto dropped, the lane check over NEW, CHANGED and WIDENED (3.2, the write-path table, 4, 8, (t)); the in-memory catalog's guard wiring, DeleteTemplateVersion refusal and (d) refusal (section 2, 4c, (s)); the restore's carry-invariant check against stored versions of t, the limit-1 read's conflict scope, test (iii) and (iv) (section 2); the loader text, the covering spec's comparison, the vtproto reuse pin, the SQL remedy by shape and site (4, 4c); citations. (3) CHANGELOG, DIVERGENCES.md ((t)'s writer), TODO.md (a v11 merge-unit block at the end), the umbrella's gate 8, ws-j-oracle/evidence-v11.txt.
Evidence: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v11.txt. Run records under /var/tmp/fdb-upgrade-recovery/: ev11/ (wsj-ev1.log, wsj-ev2.log, jvm-md.log, wsc-focus.log, justtest.log, justtest-executed.txt, justtest-tree*.txt, src.sha, src2.sha, final-targets.log, final-recordlayer-test.log), ev11a.sh, ev11b.sh, ev11c.sh, ev11-files.txt, ev11c-files.txt; wsc-v7/wsj-ev-code.log (the WSJTT, WSJUD and WSJIXK specs on the code before the evidence run).
NOT WS-J, same tree, own gates: WS-C section 7 addendum (revision 7, gating now), WS-E v9 (NAK, being revised), WS-D v13 (NAK), WS-F v2 (NAK).
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); the pre-upgrade Go builder and loader are at commit e48f5b496.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Read ALL three v10 verdicts and check that EVERY finding is resolved, the FULL v11 design and its diff from v10, the landed code and tests, the oracle and its evidence, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (the stored-side load against every (a)-(d) shape and holder, its suspended and run checks, R1's reach and R2's rules, the migration's re-application as its only validation, the one write entry against every caller and both catalogs, the in-memory wiring's lock order, the restore's carry-invariant check and conflict ranges, the remedy marking by shape and site, the option order), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.

Apply the independent storage/wire correctness review lens: stored MetaData bytes (key expressions, literal widths, option order, record-type keys, union numbers, versions), index entry bytes under the operand widening, the upgrade path for templates Go already stored (carry rule, validator options, rebuilds, former indexes), the fresh-template guard, catalog existence policies and their conflict ranges, and the driver keyspace gap.
