Reconfirm the revised RFC-257 WS-E DESIGN (v7), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 9265beef3e5c9dd9ec86caaade526c8137552976; design rfcs/257-java-upgrade-audit/ws-e-design.md SHA256 ffcf4f2edb9e0063d1711250d81b90aa7199c66a96fc1187fc1964a9a8f19dac (2495 lines). Previous: v6 TREE 66663d5d8d1946a4891985e75c682cb98a18609f (three NAKs, rfcs/257-java-upgrade-audit/ws-e-design-review-v6/*.txt); v1-v5 in ws-e-design-review-v1/-v5. The v6 design is at /var/tmp/fdb-upgrade-recovery/ws-e-design.v6.md for a diff (diff it against `git show TREE:rfcs/257-java-upgrade-audit/ws-e-design.md`).
Delta since v6 touching WS-E: (1) oracle: three UUID boundary rows added to Describe "WS-E target oracle v6" (54 pins; 387 total); evidence-run.txt regenerated (two uncached runs, 387 blocks equal, nine source hashes verified after), evidence-mutations.txt gains "Harness teardown, v7" and "Design v7" blocks; (2) harness: the Go runner's drops run under context.WithoutCancel with their own timeout through an unexported teardownExec seam (pkg/relational/conformance/plandiff/go_runner.go), TestGoSQLRunner_TeardownFailureIsReported now drives the seam and cleans up, TestGoSQLRunner_CanceledRunStillTearsDown added; (3) TestFDB_TemporalComparandDateColumn asserts R's residual plan; (4) the design revised throughout (Status paragraph lists the changes): section 5.4(c) where the ported rule meets a one-source WHERE and who decides (PLANNING, measured on a prototype), 5.4(a) the whole suite on the fold-free prototype including the conformance targets, the EvaluateConstant census and the three walk-time numeric coercions, 5.4(g)/(j); section 4.1(b)-(c): the explode rule in PLANNING only without the idempotency scan, the partition route (PartitionBinarySelectRule's uncorrelated-explode guard deleted), the single-element collapse deleted with Go's InUnion made to cover (two causes), the own-row correlated comparand as a target tie decided in Go by the EstimateCost rung, record IN; section 4.3 canonical temporal writes, the DATE-TIMESTAMP lane carried statically with year saturation, the time.Time comparison arms deleted, UUID's six parser sites and component boundary, UTF-8 over map keys and whole-batch validation; 5.3 soundness rules; section 6.4 the full readIndexState census; (5) TODO.md: the Finding 6-followup block's attribution by subtraction.
Go-side prototypes the design cites (detached worktrees; results in /var/tmp/fdb-upgrade-recovery/): /home/birdy/projects/fdb-nofold-probe (base tree 010199ee954021518dea18ebd9371111d432af6f, diff wse8-proto.diff; wse8-proto-run.txt, wse8-proto-hashinv.txt, wse8-proto-planhashinv.txt; the whole suite: wse8-proto-full.log and wse8-proto-full-logs/); /home/birdy/projects/fdb-explode-probe (base tree 2964d0e40092d14b1689132b90d02ea844ccbf9f; env-toggled variants; wse8-explode-findings.txt, wse8-explode-base.txt equal to plan_shape.golden, wse8-explode-A/A2/B/C.txt and .diff). Earlier: wse7-nofold-*.log, wse7-runB*/wse7-runTaut*. Oracle run records: wse8-all1.log, wse8-all2.log, wse8-teardown.log, wse8-td-final.log, wse8-evidence.out, wse8-src.sha, wse8-M7.log; the harness mutations wse8-td-mut-*.log, wse8-tplmut.log; just test of this tree: /var/tmp/fdb-upgrade-recovery/wse8-justtest2.log (tree 9265beef3e5c9dd9ec86caaade526c8137552976 before and after the run; Executed 3 out of 94 tests: 94 tests pass, the rest cached from runs of identical inputs; the preceding run on tree 16e56223fdb6b039e0f39dd49702f36141eb86c4, wse8-justtest.log, executed 39 and found recordlayer_test red on specs the WS-C section 7 census then converted).
NOT WS-E, same tree, own gates: WS-C section 7 addendum (OnlineIndexer desired action, found by a flake during the prototype suite run; gated separately), WS-D v13 (NAK, under revision), WS-F v2 (NAK), WS-J v5 (NAK, under revision).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). The JDK 21 UUID source is at /tmp/jdksrc/java.base/java/util/UUID.java. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool; the prototype worktrees may be read with Read/Grep.
Read ALL three v6 verdicts, the FULL v7 design and its diff from v6, the new oracle rows and evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions (the fold port meeting a one-source WHERE and PLANNING deciding; the whole-suite reds and their accounting; the explode rule's phase, termination and partition route, the guard deletion and what relied on it; the InUnion covering fix and its two causes; the own-row tie; the temporal lane and canonical writes; the UUID sites; the readIndexState census), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; no second query pipeline; Lucene separate.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes, incomplete conversions, dead code, test vacuity (pins, renders, set checks, the harness teardown), evidence that does not bind the bytes, duplicate mechanisms and maintainability.
