Reconfirm the revised complete RFC-257 WS-D DESIGN (v11), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 01b0669d8d12e6eb84eebfa31f160cfed853e5d5; design rfcs/257-java-upgrade-audit/ws-d-design.md SHA256 6a1dd51dc89cef8f155caf924982f17cc2ba0b5346367668f4e253cbb79205ec (1489 lines). Previous: v10 TREE f90241712769c8d5f92498c51c2183a4320ed9b0 (four NAKs, rfcs/257-java-upgrade-audit/ws-d-design-review-v10/*.txt); earlier rounds in -v1..-v9. Accepted WS-C source TREE f0cb29576cde624e0f1c6e22ca18a5180d1b298f.
Delta since v10 touching WS-D: (1) retry owners: v10's 1007 argument is withdrawn; new phase D-0 (section 5 'Attempt bounds of the transaction owners') ports Java's runner attempt bound to FDBDatabase.Run (TODO.md item 'Port Java's FDBDatabaseRunner default maxAttempts=10...', new TODO block at EOF points both ways) and runs each merger attempt through the runner as IndexingMerger.java:85-86 does; 1007 and 2101 classified per owner at the peel sites (section 6); (2) the v10 stride-sampled refit is WITHDRAWN on new evidence (oracle item 23: TEST-ONLY Java step guardiannPeelGeneratedProbe generating shapes in the JVM, spec 'measures the unsampled floor peel at high dimension', 16 pinned rows at d=768..4096, sampled balance falls to 243/2000 at d=4096); unsampled refits restored; the peel is admitted by a clock-free rule floor(log2(n-1))*n*d <= 4e7 (Step 2 when refused); ClusterUnsplittableError gains a cause; (3) the peel RNG split is taken once at Go-rule entry on every Go-rule path and never on a path where the target completes; (4) fixtures restated with encoding and computed write volume (d=768 HALF 3.1 MB, d=2048 n=1001 HALF 4.1 MB, d=4096 n=2000 refused by admission); balance cascade termination; Step 2 population; (5) PRODUCTION FIX (landed with pins): heartbeatIndexerID follows Java's getUUID(0) — trailing elements accepted (pkg/recordlayer/indexing_heartbeat.go), Go and JVM specs gain a versionstamp key sorting AFTER a live heartbeat (ClassCastException in Java, IndexingHeartbeatKeyError in Go) and a (UUID, x) key read as that UUID in both; indexing_heartbeat_test.go's (UUID,1) rejection re-pinned as Java's reading; DIVERGENCES and code comment updated; mutations (exactly-one-element parse; single-pass parse) redden both specs (item 24, evidence-mutations.txt); (6) citations (:223, :505-507/:349), item-15 label, skip snapshot reads incl. merge neighbour metadata, the peelBound column of the item-18 rows now prints floorBound = floor(log2(n-1)) (130 pinned lines rewritten by formula); (7) evidence rebound: evidence-run.txt from two uncached 25/25 runs with sha256 of every file in the target, README items 20 (corrected/withdrawn), 23, 24, 25 (just test summaries copied in). NOT WS-D, same tree, own gates: WS-E v3, WS-F v1, WS-J v2 (their files and review directories are in the tree).
Run records: /var/tmp/fdb-upgrade-recovery/rfc257-v11-1.log and -2.log (oracle, uncached), wsd-gen-1/-2.log and wsd-gen-mut.log (item 23), hb-v11-*.log (item 24), just-test-v11.log and just-test-v11b.log with manifests wsd-v11-manifest-before/after.txt (README item 25).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL four v10 verdicts, the FULL v11 design and its diff from v10, the oracle README and evidence, the oracle source/test, and check claims against actual Java and current Go. Challenge sufficiency and interactions with the rest of the design (especially D-0's blast radius on every record-layer transaction and the admission rule), not only whether the requested words appear. Distinguish measured target facts from remaining source hypotheses.
Preserved boundaries: GuardiANN executes in merger-supplied transactions (no invented child runner); SPFresh independent algorithms/LIRE; approved raw/nullable-array/scalar/sort extensions; C++ 7.3.77 (the raw pure-Go client keeps libfdb_c's unbounded default); format max 15/default 14; Lucene is separate upgrade-wide work, not Go TEXT; WS-D cannot complete with a refusal or fake backend.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the SPANN/SPFresh paper authors review lens and independent lifecycle safety lens. Read RFC094, relevant SPANN/SPFresh sections in .claude/skills/spfresh-reviewer/ PDFs (use pdftotext if available, else rely on RFC-094 citations and say so), and relevant unchanged SPFresh call paths. Verify the GuardiANN peel (unsampled refits, admission rule, balance) and inherited queue refusal, maintenance gate, MergeIndex and the new D-0 attempt bound do not alter LIRE or SPFresh algorithms/lifecycles. Cite paper sections for paper findings, file:line for lifecycle findings.
