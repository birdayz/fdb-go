**NAK — WS-C design authorization only.** This is neither an implementation review nor a whole-upgrade ACK.

I read the complete design, parent WS-C, retained probe, and relevant Java/Go sources. Java checkout is `fdacd162a9c8acfadc49082b89185c823ab8ae4a`; inspected Go integration files match accepted tree `48b50548b11a53bd1452a627689c440535a3d093`. Published HEAD remains `71ccd8cf8b3fd0dbafe283e91171818e36af555e`. Session metadata confirms `gpt-6-astra` / `xhigh`. No edits, tests, git mutations, publication, or delegation performed.

Exact [design](/home/birdy/projects/fdb-go/rfcs/257-java-upgrade-audit/ws-c-design.md) SHA256, verified with `sha256sum`:

`3b18df4350eaa955bf6e195e7b18c8eeec71b725eb5252be610453b7290dbe0c`

Required corrections:

1. **Protect readable transitions against uncommitted enqueues.** An FDB emptiness read cannot see queue chunks buffered in the context until commit. With completed build ranges, enqueue → checked readable setter in the same transaction can therefore publish an incomplete index. Specify a context-wide, per-index pending-enqueue guard shared across store handles, with clear/cancellation bookkeeping and a regression. The proposed guard must cover both persisted and buffered entries. Evidence: [mutation buffering](/home/birdy/projects/fdb-go/pkg/recordlayer/database.go:920), [commit ordering](/home/birdy/projects/fdb-go/pkg/recordlayer/database.go:1285), [Java emptiness read](/home/birdy/projects/fdb-go/fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/queue/PendingWritesQueue.java:269).

2. **Choose the legacy-heartbeat migration protocol now.** “Must be an explicit policy” leaves the central decision unresolved. Old Go ignores UUID tuple keys; Java extracts UUIDs before checking age and does not catch string-key type failures. Upgraded-reader dual decoding alone cannot provide reciprocal exclusion. Specify enforceable quiescence/fencing of old builders, safe removal of legacy keys, and activation conditions for UUID-only writers. Evidence: [Go decoding](/home/birdy/projects/fdb-go/pkg/recordlayer/indexing_heartbeat.go:113), [Java checking](/home/birdy/projects/fdb-go/fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/indexing/IndexingHeartbeat.java:101).

3. **Define terminal session cleanup beyond readable closeout.** Cover successful builds with readability disabled, failures, and cancellation. Java performs best-effort cleanup of this session’s heartbeats across all targets on exit. Specify cleanup transaction ownership, cancellation handling, preservation of the original error, and lease-expiry fallback. Otherwise newly exclusive ordinary builders can leave successors unnecessarily blocked. Evidence: [Java terminal cleanup](/home/birdy/projects/fdb-go/fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/IndexingBase.java:171).

4. **Make split-resource outcomes explicit.** Java fetches a KV before checking the unsplitter’s scan budget, continues assembly after a nonthrowing limit, and charges buffered lookahead bytes again when appending them. Queue time limits start at cursor construction; fail-on-limit can throw during assembly. Outer row limiting does not probe exhaustion. Specify expected counters, reasons, continuations, and any deliberate deviations—not merely “physical accounting.” Evidence: [unsplitter](/home/birdy/projects/fdb-go/fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/SplitHelper.java:1005), [clock origin](/home/birdy/projects/fdb-go/fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/cursors/CursorLimitManager.java:93), [row limiter](/home/birdy/projects/fdb-go/fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/cursors/RowLimitedCursor.java:60).

5. **Finish the executable wire/retry contract.** Pin unknown-only and known-plus-unknown duplicate proto2 operations; Go’s generated decoder accepts numeric enum values where Java’s required closed-enum parsing differs. Include slashless Any URLs in the oracle. Explicitly state that the drain iterator permits 101 attempts with 100 retries and retries failures beyond FDB’s retryable set, excluding `RunnerClosed`; document any departure. Evidence: [Go enum decoding](/home/birdy/projects/fdb-go/gen/index_build_vtproto.pb.go:1246), [Java retry handling](/home/birdy/projects/fdb-go/fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/runners/throttled/ThrottledRetryingIterator.java:268).

The design correctly retains both closeout conflict edges, fresh mixed-capability eligibility versus mixed resumed-state rejection, and the discovered snapshot-conflict, lost-ownership, heartbeat-coverage, and deletion fixes. The queue-skip evidence is accurately scoped to **one spec, three skip values**. Lucene remains an unresolved backend dependency; generic merger work cannot close it. SPFresh paper correctness is outside this review.