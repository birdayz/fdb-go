Reconfirm the revised RFC-257 WS-E DESIGN (v5), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE c9fd46b8655f19cb0b897f5d272f311121626463; design rfcs/257-java-upgrade-audit/ws-e-design.md SHA256 478dc077b98af12d13987f7e9d95cf93798d7276ebd6e46f3d91d35f96267358 (1726 lines). Previous: v4 TREE 0253cfe9f8e88ce044374b20e6d7ee3fba036cb7 (three NAKs, rfcs/257-java-upgrade-audit/ws-e-design-review-v4/*.txt); v1-v3 in ws-e-design-review-v1/-v3.
Delta since v4 touching WS-E: (1) a fifth oracle round, Describe "WS-E target oracle v5" in conformance/ws_e_probe_conformance_test.go, 75 pinned target outcomes (predicate folding with the REWRITING prune traced, the same null-strict comparison over a two-table and a seven-table schema and in both planning orders, OR over a division with a NOT FALSE disjunct, IS NULL over a NOT NULL COALESCE and over the null-strict collapse, COALESCE under AND and NOT, a collapse nested in a COALESCE head, folded-boolean nullability, typed-NULL IN items under OR/NOT/NOT IN/projection/CASE/join inner over empty and populated tables, STRING lanes with FLOAT/DOUBLE/LONG operands, nested assignment into array elements and struct fields, STRING into ENUM and UUID, index-state read conflicts, read-scope controls); (2) TEST-ONLY Java in conformance/sql_plan_steps.java: RuleTraceListener now records, at each REWRITING OptimizeGroup BEGIN with more than one final member after a simplification, every member's RewritingCostModel criteria and semanticHashCode and the model's pairwise verdict; new step indexStateReadScopeProbe; (3) harness: the plandiff Go runner takes the DML update flag and checks the RowsAffected error; its teardown reports failed drops (withTeardown, pkg/relational/conformance/plandiff/go_runner.go, all arms driven by TestWithTeardown); every catalog DDL of sql_plan_steps.java uses SYS_CATALOG_URL; ephemeralTeardownProbe reports the template and the listing sizes and the conformance_test spec asserts both absences over non-empty listings; the dry-run template of dry_run_connection_create_template is dropped after its probe with the drop and its absence asserted, and the vacuous pre-probe after(...) drop and the extraSetup field are removed; (4) the design revised throughout (its Status paragraph lists the changes); (5) a TODO.md EOF block "RFC-257 WS-E design v5 - pointers for three findings it owns". Evidence: rfcs/257-java-upgrade-audit/ws-e-oracle/evidence-run.txt (regenerated: six Describes, 333 blocks) and evidence-mutations.txt (the "Design v5" block: M1 values, M2 deletions, M3 probe-name uniqueness, M4 the dry-run template cleanup, the teardown template mutation and the withTeardown mutation). NOT WS-E, same tree, own gates: WS-D v13 (under review; SimFDB retry_limit in pkg/simfdb), WS-F v2 and WS-J v3 verdicts.
Run records under /var/tmp/fdb-upgrade-recovery/: wse6-all1.log and wse6-all2.log (two uncached runs of all six WS-E Describes, "Ran 6 of 30", 333 probe blocks equal as sorted sets), wse6-M1..M4.log (mutations), wse6-teardown.log and wse6-teardown-mut3.log, wse6-plandiff-unit.log, wse6-plandiff-mut.log and wse6-plandiff-unit2.log, wse6-stack.log (the oracle run with CONFORMANCE_DEBUG=1 holding the VerifyException stack), wse6-p/q/r.log (development runs of the trace), and the just test of this tree: a first run executed 7 of 94 targets with 93 passing and docscheck failing on a misplaced doc comment in go_runner_test.go (its target logs are wse6-justtest-conformance_test.log: Ran 1419 of 1538 Specs, 1419 passed, 119 inherited skips; wse6-justtest-rfc257_oracle_test.log: 30 of 30), the comment was moved back, and the second run, wse6-justtest.log, executed 2 of 94 with 94 passing (wse6-justtest-docscheck_test.log, wse6-justtest-plandiff_test.log). The evidence-run.txt header records the go_runner_test.go hash change.
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v4 verdicts, the FULL v5 design and its diff from v4, the oracle source and evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions (the REWRITING prune as the folding mechanism and the tie declaration, EffectiveConstant and the three predicate folds, the projection-only generic fold with type preservation, the rule set threaded through recursion, IN lists evaluated at run time via ArrayDistinctValue and the comparand IN source at open, the recursive assignment lattice and string lanes, time.Time canonicalization and the cache key, typed binding order and array element typing, the per-scanned-index state conflicts and PeekIndexStates, the store-open header read, the harness teardown), not only whether the requested words appear. Distinguish measured target facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; no second query pipeline; Lucene separate.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Goetz Graefe architecture lens: Cascades alignment (constant folding regimes, value/predicate simplification, typing and nullability derivation, arithmetic lanes, plan identity and the cache key), Java alignment, phase boundaries and statement admission.
