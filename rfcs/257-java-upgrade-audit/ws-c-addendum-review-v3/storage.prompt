Review REVISION 3 of an ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together. Repo /home/birdy/projects/fdb-go. Candidate TREE 72fe965617516b0b9d9ebef01d28a19e5ef21007; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 a1993f1579acdaa5e59f9adc75c7ac7a751dc7a030dcbdc4ffccbf4b0f25524f, 787 lines); 7.1 is revision 1, 7.2 revision 2, 7.3 revision 3, each stating what changed and why. Revision 2 was gated at tree e7f2e4d540c5ae85d402160d2ef21fba7a0ee221 and got three NAKs: read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v2/{graefe,torvalds,storage}.txt (and v1's in ws-c-addendum-review-v1/) and check that EVERY finding is resolved, not only the ones 7.3 mentions.
What changed: both trees are tree objects, so `git diff e7f2e4d540c5ae85d402160d2ef21fba7a0ee221 72fe965617516b0b9d9ebef01d28a19e5ef21007 -- <path>` shows revision 3 exactly. Files: pkg/recordlayer/online_indexer.go (GetIfMismatchPrevious returns an error for an unknown action; indexerID and lastOutcome fields; newHeartbeat; IndexBuildOutcome and LastBuildOutcome; BuildIndex and buildIndexAttempt report the outcome; Build's source-index refusals as IndexingValidationError with Java's messages; SetSourceIndex doc), pkg/recordlayer/online_indexer_queue.go (preparation heartbeat through newHeartbeat; the undefined-source MetaDataError before admission; RecordCoreStorageError at the follow-up and queue-flag checks), pkg/recordlayer/indexing_mutual.go and indexing_merger.go (newHeartbeat), cmd/frl/internal/cmd/index_write.go (the pre-read removed; indexBuildSummary; formatPartlyBuilt text), pkg/relational/core/fleet/build.go (builtBySession; the PartlyBuiltError text) and fleet.go (OutcomeNoWork doc); tests in online_indexer_test.go (Describe "session outcome, identity and mid-build state changes", nthTransactHook, the numbered setup subspaces, the ForbidRecordScan state assertions, "rejects SetSourceIndex with multi-target" with both Java messages), online_indexer_followup_test.go (RecordCoreStorageError), cmd/frl/internal/cmd/index_test.go (TestIndexBuildSummary_OneLinePerOutcome), pkg/relational/core/fleet/build_test.go (TestBuiltBySessionCountsOnlyWhatTheSessionDid); CHANGELOG.md (the two OnlineIndexer entries), DIVERGENCES.md (the new section "OnlineIndexer session start and build catcher: where Go differs"), ws-c-design.md (7.1's reproducer sentence, 7.3).
Evidence (/var/tmp/fdb-upgrade-recovery/wsc-v3/): justtest.log; mut-*.log (m17-m22 and m71a-c, each with its perl and diff; full logs mut-*.full.log); census-{skip,markreadable,catch}/ (summary.txt with perl and diff, per-target logs); loop40.log; evidence-md5.txt; evidence-tree.txt; focus1.log/focus2.log (the first runs). Revision 2's evidence stays in wsc-v2/.
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/: OnlineIndexer.java:133-316, :855-885, :1006-1100, :1240-1360; IndexingCommon.java (indexerId); IndexingBase.java:184-261, :357-530, :955-1015, :1169-1184, :1240-1390; IndexingByIndex.java:60-260; IndexingThrottle.java:330-440.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Check every v2 finding's resolution against Java and the code, the new Go-only API (LastBuildOutcome) and its callers, the identity across attempts, merges and preparation, the error classes and where the catcher now sends them, the declared divergences (are they all declared, and are they true), the test population and its vacuity (the mutation logs now carry the applied diff), and the CHANGELOG. Distinguish measured facts from source hypotheses.
Preserved boundaries: approved Go extensions; C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the independent storage/wire correctness review lens: what each attempt and each catcher retry writes and clears (index data, range sets, stamps, heartbeats, queue state), what a fallback overwrites, transaction and conflict behaviour of the admission checks and of concurrent and late builders, and the user-visible behaviour changes.
