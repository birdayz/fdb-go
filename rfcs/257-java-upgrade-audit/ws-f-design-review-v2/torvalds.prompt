Reconfirm the revised RFC-257 WS-F DESIGN v2 (planner scheduling, explicit index-entry readers, planner properties: audit items cascades W6-W15) and the test-only oracle it rests on. Repo /home/birdy/projects/fdb-go. Candidate TREE d40cc865737fe57df9f3a0880684608f1a793de0; design rfcs/257-java-upgrade-audit/ws-f-design.md SHA256 afbbf93dd308db399f98723e2523b12c1a314171167424aa14941484dce92c5e (1084 lines). Previous: v1 TREE 5e7c222e40d0557ef65133a8d7da0d88472b7ac4 (three NAKs, rfcs/257-java-upgrade-audit/ws-f-design-review-v1/*.txt).
Delta since v1 touching WS-F: (1) TEST-ONLY oracle v2 conformance/ws_f_probe_conformance_test.go: 70 probes (plain, prepared via runPreparedExtended, one DML via each engine's update path, three target rule traces), both engines pinned (wsfPins, wsfGoPins), Go setup failures rendered as SETUP-ERROR with phase, messages kept to 400 runes, SAME/DIFF comparator, 20-minute context; new target step planRuleTrace in conformance/sql_plan_steps.java (PlannerEventListeners listener: per-rule ended calls and yields; each ImplementInUnionRule final ranked at the end against its reference's other finals with the target's PlanningCostModel); evidence rfcs/257-java-upgrade-audit/ws-f-oracle/evidence-run.txt (two pinned runs identical, hashes before/after, a target-pin and a Go-pin mutation each redden); (2) the design v2: W6 REWRITING comparator decided (2.3), D7 explored-marking scoped, the plannability gate's split exit rule and census on the shipping configuration (2.6), per-phase rule placement (D12), case-insensitive stale sweep (2.8); W7 row-version gate arm, covering-Value salt/tie-break, step 6a aggregate distinct arm as plan-changing; W8 relational configuration rewritten with the measured pair verdicts, rule-level comparators, stale text (4.2); IN-list root cause MEASURED via the rule trace and decision to port the target's union-ordering computation, delete Go's unordered arm, land the size check with it (4.3); W9 IS DISTINCT FROM, sparse index implication, seven range-matchable kinds incl. PromoteValue (5); W11 message/grep (7); W12 plan-changing (8); W13 subscript params, dotted names corrected (Go SELECT works, DML and other sites fail) with the full FullIdToName population, display obligation tied to RFC-238 7c (9); continuations across plan changes (12); acceptance comparator (13); umbrella RFC WS-G list gains the TupleSource mapping. NOT WS-F, same tree, own gates: WS-D v11 (heartbeat parse, peel admission; review dir ws-d-design-review-v11), WS-E v3, WS-J v2; and a LANDED pre-design fix recorded for WS-J v3: records-descriptor validation ported from Java's validateRecords/fetchUnionDescriptor (pkg/recordlayer/metadata_validate_records.go, metadata.go, metadata_proto.go, unit and JVM conformance pins, test fixtures given unions, CHANGELOG) — out of scope here except where it touches WS-F claims.
Run records: /var/tmp/fdb-upgrade-recovery/wsf2-8.log (pins captured), wsf2-9.log and wsf2-10.log (pinned, green), wsf2-mut-java.log and wsf2-mut-go.log (red); earlier probe iterations wsf2-1..7.log.
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); tag 4.12.11.0 is available for diffs (git -C /home/birdy/projects/fdb-go/fdb-record-layer diff 4.12.11.0 4.14.2.0 -- path). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v1 verdicts, the FULL v2 design and its diff from v1, the oracle source and evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions, not only whether requested words appear. Distinguish MEASURED target facts from SOURCE claims and HYPOTHESES.
Preserved boundaries: the owner-approved Go read/raw nullable-array, scalar and sorting extensions; the Go-only IN-subquery read extension; LIMIT (approved Go extension); the materialized outer-join NLJ (RFC-152); RFC-209 companions; the rebind validator; C++ 7.3.77; format max 15/default 14; SPFresh/LIRE independent of GuardiANN; Lucene separate.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes in the decisions, steps that cannot be verified as written, test vacuity in the oracle (pins, probe/pin set equality, render and comparator functions, the Java rule-trace step), incomplete conversions, duplicate mechanisms left behind, stale-claim sweeps and their closure criteria, evidence that does not bind the bytes, maintainability.
