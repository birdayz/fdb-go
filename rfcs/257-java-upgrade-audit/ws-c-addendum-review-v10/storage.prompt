Review REVISION 10 of the ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together, as a DELTA gate over revision 9. Repo /home/birdy/projects/fdb-go. Candidate TREE 9aee2d3d3c3347375454d8cc44f91af4e486f291; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 e80e84bfbcfacbceeae1b6e73baa56249251ae1cd19f4a2d25b1b499fc85869c, 1915 lines); 7.10 is revision 10, stating what changed and why, with its evidence. Revision 9 was gated at tree d6ad3e9c2c5bc3a32c42cf0399c97afade52e098 and got three NAKs, every finding low: read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v9/{graefe,torvalds,storage}.txt and check that EVERY finding is resolved, and that nothing revision 10 changed broke what revision 9 fixed.
What changed: git diff d6ad3e9c2c5bc3a32c42cf0399c97afade52e098 9aee2d3d3c3347375454d8cc44f91af4e486f291 -- <path> (the same diff carries WS-F design v4 with its test-only oracle, WS-D design v15, and WS-E/WS-J doc notes, each gated separately). Code: pkg/recordlayer/metadata.go (AddIndex with an unknown type records the index first; RemoveIndex of an unknown name refused; Build's record-type checks in Java's order and texts over names sorted; validatePermutedIndex at Build), key_expression_validate.go and errors.go (KeyExpressionError unwrapped with Java's texts; QueryInvalidExpressionError), permuted_min_max_index_maintainer.go (PermutedSizeOption as getPermutedSize), pkg/recordlayer/query/executor/executor_new_plans.go and pkg/relational/core/embedded/cascades_generator.go (the query-side permuted count), key_expression.go and key_expression_proto.go (a flat Then; the three Go-side flatteners removed), ranked_set.go (the RANDOM hash returns its read error), rtree.go and rtree_storage.go (root split/promote in place, deleteLeafNode), the chaos verifier; tests named in 7.10; conformance/multidimensional_options_conformance_test.go (the node slot index decoded from raw bytes, independent of the production encoder), conformance/metadata_proto_conformance_test.go; conformance/fromless_select_java_probe_test.go (a flake fix found by this revision's evidence, 7.10 Evidence); CHANGELOG.md, DIVERGENCES.md.
Evidence: 7.10's mutation tables (n1-n21 unit, c6-c8 JVM) with /var/tmp/fdb-upgrade-recovery/wsc-v10/ (run.sh, mutate.sh, run.out, good/, baseline-*.log, each mut-*.log and *.full.log), on the frozen copy /home/birdy/projects/fdb-wsc10 at tree b3b0581c271c87204b52a32b651b20e31341e6f1; the two evidence runs wsc-v10/ev/ (tree b3b0581c, the flake) and wsc-v10/ev2/ (tree 512ae24895a17ec693b76bfb6d6d31cd63a8ee32: head.txt, status.txt, files.txt, src.sha, check.txt, conformance.log, oracle.log, justtest.log, *.uptime); the probe's 10-run check is described in 7.10. The gate tree 9aee2d3d3c3347375454d8cc44f91af4e486f291 differs from 512ae248 by comment-only lines in three Go files and by documents (git diff --stat 512ae24895a17ec693b76bfb6d6d31cd63a8ee32 9aee2d3d3c3347375454d8cc44f91af4e486f291); on the gate tree //pkg/simfdb/... //pkg/recordlayer/... //conformance:conformance_test build and docscheck + simfdb tests pass uncached (/var/tmp/fdb-upgrade-recovery/gate10/, tree-before and tree-after both 9aee2d3d3c3347375454d8cc44f91af4e486f291).
Java sources: fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/: metadata/RecordMetaDataBuilder.java, metadata/MetaDataValidator.java, metadata/expressions/{FieldKeyExpression,ThenKeyExpression,KeyWithValueExpression,SplitKeyExpression}.java, query/expressions/Query.java, provider/foundationdb/indexes/PermutedMinMaxIndexMaintainer*.java, query/plan/cascades/AggregateIndexMatchCandidate.java; fdb-extensions async/RankedSet.java and async/rtree/*.java; fdb-relational-core ExceptionUtil.java (the SQL mapping of KeyExpression errors).
Tree objects: run `git show TREE:path` and `git diff OLD NEW -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). The FDB 7.3.77 C++ source and the Apple Go binding are vendored read-only at /home/birdy/.cache/bazel/_bazel_birdy/cf0955a431539502aaf740b39f444075/external/foundationdb+/ (fdbclient/NativeAPI.actor.cpp, bindings/go/src/fdb/database.go). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline. (There is no Go IN-subquery read extension at this base; earlier prompts named one in error.)
Check every revision-9 finding's resolution against Java and the code; the Build order and texts against RecordMetaDataBuilder/MetaDataValidator; the permuted-size readers; the flat Then; the R-tree root handling and the raw-byte oracle; the mutation logs and baselines; the JVM specs; the flake's root cause and fix. Distinguish measured facts from source hypotheses.

Apply the independent storage/wire correctness review lens: every byte the changed maintainers write (R-tree nodes and the node slot index in both storage layouts across splits and root changes, RANK/RANDOM, PERMUTED_MIN/MAX), what Build admits or refuses that decides what gets written, the proto round trip of Then and of stored indexes, the SQL error mapping of the changed error classes, and the CHANGELOG's upgrade list of indexes to rebuild.
