Reconfirm the revised complete RFC-257 WS-D DESIGN (v13), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 3a934039479f337069d5b483269ecfaffc47817e; design rfcs/257-java-upgrade-audit/ws-d-design.md SHA256 87bfa1569cf9f133d14f6578f11063ae7dc293a7143449f21a72d84416d8355b (1791 lines). Previous: v12 TREE 5a28ecbc718291b8bad5510867c375acfcdff246 (four NAKs, rfcs/257-java-upgrade-audit/ws-d-design-review-v12/*.txt); earlier rounds in -v1..-v11. Accepted WS-C source TREE f0cb29576cde624e0f1c6e22ca18a5180d1b298f.
Delta since v12 touching WS-D: (1) phase D-0 corrected (section 5 'Attempt bounds of the transaction owners (v13)', Decision v13 and Fixtures): the route's scope stated (explicit SQL transactions, autocommit DML statement transactions, OpenContext and the heartbeat cleanup's own body named as outside it); retry limit 0 on all three backends; PRODUCTION CHANGE landed ahead of D-0: SimFDB honours retry_limit in OnError (persistent across OnError, reset by user Reset) and ReadTransact retries through OnError (pkg/simfdb/txn.go, options.go, simfdb.go; pinned by pkg/simfdb/retry_limit_test.go); what the route drops incl. 1235/1242 and 1200; the loop retries the Go-internal 1200 (declared) and states context handling; the census recounted with git grep --untracked (53 FDBDatabase lines) and classified: online indexer 14 (incl. heartbeat admin and merger :27), six lessen-work codes incl. 1004, Go-only owners incl. DST hunts with re-derived error classification (EXHAUSTED class), SQL by route (DDL one attempt; catalog bootstrap, result pages, planner store open, catalog/system/statistics reads the database default; autocommit DML unchanged, v12 double-apply claim withdrawn), SPFresh split into background (spfreshRun, unchanged) and FOREGROUND writes under the caller's bound, declared, RFC-094 section 2 corrected; chaos faults keyed to the route (attempt route: FaultCommitUnknown commits and returns 1021, FaultConflict/FaultTransactionTooOld run the body without committing, reads get FaultTransactionTooOld; client route keeps commit-then-re-execute; v12's pre-body FaultRetryable withdrawn), fault floors per scenario; census includes SPFresh foreground and DST; fixtures on pure-Go, SimFDB and libfdb_c; (2) peel admission: W = floor(log2(n-1))*n*d*max(I*(R+1),32)/32 evaluated in float64, B = 1.96e7 from the worst OBSERVED refit rate over all runs (1.02e-7 s per n*d), admitted corner n=1001 d<=2175, n=2000 d<=980, volumes restated, I=1/R=0 fixture; corner spec re-measured with n=1001 d=2175 and n=2000 d=980 (oracle item 29, 10 pinned rows); (3) PRODUCTION/TEST: heartbeat texts corrected (pkg/recordlayer/indexing_heartbeat.go, indexing_heartbeat_admin.go, DIVERGENCES.md) and the live (U) + unparseable (U,x) pair pinned in the Go spec (renamed) and the JVM spec (conformance/index_state_conformance_test.go); (4) TODO.md item and EOF block rewritten (superseded bypass recipe and v11 loop removed); README items 26 (corrected), 29, 30; evidence-run.txt regenerated from two uncached 29/29 runs. NOT WS-D, same tree, own gates: WS-J v3 (in review), WS-E v4 verdicts (NAK), WS-F v2 verdicts (their files and review directories are in the tree).
Run records: /var/tmp/fdb-upgrade-recovery/rfc257-v13-1.log, rfc257-v13-2.log (29/29 each), v13-oracle.sha, wsd-corner-v13-1.log, wsd-corner-v13-2.log, hb-v13-go.log, hb-v13-jvm.log, wsd13-justtest.log (94/94); SimFDB //pkg/simfdb/... 10/10.
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL four v12 verdicts, the FULL v13 design and its diff from v12, RFC-094's corrected section 2, the oracle README and evidence, the oracle source/test, and check claims against actual Java and current Go. Challenge sufficiency and interactions with the rest of the design (especially D-0's per-owner policies, the transactor route and chaos fault semantics per route, SimFDB, SPFresh foreground liveness, and the admission rule), not only whether the requested words appear. Distinguish measured target facts from remaining source hypotheses.
Preserved boundaries: GuardiANN executes in merger-supplied transactions (no invented child runner); SPFresh independent algorithms/LIRE; approved raw/nullable-array/scalar/sort extensions; C++ 7.3.77 (the raw pure-Go client keeps libfdb_c's unbounded default); format max 15/default 14; Lucene is separate upgrade-wide work, not Go TEXT; WS-D cannot complete with a refusal or fake backend.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Goetz Graefe architecture lens: Java alignment of statement order and state transitions, phase/state boundaries, conflict ownership, retry/commit-bound publication and attempt bounds, determinism of persisted topology, follow-up lifecycle.
