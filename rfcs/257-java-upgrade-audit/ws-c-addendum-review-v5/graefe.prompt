Review REVISION 5 of an ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together. Repo /home/birdy/projects/fdb-go. Candidate TREE f8abe2d742df8c35b44fb0c6f854f86c3cd3a016; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 46ba664d33a2ad07f2a089b41ea4c14fab834514a170b2283ab862f89355f47f, 1051 lines); 7.1 is revision 1, 7.2 revision 2, 7.3 revision 3, 7.4 revision 4, 7.5 revision 5, each stating what changed and why. Revision 4 was gated at tree 936a4aba27681bdd9a5e2938021f54d7b803e2a1 and got three NAKs: read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v4/{graefe,torvalds,storage}.txt (and v1's to v3's in ws-c-addendum-review-v1/ to -v3/) and check that EVERY finding is resolved, not only the ones 7.5 mentions.
What changed: both trees are tree objects, so `git diff 936a4aba27681bdd9a5e2938021f54d7b803e2a1 f8abe2d742df8c35b44fb0c6f854f86c3cd3a016 -- <path>` shows revision 5 exactly for these files (the same diff also carries WS-J design v9's changes to metadata*.go, builder.go and the conformance specs, and WS-E design v8's design and oracle text, gated separately). WS-C files: pkg/recordlayer/online_indexer.go (Build's target-object and empty-list refusals with MetaDataError; the success return after a retried attempt; outcome docs; IndexerID/IndexVersion on the errors), pkg/recordlayer/errors.go (SynchronizedSessionLockedError.IndexerID, PartlyBuiltError.IndexVersion), pkg/recordlayer/indexing_heartbeat.go (the info-claim wording); tests in online_indexer_test.go (the target-object spec; "reports a call as built when a retried attempt then finds the index published and leaves it alone" with multiTransactHook; the source-index spec "stamps and validates..." and the stale source's type rank under ForbidRecordScan; the empty-target spec's MetaDataError), online_indexer_session_test.go (the late-clear spec accepts 1020 or 1007), online_indexer_user_identifier_test.go (the renamed metadata's own index), indexing_heartbeat_test.go, pkg/relational/core/fleet/build_test.go (the "left to peers" arm with n:0); CHANGELOG.md (the OnlineIndexer entries), DIVERGENCES.md (the section "OnlineIndexer session start and build catcher: where Go differs": the standalone MergeIndexes heartbeat, the wire claim, the precedence bullet's scope, the follow-up bullet's SetMarkReadable(false) case), ws-c-design.md (7.4 corrections in place, 7.5, and the combined-evidence paragraph at its end).
Evidence: /var/tmp/fdb-upgrade-recovery/wsc-v5/ (mut-m34..m38 *.log with the perl expression, marker count and applied diff, and *.full.log; mut-m30.log re-run under Bazel; run-mutations.sh, mutate.sh; focus1.log, the unmutated focus; rl-full.log, recordlayer, fleet and frl tests) and /var/tmp/fdb-upgrade-recovery/ev9/ (wsc-focus.log, wsc-loop40.log, justtest.log, justtest-executed.txt, justtest-tree.txt, justtest-tree-after.txt, src.sha over the 1602 files listed in ../ev9-files.txt; drivers ../ev9a.sh and ../ev9b.sh). rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v9.txt summarises that run.
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/: OnlineIndexer.java:133-316, :855-885, :1006-1100, :1240-1360; IndexingCommon.java (indexerId); IndexingBase.java:120-135, :170-180, :184-261, :357-620, :955-1015, :1160-1184, :1285-1300; IndexingByIndex.java:60-260; IndexingHeartbeat.java; IndexingThrottle.java:330-440; IndexingMerger.java; FDBRecordStore.java:2610-2700, :3940-3990, :4841-4990, :6005-6020; and the exception classes (SynchronizedSessionLockedException, IndexingBase.PartlyBuiltException, MetaDataException).
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Check every v4 finding's resolution against Java and the code, the target-object refusal (every production caller and test that builds an indexer, and whether any passes a copy), the retried-attempt outcome and whether its spec can pass vacuously (the mutation logs carry the applied diff), the new error fields against Java's log keys, the declared divergences (are they all declared, and are they true), the test population and its vacuity, and the CHANGELOG. Distinguish measured facts from source hypotheses.
Preserved boundaries: approved Go extensions; C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the build session as a whole (launcher/catcher, session start, per-transaction state checks, publication), phase boundaries, and whether any Go-only behaviour remains undeclared.
