Review the revised RFC-257 WS-J DESIGN (v9), and the fixes it records as LANDED (sections 3.1, 3.2, 4b, 4c's validation, load order and shape-(d) refusal including the catalog's store and load refusals, the loader's value_expression and added_version readings, the section-0 instrument), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE f8abe2d742df8c35b44fb0c6f854f86c3cd3a016; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 b285bfa37aace7554d0af471a642346bcad339688e7cd10df21d031cb8fa3f94 (2196 lines). Previous: v8 TREE 936a4aba27681bdd9a5e2938021f54d7b803e2a1 (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v8/*.txt); v1-v7 in ws-j-design-review-v1/-v7. `git diff 936a4aba27681bdd9a5e2938021f54d7b803e2a1 f8abe2d742df8c35b44fb0c6f854f86c3cd3a016 -- <path>` shows v9 (both are tree objects; the diff also carries the WS-C addendum's revision 5 and WS-E design v8's text, gated separately).
Delta since v8 touching WS-J: (1) CODE AND TESTS: AmbiguousLegacyUnionError.Error() names the remedy that applies on this tree ("edit it so that only the union its records were written with is a candidate"; pkg/recordlayer/metadata_proto.go) and the five JVM expectations in conformance/metadata_proto_conformance_test.go follow it; the refuseAmbiguousLegacyUnion comment says ONLY IF and lists the replay's omissions; the SetRecordsWithUnionName doc (metadata.go) covers RecordTypeUnion (remove or rename, never demote); the metadata.LegacyUnionTemplateError doc (pkg/relational/core/metadata/builder.go) and the conformance comment define the SQL family by the replay; WSJIX (conformance/ws_j_index_fidelity_conformance_test.go, "WS-J stored index protos read as Java reads them") asserts the options the JVM returns; WSJLANE ("WS-J bit and bitmap index keys over an operand with no lane") pins Go's outcome on this tree (goNow); TestAmbiguousLegacyUnionRefusalComesAfterTheTargetsFaults reports every arm with t.Errorf (pkg/recordlayer/metadata_validate_records_test.go); the misplaced UnionDescriptor comment block is back on its Describe; the FDBMetaDataStore spec's comment (metadata_store_test.go). (2) DESIGN v9 (its Status paragraph lists the changes): the EQUIVALENT carrier's write route (CreateTemplateFromProto, both catalogs) and the write-path table's carried-version row; section 2's version guard from (t, latest + 1) with the skip sequence; the restore (refused when nothing binds (t, v), the caller's keyspace, F11 declared in (s), the scoped conflict-range claim); the lane check typing operands by the target Value's result type (order/collate BYTES, CARDINALITY INT); the DDL-origin XX000 in step 3 of section 8 with the generator consulting the lane table, and the section-8 merge wording; 4c's "only if" with the replay's omissions and the SQL family defined by the replay; the EDITED-FILE route through SaveMigratedLegacyMetaData and (p); 3.3's walk checking an IN predicate at entry, with the two new shapes; the in-memory catalog rationale; (j), (r), (t). (3) CHANGELOG (the SQL family's wording; the loader's value_expression and added_version reading with the rebuild advice), DIVERGENCES.md ((d), (j), the restore PENDING entry), ws-j-oracle/README.md (item 19), evidence-mutations.txt (v9 block), evidence-v9.txt.
Evidence: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v9.txt and evidence-mutations.txt (v9 block). Run records under /var/tmp/fdb-upgrade-recovery/: ev9/ (wsj-ev1.log, wsj-ev2.log, wsj-vr-jvm.log, wsc-focus.log, wsc-loop40.log, justtest.log, justtest-executed.txt, justtest-tree*.txt, src.sha), ev9a.sh, ev9b.sh, ev9a.out, ev9b.out, ev9-files.txt; wsj9mut/ (wm9-*.log with perl, marker count and diff, *.full.log, wm9-0-baseline.full.log, run.sh, all.sh, all.out).
NOT WS-J, same tree, own gates: WS-C section 7 addendum (revision 5, gating now), WS-D v13 (NAK, under revision), WS-E v8 (in revision; its text in this tree is a draft), WS-F v2 (NAK).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); the pre-upgrade Go builder and loader are at commit e48f5b496 (`git show e48f5b496:pkg/recordlayer/metadata.go`, metadata_proto.go, store.go). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v8 verdicts and check that EVERY finding is resolved, the FULL v9 design and its diff from v8, the landed code and tests, the oracle and its evidence, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (the replay against every way a file can hold a message named UnionDescriptor and every path that builds, stores or loads metadata, the catalog's store/load refusals, the migration arms and the edited-file route, the EQUIVALENT carrier and its write route, the version guard and the restore, the write-path table and the lane check with its result typing, the DDL-origin refusal's placement, the template-statement walk, test 5, the merge unit), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the independent storage/wire correctness review lens: stored MetaData bytes (key expressions, literal widths, option order, record-type keys, union numbers, versions), index entry bytes under the operand widening, the upgrade path for templates Go already stored (carry rule, validator options, rebuilds, former indexes), the fresh-template guard, catalog existence policies and their conflict ranges, and the driver keyspace gap.
