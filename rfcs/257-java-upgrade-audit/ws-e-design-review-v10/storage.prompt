Reconfirm the revised RFC-257 WS-E DESIGN (v10), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 4d41fb8144ee269e1f5cd5e145f9f54ab0e0e99d; design rfcs/257-java-upgrade-audit/ws-e-design.md SHA256 df8dfdc91782423462b8ef3855db836c72406decb7408098199b8033b55938a6 (3294 lines). Previous: v9 TREE 1cf2b55d3598fca1645cd2f684b6c7b8d7529937 (three NAKs, rfcs/257-java-upgrade-audit/ws-e-design-review-v9/*.txt); v1-v8 in ws-e-design-review-v1/-v8. The v9 design is at /var/tmp/fdb-upgrade-recovery/ws-e-design.v9.md (also `git show 1cf2b55d3598fca1645cd2f684b6c7b8d7529937:rfcs/257-java-upgrade-audit/ws-e-design.md`); diff it against `git show TREE:rfcs/257-java-upgrade-audit/ws-e-design.md`.
Delta since v9 touching WS-E (the design's Status paragraph lists it): (1) 5.4(c): v9's provenance-scoped boundary prune WITHDRAWN; the fold is decided by WS-F's physical REWRITING prune (ws-f-design.md 2.3, D7-D12), and the simplification rule lands after WS-F step 7's option is the default (D1 progress, D4 conditional task, D5 staleness gate, D12's deletion of the standalone DecorrelateValuesRule registrations at default_rules.go:159/:400 and the old rule at :398, OptionalRewritingRuleNames expanding chains, and NormalizePredicatesRule moved to PLANNING WITH PredicateToLogicalUnionRule); the registry's one-of-two-instances note; the prototype measurements re-scoped as the v8 prototype's; the (j) tests (deduplicated-variant rows with the member set asserted); (2) 4.1(b): the nested_ins / two-source IN-union derivation from a prototype memo dump (the target's member never BUILT because Go's PartitionSelectRule puts spanning predicates upper, RFC-043), the partition arm scoped to all-explode uppers (full arm: 158 tests broken; explode-predicate variant: seed 17 at 382,371 tasks; scoped arm: 43,919), the executor's multi-binding IN-union (the target's cartesian execution), #7 still not built and labelled a HYPOTHESIS; the NaN index equality re-stated against RFC-208 (two NaN blocks by the existing builder, last bound component, later comparisons residual, not physically fixed, no UNIQUE proof, declared extension, measured round v10); the dedup equality over composites (deepValueEqual); the cost of a comparand IN source (LiteralFanout unknown, as CardinalitiesProperty); the signed-zero IN-union's continuation; (3) 4.3 narrowed to VALUES: round v10 measures no TIMESTAMP type and no temporal CAST in the target and Go's DATE/TIMESTAMP columns reading back as STRING; the column claims withdrawn (canonicalization by column type, SET d = d, the lane, the stored-text table); the producers enumerated; ONE parser ParseTemporalText with a year domain (offsets); functions.ParseTimestamp, CastValue and the time.Time arms of CompareValues and the residual comparison deleted; the assignment through the DATE/TIMESTAMP-to-STRING edges and the coercer's arms; GREATEST/LEAST and mixed comparisons of values by the common type; (4) the census matcher widened to Eval; (5) the oracle: Describe "WS-E target oracle v10" (12 pins) in conformance/ws_e_probe_conformance_test.go; (6) section 8's order (after WS-F step 7) and its list (the v10 temporal rows, the NaN extension, #7); (7) the evidence's zero paired with its count.
Records (results in /var/tmp/fdb-upgrade-recovery/): wse11-oracle/ (capture.log, pins.txt, pinned1.log), wse11-all1.log, wse11-all2.log, wse11-teardown.log, wse11-td-final.log, wse11-docscheck-oracle.log, wse11-evidence.sh/.out, wse11-src.sha, wse11-check1.txt, wse11-check2.txt, wse11-M10.log; the prototype /home/birdy/projects/fdb-explode-probe (probes zz_probe_memodump.go, PROBE_LOWERSPAN in rule_partition_select.go, the multi-binding InUnion in executor_new_plans.go, PROBE_FILTERDBG in rule_implement_filter.go) and its logs wse11-nested-memo*.log, wse11-twoin-memo.log, wse11-explode-{C2,D,E,F,G}*; earlier records named in ws-e-design-review-v9/SCOPE.md and v8's.
NOT WS-E, same tree, own gates: WS-C section 7 addendum (revision 7, NAK, being revised), WS-J design v11 (NAK, being revised), WS-D v13 (NAK), WS-F v2 (NAK).
Tree objects: run `git show TREE:path`, `git grep ... TREE -- pathspec` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v9 verdicts and check that EVERY finding is resolved, the FULL v10 design and its diff from v9, the oracle and its evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions (the dependency on WS-F's physical prune and step 7, whether any fold can still be rescued before that prune exists, the partition arm's scope and the multi-binding executor, the #7 hypothesis, the NaN blocks against the binder, ordering and uniqueness, the temporal producers' canonical property and the one parser's callers, the deletions' reachability), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the independent storage/wire correctness review lens: bytes a bound value stores (time.Time by instant, slices, the assignment lattice for every source, UTF-8 at bind and at save), index keys, isolation and read-conflict semantics (the measured read scope, always-snapshot sites, the index-state conflict range), DRY_RUN and snapshot admission, connection options and pool reset, continuation ownership, and write divergences.
