Review the revised RFC-257 WS-J DESIGN (v8), and the fixes it records as LANDED (sections 3.1, 3.2, 4b, 4c's validation, load order and shape-(d) refusal including the catalog's store and load refusals, the loader's value_expression and added_version readings, the section-0 instrument), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 936a4aba27681bdd9a5e2938021f54d7b803e2a1; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 ff925afc915653a557fae1daef0be2419749e8d0d270eb776dcab14224f1d28c (2029 lines). Previous: v7 TREE 1718c9c7437d3038bdadd6defd996d7ca6824997 (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v7/*.txt); v1-v6 in ws-j-design-review-v1/-v6. `git diff 1718c9c7437d3038bdadd6defd996d7ca6824997 936a4aba27681bdd9a5e2938021f54d7b803e2a1 -- <path>` shows v8 (both are tree objects; the diff also carries the WS-C addendum's revisions 3 and 4, gated separately); the v7 design is also at /var/tmp/fdb-upgrade-recovery/ws-j-design.v7.md.
Delta since v7 touching WS-J: (1) PRODUCTION: shape (d) is decided by what the pre-upgrade build could have framed (pkg/recordlayer/metadata_proto.go: legacyUnionRecordTypes replays the pre-upgrade union loop, name first; refuseAmbiguousLegacyUnion(md, stored) takes the built metadata and, on the stored path, replays the pre-upgrade loader's outcome with preUpgradeLoaderLoads; RefuseAmbiguousLegacyUnionAsStored exported; reachableThroughFields deleted); metadata.go (loadedFromProto; Build passes it; SetRecords and SetRecordsWithUnionName docs); the loader folds value_expression into the root and defaults an absent added_version to 1 as Java's Index(proto) does (indexFromProto, concatFlattening); the relational catalog refuses to STORE (serializeTemplate) and LOAD (deserializeTemplate) a (d) template with 0A000 (pkg/relational/core/catalog/fdb_template_catalog.go, metadata.LegacyUnionTemplateError in pkg/relational/core/metadata/builder.go). (2) TESTS: TestStoredMetaDataWithAnAmbiguousLegacyUnionIsRefused (17 shapes, each through the loader, SetRecords, SetRecordsWithUnionName with RefuseAmbiguousLegacyUnionAsStored, and the named build's ToProto loaded back); TestAmbiguousLegacyUnionRefusalComesAfterTheTargetsFaults asserts each fault by message on both paths; the FDBMetaDataStore spec "refuses metadata over a records file with a legacy UnionDescriptor, even when the union was named" (metadata_store_test.go); the JVM records spec gains four shapes (35); new oracle specs "WS-J stored index protos read as Java reads them" (with the Java step wsjIndexFromProtoJava in conformance/sql_plan_steps.java), "WS-J a column typed by a table" and "WS-J bit and bitmap index keys over an operand with no lane". (3) DESIGN v8 (its Status paragraph lists the changes): 4c's (d) rule, the catalog's refusals, SetRecords' pre-upgrade behaviour, the migration arms; section 4's EQUIVALENT carrier as the validated gen.MetaData bytes and the loader premise; section 2's version guard over bindings at or above the saved version with the two restore sequences, and the restore (keyspace resolution, missing header, read-conflict range, scoped batching claim); 3.2's write-path table (Initialize, the (d) column, raw write defined) and the lane check's scope, nested result types and the measured DDL shapes; 3.3's template-statement walk with the IN-subquery and NULL-in-IN refusals; section 9 (j), (r), (t), (v); stale text. (4) CHANGELOG (shape (d)), DIVERGENCES.md (the (d) entry rewritten; PENDING entries for (j), (o), (t) appended at the end), ws-j-oracle/README.md (item 18), evidence-mutations.txt (v8 block), evidence-v8.txt.
Evidence: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v8.txt and evidence-mutations.txt (v8 block). Run records under /var/tmp/fdb-upgrade-recovery/: ev8/ (wsj-ev1.log, wsj-ev2.log, wsj-vr-jvm.log, wsc-focus.log, wsc-loop40.log, justtest.log, justtest-executed.txt, justtest-tree*.txt, src.sha), ev8-evidence.sh/.out, ev8-files.txt; ev8-run1/ (a discarded first run that failed to build the fleet test); wsj8mut/ (wm8-1..16 logs with perl, marker count and diff, *.full.log, run.sh, all.sh); wsj8-tt.log, wsj8-lane.log, wsj8-vr.log, wsj8-store.log, wsj8-ix.log (the first focused runs).
NOT WS-J, same tree, own gates: WS-C section 7 addendum (revision 4, gating now), WS-D v13 (NAK, under revision), WS-E v7 (NAK, under revision), WS-F v2 (NAK).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); the pre-upgrade Go builder and loader are at commit e48f5b496 (`git show e48f5b496:pkg/recordlayer/metadata.go`, metadata_proto.go, store.go). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v7 verdicts and check that EVERY finding is resolved, the FULL v8 design and its diff from v7, the landed code and tests, the oracle and its evidence, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (the replay of the pre-upgrade loop and loader against every way a file can hold a message named UnionDescriptor and every path that builds, stores or loads metadata, the catalog's store/load refusals and the in-memory catalog, the migration arms, the EQUIVALENT carrier, the version guard and the restore, the write-path table and lane check, the template-statement walk, test 5, the merge unit), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes in the landed fixes (option order, carrier arm, downgrade, INT refusal), test vacuity (class ratchet, companion canonicalization, discriminating reads, pins), incomplete conversions, evidence that does not bind the bytes, duplicate mechanisms, maintainability.
