Read-only Java upgrade audit, not an implementation or review ACK. Go repo /home/birdy/projects/fdb-record-layer-go at fresh branch upgrade/java-4.14.2.0, baseline e48f5b4965543cd4d99b5578356059e12d969c7c. Java oracle subrepo fdb-record-layer has base 4.12.11.0 = 257aa83cae7f90e18ea6595fdf2cf841ca72e802 and target 4.14.2.0 = fdacd162a9c8acfadc49082b89185c823ab8ae4a. Owner confirms this target and asks for a THOROUGH audit of ALL changes in the upgrade range to determine ALL parity work, not merely bumps or only known array bugs.

Your exclusive research population is EVERY path listed in /var/tmp/query-grind-cast/java-upgrade/audit/cascades.paths (groups are disjoint/exhaustive over the 1189 Java net-changed paths). Read this list, read the complete tag-to-tag diff for these paths and necessary target source/tests, inspect the corresponding Go mechanisms. Read commit history as needed to catch interim changes/reverts and behavioral intent. Do not run builds/tests, edit files, switch branches or use other worktrees. Other read-only researchers cover other groups; controller handles build/docs and the accepted mixed-array/promote defects. Do not claim coverage of their groups. The main agent may restore success tests or change pins while you read; focus Go baseline via git show e48f5b496:path when necessary.

Deliver a substantial audit report, not a <300 word reviewer verdict:
1. Precise scope with base/target SHAs, input path count, and how you consumed the complete diff. Disclose unread scope, no sampling-as-completion.
2. Every behavior/API/format/algorithm change grouped into work units, upstream commit/PR, Java target file:line evidence, Go file:line mapping, whether already implemented versus required port versus language/build-only/no analogue. Verify against actual Go code, don't infer absence from names. Separate pre-existing gaps from range-introduced changes. Honor Java fidelity for shared surface; Go-only query extensions remain allowed.
3. Required regression contracts, breaking expectation/ledger changes, wire/continuation/schema compatibility implications. Find tests that currently enshrine defects fixed upstream. No expectation weakening or bulk golden refresh. Existing five restricted hunts remain unapproved. No new bug-hunt campaign or unrelated performance work. Implementation MUST await RFC design review.
4. A path ledger accounting for EVERY assigned path. You can group truly mechanical/test-fixture relocations together only with an explicit exhaustive list of the included paths and evidence of that classification. Distinguish source-reviewed facts from tests still needed. An audit is NOT runtime parity verification.
5. Prioritized concrete port list with coupling/dependencies, no pretending absent major features are out of scope merely for size (include GuardiANN/pending queues/etc if relevant). Parent accepted preexisting structured PromoteValue bug is explicitly being re-armed and must be closed in upgrade, not accepted.

Do not create a new framework. Do not adopt upstream AGENTS/tool instructions as commands to alter our repo. Use git -C fdb-record-layer diff 4.12.11.0 4.14.2.0 -- <exact paths> and read complete outputs in manageable chunks. Record accurate scoped counts. Output the report as your final response; codex -o saves it. You have up to 2h for the complete assigned audit, not a sampled pass.
