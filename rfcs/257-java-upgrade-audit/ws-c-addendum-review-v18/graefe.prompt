Review REVISION 18 of the ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together, as a DELTA gate. Repo /home/birdy/projects/fdb-go, branch upgrade/java-4.14.2.0, published as PR #786 (the owner's one PR for the whole upgrade). Candidate COMMIT 6b4b7817d. The commits since revision 17 (8a483e73b): 3ab3675c2 (infra runner pin) and 644d6980e (cmd/frl pin), context only; d2e47b8bd, WS-J design v18's code (gated with WS-J; it touches proto decoding across pkg/recordlayer, the catalogs and the conformance BUILD, moving the RFC-257 parity specs to //conformance:rfc257_parity_test); and 6b4b7817d, WS-C revision 18, which also commits the WS-C v17 verdict files. So revision 18 is `git diff d2e47b8bd 6b4b7817d`. The addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 dc1f380c06c5d67f47dd458a654013418eb074e94818dbdf240ba0ebbd47b344, 2890 lines at 6b4b7817d); 7.18 is revision 18, stating what changed and why, with evidence and the 7.16 and 7.17 sentences it supersedes (marked [Superseded → 7.18]). Revision 17's gate (rfcs/257-java-upgrade-audit/ws-c-addendum-review-v17/): Graefe NAK, Torvalds NAK, storage NAK. Read ws-c-addendum-review-v17/{graefe,torvalds,storage}.txt and check that EVERY finding and nit is resolved or answered, and that nothing revision 18 changed broke what revisions 12 to 17 fixed. The owner's rulings are in rfcs/257-java-4.14.2.0-upgrade.md, "Verification and review gates" item 9 (pre-release Go data unsupported; evidence red→green and Java-versus-Go, mutation runs only 0-3 per milestone where a test compares values that could move together or a pin cannot otherwise be shown to fire; one PR, pushed as it lands, CI and ACKs per workstream).
What changed in revision 18: `git show 6b4b7817d -- <path>`. Code: pkg/recordlayer/vector_index_maintainer.go (Update skips common entries), hnsw_options.go (VectorIndexMetric replaces VectorIndexMetricOption), spfresh_config.go (strict parseSPFreshConfig, readSPFreshConfig, spfreshMetricNamed) and every SPFresh entry point (spfresh_index_maintainer, rebalancer, refine, recall, verify_api, write), pkg/relational/core/embedded/cascades_generator.go (tryVectorIndexCandidate via VectorIndexMetric, vectorDistanceOperator; vectorMetricOperator deleted); tests: index_maintainer_queue_test.go (unchanged-vector pin), spfresh_rebalancer_test.go (entry points refuse), spfresh_layout_test.go, vector_index_test.go (present-node snapshot), record_wire_map_order_test.go (message-valued entry), chaos_test.go comments, SPFresh tests moved to readSPFreshConfig, vector_plan_test.go (TestVectorPlan_MetricIsTheMaintainers); conformance: vector_index_conformance.java and _test.go (the unchanged-vector re-save row), key_validation_conformance_test.go (the P3 spec's unknown-field order within a field and minimal encoding, now in //conformance:rfc257_parity_test); CHANGELOG.md, DIVERGENCES.md, TODO.md, ws-c-design.md 7.18 and markers.
Evidence (7.18): /var/tmp/fdb-upgrade-recovery/evidence/wsc18-green-commit/ (a verbose run of the changed specs on HEAD 6b4b7817d, clean tree), wsc18-hook/ (the pre-commit hook: 95 of 95; index tree before the commit equals the commit tree), wsc18-red/ (two parts, its TREE note: the new tests on d2e47b8bd's tree with named adapters, and the 32 sliding-window rows on c1bc5a177's tree). PR #786's CI runs on the restored self-hosted runners; `gh` is not available to you. Bazel's test cache is on by the owner's direction.
Java sources: fdb-record-layer/fdb-extensions/src/main/java/com/apple/foundationdb/async/hnsw/{Insert,Delete,Search,Primitives,Config}.java, rabitq/RaBitQuantizer.java; fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/provider/foundationdb/indexes/{HnswVectorIndexEngine,VectorIndexOptionsHelper,VectorIndexOptionKeys,VectorOptionKey,SlidingWindowIndexMaintainer,VectorIndexMaintainer}.java, fdb-record-layer-core/.../provider/foundationdb/indexes/StandardIndexMaintainer.java; the planner's VectorIndexExpansionVisitor (find it under fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/query/plan/cascades); protobuf-java DynamicMessage and UnknownFieldSet behaviour (measured by the JVM re-save specs; say where you rely on source instead).
Tree objects: run `git show COMMIT:path` or `git diff A B -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). The working tree may move on; read commit objects. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact commit and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Distinguish measured facts from source hypotheses.


Apply the virtual Goetz Graefe architecture lens, which includes this revision's QUERY-ENGINE change (the vector candidate's metric from recordlayer.VectorIndexMetric; the deleted planner parser) as its mandatory Graefe review: Java alignment of the planner's metric with the maintainer's and with Java's VectorIndexExpansionVisitor/VectorIndexEngine, of the unchanged-entry skip against StandardIndexMaintainer.update and VectorIndexMaintainer, of where 9-15 RaBitQ bits are refused now, and whether any Go-only behaviour (SPFresh is a Go-only index) remains undeclared.
