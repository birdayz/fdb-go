DELTA gate of REVISION 20 of the ADDENDUM to RFC-257 WS-C (section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md). Repo /home/birdy/projects/fdb-go, branch upgrade/java-4.14.2.0, PR #786. Candidate COMMIT 2e318b99b; design SHA256 431f99e2871e6994e1763fe1df50938068c5f67295608149d68af2042784c353 (3004 lines at 2e318b99b). Revision 20 is `git diff 97f21019d 2e318b99b` (it also commits the WS-J v19 verdict files, context only). Revision 19's gate (rfcs/257-java-upgrade-audit/ws-c-addendum-review-v19/{graefe,torvalds,storage}.txt) was Graefe, Torvalds and storage NAK, every finding Low or nit, no code defect. Revision 20 folds them by correcting the text in place in section 7.19 (no new section) and adding tests; the commit message lists each fold.
This is a delta confirmation, not a fresh full review: check that EVERY r19 finding and nit of your lens (and of the other two lenses where they overlap yours) is resolved or answered, and review what revision 20 changed (code, tests, text, evidence) for new problems. Do not re-review unchanged ground unless the delta touches it.
Evidence: /var/tmp/fdb-upgrade-recovery/evidence/wsc20-red/ (README; the current tests on d2e47b8bd's tree, hashed, with the adapter and sed its README names), wsc19-mutation/ (README, samples.patch and queue.patch with hashes), wsc20-hook/ (the pre-commit hook of 2e318b99b: 96 of 96 targets passing, 37 executed; the index tree before the commit equals the commit's tree), wsj19-red/README (the HEAD versus index-tree note). The new SQL pin: pkg/relational/sqldriver/spfresh_refused_config_fdb_test.go. PR #786's CI runs on self-hosted runners; `gh` is not available to you.
Java sources as before (fdb-record-layer/, tag 4.14.2.0), e.g. VectorIndexMaintainerFactory.java, VectorIndexMaintainer.java, SlidingWindowIndexMaintainer.java, the hnsw package in fdb-extensions.
Tree objects: run `git show COMMIT:path` or `git diff A B -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). The working tree has other uncommitted work; read commit objects. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy. No tests/probes/mutations/edits/commits. Return ACK or NAK, the exact commit and design hash, a table of the r19 findings of your lens with their status, and any new findings with severity and file:line. Incomplete review is not ACK. Under 700 words.
Distinguish measured facts from source reading.

Apply the storage / wire-compatibility lens (what each engine writes and refuses; the queue, drain and window paths; the SQLSTATE a refused configuration surfaces with).
