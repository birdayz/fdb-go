**ACK — IMPLEMENTATION CODE** at tree `15c853e0f15c1daed5214da4998308e30c577ed7`. No required implementation fix remains in this delta.

Reviewed the complete 24-file delta, frozen additions, actual prior ACK/NAKs and prompts, and relevant Java 4.14.2.0 source at `fdacd162a9c8acfadc49082b89185c823ab8ae4a`. Session metadata confirms **gpt-6-astra / xhigh / read-only**.

- **Qualified whole-object resolution:** [logical_predicate.go:3180](/home/birdy/projects/fdb-go/pkg/relational/core/embedded/logical_predicate.go:3180) derives alias eligibility and name from the resolved attribute, excluding nested access. [plan_visitor.go:1855](/home/birdy/projects/fdb-go/pkg/relational/core/embedded/plan_visitor.go:1855) preserves exact record QOVs. Together these close the prior NAK without admitting lazy name resolution. Both constructors assert `42702 / Ambiguous alias X`; ordinary-member ownership controls remain.
- **UNION ambiguity:** [logical_predicate.go:7386](/home/birdy/projects/fdb-go/pkg/relational/core/embedded/logical_predicate.go:7386) rejects multiple label matches before assigning an ordinal or checking authored duplicates. Repeated publication remains legal; missing-name precedence and quoted-versus-qualified handling survive.
- **Star width:** [logical_predicate.go:5936](/home/birdy/projects/fdb-go/pkg/relational/core/embedded/logical_predicate.go:5936) prepares actual source bodies before classification and uses the ordinary expansion gate. Prepared bodies and binding identities survive the subsequent build. The direct elision regression guards against unnecessary projections.
- **Normalization:** [cascades_translator.go:5779](/home/birdy/projects/fdb-go/pkg/relational/core/query/cascades_translator.go:5779) preserves aligned rows before applying the existing deduplication rule to newly allocated normalization fields. Input rows and SQL labels remain intact. Ordinal projection and the exact-result equality check still enforce the contract. [Regression coverage:157](/home/birdy/projects/fdb-go/pkg/relational/core/embedded/union_exact_output_contract_test.go:157) distinguishes unchanged repeated-name rows from normalized `ID,V,ID_2,V_2` rows.

All **6,457 frozen hashes** match the candidate and workspace. Completed evidence reconciles five uncached affected targets, **9,886 RUN/PASS entries**, zero skips/failures, and **50 Java probe records** with explicit extension expectations. All six mutation source/log hashes reconstruct correctly; intended assertion failures occurred and sources are restored. Invalid, build-only, nonunique-pattern and interrupted attempts receive no credit. The golden blob is unchanged.

**Execution gates:** the single inspected snapshot credits fuzz **30 seconds / four workers / 2,869,998 executions**, without coverage guidance. Full93, race15, `just test`, and nominal1M ABBA remain uncredited. Cancelled suites are not greens. The baseline is clean at `e48f5b4965543cd4d99b5578356059e12d969c7c`.

**Publication gates:** HEAD remains `71ccd8cf8b3fd0dbafe283e91171818e36af555e`; PR786 stays draft and commit/push/merge remain unauthorized. WS-B–K and the whole upgrade remain open. Storage unchanged-scope ACK, FDB C++7.3.77, legacyRaBitQ disable/rebuild prerequisite, and approved nullable-array/raw/scalar/sorting extensions stand. Bounded GRV/RPC attribution establishes neither universal parity nor historical timeout causation.

No edits, builds/tests, agents/worktrees, Git mutations, or GitHub contact occurred.