Perform the independent Codex correctness review. Challenge ownership across every changed path, error/duplicate semantics, Java contract, and whether the regressions actually discriminate the broken behavior.

Reconfirm the WS-A/WS-E1 implementation milestone READ ONLY in /home/birdy/projects/fdb-go. This is the mandatory delta reconfirmation after findings, not a fresh intermediate-commit review. Prior rejected TREE 61b00938a369bc94d683c44d172dbb2cbaffbd41; candidate frozen TREE 36a716b31e3f6e0596377fd76167da46e143a549. These are Git trees, NOT commits; published HEAD remains 71ccd8cf8b3fd0dbafe283e91171818e36af555e. Read all four reports in rfcs/257-java-upgrade-audit/recovery-2026-09-20/group-alias-reviews/. Read the COMPLETE delta with git diff OLD_TREE NEW_TREE including new/untracked tests through git show TREE:path; do not omit untracked content via ordinary worktree diffs. Read corresponding Java 4.14.2.0 source at fdacd162a9c8acfadc49082b89185c823ab8ae4a. Do not re-review unchanged milestone code except necessary interactions.

Prior verdicts: storage ACK; Graefe/Torvalds/independent NAK for SELECT alias ownership lost across visitor -> catalog binding. Original reproducer SELECT p.id AS z,q.id AS "P.ID" FROM pair_t p,pair_t q GROUP BY p.id,q.id AS "P.ID" ORDER BY z,q.id DESC must produce labels Z,P.ID and rows(1,2),(1,1),(2,2),(2,1). Reproduced wrong rows/value, repaired by carrying the selected output slot in existing SortKey.Pos (already also used for resolved UNION names) and native aggregate ordinal. Authored numeric syntax remains sq.orderBy.pos, so resolved named aliases do not acquire numeric duplicate semantics. Exact projected Value instances and surviving slot references remain authoritative through later passes. Both main visitor and shell builder preserve slot ownership; shell no longer mutates parsed ORDER names while constructing keys.

Following that dataflow found/reproduced three related defects, all repaired: (1) GROUP alias rewriting in classifier can replace the SELECT-alias name before shell construction (GROUP BY q.id AS z, ORDER BY z with SELECT p.id AS z). (2) An unresolved GROUP-only alias G rebases to Q.ID, then a SELECT alias "Q.ID" redirects it to p.id. (3) Output alias ID should shadow ambiguous source ID, but the ambiguity validator used its rewritten name and emitted42702. Alias lookup now reads authored parse-tree segments, not rewritten display strings, at shell construction, catalog rebinding and ambiguity validation. Removed the redundant private bare-identifier walker in favor of existing splitColumnRef, with its unused import. Qualified/computed inputs cannot alias-bind. Java QueryVisitor.visitOrderByClauseForSelect looks up the original Identifier and retains the matched Expression; corresponding Go boundary retains slot/value ownership.

Coverage: two exact-metadata tests. SELECT owner has four SQL variants and exercises catalog visitor + catalog shell, plus direct VisitQueryBody(nil metadata) + shell before catalog binding. Important: NewPlanVisitor(nil).VisitQuery would silently use the shell, so the test explicitly calls VisitQueryBody to actually hit both constructors. Asserts exact projection Value pointers, explicit source P field0, selected Pos1/native aggregate ordinal0 before catalog, qualified secondary key Pos0. GROUP-source regression checks actual Q/P owners, not just shared bare ID or coincident field ordinal0. Draft Values are pre-translation source/group expressions; translator later binds them onto the real native aggregate-row owner. SortKey comment corrected accordingly; no runtime format change. Fifteen new real-FDB alias_owner cases pin full rows/labels, computed/aggregate outputs, nested LIMIT, named vs numeric duplicates, qualified vs quoted aliases and invalid names42602. Broader existing positional/star tests retained unchanged.

Live Java probe expanded14->22. Raw completed log /var/tmp/fdb-upgrade-recovery/runtime/alias-owner-authored-java-green.log has22 GROUP-ALIAS-PROBE records with assertions. New cross-source order shapes Java0AF00 while approved Go sorting extension produces independent exact rows. GROUP-only alias G case is Java42703: QueryVisitor.visitSimpleTable removes ephemeral GROUP aliases before ORDER resolution; Go's already-existing grouped-sort extension retains them. That original mistaken0AF00 expectation was corrected from executed Java and target source, not a Go behavior relaxation. Expression-shaped invalid aliases remain Java/Go42602. The initial14 cases remain, including independent Go positional/star extensions where Java42803.

Seven verified-applied, compiled, killed, restored mutations in /var/tmp/fdb-upgrade-recovery/runtime/alias-owner-authored-mutations.json and script alias-owner-authored-mutations.py: visitor slot, shell slot, output-bound guard, authored numeric classification, shell authored identifier, catalog authored identifier, ambiguity authored identifier. They use the existing exact-metadata and NoFromSelectProbe test families, not a QSC hunt/new corpus campaign. A surviving first mutation led to fixing the test entry point so it really exercises the visitor; all seven final runs discriminate. Sources restored after every mutation. Focused passing evidence alias-owner-authored-focused-green.log predates only that entry-point correction; final full/race verification below validates the final tree. No assertions/goldens/thresholds weakened, no new skips.

Final verification is running SEQUENTIALLY under /var/tmp/fdb-upgrade-recovery/runtime/alias-owner-final-verification/, driven by alias-owner-final-verify.py; freeze.json and tree.txt identify the exact6391-file candidate. Full93 uncached, affected race14 uncached, positional fuzz30s/4workers, just test, nominal1M ABBA2samples per side against clean same-filesystem baseline commite48f5b4965543cd4d99b5578356059e12d969c7c. Read COMPLETED results/population JSONs and logs only if available; absence/in-flight is NOT failure or success. Separate code verdict from outstanding execution gates; do not block on or poll these jobs. Earlier full/race green belongs to tree2d5396e6fafeb341b82f750d757fc1adb09d2648 and does not validate later fixes. That earlier runner stopped at fuzz sandbox setup with missing /tmp/alias-owner-fuzz (zero tests ran); directory now explicitly created. Do not credit the failed invocation as fuzz evidence. Prior accepted latency investigation remains unchanged; no new universal parity claim.

Return ACK or NAK for IMPLEMENTATION CODE at frozen candidate36a716b31e3f6e0596377fd76167da46e143a549, with exact file:line evidence and necessary fix, under800words. Separately list unmet verification/publication gates. Storage/RaBitQ/array/descriptor/fixture unchanged accepted scope stays accepted. FDB C++ remains7.3.77. Approved nullable-array/raw/scalar/sorting extensions stay. Historical executor startup-timeout requires unavailable original artifacts; CLI fix and green runs do not explain it. WS-B–K and whole-upgrade completion remain open. Owner still forbids commit/push/merge, PR786 stays draft/unchanged. This is not an authorization to publish or merge.

Actual model MUST be gpt-6-astra, xhigh, read-only, without substitution. Do NOT edit files, run builds/tests, create agents/worktrees, change Git state, contact GitHub, or launch other commands that mutate the frozen tree. Incomplete review is not ACK.
