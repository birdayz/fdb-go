Apply the Java Record Layer storage/vector maintainer lens: ensure the SQL repair composes with previously accepted WS-A/E1 wire/proto/vector semantics and preserves the approved extensions.

Reconfirm the WS-A + WS-E1 implementation milestone, READ ONLY, in /home/birdy/projects/fdb-go. This is the final delta reconfirmation after your previous NAK, not a new per-commit full review. Latest rejected TREE 9f8d0f63cc15aa364dd237a62650a4bf73e5babe; candidate TREE 664b241c57b71829cb40687a1e26cc3dc3e145e6. These are Git trees, NOT commits. Milestone base commit remains 71ccd8cf8b3fd0dbafe283e91171818e36af555e. Read all four prior reports in rfcs/257-java-upgrade-audit/recovery-2026-09-20/delta-reviews/. Read the COMPLETE tree-to-tree delta with git diff OLD_TREE CANDIDATE_TREE and necessary surrounding source from frozen blobs (git show TREE:path). Do not use a one-sided worktree diff: new implementation files are untracked against HEAD, which makes that population misleading. Java reference fdb-record-layer at fdacd162a9c8acfadc49082b89185c823ab8ae4a (4.14.2.0). Do not repeat a full review of unchanged milestone code except to verify interactions.

Prior findings: positional ORDER used output labels as identity in parser AND visitor, rejecting different source columns with matching labels; GROUP captured bound star values but ignored/lost them during equivalence checks, leading-segment stripping, aggregate reclassification, source validation and native output matching. All four reports accepted the RaBitQ rounding/architecture contract, checked-array nil guard, transactional descriptor closure, and fixture fixes. Those are unchanged; preserve that scope rather than requesting a fresh lap on identical code.

Current repair: repeated ORDER ordinals reject during parsing; distinct ordinals preserve all keys through visitor construction, and direct-column duplicates with at least one positional key compare resolved semantic values after binding. Named-only duplicate checking stays parser-owned and computed expression slots remain allowed. The retained typed expanded selectSlots distinguish columns/star attributes from expressions. GROUP uses bound semantic identity before spelling, preserves Value through both strip branches and all three aggregate reclassification sites, validates names only when unbound, and matches native aggregate output slots by bound identity. Ungrouped bound outputs reject 42803 after grouping resolution instead of leaking invalid ordinal 0AF00.

Tests: ten new positive real-FDB cases (two-source stars, aliased explicit columns, duplicate source aliases, grouped stars with/without aggregates, stars between aggregates, heterogeneous INT/STRING grouped CTE, repeated computed slots) assert all rows and output labels. Eight negative controls preserve repeated ORDER position/source and named/positional/alias rejection, repeated GROUP position/source and ungrouped-source 42803. Eight unit arms cover the positional gate, plus both strip branches. Three compiled mutations independently failed intended controls: bypass post-bind ORDER -> four failures; ignore bound GROUP identity -> three failures; drop strip Value -> unit failure. Sources restored and focused targets passed. Later comment clarification/fuzz addition are explicitly not claimed as mutation-tested. FuzzPositionalSortColumnIdentity: 20s, 9,387,604 executions.

Live Java: retained probe passes 64 composition cases + two index controls. Java REFUSES all four added cross-source ORDER cases, including the named equivalents, with UnableToPlan/0AF00. These are not Java-positive claims. Independent exact Go positives preserve the approved sorting extension. Earlier wrongly assumed Java named positives were corrected from measured results, never weakening the Go rows. Zero-match filter and failed attempts remain raw evidence but are not credited as passes.

Fresh evidence in rfcs/257-java-upgrade-audit/recovery-2026-09-20/slot-identity-verification.json; raw /var/tmp/fdb-upgrade-recovery/runtime. Verified source TREE fd415c72fcd9bdd8323659a70e352deef48cce9f: all93 targets executed uncached/pass; Go verbose markers (INCLUDING indented subprocess output)40650RUN=40645PASS+five existing owner-restricted hunt skips. Per-target RUN and PASS/SKIP name multisets reconcile. All14 affected race targets uncached/pass,21103RUN=PASS,zero Go skips/fails. Both full/race Java conformance1357/1476,119 existing filtered.6296 source hashes unchanged through suites/stress/profiles. Candidate differs from verified tree ONLY in TODO.md, RFC and new evidence JSON. Post-documentation just test passes93 (2executed91cached). Final6297-file freeze runtime/slot-identity-final-freeze.json. Do not mistake cached docs follow-up for the uncached code execution.

Fresh 1M: baseline commit e48f5b4965543cd4d99b5578356059e12d969c7c (recorded merge-base) vs verified fd415 tree. Same filesystem /dev/nvme0n1p2 at9% used; identical go.mod/Go1.26.6. Eight sequential runs (four per side),24RUN/PASS each, exact1M count,all row counts agree,zero fixture retries,11 EXPLAIN lines agree. No verification/benchmark concurrency. Early point-read medians are WORSE in this new sample population (including52ms index and41ms PK outliers); cause unestablished, explicitly not dismissed by later needle timings. Variable background load recorded. Existing six planner shape benchmarks:3x200iterations each per tree,CPU+alloc profiles; median ratios0.9955..1.0197. Planner timings DO NOT explain or bound end-to-end variation. No universal performance-parity/speedup assertion. Earlier stress data names its separate tree. Keep unmet performance/verification gates separate from actual code defects, and state any needed investigation precisely.

Return ACK or NAK for IMPLEMENTATION CODE at candidate tree664b241c57b71829cb40687a1e26cc3dc3e145e6, with exact file:line evidence and necessary fix for remaining defects. List unmet verification/publication gates separately. Preserve approved nullable-array/raw/scalar/sorting extensions; FDB C++ stays7.3.77. Does NOT approve WS-B–K, whole-upgrade completion, publication or merge. Historical executor startup-timeout cause remains unavailable from recovered artifacts; CLI networking fix is separate. GitHub authentication has NOW been restored/verified as birdayz, but owner no-commit/no-push constraint remains. PR786 stays draft/unchanged. No automatic merge. Historical design/security ACKs are not implementation ACKs. Do not edit files, create agents/worktrees, run tests/builds, change Git state or contact GitHub. Actual model must remain gpt-6-astra/xhigh/read-only, no substitution. Under700 words; incomplete review is not ACK.
