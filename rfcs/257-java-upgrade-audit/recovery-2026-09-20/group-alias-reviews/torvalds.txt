**NAK — WS-A + WS-E1 implementation code** at candidate tree `61b00938a369bc94d683c44d172dbb2cbaffbd41`.

I reviewed all four prior reports, the complete 95-file tree delta from `664b241c57b71829cb40687a1e26cc3dc3e145e6`, necessary frozen-source interactions, and the structured evidence populations. One ownership conversion remains incomplete.

**P2 — A rebased SELECT alias can still be rebound to another SELECT output, producing incorrect ordering.**

Using the retained `pair_t` fixture containing IDs 1 and 2:

```sql
SELECT p.id AS z, q.id AS "P.ID"
FROM pair_t p, pair_t q
GROUP BY p.id, q.id AS "P.ID"
ORDER BY z, q.id DESC
```

Expected labels are `Z,P.ID`; expected rows are `(1,2), (1,1), (2,2), (2,1)`.

The visitor correctly rebases `Z` to the first output’s `P.ID` at [plan_visitor.go:1675](/home/birdy/projects/fdb-go/pkg/relational/core/embedded/plan_visitor.go:1675). Its new ownership flag prevents GROUP-alias substitution, but that flag is discarded when constructing the sort key at [plan_visitor.go:1688](/home/birdy/projects/fdb-go/pkg/relational/core/embedded/plan_visitor.go:1688), which retains `BareRef=true`.

The subsequent pass therefore interprets the **rebased** spelling `P.ID` as the second output’s quoted SELECT alias. [logical_predicate.go:6198](/home/birdy/projects/fdb-go/pkg/relational/core/embedded/logical_predicate.go:6198) redirects it to `Q.ID`, and line 6203 copies that second output’s value. The exact-value guard at line 6279 then preserves the incorrect binding. Both sort keys consequently reference `q.id`; the requested primary ordering by `p.id` is lost.

This is a frozen-source finding, not an executed reproducer. It directly extends the retained SELECT-alias-priority case by aliasing its second output. The six recorded mutations do not exercise this subsequent alias lookup.

**Required fix:** carry the selected output slot or resolved value through the visitor-to-binding boundary. Resolve SELECT aliases once against the authored reference; never reinterpret an already-rebased expression as another alias. Retain exact rows/labels for the query above and a regression that detects downstream rebinding.

The original bound-star overwrite and concealed-ungrouped-source paths are closed by [select_parser.go:1633](/home/birdy/projects/fdb-go/pkg/relational/core/embedded/select_parser.go:1633). Qualified argument guards, named ORDER segment comparison, and the diagnostic namespace repair address their reported defects.

Evidence integrity reconciles:

- **93 uncached targets:** 40,665 RUN = 40,660 PASS + five existing skips. **14 uncached race targets:** 21,118 RUN = PASS. Both Java populations are 1,358/1,477.
- All **187 artifact hashes** and **6,305 / 6,306 / 6,383-file freezes** match. Post-suite source changes are confined to the three manual-stress files.
- Six compiled mutation kills, fourteen SQL regressions, fourteen measured Java cases, validator/isolation populations, and final **40 RUN/PASS** reconcile. Java’s two positional-star refusals remain correctly distinguished from independent Go positives.
- All **80 instrumented samples**, **88 nominal timings**, **32 profiles**, region budgets and three critical flow records reconcile. Cached follow-ups and the cancelled race run receive no uncached-green credit.

Separate unmet verification/publication gates:

- Verify the remaining binding repair and its regression before implementation ACK.
- The requested ABBA attribution is substantiated for the sampled traces. The **<5ms aspiration remains unmet**; server versus network/kernel delay and the older untraced 52/41ms outliers remain unresolved. No universal parity follows.
- Historical executor startup-timeout causation still requires unavailable original artifacts; CLI networking fixes and greens do not establish it.
- Legacy RaBitQ disable/rebuild remains a deployment prerequisite. Authentication restoration does not lift the owner’s no-commit/no-push restriction; PR786 remains draft/unchanged.

Previously accepted RaBitQ, array, descriptor and fixture scope remains accepted. Approved nullable-array/raw/scalar/sorting extensions remain required; FDB stays **7.3.77**. This approves neither WS-B–K, whole-upgrade completion, publication nor merge.

No edits, builds/tests, agents, worktrees, Git-state changes or GitHub contact occurred.