Reconfirm the revised RFC-257 WS-E DESIGN (v4), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 0253cfe9f8e88ce044374b20e6d7ee3fba036cb7; design rfcs/257-java-upgrade-audit/ws-e-design.md SHA256 22c3582f858fa323d83b3dbf4b9fae5cf049bc764d6cc4728836eb0ba9403337 (1384 lines). Previous: v3 TREE 204151ab1e3aafa12c1b0202bc6c76c5f45c6c09 (three NAKs, rfcs/257-java-upgrade-audit/ws-e-design-review-v3/*.txt); v1/v2 in ws-e-design-review-v1/-v2.
Delta since v3 touching WS-E: (1) oracle refactor, TEST-ONLY: all five Describes of conformance/ws_e_probe_conformance_test.go render, bind and invoke through shared helpers (wseRender with result types, nullability, exact values and the DML update count; wseGoArg; wseOracle.plain/prepared/readScope/check with a uniqueness check per probe); plandiff.RowSet gains UpdateCount (pkg/relational/conformance/plandiff/runsql.go, go_runner.go); every Java prepared statement runs through runPreparedExtended (the redundant runPreparedWithSetup step is deleted, its Javadoc fixed); v2 and v2-extended pins re-rendered (formatting and COUNT only; mutation evidence M1/M2 over all five Describes); in_list_order rebound to discriminate, order_by_then_where_order renamed select_expr_then_where_order; (2) a fourth round, Describe "WS-E target oracle v4", 89 pinned target outcomes (COALESCE heads of every folding class in WHERE and SELECT with EXPLAINs, bound heads, null-strict collapse, inline/typed NULL arithmetic, STRING/BOOLEAN lanes, IN NULL timing incl. EXPLAIN/mixed/filtered, GREATEST/LEAST at 0/NaN/Inf/LONG, overflowing literal, DESCRIBE/DESC/HELP on a snapshot connection, DRY_RUN over DELETE and DDL, assignment of expressions and column sources per lane, ARRAY parameters in DML, HAVING controls, array comparisons with NULL elements, and the snapshot read scope in explicit transactions via a new TEST-ONLY Java step snapshotReadScopeProbe); (3) HARNESS FIX: conformance/sql_plan_steps.java runWithEphemeralSchema's teardown leaked every ephemeral database/template (it dropped through a schema-less /__SYS connection and swallowed "No Schema specified"); it now drops through the CATALOG connection and reports a failed drop; pinned by conformance_test spec "Conformance server ephemeral schema teardown" (Java step ephemeralTeardownProbe) with two mutations; TODO.md EOF block records it; (4) the design revised throughout (see its Status paragraph); TODO.md CQ-78 item points to its EOF block. NOT WS-E, same tree, own gates: WS-D v12 (under review), WS-F v2 and WS-J v2 verdicts (their files and review directories are in the tree).
Run records: /var/tmp/fdb-upgrade-recovery/wse5-d.log and wse5-e.log (two uncached runs, 5 of 27 specs each, 258 blocks equal), wse5-M1.log and wse5-M2.log (mutations), wse5-a..i.log (development runs), teardown-1.log and teardown-mut1/2.log, just-test-wse4.log (just test of this tree: 4 of 94 executed, 94/94 pass; conformance_test Ran 1419 of 1538, 1419 passed, 119 inherited skips; rfc257_oracle_test 27/27) with manifests wse-v4-manifest-before/after.txt (only WS-D gate transcripts changed).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v3 verdicts, the FULL v4 design and its diff from v3, the oracle source and evidence (ws-e-oracle/evidence-run.txt, evidence-mutations.txt), and check claims against actual Java and current Go. Challenge sufficiency and interactions (the two simplification rule sets and the confined fold, the arithmetic lane table shared with WS-J F4, IN-list NULL timing and the EXPLAIN divergence, handleArray promotion vs the nullable-array extension, typed binding order, time.Time by instant, the assignment lattice for every source, connection options/SetOption/pool reset, statement classification, the read-scope census and the index-state conflict range), not only whether the requested words appear. Distinguish measured target facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; no second query pipeline; Lucene separate.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes, incomplete conversions, dead code, test vacuity (pins, renders, set checks, the harness teardown), evidence that does not bind the bytes, duplicate mechanisms and maintainability.
