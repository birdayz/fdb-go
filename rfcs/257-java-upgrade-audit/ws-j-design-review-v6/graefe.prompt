Review the revised RFC-257 WS-J DESIGN (v6), and the fixes it records as LANDED (sections 3.1, 3.2, 4b, 4c's validation and load order, the section-0 instrument), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 10a2023678daaa361c9d5cad4826ae1609c51324; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 8e62bbd0aaecdc2c1fe9ccfc4f40582d986c35e00845df848eea4ce8a4fc85aa (1650 lines). Previous: v5 TREE f041bee2e4f12ba0ee362732fe5393761ceb6d34 (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v5/*.txt); v1-v4 in ws-j-design-review-v1/-v4. The v5 design is at /var/tmp/fdb-upgrade-recovery/ws-j-design.v5.md for a diff.
Delta since v5 touching WS-J: (1) PRODUCTION: refuseAmbiguousLegacyUnion (pkg/recordlayer/metadata_proto.go) checks only the name UnionDescriptor and refuses only when that message is no field's type anywhere in the file (a SQL table or STRUCT of that name is a data message), and runs after Build; the loader reads each index's record types before the index and adds it before the next (Java's loadProtoExceptRecords order), refuses a RecordType entry naming no record type, and the legacy-union declarations moved out from between loadSubspaceKeySettingsFromProto's doc and its function; SetRecordsWithUnionName refuses an empty name (metadata.go); literalKeyCarrier refuses a non-NULL value with no static type (generator.go). (2) TESTS: TestStoredMetaDataLoadsInJavasOrder (three new cases), TestStoredMetaDataWithAnAmbiguousLegacyUnionIsRefused (the two data-message shapes), TestSetRecordsWithUnionNameRefusesAnotherUnion (empty name), TestLiteralKeyCarrier/RefusesUnknownPairs (untyped refused, STRING and NULL kept); the JVM spec "Java refuses the same records descriptors" gains six files (29: three load-order shapes, two data-message shapes, and shape (d2)); a new JVM spec "WS-J a table or struct named UnionDescriptor" (the target creates both, Go loads the target's metadata and its own build); the long-arithmetic probe gains six coverage reads pinned on both engines; the F2b spec gains four rows where only the FLOAT column saturates or overflows. (3) DESIGN v6 (its Status paragraph lists the changes): 4c's (d) condition, SQL shapes, (d) migration arms and output check, load order; 3.5's coverage rule measured; 3.3's LIMIT/OFFSET precedence at the whole template statement; 3.2's no-lane refusal at the catalog chokepoint with its lane table in step 3; section 4's raw-byte comparison and verbatim EQUIVALENT re-emission; test 5 above the inline threshold from the pre-port builder; section 2's restore header check; section 8's step 1 exits and the tree merging as one; section 9 entries (r) to (v); stale populations and cites. (4) CHANGELOG (shape (d) and the load order), DIVERGENCES.md (the (d) entry, PENDING marks, the coverage measurement), TODO.md (EOF block "RFC-257 WS-J merge unit — revised by design v6"), the umbrella RFC's gate 8, ws-j-oracle/README.md items 2 and 16, evidence-mutations.txt (header count, the F12 population, the v6 block).
Evidence: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v6.txt (49 hashed files including TODO.md, two passes, the JVM records spec, just test with its executed set) and evidence-mutations.txt (the v6 block). Run records under /var/tmp/fdb-upgrade-recovery/: wsj6-ev1.log, wsj6-ev2.log, wsj6-vr-jvm.log, wsj6-justtest.log and wsj6-justtest-*.log, wsj6-evidence.sh/.out, wsj6-src.sha, wsj6-files.txt, wsj6-ud.log, wsj6-mut-*.txt/.log, wsj6-f2b-measure.log, wsj6-f2b.log, wsj6-cover-measure.log, wsj6-cover.log.
NOT WS-J, same tree, own gates: WS-C section 7 addendum (NAK, under revision), WS-D v13 (NAK, under revision), WS-E v7 (under review), WS-F v2 (NAK).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v5 verdicts, the FULL v6 design and its diff from v5, the landed code and tests, the oracle and its evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions (the (d) condition against every way a file can hold a message named UnionDescriptor, the migration arms and their output check, the per-index load order, the coverage rule, the LIMIT/OFFSET precedence, the raw-byte carry comparison, test 5, the restore check, the merge unit), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the DDL front end, IndexSpec and QuantifierValues on the translated graph, value-index expansion (section 3.5) and the non-integer match policy, metadata assembly order, lane resolution at construction, and the evolution/carry policy for stored templates.
