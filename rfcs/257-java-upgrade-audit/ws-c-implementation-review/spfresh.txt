**NAK** — reviewed candidate tree `2a4d33046f8640673643ea989709665f425cbbc5` against base tree `ba06f34ff8998f61040a491e4f3a3e8b8de7ccf9`.

Read the full 54-file source diff, accepted design, scope and sibling evidence, corresponding Java source at `fdacd162a9c8acfadc49082b89185c823ab8ae4a`, RFC-094 and relevant paper sections.

1. **P1 — Fresh builds erase legacy heartbeats before validating them.**  
   [online_indexer_queue.go:60](/home/birdy/projects/fdb-go/pkg/recordlayer/online_indexer_queue.go:60) calls `ClearAndMarkIndexWriteOnly*` before heartbeat admission at line 91. That clear removes heartbeat subkey 7 through [index_state.go:956](/home/birdy/projects/fdb-go/pkg/recordlayer/index_state.go:956). Consequently, a fresh build starting from READABLE or DISABLED can silently delete legacy string or malformed heartbeat keys and proceed without `IndexingHeartbeatKeyError`. This violates the accepted design’s mandatory refusal and explicit administrative-cleanup boundary. Validate every target’s heartbeat keyspace before any preparation/open-time lifecycle operation can clear it. Add fresh-build regressions asserting refusal and preservation of those keys.

2. **P2 — Mutual heartbeat renewal makes otherwise disjoint workers conflict.**  
   [indexing_heartbeat.go:99](/home/birdy/projects/fdb-go/pkg/recordlayer/indexing_heartbeat.go:99) now performs a serializable read of the entire heartbeat range even when `allowMutual` is true. Every mutual batch invokes it via [indexing_mutual.go:257](/home/birdy/projects/fdb-go/pkg/recordlayer/indexing_mutual.go:257). Two workers reading the same version and updating their distinct heartbeat keys therefore cannot both commit: the first invalidates the second’s range read, regardless of disjoint build fragments. This introduces index-wide contention into mutual indexing; eventual-completion tests can pass while masking it. Tagged Java’s mutual branch only renews its own key (`IndexingHeartbeat.java:90`). Separate compatibility admission from periodic mutual renewal while retaining exclusive-session conflict protection. Verify both disjoint transactions can commit without retry.

The scoped **SPFresh paper-preservation check passes**. Against SPANN §3.2 and SPFresh §§3.2–3.4, 4.1–4.2.2, inherited queue refusal ([index_maintainer_queue.go:12](/home/birdy/projects/fdb-go/pkg/recordlayer/index_maintainer_queue.go:12)) preserves SPFresh’s own foreground/staging update path ([spfresh_index_maintainer.go:287](/home/birdy/projects/fdb-go/pkg/recordlayer/spfresh_index_maintainer.go:287)). The inherited no-op `MergeIndex` does not replace its separate LIRE executor ([spfresh_rebalancer.go:124](/home/birdy/projects/fdb-go/pkg/recordlayer/spfresh_rebalancer.go:124)). No change-induced paper finding or whole-index performance verdict.

Existing final evidence inspected: **93/93**, **42 executed/51 cached**; **147 race specs plus format unit tests**, and **one live JVM format15 spec**. Earlier increment evidence was kept historical. No tests, mutations or CI were triggered.

Format15 support/default14 remain correctly separated. HNSW remains synchronous. This review proves no downstream backend callback consumption: GuardiANN remains WS-D; absent Lucene remains an upgrade-wide requirement, distinct from Go TEXT. Overall migration completion is not established.