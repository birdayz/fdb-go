**NAK** — reviewed candidate tree `2a4d33046f8640673643ea989709665f425cbbc5` against base tree `ba06f34ff8998f61040a491e4f3a3e8b8de7ccf9`.

Read the full 54-file source diff, SCOPE.md, accepted design, sibling evidence READMEs, and corresponding Java control flow at `fdacd162a9c8acfadc49082b89185c823ab8ae4a`.

1. **P1 — Fresh preparation deletes the evidence required to refuse incompatible builders.** [online_indexer_queue.go:67](/home/birdy/projects/fdb-go/pkg/recordlayer/online_indexer_queue.go:67) calls `ClearAndMarkIndexWriteOnly*` before heartbeat admission at line 91. That clear reaches `eraseAllIndexingDataButTheLockAndRangeSet`, which deletes heartbeat subkey 7 ([index_state.go:956](/home/birdy/projects/fdb-go/pkg/recordlayer/index_state.go:956)). Consequently, a fresh build encountering legacy/malformed heartbeat keys deletes them and then passes admission against its own cleared view. This violates the accepted requirement to refuse **any** incompatible key until explicit cleanup after worker fencing. Validate every target’s original heartbeat keyspace before destructive preparation; cover fresh builds, including a fresh primary with an existing follower session.

2. **P2 — Mutual heartbeat refresh creates conflicts between disjoint builders.** [indexing_heartbeat.go:99](/home/birdy/projects/fdb-go/pkg/recordlayer/indexing_heartbeat.go:99) reads the entire heartbeat range serializably on every batch, including mutual mode. Skipping peer validation at line 111 does not remove those read conflicts. Two mutual workers can process disjoint fragments, but each writes a heartbeat inside the other’s read-conflict range, so the first commit aborts the other transaction. This introduces index-wide contention unrelated to range ownership. Tagged Java’s mutual branch only updates its own heartbeat. Preserve incompatible-key admission checks without repeatedly conflicting with valid peer UUID updates; verify with overlapping transactions on disjoint fragments.

3. **P2 — Sequential drains let other targets’ session leases expire.** [online_indexer_queue.go:102](/home/birdy/projects/fdb-go/pkg/recordlayer/online_indexer_queue.go:102) drains each target to completion before proceeding, while the commit callback refreshes only the current index ([line 138](/home/birdy/projects/fdb-go/pkg/recordlayer/online_indexer_queue.go:138)). If draining A lasts beyond the lease—30 seconds by default—B’s heartbeat expires despite the build remaining active. A permitted single-target takeover of B can then acquire it, causing the original build to fail when it returns to B. Maintain liveness across all still-owned targets throughout drain follow-up, including long-running or continuously replenished queues.

Existing latest local evidence reports **93/93, 42 executed / 51 cached**, plus the scoped race run of 147 recordlayer specs, format unit tests, and one live JVM format15 spec. I performed no reruns or mutations and did not count earlier increments as final-tree execution.

Format15 support/default14, SPFresh queue exclusion, and synchronous HNSW remain the intended boundaries. This review provides no downstream backend callback proof: GuardiANN remains WS-D, and absent Lucene remains an upgrade-wide requirement distinct from Go TEXT.