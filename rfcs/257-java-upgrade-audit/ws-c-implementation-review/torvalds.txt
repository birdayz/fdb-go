**NAK — reviewed candidate tree `2a4d33046f8640673643ea989709665f425cbbc5`**, against base tree `ba06f34ff8998f61040a491e4f3a3e8b8de7ccf9`.

Read all 54 changed source files, the scope, accepted design, sibling evidence READMEs, and corresponding Java source at `fdacd162a9c8acfadc49082b89185c823ab8ae4a`.

1. **P1 — Fresh preparation erases heartbeat evidence before admission checks.** [online_indexer_queue.go:60](/home/birdy/projects/fdb-go/pkg/recordlayer/online_indexer_queue.go:60) clears every target before `CheckAndUpdate` at line 91. `ClearAndMarkIndexWriteOnly*` reaches `eraseAllIndexingDataButTheLockAndRangeSet`, which clears heartbeat subkey 7. A readable/disabled target containing a legacy string key or malformed key therefore passes preparation: the forbidden evidence has already disappeared. Active UUID peers on cleared follower targets disappear too. This defeats the required fail-closed upgrade preflight. Validate all target heartbeat keyspaces **before any destructive preparation**. Existing heartbeat tests call `CheckAndUpdate` directly and miss this lifecycle bypass.

2. **P1 — The advertised 30-second terminal cleanup bound does not bound its commit.** [online_indexer_queue.go:165](/home/birdy/projects/fdb-go/pkg/recordlayer/online_indexer_queue.go:165) wraps `db.Run` in a deadline but neither owns/cancels its active transaction nor sets an FDB timeout. The shipped libfdbc backend explicitly detaches auto-commit from cancellation and waits on `Commit().Get()` without an FDB timeout ([backend.go:188](/home/birdy/projects/fdb-go/pkg/fdbgo/libfdbc/backend.go:188)). A stalled cleanup commit can therefore hold `BuildIndex`’s deferred cleanup beyond 30 seconds, including after cancellation or an original failure. Use an owned cleanup transaction/retry lifecycle with the remaining deadline enforced through commit, and retain a blocked-commit regression. Successful cleanup tests do not establish this bound.

3. **P2 — Nested queue payloads bypass Java-compatible `Any` validation.** [index_maintainer_queue.go:53](/home/birdy/projects/fdb-go/pkg/recordlayer/index_maintainer_queue.go:53), its sliding equivalent at line 155, and [indexing_pending_write_queue.go:58](/home/birdy/projects/fdb-go/pkg/recordlayer/indexing_pending_write_queue.go:58) call Go’s `Any.UnmarshalTo` directly. That accepts a slashless message full name; Java’s `Any.unpack` rejects it. The explicit slash check protects only the outer envelope. Consequently, a valid outer envelope containing slashless UPDATE, sliding, or DELETE_WHERE data is applied and removed by Go while Java refuses it. Apply the same compatibility validation at every nested `Any` boundary; add replay-level negative cases alongside custom-prefix positive cases.

Existing logs confirm 93/93 local tests, 42 executed/51 cached, plus the reported scoped race runs. No tests or mutation experiments were run during this review.

This verdict covers WS-C only. HNSW supplies no deferred-backend proof; GuardiANN remains WS-D, and absent Lucene remains an upgrade-wide requirement distinct from Go TEXT.