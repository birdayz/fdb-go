Reconfirm the revised complete RFC-257 WS-D DESIGN (v4), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 4b8b8c15a3b65ce74c1856f8175ff2d3e7883c8d; design rfcs/257-java-upgrade-audit/ws-d-design.md SHA256 81fdd5f9c7a4d15222c0ad47c7056494f108e7e6bc3f689b788adbe2126809a8. Previous design TREEs: v1 607dbcf313e0c7a1b75333ae48ca36027bf7428e (four NAKs, ws-d-design-review-v1/*.txt), v2 236888f5b174f2e5d8894be7845992a8650bd8d9 (Graefe NAK ws-d-design-review-v2/graefe.txt; other v2 runs incomplete), v3 1390a2feec185fc510f4b17c679bd1ae307951af (four NAKs, ws-d-design-review-v3/*.txt). Accepted WS-C source TREE f0cb29576cde624e0f1c6e22ca18a5180d1b298f; no production source delta since. Tree objects: use git diff OLD NEW and git show TREE:path, never a working-tree diff. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (a plain checkout without .git; the pinned Maven jars under the Bazel cache are the same release).
Read ALL prior verdicts (especially the four v3 NAKs), the FULL v4 design and its diff from v3, the three ws-d-research/*.txt reports with ws-d-research/README.md corrections, and check claims against actual Java and current Go. v4 answers the v3 NAKs: a throw-site terminal rule for split with no usable candidate (in-place reconcile of all physical references, shared metadata helper with CollapseTask); search read lock held for the materializing search computation only (declared hold-span divergence, single deferred release, no cross-caller hold, deterministic pause-hook tests); capacity recovery only for sessions that hit the cap (Java drain-then-merge otherwise), a separate typed-capacity branch in both retry owners leaving shouldLessenWork pinned, consumed-count limit reduction, prefix-targeted merge with point-read count/lease and a progress-based stop, index/prefix in the typed error, merger errors propagate; two-pass zero-primary handling with replica recomputation, all-minted cause set, surviving-only bounce; empty merge core with replica pruning and forced repair; config-only encode-new with a plain-only invariant for unsupported extra bits; one per-operation admission matrix with mutable-knob repair path; sliding-window whole-effect gate; catch-and-commit capacity residue removed by ordering; parallel collector join rule; explicit VectorId hash formula confirmed by javap/jshell and runtime version assertions. Challenge sufficiency and interactions with the rest of the design, not only whether the requested words appear. Source hypotheses are not claimed as measured defects.
Preserved boundaries: GuardiANN executes in merger-supplied transactions (no invented child runner); SPFresh independent algorithms/LIRE; approved raw/nullable-array/scalar/sort extensions; C++ 7.3.77; format max 15/default 14; Lucene is separate upgrade-wide work, not Go TEXT; WS-D cannot complete with a refusal or fake backend.
Actual reviewer gate run via `claude -p` (model opus = claude-opus-5-5, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the SPANN/SPFresh paper authors review lens and independent lifecycle safety lens. Read RFC094, relevant SPANN/SPFresh sections in .claude/skills/spfresh-reviewer/ PDFs (use pdftotext if necessary), and relevant unchanged SPFresh call paths. Verify inherited queue refusal, maintenance gate and MergeIndex do not alter LIRE or algorithms. Cite paper sections for paper findings, file:line for lifecycle findings. Initial paper check passed but overall NAK; confirm all relevant fixes and interactions, not paper preservation alone.
