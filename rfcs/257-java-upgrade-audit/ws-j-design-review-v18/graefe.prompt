Review the revised RFC-257 WS-J DESIGN (v18), and the fixes it records as LANDED, not the rest of the implementation. Repo /home/birdy/projects/fdb-go, branch upgrade/java-4.14.2.0, published as PR #786 (the owner's one PR for the whole upgrade). Candidate COMMIT d2e47b8bd; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 5f6990f66f23f4a12f56766a016217d0adf8edfd77a826bfc3eaf8e05ea9643f (2950 lines at d2e47b8bd). Previous: v17 at d1ccff8c6 (Graefe, Torvalds and storage NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v17/{graefe,torvalds,storage}.txt); v1-v16 in ws-j-design-review-v1/-v16. `git diff d1ccff8c6 d2e47b8bd -- rfcs/257-java-upgrade-audit/ws-j-design.md` shows v18 (the tree diff between those commits also carries WS-J step 3's first commit c1bc5a177, WS-C revision 17 8a483e73b, the infra runner pin 3ab3675c2 and the cmd/frl pin 644d6980e; only d2e47b8bd is this revision's code).
The owner's rulings (rfcs/257-java-4.14.2.0-upgrade.md, "Verification and review gates" item 9): data written by pre-release Go builds (v0.1.0 included) is not supported, nothing is built to migrate, detect or refuse it; Go answers every query the target answers; evidence is red→green and Java-versus-Go, mutation only 0-3 per milestone where values could move together; one PR, pushed as it lands, CI and ACKs per workstream. Delta since v17 (the Status paragraph and the new section 4f list it; rewritten normative sites are marked [v18]): a closed enum's undeclared number read as protobuf-java reads it at every decode of shared bytes (proto_closed_enums.go; records, map entries, stored meta-data, store headers, stamps, pending writes, continuations, catalogs), protobuf-java's recursion limit at the same decodes, an index predicate's operand read as Java reads it, the three field readers pinned to Java's getFieldOnMessage and MessageTuple, a stored template's load failure reported with Java's code, the restore failing closed over an unreadable index, the in-place sweep of the withdrawn design, the evidence plan restated per the ruling, and the RFC-257 parity JVM specs moved to //conformance:rfc257_parity_test.
WS-J code in d2e47b8bd: `git show --stat d2e47b8bd`. Key files: pkg/recordlayer/proto_closed_enums.go (+_test), record_wire_map_order.go (newTypeReach), metadata.go, store.go (decoders, UnknownStoreLockStateError), metadata_proto.go, key_expression_proto.go, index_predicate.go, the UnmarshalAsJava / unmarshalVTAsJava call sites (cursor_combinators, dedup_cursor, index_state, indexing_heartbeat*, key_value_cursor, merge_cursor, multidimensional_index_maintainer, pending_writes_queue, store_api, store_builder, store_state_cache, time_window_leaderboard, vector_index_maintainer, metadata_store), query/plan/cascades/index_predicate_to_query.go, query/executor/read_path_agreement_test.go, orelse_continuation_test.go, error_path_test.go; pkg/relational/core/catalog/{fdb_template_catalog,fdb_store_catalog,template_catalog,template_restore}.go and template_restore_fdb_test.go; conformance BUILD.bazel (rfc257_parity_test), key_expression_absent_child_conformance_test.go, key_validation_conformance_test.go (the closed-enum spec), null_standin_conformance_test.go and metadata_proto_conformance.java (MessageTuple), store_helpers_test.go; CHANGELOG.md, DIVERGENCES.md, TODO.md.
Evidence: /var/tmp/fdb-upgrade-recovery/evidence/wsj18-red/ (the new specs on 644d6980e's tree in /home/birdy/projects/fdb-wsc10 with the adapter its TREE note names; 10 JVM specs red, the pins of behaviour that already held green) and wsj18-hook/ (the pre-commit hook of d2e47b8bd: 95 of 95 targets passing; index tree before the commit equals the commit's tree). PR #786's CI runs on the restored self-hosted runners; `gh` is not available to you. Bazel's test cache is on by the owner's direction.
Java tag 4.14.2.0 in fdb-record-layer/; the pre-upgrade Go builder and loader are at commit e48f5b496. Relational catalog Java: fdb-record-layer/fdb-relational-core/src/main/java/com/apple/foundationdb/relational/recordlayer/{catalog/RecordLayerStoreCatalog,catalog/RecordLayerStoreSchemaTemplateCatalog,MessageTuple}.java and ExceptionUtil.java; key expressions fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/metadata/expressions/; metadata/IndexComparison.java; RecordMetaDataBuilder.java; MetaDataEvolutionValidator.java; cursors/OrElseCursor.java; FDBRecordStore.java validateStoreLockState. protobuf-java's parser behaviour (closed enums, recursion limit, UnknownFieldSet order) is measured by the JVM specs named in 4f; say where you rely on protobuf-java source instead.
Tree objects: run `git show COMMIT:path` or `git diff A B -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read commit objects, not the working tree, which moves on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline; wire compatibility with Java-written stores is the hard line.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact commit and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Read the three v17 verdicts and check that EVERY finding is resolved, answered or made moot (and say which), the v18 delta, the landed code and tests, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (every decode of shared bytes and whether any escapes the Java reading; what the closed-enum move changes for records, indexes and re-saves; the catalog; step 3's owed work), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.

Apply the virtual Goetz Graefe architecture lens: Java alignment of reading shared bytes (closed enums, recursion limit), the index predicate operand and the query engine's literalFromProtoValue, the field readers against getFieldOnMessage and MessageTuple, the catalog's error codes, the carry rule and route step 3 owes, and whether any Go-only behaviour remains undeclared.
