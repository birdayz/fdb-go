Review REVISION 13 of the ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together, as a DELTA gate over revision 12. Repo /home/birdy/projects/fdb-go, branch upgrade/java-4.14.2.0 (the owner's one branch for all migration code). Candidate COMMIT ba357754d; its parent 51c3f90eb is WS-J's section-8 step 1 (gated with WS-J, not here), and 51c3f90eb's parent 9ba005539 is WS-J's IndexMaintenanceFilter port (gated with WS-J), whose parent 426da82a5 is revision 12. So the WS-C delta is exactly `git diff 51c3f90eb ba357754d`. The addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 0aa1505fca3652078a8d2bd24b61c8cd2dfd703b853ca17d3a2b000eeba104c1, 2428 lines at ba357754d); 7.13 is revision 13, stating what changed and why, with its evidence and the 7.11/7.12 sentences it supersedes. Revision 12 got three NAKs (one Medium from Torvalds, one Medium from storage, the rest Low): read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v12/{graefe,torvalds,storage}.txt and check that EVERY finding and nit is resolved or answered, and that nothing revision 13 changed broke what revision 12 fixed. The owner ruled on 2026-09-24 that data written by pre-release Go builds (v0.1.0 included) is not supported, and directed that evidence be red→green and Java-versus-Go rather than mutation (rfcs/257-java-4.14.2.0-upgrade.md, "Verification and review gates" item 9).
What changed: `git show ba357754d` or `git diff 51c3f90eb ba357754d -- <path>`. Code: pkg/recordlayer/record_wire_map_order.go (NEW: recordWire, collectWireMapEntries, wireEntriesMatchMap, messageReachesMap, sortedMapEntries), key_expression.go (evaluateMap), store.go (serializeUnion's map arm; deserializeAndDiscover returns the wire; FDBStoredRecord.wire; the old-record sites), key_value_cursor.go, store_batch.go, store_api.go; record_count.go (GetSnapshotRecordCountForRecordType over snapshotRecordCountForRecordType(name, filter)), store_builder.go (the rebuild copy and recordTypeKeyInt64 deleted; singleRecordTypeWithPrefixKey, recordsRangeEmpty, indexedRecordTypesRange for any type key), online_indexer.go (computeRecordsRange), aggregate_function.go and errors.go (unknownRecordTypeError; MetaDataError.Cause/Unwrap), key_expression_validate.go (validateDimensionsKeyExpression), vector_index_validation.go, index_predicate.go (resolveFieldPath through groups), metadata.go (SetPrimaryKey guard), query/plan/cascades/values/proto_field_type.go (comment); cmd/frl/internal/cmd/record_count.go and integration_test.go; tests: record_wire_map_order_test.go (NEW), rebuild_count_selection_test.go, online_indexer_preset_test.go, key_expression_validate_test.go, sliding_window_index_test.go, index_predicate_test.go, multidimensional_index_test.go, primary_key_translation_test.go, error_path_test.go, save_error_test.go, record_count_rollup_test.go; conformance: key_validation_conformance_test.go (the wire-order Describe; three dimensions rows; the group-predicate row), count_per_type_conformance_test.go (class assert); CHANGELOG.md, DIVERGENCES.md (VECTOR entry), the umbrella RFC's items 8 and 9, ws-c-design.md 7.11-7.13.
Evidence (7.13): /var/tmp/fdb-upgrade-recovery/evidence/wsc13-*/ (each with console.txt, the test logs and TREE naming HEAD, the index tree and the working diff's sha256; save-evidence.sh refuses a run that executed no test), including the red runs on 51c3f90eb's tree in /home/birdy/projects/fdb-wsc10; the pre-commit hook of ba357754d ran just generate/lint/build/test: "Executed 26 out of 94 tests: 94 tests pass" (/tmp/wsc13-commit.log may be gone; the commit exists only because the hook passed). Bazel's test cache was on by the owner's direction. No mutations were run.
Java sources: fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/: provider/foundationdb/FDBRecordStore.java (getSnapshotRecordCountForRecordType :2431-2453, getRecordCountForRebuildIndexes :5062-5100, singleRecordTypeWithPrefixKey, saveTypedRecord :580-600), provider/foundationdb/IndexFunctionHelper.java, provider/foundationdb/OnlineIndexer and IndexingCommon.java (computeRecordsRange), RecordMetaData.java (unknownTypeException :825-827), metadata/RecordType.java (getRecordTypeKeyTuple), metadata/expressions/{DimensionsKeyExpression,NestingKeyExpression}.java, metadata/IndexPredicate.java, provider/foundationdb/indexes/{VectorIndexMaintainerFactory,VectorIndexHelper,StandardIndexMaintainer,TextIndexMaintainer}.java, provider/common/DynamicMessageRecordSerializer.java; query/plan/cascades/values/{FieldValue,MessageHelpers}.java.
Tree objects: run `git show COMMIT:path` or `git diff A B -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). The working tree may move on; read commit objects. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact commit and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Distinguish measured facts from source hypotheses.

Apply the storage/wire lens: bytes each engine writes and reads (index entries of a map fan-out on records either engine wrote, the record bytes Go now writes for a type holding a map, the online indexer's preset range-set bytes for a non-integer record-type key, the per-type count's reads), the CHANGELOG/DIVERGENCES statements a user relies on, and the owner's pre-release-data ruling.
