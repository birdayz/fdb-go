Review REVISION 8 of an ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together. Repo /home/birdy/projects/fdb-go. Candidate TREE 97d0b9181d02696345e166b12d9ba09b18141c49; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 2e97dd6796b4740432364e8d63ab16e4ecb1dcf84aacf1e3fc340bb3bea92400, 1477 lines); 7.1 to 7.8 are revisions 1 to 8, each stating what changed and why. Revision 7 was gated at tree aa29308afd7637d7e98a42ef1d108335013ead8c and got three NAKs with low findings: read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v7/{graefe,torvalds,storage}.txt (and v1-v6's) and check that EVERY finding is resolved. Revision 7's design is at /var/tmp/fdb-upgrade-recovery/ws-c-design.r7.md.
What changed: `git diff aa29308afd7637d7e98a42ef1d108335013ead8c 97d0b9181d02696345e166b12d9ba09b18141c49 -- <path>` (both are tree objects; the same diff carries WS-J design v12, gated separately, and WS-E's review records). WS-C revision 8 files: pkg/recordlayer/metadata_evolution_validator.go (every message Java's first; validateIndex's roots and comparePrimaryKeys by keyExpressionEquals; validateRecordTypes in Java's order and walked by name; validateField in Java's order with Java's label checks (an optional field made required admitted); the RANK, R-tree and vector option checks by effective value with Java's messages; the base option walk sorted); pkg/recordlayer/index.go (SetSubspaceKey: typed nil, sticky refusal); pkg/recordlayer/subspace_key_identity.go (typed nils to nil, fdb.Key copied, isNilSubspaceKey, a comment corrected); pkg/recordlayer/metadata.go (Build: every index's refusal and a nil former-index key; the struct-literal default key in addIndexCommon; Java's former-index version messages; GetIndexFromSubspaceKey's comment; the collision comment); pkg/recordlayer/errors.go (the index version message); pkg/recordlayer/metadata_proto.go (the absent-root refusal and HeldBy by reachability, both WS-J's) and key_expression_proto.go (Java's text for a malformed key expression); tests: subspace_key_identity_test.go (TestNilSubspaceKeyIsRefusedOnEveryPath, TestBuildFormerIndexVersionMessagesAreJavas, TestEvolutionComparesRootsAsJavaDoes, TestSavedSourceIndexResolvesANestedTupleKey), metadata_evolution_validator_test.go (the label spec, the effective-value spec, TestVectorOptionsComparedByEffectiveValue, TestRecordTypeViolationsNamedInNameOrder, messages), indexing_merger_test.go ("admits a mutual builder beside the merge, and a merge transaction after it fails, not the builder"), and the tests corrected (metadata_test.go, metadata_store_test.go, index_maintainer_dispatch_test.go, metadata_proto_compat_test.go, metadata_proto_test.go, metadata_validate_records_test.go); conformance/subspace_key_identity_conformance_test.go (five pairing shapes, the Describe "Field and record-type changes in meta-data evolution", Java's whole message as the prefix); CHANGELOG.md, DIVERGENCES.md (the merge text; "BY_INDEX resume over a nested-tuple source key"), ws-c-design.md (7.7's count corrected; 7.8).
Evidence: /var/tmp/fdb-upgrade-recovery/wsc-v8/ (baseline.log and the mutations mut-m61..m80 *.log with perl, marker count, applied diff and red set, *.full.log; run-mutations.sh, mutate.sh, run-mutations.out; the .good copies; jvm-subspace2.log, jvm-wsjixk.log, jvm-wsjtt.log) and the combined run /var/tmp/fdb-upgrade-recovery/ev12/ (driver ../ev12.sh, file list ../ev12-files.txt, src.sha, check.txt, wsj-ev1/2.log, jvm-md.log, wsc-focus.log, justtest.log, justtest-executed.txt, justtest-tree*.txt, metadata-test-v.log, final-targets.log), summarised in rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v12.txt.
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/: metadata/Index.java, FormerIndex.java, TupleTypeUtil.java, MetaDataValidator.java, MetaDataEvolutionValidator.java, IndexValidator.java, expressions/*KeyExpression.java, provider/foundationdb/indexes/{RankIndexMaintainerFactory,MultidimensionalIndexMaintainerFactory,PermutedMinMaxIndexMaintainerFactory,TextIndexMaintainerFactory,VectorIndexOptionsHelper}.java, provider/foundationdb/OnlineIndexer.java, IndexingBase.java, IndexingHeartbeat.java.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Check every revision-7 finding's resolution against Java and the code: every evolution-validator message against Java's (is any Go message left without Java's prefix beyond the three declared?), the root and primary-key comparisons, validateField's order and label rule against Java's, the option families' effective values against Java's configs, the nil-key paths (set after AddIndex, sticky, typed nil, struct literal, former index), Build's messages and their declared Go parts, the merge's lock-out spec, the mutation logs and baseline, the JVM specs. Distinguish measured facts from source hypotheses.

Apply the independent storage/wire correctness review lens: what each attempt and each catcher retry writes and clears (index data, range sets, stamps, heartbeats, queue state), what a fallback overwrites, transaction and conflict behaviour of the admission checks and of concurrent and late builders, and the user-visible behaviour changes.
