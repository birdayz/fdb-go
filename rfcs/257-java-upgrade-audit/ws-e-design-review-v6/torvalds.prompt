Reconfirm the revised RFC-257 WS-E DESIGN (v6), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 66663d5d8d1946a4891985e75c682cb98a18609f; design rfcs/257-java-upgrade-audit/ws-e-design.md SHA256 a0703c84462f9a6dc3e47ceee69f64b2ea5c54a9c597b1b95c1fa54b1b79b853 (2154 lines). Previous: v5 TREE c9fd46b8655f19cb0b897f5d272f311121626463 (three NAKs, rfcs/257-java-upgrade-audit/ws-e-design-review-v5/*.txt); v1-v4 in ws-e-design-review-v1/-v4. The v5 design is at /var/tmp/fdb-upgrade-recovery/ws-e-design.v5.md for a diff (diff it against `git show TREE:rfcs/257-java-upgrade-audit/ws-e-design.md`).
Delta since v5 touching WS-E: (1) a sixth oracle round, Describe "WS-E target oracle v6" in conformance/ws_e_probe_conformance_test.go, 51 pinned target outcomes (a constant expression as a scan comparand and its trace; folds to false/true beside another conjunct and their traces; IS [NOT] NULL over values whose nullability decides it and over nullable values that fail; CAST nullability; the CASE rows locating a target defect; a correlated IN item of the select's own row; STRING to UUID on eleven spellings as a literal, a bound string and a CAST; STRING to ENUM on three); (2) harness: the Java teardown catches every exception of a drop; the ephemeral template name comes from the harness (runWithEphemeralSchemaNamed); ephemeralTeardownProbe fails on an empty catalog listing; the dry-run template absence asserts 42F55; TestGoSQLRunner_TeardownFailureIsReported pins the Go runner's deferred-drop wiring; the dead SIMPLIFIED-REF instrument is removed from planRuleTrace and its pins; (3) Go tests added: TestPlanHarness_ConstantExpressionComparandIsSargable (pkg/relational/core/embedded/plan_harness_test.go) and TestFDB_TemporalComparandDateColumn (pkg/relational/sqldriver/temporal_comparand_date_column_fdb_test.go, today's DATE-vs-TIMESTAMP behaviour through an index and a residual copy, the baseline the design flips); (4) the design revised throughout (its Status paragraph lists the changes; section 5.4 is rewritten: translator folds deleted, generic fold and Go-only predicate driver deleted, one ConstantFoldingRuleSet for the whole-conjunction rule and rejectsNull, the tautology filter, tie pins per schema; section 4.1 (b)-(c): the target's explode rule and IN-source admission; section 4.3: time.Time as canonical TIMESTAMP text with a DATE-TIMESTAMP comparison lane and DATE-key projection, the JDK UUID parser, UTF-8 in serializeUnion; section 5.3 nullability table; section 6.4 per-index key sites); (5) TODO.md: the WS-E pointer block's mutation claim corrected, and an EOF block recording the Finding 6-followup (dense predicate-level map) measurement.
Go-side measurements the design cites (each on a detached worktree of a named tree; the worktree is /home/birdy/projects/fdb-nofold-probe and now holds the fold-free variant): translator folds made the identity on tree 3e7ea9aa4b29a2d3f9c41e1e860be337bbc0ca47 (wse7-nofold-bazel.log: 90/92 targets; wse7-nofold-embedded-test.log; wse7-nofold-shadow.log); the REWRITING variants on tree 34c824e272259d45f538d459dbddfc8b1170bc42 (wse7-runB.py applies both; wse7-runB-both.go and wse7-runB-taut.go are the two variants of designated_final.go; wse7-runB-bazel.log both 89/92; wse7-runTaut-bazel.log tautology only 92/92; wse7-golden-both.txt and wse7-golden-both-changed.txt the corpus dump; wse7-runB-*.log the failing targets' logs).
Oracle evidence: rfcs/257-java-upgrade-audit/ws-e-oracle/evidence-run.txt (seven Describes, 384 blocks, two runs equal, nine source hashes) and evidence-mutations.txt (the "Design v6" block and "Harness teardown, v6"). Run records under /var/tmp/fdb-upgrade-recovery/: wse7-all1.log, wse7-all2.log, wse7-teardown.log, wse7-teardown2.log, wse7-M1.log, wse7-M2.log, wse7-listmut.log, wse7-td-wire.log, wse7-td-wire-mut2.log, wse7-debug.log (CONFORMANCE_DEBUG=1 run, the PickValue frame), wse7-temporal2.log and wse7-temporal-mut.log (the DATE test and its mutation), and the just test of this tree: the tree before and after it is 66663d5d8d1946a4891985e75c682cb98a18609f (wse7-justtest-tree.txt, wse7-justtest-tree-after.txt); wse7-justtest.log: "Executed 8 out of 94 tests: 94 tests pass."; the eight executed targets are listed in wse7-justtest-executed.txt (test.log files newer than the tree record) and each target's log is copied as wse7-justtest-<target>.log (conformance_test: Ran 1424 of 1543 Specs, 119 inherited skips; rfc257_oracle_test: Ran 32 of 32; sqldriver_test includes TestFDB_TemporalComparandDateColumn; embedded_test includes TestPlanHarness_ConstantExpressionComparandIsSargable).
NOT WS-E, same tree, own gates: WS-D v13 (NAK, under revision), WS-F v2 (NAK), WS-J v4 (NAK, rfcs/257-java-upgrade-audit/ws-j-design-review-v4/).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). The JDK 21 UUID source is at /tmp/jdksrc/java.base/java/util/UUID.java. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v5 verdicts, the FULL v6 design and its diff from v5, the new oracle rows and evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions (deleting the translator folds and what else relied on them; the whole-conjunction rule and rejectsNull on one rule set; the tautology filter and the decision NOT to take the dense level map; the tie pins; the target's explode rule shape reading a LogicalFilterExpression as a one-quantifier select, and the IN-source admission; the DATE-TIMESTAMP lane and its index projection; the UUID port and its sites; the nullability table; the per-index key sites), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; no second query pipeline; Lucene separate.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes, incomplete conversions, dead code, test vacuity (pins, renders, set checks, the harness teardown), evidence that does not bind the bytes, duplicate mechanisms and maintainability.
