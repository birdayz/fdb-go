Review the revised RFC-257 WS-J DESIGN (v4), and the fixes it records as LANDED (sections 3.1, 3.2, 4b, 4c's validation and the section-0 instrument), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE eb79c50e127ebbed64612a10811d9020bc723b18; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 508e5cd5fe89179d448aa44a9f14b36f8ed000362a69bb588fab910aff5b3ffe (1310 lines). Previous: v3 TREE 256afd191053c0f4f5e47b88bdeddbe19597f8fc (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v3/*.txt); v1/v2 in ws-j-design-review-v1/-v2.
Delta since v3 touching WS-J: (1) PRODUCTION: literalKeyCarrier sets the carrier from the static type for every Go integer kind (pkg/relational/core/query/ddl/generator.go literalInteger); the widening arm is one-way (pkg/recordlayer/metadata_evolution_validator.go literalValuesEquivalent/literalOnlyField); SetRecordsWithUnionName runs fetchUnionDescriptor and refuses another name, Build returns the first recorded fault, validateRecordUnion reports the non-message arm in field order (pkg/recordlayer/metadata.go, metadata_validate_records.go); uint32/fixed32 read signed and typed INT everywhere, written from the INT range as their bits, uint64/fixed64 LONG and written as bits (values/values.go ProtoScalarKindToRowValue, values/proto_field_type.go, relational/core/functions/proto_value.go, recordlayer/query/executor/executor.go, rlcatalog.go); javaTupleValueClassName names DynamicMessage for a run-time-descriptor message (long_arithmetic_function.go); frl meta evolve-check --allow-literal-carrier-widening (cmd/frl/internal/cmd/meta.go); comments only: the plan-cache/literal-matching coupling (cascades/value_equivalence.go, embedded/query_hash.go) and stale text (fdb_store_catalog.go, metadata.go, meta.go); (2) TESTS: literal_key_carrier_test.go, metadata_evolution_validator_test.go (one-way), metadata_validate_records_test.go (precedence, the relational union as its own field, TestSetRecordsWithUnionNameRefusesAnotherUnion, TestPreviouslyStoredMetaDataIsRefusedOnLoad), metadata_union_evolution_test.go (fixture now marks its union), index_option_order_test.go, long_arithmetic_function_test.go (TestJavaTupleValueClassName), executor_test.go, functions/proto_value_test.go, values/proto_scalar_shape_test.go, cmd/frl meta_test.go; (3) INSTRUMENT: conformance/ws_j_index_fidelity_conformance_test.go (five literal shapes; Go outcome digests pinned beside classes in conformance/testdata/rfc257/wsj_go_classes.json with a double-build determinism check; the Go-stored spec's four non-bitmap T1 reads; F2b raw hex; the INT_MAX lane probe with Go pins; the "WS-J duplicate index option" spec), conformance/sql_plan_steps.java (wsjIndexEntriesJava emits real hex; step wsjDuplicateIndexOptionJava), conformance/metadata_proto_conformance_test.go (five more records-descriptor shapes), wsj_java_pins.json (five shapes added, nothing else moved); (4) DOCS: the design revised throughout (Status paragraph), ws-j-oracle/README.md, evidence-v4.txt (every landed file and test hashed, two passes, just test), evidence-mutations.txt (the v4 block, each with text and presence), CHANGELOG.md, DIVERGENCES.md (two appended paragraphs/entries at EOF), TODO.md F11 block corrected. NOT WS-J, same tree, own gates: WS-E v5 verdicts (in ws-e-design-review-v5), WS-D v13 verdicts, WS-F v2 verdicts, WS-E harness teardown and oracle changes.
Run records under /var/tmp/fdb-upgrade-recovery/: wsj4-ev1.log and wsj4-ev2.log (two uncached passes of every WS-J spec), wsj4-justtest.log (just test on the hashed bytes: 54 of 94 executed, 94 pass), wsj4-vr-jvm.log (the 18 records-descriptor shapes on both engines), wsj4-evol.log, wsj4-mut-*.log (the twelve v4 mutations), wsj4-a/b/gs1/full1/full2.log (development runs), and wsj4-src.sha (the hashed file list).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v3 verdicts, the FULL v4 design and its diff from v3, the landed code and tests, the oracle and its evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions (the IndexSpec port's Go-class arms and top-node check on the real translated graph, the stored-key decline versus the target's whole-table failure, the plan-cache coupling, the one-way widening arm and its reachability through section 4, the carry rule's EQUIVALENT definition, the re-added-name refusal, the (name, version) guard and its concurrency, F13's scope and migration, signed readers and writers, the single-merge claim), not only whether the requested words appear. Distinguish measured target facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT; the materialized outer-join NLJ (RFC-152); RFC-209 companions; the rebind validator; C++ 7.3.77; no second query pipeline; Lucene separate.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the DDL front end, IndexSpec and QuantifierValues on the translated graph, value-index expansion (section 3.5) and the non-integer match policy, metadata assembly order, lane resolution at construction, and the evolution/carry policy for stored templates.
