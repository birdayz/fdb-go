Review the revised RFC-257 WS-J DESIGN (v10), and the fixes it records as LANDED (sections 3.1, 3.2, 4b, 4c's validation, load order and shape-(d) refusal including the catalog's store and load refusals, the loader's value_expression, added_version and now subspace-key readings, the section-0 instrument), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE c789832131f2e6206ee2ebad9b92a3ae463d6f6d; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 bcccfc9e77a3c12ab751b2b09ffdf9ca76233e3b39058da0fd2d55e275bfe8b6 (2356 lines). Previous: v9 TREE f8abe2d742df8c35b44fb0c6f854f86c3cd3a016 (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v9/*.txt); v1-v8 in ws-j-design-review-v1/-v8. `git diff f8abe2d742df8c35b44fb0c6f854f86c3cd3a016 c789832131f2e6206ee2ebad9b92a3ae463d6f6d -- <path>` shows v10 (both are tree objects; the diff also carries the WS-C addendum's revision 6 and WS-E's text, gated separately).
Delta since v9 touching WS-J: (1) CODE AND TESTS: the loader reads a stored subspace key as Java does (pkg/recordlayer/metadata_proto.go, refusing an empty key, several items and a null item; RecordCoreArgumentError gains SubspaceKey and renders only its set fields, pkg/recordlayer/rank_scan.go; TestStoredIndexSubspaceKeyIsReadAsJavaReadsIt in metadata_proto_test.go); AmbiguousLegacyUnionError.SQLTemplate and LegacyUnionTemplateError marking it (pkg/relational/core/metadata/builder.go) so the SQL family's 0A000 names the SQL remedy; conformance/ws_j_index_fidelity_conformance_test.go: WSJTT's two shapes (a column named _A, a table named "UUID") and the SQL-remedy assertion, WSJLANE's two-fault shape, WSJIX's option-list case and the It "refuses each malformed stored subspace key as Java does". (2) DESIGN v10 (its Status paragraph lists the changes): the edited-file route under the ordinary save's evolution validation (4c, (p)); the restore's in-transaction binding read and template-row read with the interleaving tests (section 2); the version guard in both catalogs; the carried route (LoadTemplateProto, CreateTemplateFromProto running deserializeTemplate's whole path in both catalogs, fleet.SaveTemplate returning the stored template, the lane check over defined indexes only) and the eight stored no-lane shapes ((m), (t)); (t)'s scope, the operand with no Java Value, WSJLANE's comparison on SQLSTATE and message and its precedence shape; section 8's steps 1 and 3; the WSJTT overclaim, the message quote, the restore signature. (3) CHANGELOG (the subspace-key reading; the SQL remedy), DIVERGENCES.md (the (t) entry rewritten, the guard's title and body with the restore's F11 limitation), TODO.md (a merge-unit block at the end), the umbrella's gate 8, ws-j-oracle/README.md item 20, evidence-mutations.txt (v10 block), evidence-v10.txt.
Evidence: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v10.txt and evidence-mutations.txt (v10 block). Run records under /var/tmp/fdb-upgrade-recovery/: ev10/ (wsj-ev1.log, wsj-ev2.log, wsj-vr-jvm.log, wsc-focus.log, wsc-loop40.log, justtest.log, justtest-executed.txt, justtest-tree*.txt, src.sha), ev10a.sh, ev10b.sh, ev10a.out, ev10b.out, ev10-files.txt; wsj10/ (capture.log, capture2.log, wsj-all.log, sql-remedy2.log, the first focused runs); wsj10mut/ (wm10-*.log, *.full.log, wm10-0-baseline.full.log, run.sh, all.sh, all.out).
NOT WS-J, same tree, own gates: WS-C section 7 addendum (revision 6, gating now), WS-E v8 (NAK, being revised), WS-D v13 (NAK), WS-F v2 (NAK).
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); the pre-upgrade Go builder and loader are at commit e48f5b496.
Read ALL three v9 verdicts and check that EVERY finding is resolved, the FULL v10 design and its diff from v9, the landed code and tests, the oracle and its evidence, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (the edited-file route's validation and its one relaxation, the restore's reads against every interleaving of DROP SCHEMA, fresh create and restore, the guard in both catalogs, the carried route and the lane check's scope, the eight stored no-lane shapes, the subspace-key reading against every stored shape and every writer Go has, the SQL remedy), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the DDL front end, IndexSpec and QuantifierValues on the translated graph, value-index expansion (section 3.5) and the non-integer match policy, metadata assembly order, lane resolution at construction, and the evolution/carry policy for stored templates.
