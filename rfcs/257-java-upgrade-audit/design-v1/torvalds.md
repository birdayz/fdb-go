**NAK — WS-A design only**, for exact virtual tree `f6efe9b8fd99522d2b95568a99eaac9ae7acb72b`.

1. **Specify compatibility with existing Go access-info bytes.** [RFC:161](/home/birdy/projects/fdb-record-layer-go/rfcs/257-java-4.14.2.0-upgrade.md:161) classifies access-info plain bytes as transformed. That matches [Java StorageAdapter:247](/home/birdy/projects/fdb-record-layer-go/fdb-record-layer/fdb-extensions/src/main/java/com/apple/foundationdb/async/hnsw/StorageAdapter.java:247), but existing Go deletion returns the candidate’s original bytes at [hnsw.go:1132](/home/birdy/projects/fdb-record-layer-go/pkg/recordlayer/hnsw.go:1132) and persists them at [hnsw.go:882](/home/birdy/projects/fdb-record-layer-go/pkg/recordlayer/hnsw.go:882). A pre-centroid candidate can therefore become **raw DOUBLE access-info under an active transform**. Location provenance alone would misinterpret that persisted state. Define repair/rebuild/refusal behavior and require a parent-produced fixture covering centroid establishment, entry deletion, and upgraded reads.

2. **Define coercion ownership and preparation lifetime.** [PromoteValue:5130](/home/birdy/projects/fdb-record-layer-go/pkg/recordlayer/query/plan/cascades/values/values.go:5130) exposes mutable `Child`/`Target`; nested types also alias mutable graphs. [replace.go:278](/home/birdy/projects/fdb-record-layer-go/pkg/recordlayer/query/plan/cascades/values/replace.go:278) bypasses constructors, while [FinalizePlan:41](/home/birdy/projects/fdb-record-layer-go/pkg/recordlayer/query/plan/cascades/plan_finalize.go:41) establishes concurrently shared plans. Specify when coercions and descriptors are prepared, how literals/reconstruction participate, and how subsequent drift is rejected or isolated. Require mutation/reconstruction and concurrent-evaluation tests; avoid per-row descriptor compilation or lazy writes into shared values.

The field-16 cloning direction and restored positive mixed-width regressions are sound. Audit accounting reconciles 1,189 net paths and 84 history-only paths; this covers complete net-diff accounting plus necessary history, **not 228 independently reviewed commits**. The five research reports are not reviewer ACKs.

No implementation ACK or later-workstream authorization follows. The suite remains red; I ran no builds/tests and changed nothing.