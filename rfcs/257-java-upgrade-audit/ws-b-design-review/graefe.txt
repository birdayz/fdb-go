**ACK — WS-B design only. Required design changes: none.**

Reviewed the complete design with SHA-256 `699f084274f4acf98ebf40c9329a577bac952657e4c57f527f34ce5cb1b17390`, parent RFC257 WS-B, both retained research reports, pinned Java `fdacd162a9c8acfadc49082b89185c823ab8ae4a`, and relevant current Go implementations. Published Go HEAD matches `71ccd8cf8b3fd0dbafe283e91171818e36af555e`.

Applying the requested virtual Graefe architectural lens, all five sections specify implementable mechanisms and observable acceptance criteria:

1. **Sliding-window insertion — [design:21](/home/birdy/projects/fdb-go/rfcs/257-java-upgrade-audit/ws-b-design.md:21).** The exact tracked-entry read precedes mutation. Repeated insertion preserves bookkeeping and refreshes only an in-window delegate. Ordinary versus write-only callbacks, ordinary eviction/promotion callbacks, filtering, corruption handling and operation counters match the tagged mechanism. The size-one overflow correction remains explicit.

2. **Deletion and cache coherence — [design:50](/home/birdy/projects/fdb-go/rfcs/257-java-upgrade-audit/ws-b-design.md:50).** Conditional stamp invalidation requires the accompanying cache-admission and old-cacheability fixes; the design includes them. Tuple-subspace clearing and removal of deferred version mutations prevent delayed writes from restoring deleted data. Header conflicts protect overlapping transactions without introducing a global serializable stamp dependency. Pinned read versions, sentinel writes and first-commit assertions make the conflict contract testable.

3. **Replacement lifecycle — [design:81](/home/birdy/projects/fdb-go/rfcs/257-java-upgrade-audit/ws-b-design.md:81).** Separating changed-index enumeration from build eligibility closes both initialization and build-all paths. DISABLED selection precedes policy invocation; retirement requires every replacement to be exactly READABLE. Transaction-visible state, conflicts on negative maintenance decisions, context-level check deduplication, all commit entry points and preservation of build locks form a coherent lifecycle. Explicit original rebuilds remain supported.

4. **Options and metadata identity — [design:132](/home/birdy/projects/fdb-go/rfcs/257-java-upgrade-audit/ws-b-design.md:132).** Copied configuration and a caller-supplied mutable remainder preserve the intended validation boundary. Explicitly ignoring `unique` is Java behavior; unrelated validation remains active. UNION-tag correspondence, recursive descriptor-pair validation, actual message descriptors, separate default/serialization/deserialization tag rules and descriptor-compatible factories address persisted identity through renames, swaps and name reuse. The existing old-descriptor rewrite loop is correctly retained.

5. **Rank-valued scans — [design:175](/home/birdy/projects/fdb-go/rfcs/257-java-upgrade-audit/ws-b-design.md:175).** Bounds validation, declared score-column extraction, absolute per-group ranks, singleton integer/NULL values and serializable rank lookup match Java. The false flag retains existing behavior and cost. Cursor obligations and exact value/packed-byte comparison close the current key-only conformance blind spot without changing stored rank bytes.

The delayed-retirement disposition at [design:110](/home/birdy/projects/fdb-go/rfcs/257-java-upgrade-audit/ws-b-design.md:110) is acceptable. Source inspection identifies the captured-callback hazard; it does **not** constitute an executed reproducer. The retained live Java probe remains required before implementing that composition. If confirmed, cancellation and no resurrection must be recorded as an intentional correction, with a regression and report text. External publication remains unauthorized.

The [acceptance requirements](/home/birdy/projects/fdb-go/rfcs/257-java-upgrade-audit/ws-b-design.md:199), together with the section-specific matrices and retained research, adequately cover real-FDB execution, errors, both conflict orders, committed reopen, wire interoperability and preservation of existing extensions. These are outstanding implementation obligations, not completed evidence.

This ACK leaves final implementation review separate. Queue/state4/format15 remain WS-C; WS-C–K and the whole upgrade remain open. No files changed, builds/tests ran, agents spawned, Git mutations occurred, or GitHub contact was made.