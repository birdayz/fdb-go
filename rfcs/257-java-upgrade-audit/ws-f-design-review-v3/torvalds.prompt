Reconfirm the revised RFC-257 WS-F DESIGN v3 (planner scheduling, explicit index-entry readers, planner properties: audit items cascades W6-W15) and the test-only oracle it rests on. Repo /home/birdy/projects/fdb-go. Candidate TREE 8d721bd13b28b59be91f59a35b71acbd15d7131c; design rfcs/257-java-upgrade-audit/ws-f-design.md SHA256 c54897e41bf8cee43fbeb32d7402452a25a0757361304d3c936c95e33c2608f9 (1480 lines). Previous: v2 TREE d40cc865737fe57df9f3a0880684608f1a793de0 (three NAKs, rfcs/257-java-upgrade-audit/ws-f-design-review-v2/*.txt); v1 in -v1. Diff the design with: git diff d40cc865737fe57df9f3a0880684608f1a793de0 8d721bd13b28b59be91f59a35b71acbd15d7131c -- rfcs/257-java-upgrade-audit/ws-f-design.md (the trees also differ by WS-C/WS-D/WS-E/WS-J work that has its own gates).
Delta since v2 touching WS-F: (1) TEST-ONLY oracle v3 conformance/ws_f_probe_conformance_test.go: 130 probes plus a verdict-arm spec; new target steps runPreparedSequence (same-connection binding sequences) and wsfRecordLayerIn (new file conformance/wsf_record_layer_probe.java, registered in conformance/conformance_server.java; the target's record layer planner under its default and relational configurations), planRuleTrace modes IN-UNION-PARTITIONS, ROOT-PAIRS (with unmatched-field counts) and REWRITING-RESULT in conformance/sql_plan_steps.java; Go runner refactor in pkg/relational/conformance/plandiff/go_runner.go (withEphemeralSchema, rowSetFrom, RunPreparedSequenceWithSetup; pinned in go_runner_test.go; the docscheck seam allowlist entry renamed to go_runner.go:withEphemeralSchema); acceptance as code (wsfAcceptance/wsfOpenUntil ratchet with SAME / SAME-STATE / DIFF verdicts, section 13); evidence rfcs/257-java-upgrade-audit/ws-f-oracle/evidence-run.txt (two pinned runs identical, 780 lines; mutations: m1 target pin and m2 Go pin exit 3; m3/m4 exit 1 as build failures of unformatted mutants, re-run formatted as m3b owner-dropped and m4b closed-row-owned, both exit 3); (2) the design v3: the in-union rule's view dumped on both engines, the union-ordering arithmetic MEASURED identical; the ORDER BY id difference located upstream in the data-access match's record-type position (the target's match ordering stops at the record-type key position of relational PKs, Go's continues): decision to keep Go's match extension, declared, while the in-union rule reads its legs as the target's match would (4.3); the partition memoized whole; the target's cost relation over root members MEASURED cyclic on the covering class (COVERING < ISCAN < SCAN < COVERING) under three name sets, Go keeps its total-preorder rank, declared (4.2, F-7c); the target's record layer default configuration (in-union size 0) MEASURED to fail in-union execution (4.3); the target's LEFT JOIN REWRITING survivor MEASURED as the rewritten form: decision to keep the outerJoinCount omission plus the PLANNING RewriteOuterJoinRule, with its own census arm (2.3, 2.6); sparse-index prepared form and same-connection binding sequence MEASURED (the target uses I4 under a plan constraint; Go: a '?' implies nothing until WS-H) (5); IN-subquery refused by both engines (0AF00); F-7 split into F-7a (the configuration plus metadata reach the rule-level comparators), F-7b (IN list) and F-7c (preference) (12); section 12 continuations rewritten with a transitions table; D7 marks every restricted reference explored, as Java does (2.4); section 14 owner items. Additional Go trace (scratch instrument, not in the tree): /var/tmp/fdb-upgrade-recovery/wsf3-go-iup.log (Go in-union partition view, 100 lines). NOT WS-F, same tree, own gates: WS-D v14 (ws-d-design.md, SimFDB declinedRetryError, RFC-094 section 2, connection.go comment), WS-C design text r9 (NAK, being revised), WS-J v13 and WS-E v11 (NAKs; their review directories are excluded from this tree). WS-E v11's Graefe verdict relies on this v3 for the RFC-182 second crossing (REWRITING winner only crosses, unified_tasks.go:98-108): say whether v3 states it.
Run records (bound by sha256 lists): /var/tmp/fdb-upgrade-recovery/wsf3ev/ (run.sh, src.sha over the 7 oracle files, check.txt/check2.txt all OK; run1.log/run2.log with run1.lines == run2.lines; m1..m4b .mut/.log; run.out), /var/tmp/fdb-upgrade-recovery/wsdf14/ (oracle 61 of 61 specs; conformance 1523 passed of 1642, 119 skipped by the suite's existing filters) and /var/tmp/fdb-upgrade-recovery/wsdf14b/ (simfdb + plandiff + docscheck 12 of 12 uncached after the allowlist rename; every hashed blob equals this TREE's).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); tag 4.12.11.0 is available for diffs (git -C /home/birdy/projects/fdb-go/fdb-record-layer diff 4.12.11.0 4.14.2.0 -- path). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v2 verdicts, the FULL v3 design and its diff from v2, the oracle source, target steps and evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions, not only whether requested words appear. Distinguish MEASURED target facts from SOURCE claims and HYPOTHESES.
Preserved boundaries: the owner-approved Go read/raw nullable-array, scalar and sorting extensions; the Go-only IN-subquery read extension; LIMIT (approved Go extension); the materialized outer-join NLJ (RFC-152); RFC-209 companions; the rebind validator; C++ 7.3.77; format max 15/default 14; SPFresh/LIRE independent of GuardiANN; Lucene separate.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes in the decisions, steps that cannot be verified as written, test vacuity in the oracle (pins, probe/pin set equality, render and comparator functions, the Java rule-trace step), incomplete conversions, duplicate mechanisms left behind, stale-claim sweeps and their closure criteria, evidence that does not bind the bytes, maintainability.
