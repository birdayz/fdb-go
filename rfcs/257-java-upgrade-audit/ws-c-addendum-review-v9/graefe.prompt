Review REVISION 9 of an ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together. Repo /home/birdy/projects/fdb-go. Candidate TREE d6ad3e9c2c5bc3a32c42cf0399c97afade52e098; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 3d3898502df800e95c35f86d54c7da360fe19e879338b2b56c92b9d647b472c6, 1668 lines); 7.1 to 7.9 are revisions 1 to 9, each stating what changed and why. Revision 8 was gated at tree 97d0b9181d02696345e166b12d9ba09b18141c49 and got three NAKs sharing one medium finding (the RANK and R-tree option checks went through Go config parsers that were not Java's, and the maintainers behind them wrote other bytes than Java) plus low findings: read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v8/{graefe,torvalds,storage}.txt (and v1-v7's) and check that EVERY finding is resolved. Revision 8's design is at /var/tmp/fdb-upgrade-recovery/ws-c-design.r8.md.
What changed: `git diff 97d0b9181d02696345e166b12d9ba09b18141c49 d6ad3e9c2c5bc3a32c42cf0399c97afade52e098 -- <path>` (both are tree objects; the same diff carries WS-J design v13 and WS-E design v11, gated separately). Section 7.9 lists revision 9's changes. Code: pkg/recordlayer/java_parse.go (IllegalArgumentError, NumberFormatError, javaParseInt as Integer.parseInt, javaParseBoolean as Boolean.parseBoolean); ranked_set.go (the JDK, CRC, RANDOM and MURMUR3 hash registry, Guava's murmur3_32 seed 0, RANDOM drawn through the DST seam and bound by config.withEnv, HashFunctionName); rank_index_maintainer.go and the leaderboard maintainer (parseRankedSetConfig returns Java's errors, levels [2,8]); multidimensional_index_maintainer.go, rtree.go, rtree_storage.go (parseRTreeConfig as MultiDimensionalIndexHelper.getConfig including its Hilbert quirk; BY_SLOT storage; the node slot index kept as a per-operation difference over touched intermediate nodes in the secondary subspace; deleteWhere clearing it); index.go and text_index_maintainer.go (booleans and the tokenizer version parsed as Java parses them); metadata_evolution_validator.go (the configs parsed old first, sorted walks, the union-less path deleted, unbuilt meta-data refused); rename_fields_visitor.go (Java's messages and class); metadata.go (Java's Build messages, sorted loops, the rootless-index refusal, faults in program order through a global sequence, SetSubspaceKey's refusal recorded without marking the key explicit, SubspaceKeyError); errors.go (KeyExpressionDeserializationError); key_expression_proto.go, metadata_proto.go; chaos verify_rank; tests java_parse_test.go, rtree_layout_test.go and the corrected and added unit tests named in 7.9; conformance/index_option_evolution_conformance_test.go (30 shapes and "Text tokenizer version parsed at build"), rank_index_conformance_test.go ("RANK ranked set per hash function"), multidimensional_options_conformance_test.go (six option sets) and their Java steps; CHANGELOG.md, DIVERGENCES.md.
Evidence: 7.9's mutation tables (m81-m104 unit, c1-c5 JVM) with /var/tmp/fdb-upgrade-recovery/wsc-v9/final/ (run.sh, mutate.sh, run.out, good/, baseline.log, baseline-conf.log, each mut-*.log and *.full.log; the superseded preliminary run is /var/tmp/fdb-upgrade-recovery/wsc-v9/mut-*.log); the combined evidence run rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v13.txt with /var/tmp/fdb-upgrade-recovery/ev13/ (driver ../ev13.sh, src.sha, check.txt, M11.log, oracle-1/2.log, jvm-wsc.log, recordlayer-v.log, justtest.log, justtest-executed.txt, final-targets.log, run.out); the earlier JVM run /var/tmp/fdb-upgrade-recovery/wsc9-conf2.log. Every run is on /home/birdy/projects/fdb-wsc9, a frozen copy of tree 3b1587fafbd31525e918fbb44e71e394bf97c44c; the gate tree adds to it only ws-c-design.md's 7.9 tables and evidence paragraph, ws-j-oracle/evidence-v13.txt and the regenerated ws-e-oracle/evidence-run.txt (`git diff --stat 3b1587fafbd31525e918fbb44e71e394bf97c44c d6ad3e9c2c5bc3a32c42cf0399c97afade52e098`).
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/: provider/foundationdb/indexes/{RankedSetIndexHelper,RankIndexMaintainer,MultiDimensionalIndexHelper,MultidimensionalIndexMaintainer,TextIndexMaintainerFactory}.java; in fdb-record-layer/fdb-extensions/src/main/java/com/apple/foundationdb/async/: RankedSet.java and rtree/{RTree,BySlotStorageAdapter,ByNodeStorageAdapter,NodeSlotIndexAdapter,AbstractStorageAdapter}.java; and in core metadata/Index.java, MetaDataEvolutionValidator.java, RecordMetaDataBuilder.java, expressions/*KeyExpression.java.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which has moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Check every revision-8 finding's resolution against Java and the code: every parser against Java's (Integer.parseInt, Boolean.parseBoolean, the hash registry and Guava's murmur3, getConfig's order and quirks), the bytes each maintainer writes against Java's adapters (BY_SLOT pairs, node slot index entries and levels, the RANK sets per hash), the validators' messages and walks, the nil-key and rootless paths, Build's program order, the mutation logs and baselines, the JVM specs. Distinguish measured facts from source hypotheses.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the option parsing and the maintainers as a whole (configuration read once, as Java's helpers read it, at the points Java reads it), phase boundaries, and whether any Go-only behaviour remains undeclared.
