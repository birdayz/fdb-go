Review the revised RFC-257 WS-J DESIGN (v16), and the fixes it records as LANDED, not the rest of the implementation. Repo /home/birdy/projects/fdb-go, branch upgrade/java-4.14.2.0, published as PR #786 (the owner's one PR for the whole upgrade). Candidate COMMIT f53cd6fc6; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 4b2070eada96626795353a7f03435d0d6e8d8b2aca48e9aa48e36a580b9ca717 (2742 lines at f53cd6fc6). Previous: v15 TREE 0e057cc76c3121918e75b71d2b51a37fed21faf9 (Graefe NAK, rfcs/257-java-upgrade-audit/ws-j-design-review-v15/graefe.txt; the v15 Torvalds and storage lenses were stopped when the owner's ruling made their subject moot); v1-v14 in ws-j-design-review-v1/-v14. `git diff 0e057cc76c3121918e75b71d2b51a37fed21faf9 f53cd6fc6 -- rfcs/257-java-upgrade-audit/ws-j-design.md` shows v16 (the tree diff also carries WS-C revisions 11-14, WS-D and WS-E design text and bookings, gated separately).
The owner's rulings (rfcs/257-java-4.14.2.0-upgrade.md, "Verification and review gates" item 9): data written by pre-release Go builds (v0.1.0 included) is not supported, nothing is built to migrate, detect or refuse it; Go answers every query the target answers; evidence is red→green and Java-versus-Go, not mutation; one PR, pushed as it lands, CI and ACKs per workstream. Delta since v15 (the design's Status paragraph lists it): section 4d rewritten under the ruling (the landed shape (d) refusal deleted; v7-v15's migration, edited-file route, writer argument, R1, R2, reservation rule, store-open probe and detector withdrawn; 9 (p), (r), (y) withdrawn); SetRecords processing the target's extension options; the deserialization port (3.6) with v15 Graefe's corrections (NullStandin, function-key arity and column sizes, the refusal of a key function the target cannot load, absent children, Go's untyped refusals declared); 9 (x), (z) corrections; and since then, recorded in section 4d and section 8 step 1's STATUS: the IndexMaintenanceFilter port with the BY_INDEX write-only dispatch, and section 8 step 1 LANDED (the version guard in both catalogs, the gone-version refusal, the DeleteTemplateVersion refusal, fleet.RestoreTemplateVersion with its header batches, in-transaction reads and carry checks (i)-(iv), and a LiteralKeyExpression int_value fix found on the way). Steps 2 and 3 of section 8 are NOT landed; they are the next implementation work and complete the merge unit.
WS-J code in the commits since v15: a33336527 (NullStandin, proto3 absence and query field reads, the FunctionSpec registry, SetRecords extension options, the deserialization refusals), 9ba005539 (pkg/recordlayer/index_maintenance_filter.go and the maintainers), 51c3f90eb (pkg/relational/core/catalog/template_bindings.go, fdb_template_catalog.go, template_catalog.go, store_catalog.go, fdb_store_catalog.go, pkg/recordlayer/index_carry_class.go, metadata_evolution_validator.go's symmetric carrier option, api LoadTemplateProto, the relational validator moved to catalog), 529753bf6 (pkg/relational/core/catalog/template_restore.go, fleet/migrate.go's RestoreTemplateVersion, key_expression.go's LiteralKeyExpression.Evaluate); tests: pkg/relational/core/catalog/template_version_guard_fdb_test.go, template_version_guard_test.go, template_restore_fdb_test.go, pkg/recordlayer/index_carry_class_test.go, index_maintenance_filter_test.go, conformance "RFC-257 NullStandin" and "A literal key column is maintained as Java maintains it" (conformance/null_standin_conformance_test.go, key_validation_conformance_test.go).
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); the pre-upgrade Go builder and loader are at commit e48f5b496. Relational catalog Java: fdb-record-layer/fdb-relational-core/src/main/java/com/apple/foundationdb/relational/recordlayer/catalog/{RecordLayerStoreCatalog,RecordLayerStoreSchemaTemplateCatalog}.java.
Tree objects: run `git show COMMIT:path` or `git diff A B -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read commit objects, not the working tree, which moves on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool (step 1's red runs: evidence/ and the design's STATUS).
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline; wire compatibility with Java-written stores is the hard line.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact commit and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Read the v15 Graefe verdict and check that EVERY finding is resolved or made moot by the ruling (and say which), the FULL v16 design and its diff from v15, the landed code and tests, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (what the ruling removes and what it leaves the design owing to Java-written stores; SetRecords and its extension options against the target; the deserialization port; the version guard, gone-version refusal and restore against the target's catalog and against concurrency; the carry rule steps 2 and 3 still owe), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.

Apply the independent storage/wire correctness review lens: what each catalog route reads and writes (stored templates, schema rows, TEMPLATES_VALUE_INDEX, restores, the carried bytes steps 2-3 will write), the transaction and conflict behaviour of CreateTemplate, DeleteTemplateVersion, the restore and a concurrent bind or DROP SCHEMA, the index entries IndexMaintenanceFilter admits against Java's, and wire compatibility of every stored template and records file with the target.
