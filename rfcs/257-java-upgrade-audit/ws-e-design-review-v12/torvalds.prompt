Reconfirm the revised RFC-257 WS-E DESIGN (v12), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE c59b32e13624a3bbf65545c594c051499d5633af; design rfcs/257-java-upgrade-audit/ws-e-design.md SHA256 7e04ea6f2b17e701f48af4981ee15862f0893b32ee799fa0b07ce1829f4095b0 (3929 lines). Previous: v11 TREE d6ad3e9c2c5bc3a32c42cf0399c97afade52e098 (three NAKs, rfcs/257-java-upgrade-audit/ws-e-design-review-v11/*.txt); v1-v10 in ws-e-design-review-v1/-v10. The v11 design is at /var/tmp/fdb-upgrade-recovery/ws-e-design.v11.md; diff with git diff d6ad3e9c2c5bc3a32c42cf0399c97afade52e098 c59b32e13624a3bbf65545c594c051499d5633af -- rfcs/257-java-upgrade-audit/ws-e-design.md (the tree diff also carries WS-C r10, WS-F v4, WS-D v15/v16 and WS-J v14, gated separately).
Delta since v11 (the design's Status paragraph lists it; each change is marked v12 at its site): the compensation guard's explode arm (a residual on an explode alias admitted when the probe is correlated; a table alias the probe does not feed stays refused), measured on the prototype: #7 built, the RFC-150 shape refused where v11's GUARD admitted it, PIN 1 shown not to reach the branch; the planning cost root-caused (Go's ImplementInJoinRule adds a Preserve ordering and repeats its memoization per inner plan; aligned, 4 lists 44,099 tasks vs 460,629) with the TARGET's task counts measured (new TASK-COUNT mode of Java step planRuleTrace; 3,835 / 19,366 / 117,131 for 2-4 lists); the 150,000-task tripwire kept and five-list queries declared; the NaN producer census (MIN/MAX measured to return the operand's NaN in the target), the CAST grammar measured over 22 spellings and ported, the binder's four callers each with an arm (aggregate and vector keep the refusal, declared), a Go-written NaN in a target store with a UNIQUE index pinned to today's outcome; section 8's NaN declaration rewritten; the DATE repair scoped, batched, duplicate-checked; the type-merging operators promoted; the remaining time.Time sites decided; the multi-binding product measured on the target (5x5 refused), saturating, signed-zero and resume boundaries; the covering fix in F-7's terms with re-measurement; ARRAY<RECORD> refusal kept; tie pins split by phase; the RFC-182 crossing gating step (4) by name; step dependencies stated; section 0 and evidence regenerated.
Code (test-only): conformance/ws_e_probe_conformance_test.go gains Describes "WS-E target oracle v12" (92 pins), "... v12 planning cost" (9), "... v12 cross-engine NaN" (6) and wseRequireNaNPinArch; conformance/sql_plan_steps.java gains planRuleTrace's TASK-COUNT mode (tasks per phase and per task/rule kind). Evidence: rfcs/257-java-upgrade-audit/ws-e-oracle/evidence-run.txt with /var/tmp/fdb-upgrade-recovery/wse12ev/ (run.sh, extract.py, all1/2.log, .sorted, .uptime, check.txt, teardown.log, plandiff-docscheck.log); captures and runs wse12-*.log, wse12x-*.log; mutation wse12-mut.log. Prototype records: /var/tmp/fdb-upgrade-recovery/wse13-README.txt (driver wse13-proto2.sh, wse13b-*, wse13-in4-*.log, wse13b-walltime.log, wse13b-prototype.diff against base 71ccd8cf8; the prototype source is /home/birdy/projects/fdb-explode-probe), and wse12-suites-README.txt for the v11 records.
Tree objects: run `git show TREE:path` and `git diff OLD NEW -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v11 verdicts and check that EVERY finding is resolved, the FULL v12 design and its diff from v11, the oracle source and evidence, the prototype records, and check claims against actual Java and current Go. Challenge sufficiency and interactions, not only whether requested words appear. Distinguish MEASURED target facts from SOURCE claims and HYPOTHESES.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT (approved Go extension); the materialized outer-join NLJ (RFC-152); RFC-209 companions; C++ 7.3.77; format max 15/default 14; no second query pipeline. (There is no Go IN-subquery read extension at this base.)
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes, incomplete conversions, dead code, test vacuity (each new pin and whether it can fail, the guard pins, the planning-cost pins, the arch guard), the traceability of every prototype number to a recorded run, and whether each declared divergence is pinned.
