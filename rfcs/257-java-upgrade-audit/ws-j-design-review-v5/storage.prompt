Review the revised RFC-257 WS-J DESIGN (v5), and the fixes it records as LANDED (sections 3.1, 3.2, 4b, 4c's validation and load order, the section-0 instrument), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE f041bee2e4f12ba0ee362732fe5393761ceb6d34; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 a0596896a4b4c5ec5efc577759369eafe0009f09cbb51415c3bc175a892e9446 (1481 lines). Previous: v4 TREE eb79c50e127ebbed64612a10811d9020bc723b18 (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v4/*.txt); v1-v3 in ws-j-design-review-v1/-v3. The v4 design is at /var/tmp/fdb-upgrade-recovery/ws-j-design.v4.md for a diff.
Delta since v4 touching WS-J: (1) PRODUCTION: one builder function `setRecords` holds the "Records already set." guard, the union search and the name check (SetRecords, SetRecordsWithUnionName and the loader call it; pkg/recordlayer/metadata.go); RecordMetaDataFromProto follows Java's loadFromProto order (subspace-key settings, then records, returning the first records fault before any index is read) and refuses an index on an unknown record type with the target's "Unknown record type <name>"; it refuses shape (d), metadata the pre-upgrade loader read under a different union (AmbiguousLegacyUnionError, refuseAmbiguousLegacyUnion; Go-only, declared) (pkg/recordlayer/metadata_proto.go); literalKeyCarrier refuses every unrecognised (static type, Go kind) pair (pkg/relational/core/query/ddl/generator.go); the widening-arm doc comment is one-way; the plan-cache coupling comments (value_equivalence.go, query_hash.go) say Go does not match function-key literals today and a ParameterValue must never match one. (2) TESTS: TestStoredMetaDataLoadsInJavasOrder, TestStoredMetaDataWithAnAmbiguousLegacyUnionIsRefused, TestLiteralKeyCarrierRefusesUnknownPairs; the JVM spec "Java refuses the same records descriptors" gains 5 files (23); "WS-J duplicate index option" drives Go's loader beside the JVM; the fidelity oracle gains four top_* shapes (479 runs, pins updated); the non-integer spec gains the INT-edge ordered projection (21 reads); the F2b spec gains FLOAT columns compared across engines; sql_plan_steps.java's orphaned javadoc restored and its F2b insert step takes a FLOAT. (3) DESIGN v5 (its Status paragraph lists the changes): section 4's three-class carry rule (EQUIVALENT, WIDENED, CHANGED), section 8's merge unit, 4c's shape (d) and named-union migration, 2's DeleteTemplateVersion refusal and RestoreTemplateVersion, 3.2's template-save refusal of a key with no lane and the never-covered function value, 3.3's top-check precedence. (4) CHANGELOG (the migration APIs marked PENDING, shape (d)), DIVERGENCES.md (three EOF entries), TODO.md (EOF blocks "RFC-257 WS-J merge unit" and "F11 driver-keyspace migration"), the umbrella RFC's gate 8, ws-j-oracle/README.md item 15.
Evidence: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v5.txt (48 hashed files, two passes, the JVM records spec, two just test runs with their executed sets) and evidence-mutations.txt (the v5 block: nine mutations with text and presence, and the F12 revert's class/digest count). Run records under /var/tmp/fdb-upgrade-recovery/: wsj5-ev1.log, wsj5-ev2.log, wsj5-vr-jvm.log, wsj5-justtest.log and wsj5-justtest-*.log, wsj5-evidence.sh/.out, wsj5-mut-*.log, wsj5-f12-moved.txt, wsj5-topshapes*.log, wsj5-intedge.log, wsj5-f2bfloat.log, wsj5-src.sha and wsj5-files.txt.
NOT WS-J, same tree, own gates: WS-D v13 (NAK, under revision), WS-E v6 (NAK, under revision: rfcs/257-java-upgrade-audit/ws-e-design-review-v6/), WS-F v2 (NAK).
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which moves on for other workstreams. Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v4 verdicts, the FULL v5 design and its diff from v4, the landed code and tests, the oracle and its evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions (the three-class carry rule and which proto fields each class takes from which side, test 5's reachability of the widening arm, the merge unit and whether it closes every interim hazard, shape (d)'s refusal and the named-union migration, the load order, the template-save lane refusal against allowIndexRebuilds, the DeleteTemplateVersion refusal and restore, the top-check precedence and the LIMIT decision, the never-covered function value, the F2b FLOAT pins), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT; the materialized outer-join NLJ (RFC-152); RFC-209 companions; the rebind validator; C++ 7.3.77; no second query pipeline; Lucene separate.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the independent storage/wire correctness review lens: stored MetaData bytes (key expressions, literal widths, option order, record-type keys, union numbers, versions), index entry bytes under the operand widening, the upgrade path for templates Go already stored (carry rule, validator options, rebuilds, former indexes), the fresh-template guard, catalog existence policies and their conflict ranges, and the driver keyspace gap.
