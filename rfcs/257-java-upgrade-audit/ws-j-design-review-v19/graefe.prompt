Review the revised RFC-257 WS-J DESIGN (v19) together with WS-J STEP 3's IMPLEMENTATION (the catalog carry, the version refusal and the index lane check), which lands in the same commit. Repo /home/birdy/projects/fdb-go, branch upgrade/java-4.14.2.0, published as PR #786 (the owner's one PR for the whole upgrade). Candidate COMMIT 1ad10b73e; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 c58f713a8c07ec0b5bce1f4a76f1cca124cf0304da785f98de0014669295bfbd (3163 lines at 1ad10b73e). Previous: v18 at d2e47b8bd (Graefe, Torvalds and storage NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v18/{graefe,torvalds,storage}.txt); v1-v17 in ws-j-design-review-v1/-v17. The revision is `git diff 6b4b7817d 1ad10b73e` (6b4b7817d is WS-C revision 18, the parent; 1ad10b73e also carries WS-C revision 19, gated separately in ws-c-addendum-review-v19, and commits the v18 verdict files). `git diff d2e47b8bd 1ad10b73e -- rfcs/257-java-upgrade-audit/ws-j-design.md` shows the design delta; section 4g lists it item by item (1-12) and rewritten normative sites are marked [v19].
The owner's rulings (rfcs/257-java-4.14.2.0-upgrade.md, "Verification and review gates" item 9): data written by pre-release Go builds (v0.1.0 included) is not supported, nothing is built to migrate, detect or refuse it; Go answers every query the target answers; evidence is red→green and Java-versus-Go, mutation only 0-3 per milestone where values could move together; one PR, pushed as it lands, CI and ACKs per workstream.
What changed (`git show --stat 1ad10b73e`): the decode rule rewritten (pkg/recordlayer/proto_closed_enums.go: javaDecodeRule for root, VT, record and partial decodes; a plan table scan for an undeclared closed-enum number, then protobuf-java's occurrence-by-occurrence merge and CheckInitialized; map entries kept by a DynamicMessage and moved whole to unknown by a generated type; legacyEnumFieldTreatedAsClosed via java feature 1001; rt.asJava on saves in store.go/store_api.go/store_batch.go; RecordMetaDataFromProto on a clone; the pending_writes_queue special case deleted); every plain proto.Unmarshal/UnmarshalVT of shared bytes under pkg/recordlayer, pkg/relational and cmd routed through it (executor continuations, rematerializeProtoScalar, cmd/frl); errors.go (MetaDataError unwraps to RecordCoreError; IsMetaDataException, OutermostJavaError, RecordCoreMessages; deserializeTemplate and frl record_count use them); pkg/recordlayer/internal/protovalue (one proto-operand reader; the planner's literalFromProtoValue deleted); values.ProtoFieldReadsValue no longer reads a declared default (measured: Java's QueryResult.fromQueriedRecord deep-copies into the plan type); metadata/proto_types.go (a vector-options field is a VECTOR); step 3: pkg/relational/core/catalog/{template_carry,index_lanes,fdb_template_catalog,template_catalog}.go, values/arithmetic_lanes.go (107 lane rows checked against Java), ddl/save_schema_template.go (the save calls only CreateTemplate), fleet/migrate.go (SaveTemplate returns the stored template; validateSchemaRebind allows index rebuilds), query/ddl/generator.go (encapsulateLane raises the target's XX000 texts). Tests: catalog/template_carry_fdb_test.go, fdb_catalog_test.go (both routes), template_restore_fdb_test.go, template_version_guard_test.go, sqldriver/carry_rule_fdb_test.go (section 4's tests 2, 3, 4, 6, 9, 10, 11), ddl_errors_probe_test.go, index_definition_production_fdb_test.go, proto_closed_enums_test.go and _bench_test.go, errors_java_class_test.go, from_proto_input_immutability_test.go, values tests, metadata/proto_types_test.go; conformance: key_validation_conformance_test.go and metadata_store_conformance.java (closed-enum records, held-record save/update/delete, the required refusal), key_expression_absent_child_conformance_test.go (fan type 7, depth 101/103), null_standin_conformance_test.go, ws_j_index_fidelity_conformance_test.go and sql_plan_steps.java (unset default, carry, lane refusal, Go-stored template planned). CHANGELOG.md, DIVERGENCES.md, TODO.md (new block at EOF "RFC-257 WS-J design v19 and step 3; WS-C revision 19", and an unmeasured source finding "The Go driver folds an unquoted ?schema= ...").
Evidence: /var/tmp/fdb-upgrade-recovery/evidence/wsj19-red/ (README and per-target TREE: the new tests on 6b4b7817d's tree in /home/birdy/projects/fdb-wsc10 with named uncommitted adapters and one sed substitution; parity 6 red of 21, oracle 15 of 15 red, catalog, sqldriver and recordlayer reds, and the tests that hold on the old tree labelled as pins), wsj19-green/ (the same parity and oracle populations on 1ad10b73e, 21 and 15 passing), wsj19-hook/ (the pre-commit hook of 1ad10b73e: 96 of 96 targets passing, 2 executed and the rest cached from an earlier run of the same inputs; index tree before the commit equals the commit's tree). The decode benchmark is /var/tmp/fdb-upgrade-recovery/scratch19/bench-decode.txt. PR #786's CI runs on the restored self-hosted runners; `gh` is not available to you. Bazel's test cache is on by the owner's direction.
Java tag 4.14.2.0 in fdb-record-layer/; the pre-upgrade Go builder and loader are at commit e48f5b496. Relational Java: fdb-record-layer/fdb-relational-core/src/main/java/com/apple/foundationdb/relational/recordlayer/{catalog/RecordLayerStoreSchemaTemplateCatalog,catalog/RecordLayerStoreCatalog,MessageTuple,metadata/RecordLayerSchemaTemplate,query/QueryPlan}.java, the DDL visitor and SemanticAnalyzer under fdb-relational-core/.../recordlayer/query/, QueryResult; core: RecordMetaDataBuilder.java, MetaDataEvolutionValidator.java, metadata/IndexComparison.java, metadata/expressions/, the Cascades arithmetic value (ArithmeticValue's operator table) and the bit/bitmap index key functions; FDBRecordStore.java; protobuf-java's parser behaviour (closed enums, recursion limit, map entries, required fields) is measured by the JVM specs named above; say where you rely on protobuf-java source instead.
Tree objects: run `git show COMMIT:path` or `git diff A B -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read commit objects, not the working tree, which moves on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline; wire compatibility with Java-written stores is the hard line.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact commit and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Read the three v18 verdicts and check that EVERY finding is resolved, answered or made moot (and say which), the v19 delta, the landed code and tests, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (every decode and save of shared bytes and whether any escapes the Java reading; the save path's move and what it changes for index entries and re-saved bytes; the carry against Java's catalog and evolution validator, concurrent saves of one name, the lane table against Java's operator table; what step 4 still owes), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.

Apply the virtual Goetz Graefe architecture lens, which includes this revision's QUERY-ENGINE changes as their mandatory Graefe review (ProtoFieldReadsValue no longer reading declared defaults and the conformance spec that measures Java's query read; the one proto-operand reader replacing the planner's literalFromProtoValue in index_predicate_to_query.go; the lane table values/arithmetic_lanes.go, which lives in the planner's values package but is read only by the catalog's lane check and the DDL generator, not by the planner's own arithmetic typing or evaluation: say whether that split is sound): Java alignment of the decode and save rule, the carry and its route, the lane check against Java's DDL and ArithmeticValue, the error classes, and whether any Go-only behaviour remains undeclared.
