Review the revised RFC-257 WS-J DESIGN (v13), and the fixes it records as LANDED (the absent-root refusal as Java's KeyExpression.DeserializationException, Build and indexToProto refusing a rootless index, HeldBy's array-element arm, the union-less seed branch deleted), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE d6ad3e9c2c5bc3a32c42cf0399c97afade52e098; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 bc8b6436980df50daf559f037a45d02397e28fd06f9948719a0683b69cb8ca11 (2875 lines). Previous: v12 TREE 97d0b9181d02696345e166b12d9ba09b18141c49 (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v12/*.txt); v1-v11 in ws-j-design-review-v1/-v11. The v12 design is at /var/tmp/fdb-upgrade-recovery/ws-j-design.v12.md; `git diff 97d0b9181d02696345e166b12d9ba09b18141c49 d6ad3e9c2c5bc3a32c42cf0399c97afade52e098 -- <path>` shows v13 (both are tree objects; the diff also carries the WS-C addendum's revision 9 and WS-E design v11, gated separately).
Delta since v12 touching WS-J (the design's Status paragraph lists it): the restore's check (iii) (EQUIVALENT or WIDENED unless the higher version raised the index's last-modified version above the lower version's metadata version) and check (i)'s own options including allowIndexRebuilds; a union both builds choose defined by the pre-upgrade loader's findUnionDescriptorName, and the ORDINARY load refusing a union whose two framings differ visibly (shape (f), a _X typed by another message; shape (g), two union fields of one type with an implicit record-type key) through LegacyFramingError, declared in section 9 (y); the as-framed union a new unreferenced message, a holder-and-retyped union refused; R1's reservation paired as the validator pairs messages and monotone, one relaxations hook for R1 and R2; CreateTemplate refusing an exact duplicate first (DUPLICATE_SCHEMA_TEMPLATE), then v' <= latest, then the relational validator, declared in 9 (w); the locked sections calling unlocked helpers (saveSchemaLocked, loadSchemaTemplateLocked, loadSchemaTemplateHeld); the WIDENED lane arm not built, a lane-table property test with step 3; the restore's refusals and R2's scoping declared in 9 (x); citations naming the function and message; the WS-J conformance root case asserting KeyExpressionDeserializationError. Code of this tree touching WS-J: pkg/recordlayer/errors.go (KeyExpressionDeserializationError), key_expression_proto.go, metadata_proto.go, metadata.go (Build's rootless refusal), the tests named in the design, conformance/ws_j_index_fidelity_conformance_test.go.
Evidence: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v13.txt, records /var/tmp/fdb-upgrade-recovery/ev13/ (driver ../ev13.sh, src.sha, check.txt, oracle-1/2.log with the WS-J Describes and the WSJ-TALLY line, jvm-wsc.log, recordlayer-v.log, justtest.log, justtest-executed.txt, final-targets.log, run.out), on /home/birdy/projects/fdb-wsc9, a frozen copy of tree 3b1587fafbd31525e918fbb44e71e394bf97c44c; the gate tree adds to it only document edits (`git diff --stat 3b1587fafbd31525e918fbb44e71e394bf97c44c d6ad3e9c2c5bc3a32c42cf0399c97afade52e098`).
NOT WS-J, same tree, own gates: WS-C section 7 addendum (revision 9, gating now), WS-E v11 (gating now), WS-D (v13 NAK, v14 being prepared), WS-F (v2 NAK, v3 being prepared).
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); the pre-upgrade Go builder and loader are at commit e48f5b496.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which has moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Read ALL three v12 verdicts and check that EVERY finding is resolved, the FULL v13 design and its diff from v12, the landed code and tests, the oracle and its evidence, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (the restore's check (iii) against every way two histories can agree in keys and differ in meaning, the ordinary load's framing refusal against every stored shape and its reach into loads that worked before, CreateTemplate's order against Java's, the lock order, R1's pairing and monotonicity, R2's scoping), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the DDL front end, IndexSpec and QuantifierValues on the translated graph, value-index expansion and the non-integer match policy, metadata assembly order, lane resolution at construction, and the evolution/carry policy for stored templates.
