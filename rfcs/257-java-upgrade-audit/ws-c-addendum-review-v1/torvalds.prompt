Review an ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together, since the change is small and already implemented in the working tree. Repo /home/birdy/projects/fdb-go. Candidate TREE 9265beef3e5c9dd9ec86caaade526c8137552976; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 7b32e4008a889f98a9b801831a2f51a66dceb72b0ef56f0d6eeb9f49d83a996c, 387 lines). WS-C itself was accepted earlier (ws-c-final-delta-review/); this review covers only the addendum.
What changed (diff the listed files against HEAD with `git diff HEAD TREE -- <path>` is NOT possible for untracked files; read them with `git show TREE:path`; backups of the pre-change versions are at /var/tmp/fdb-upgrade-recovery/online_indexer.go.pre-desired, online_indexer_queue.go.pre-desired, online_indexer_test.go.pre-desired, online_indexer_session_test.go.pre-desired, online_indexer_followup_test.go.pre-desired, index_queued_dispatch_test.go.pre-desired, so `diff` against those shows exactly the addendum): pkg/recordlayer/online_indexer.go (IndexingDesiredAction, IndexingPolicy IfDisabled/IfWriteOnly/IfReadable, GetStateDesiredAction, BuildIndex's session start, markWriteOnly's return), pkg/recordlayer/online_indexer_queue.go (prepareIndexingState ports IndexingBase.handleStateAndDoBuildIndexAsync's state transaction), the tests (Describe "index state desired action" and the spec "a finished builder's publication survives a late peer's session start" in pkg/recordlayer/online_indexer_test.go; two older specs rewritten to Java's semantics; call sites of the new signatures in online_indexer_session_test.go, online_indexer_followup_test.go, index_queued_dispatch_test.go), CHANGELOG.md (the OnlineIndexer entry under [Unreleased] Changed), TODO.md (EOF block "RFC-257 WS-C addendum"). A pure rename (clear -> toClear) was made after the mutation runs. Section 7 also records a census of every spec that reached the new skip outcome (skip-census.log, skip-census2.log) and the test setups converted to start from DISABLED or to ask for REBUILD (backups of each pre-census test file: /var/tmp/fdb-upgrade-recovery/*.pre-census, and cmd/frl write_integration_test.go.pre-census); after them: desired-fixed.log.
Evidence (/var/tmp/fdb-upgrade-recovery/): the flake that found it: wse8-proto-full-logs/pkg_recordlayer_recordlayer_test.log; the reproducer red before the fix: desired-red.log; green after: desired-green.log (18 specs); mutations: desired-mut1.log (READABLE cleared as fresh), desired-mut2.log (the continued follower term dropped), desired-mut23.log (DISABLED default made CONTINUE, combined with mut2); 40 uncached runs of the four-builder spec and the reproducer: desired-loop.log, desired-loop-logs/; just test of this tree: /var/tmp/fdb-upgrade-recovery/wse8-justtest2.log (tree 9265beef3e5c9dd9ec86caaade526c8137552976 before and after; Executed 3 out of 94 tests: 94 tests pass, the rest cached from runs of identical inputs, the recordlayer, chaos and frl targets among them from desired-fixed.log).
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/: IndexingBase.java:184-261 (handleStateAndDoBuildIndexAsync, markIndexesWriteOnly :272-283), OnlineIndexer.java:927-932 (DesiredAction), :1112-1120 (getStateDesiredAction), :1249-1253 (builder defaults), FDBRecordStore.markIndexReadable (:3914-3960).
Tree objects: run `git show TREE:path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Check the port against Java line by line (every desired action, the follower rules, continued vs fresh, the queue marking, what MARK_READABLE and a skipped session publish and clean up, heartbeats), what else in Go relied on the old clear-on-readable behaviour (every caller of BuildIndex and markWriteOnly, the fleet builder, rebuild paths, tests that pinned it), the test population and its vacuity, and the CHANGELOG claim. Distinguish measured facts from source hypotheses.
Preserved boundaries: approved Go extensions; C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Linus Torvalds code quality lens: logic holes, incomplete conversions (every caller and every test that relied on clear-on-readable), dead code, test vacuity (specs that pass without exercising a build), and maintainability.
