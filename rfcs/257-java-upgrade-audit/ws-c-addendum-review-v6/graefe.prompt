Review REVISION 6 of an ADDENDUM to the accepted RFC-257 WS-C (persistent pending writes and indexing sessions): DESIGN and IMPLEMENTATION together. Repo /home/birdy/projects/fdb-go. Candidate TREE c789832131f2e6206ee2ebad9b92a3ae463d6f6d; the addendum is section 7 of rfcs/257-java-upgrade-audit/ws-c-design.md (design SHA256 10d7b1cdd23f498b3d65bf3a6dd2eaa7cf17d22331400c423623ab0daba8bb79, 1133 lines); 7.1 to 7.6 are revisions 1 to 6, each stating what changed and why. Revision 5 was gated at tree f8abe2d742df8c35b44fb0c6f854f86c3cd3a016 and got three NAKs with low findings: read rfcs/257-java-upgrade-audit/ws-c-addendum-review-v5/{graefe,torvalds,storage}.txt (and v1-v4's) and check that EVERY finding is resolved.
What changed: `git diff f8abe2d742df8c35b44fb0c6f854f86c3cd3a016 c789832131f2e6206ee2ebad9b92a3ae463d6f6d -- <path>` (both are tree objects; the same diff carries WS-J design v10's and WS-E's changes, gated separately). WS-C files: pkg/recordlayer/index.go (Index.equalsJava, javaObjectsEqual), pkg/recordlayer/online_indexer.go (the builder's setIndexErr and SetIndex; Build's nil guard, dedup by equalsJava, stable sort before the identity check, the single mode deleted; the IndexBuildOutcomeBuilt doc), pkg/recordlayer/indexing_merger_test.go (Describe "a standalone MergeIndexes session", afterTransactHook), pkg/recordlayer/online_indexer_test.go (the target-object spec's duplicate and order cases; "combines SetIndex and AddTargetIndex as Java's builder does"; the retitled spec; the hook docs); CHANGELOG.md (the builder entry, the IndexerID wording, the MergeIndexes entry), DIVERGENCES.md (the merge bullet and the closing sentence of "OnlineIndexer session start and build catcher: where Go differs"), ws-c-design.md (7.4's spec title, 7.5's citation corrected in place, 7.6, and the combined-evidence paragraph at its end).
Evidence: /var/tmp/fdb-upgrade-recovery/wsc-v6/ (new-specs.log; focus1.log, the unmutated focus 378/378; mut-m39..m44 *.log with perl, marker count and applied diff, *.full.log; run-mutations.sh, mutate.sh; the .good copies) and /var/tmp/fdb-upgrade-recovery/ev10/ (wsc-focus.log, wsc-loop40.log, justtest.log, justtest-executed.txt, justtest-tree*.txt, src.sha over ../ev10-files.txt; drivers ../ev10a.sh, ../ev10b.sh). rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v10.txt summarises the combined run.
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/fdb-record-layer-core/src/main/java/com/apple/foundationdb/record/: provider/foundationdb/OnlineIndexer.java:409-419, :655-730, :855-890; metadata/Index.java:680-720; provider/foundationdb/IndexingBase.java:950-1000, :1085-1096; IndexingHeartbeat.java.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Check every v5 finding's resolution against Java and the code: the dedup's equality against Index.equals field by field and the HashSet's first-kept semantics, the sort's placement and stability, SetIndex/AddTargetIndex/SetTargetIndexes against Java's builder in every order and with nil, whether any caller relied on the deleted single mode, the four MergeIndexes specs (can any pass vacuously; the afterTransactHook's observation point), the declared divergence's completeness and truth, the mutation logs, and the CHANGELOG. Distinguish measured facts from source hypotheses.
Preserved boundaries: approved Go extensions; C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the build session as a whole (launcher/catcher, session start, per-transaction state checks, publication), phase boundaries, and whether any Go-only behaviour remains undeclared.
