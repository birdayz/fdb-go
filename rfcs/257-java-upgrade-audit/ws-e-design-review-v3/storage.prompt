Reconfirm the revised RFC-257 WS-E DESIGN (v3), not implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 204151ab1e3aafa12c1b0202bc6c76c5f45c6c09; design rfcs/257-java-upgrade-audit/ws-e-design.md SHA256 cbb0316f31798de432948a57aef6379728854e31b3d9d3fe2f7a43a7e1eb65d1 (963 lines). Previous: v2 TREE cec59465947ae903c45373d8935f91aab96628e0 (three NAKs, rfcs/257-java-upgrade-audit/ws-e-design-review-v2/*.txt); v1 in ws-e-design-review-v1.
Delta since v2 touching WS-E, all TEST-ONLY outside rfcs/ and TODO.md: (1) a third oracle round, Describe "WS-E target oracle v3" in conformance/ws_e_probe_conformance_test.go, 46 pinned target outcomes (nullability of all-NOT-NULL calls; COALESCE(TRUE, 1/0 = 1) in WHERE and SELECT; IN and array NULL timing over an empty table; GREATEST/LEAST over negative doubles/floats/longs; array comparison operands; DRY_RUN and ISOLATION_LEVEL_SNAPSHOT as CONNECTION options over DML, DDL, SHOW and EXPLAIN INSERT; positional parameter order in UPDATE, INSERT ... SELECT, HAVING, IN lists, subqueries and SELECT; too few/too many parameters; FLOAT/DOUBLE/INT bindings and DOUBLE/FLOAT literals into FLOAT/BIGINT columns; an untyped bound NULL projected, in arithmetic and inserted), with a v3 pin mutation; (2) the first Describe re-rendered with result types, nullability and exact integers (74 pins re-generated; the diff of every Java outcome against the v2 pins was formatting only: nil->NULL, exact integer), its Go side binding an int kind as int32, and a probe-set = pin-set check; (3) the Java harness: bindExtended gains a "float" kind and runPreparedWithSetup now calls bindExtended instead of duplicating it (conformance/sql_plan_steps.java); (4) a v2-extended pin mutation; (5) evidence rebound: ws-e-oracle/evidence-run.txt regenerated from two uncached runs of all four Describes (per-probe blocks compared as sorted sets because Ginkgo randomizes top-level Describe order), with source hashes, evidence-mutations.txt extended; (6) cascades.md:60 corrected; TODO.md gains an appended block booking the snapshot continuation contract on CQ-78/RFC-203. NOT WS-E, same tree, own gates: RFC-257 WS-D (design v10 under review; heartbeat key-parse production fix) and WS-J (literal-carrier production fix in generator.go/walk.go/long_arithmetic_function.go, its tests, the WS-J oracle v2, a WS-J spec measuring long arithmetic key functions over DOUBLE/FLOAT operands); fromless_select_java_probe_test.go renders columns+rows only (its pins predate RowSet.Nullability). Out of scope except where they touch WS-E claims.
Frozen-tree run records: both conformance targets run uncached on this tree: //conformance:rfc257_oracle_test Ran 23 of 23 Specs, 23 Passed; //conformance:conformance_test Ran 1405 of 1524, 1405 Passed, 119 Skipped (inherited from master, TODO.md) (/var/tmp/fdb-upgrade-recovery/rfc257-wse3.log and bazel-testlogs). The last full `just test` (/var/tmp/fdb-upgrade-recovery/just-test-v10b.log, 94/94) predates the WS-E test-only edits above.
Tree objects: run `git show TREE:path` and `git diff OLD NEW` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied). Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Read ALL three v2 verdicts, the FULL v3 design and its diff from v2, the oracle source and evidence, and check claims against actual Java and current Go. Challenge sufficiency and interactions, not only whether requested words appear. Distinguish measured target facts from SOURCE claims.
Preserved boundaries: the owner-approved Go read/raw nullable-array, scalar and sorting extensions; the Go-only IN-subquery read extension; LIMIT (approved Go extension); C++ 7.3.77; format max 15/default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Apply the independent storage/wire correctness review lens: bytes a bound value stores, index keys, isolation and read-conflict semantics, DRY_RUN and snapshot admission, continuation ownership, and write divergences.
