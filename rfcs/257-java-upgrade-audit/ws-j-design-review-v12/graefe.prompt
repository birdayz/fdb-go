Review the revised RFC-257 WS-J DESIGN (v12), and the fixes it records as LANDED (sections 3.1, 3.2, 4b, 4c's validation, load order and shape-(d) refusal including the catalog's store and load refusals, the loader's value_expression, added_version, subspace-key, option-order and absent-root readings, HeldBy, the section-0 instrument), not the rest of the implementation. Repo /home/birdy/projects/fdb-go. Candidate TREE 97d0b9181d02696345e166b12d9ba09b18141c49; design rfcs/257-java-upgrade-audit/ws-j-design.md SHA256 08b7a4e968f2908a700744c1a438fd17f0eaafc1d8886d97803a951eae61ef9c (2721 lines). Previous: v11 TREE aa29308afd7637d7e98a42ef1d108335013ead8c (three NAKs, rfcs/257-java-upgrade-audit/ws-j-design-review-v11/*.txt); v1-v10 in ws-j-design-review-v1/-v10. The v11 design is at /var/tmp/fdb-upgrade-recovery/ws-j-design.v11.md; `git diff aa29308afd7637d7e98a42ef1d108335013ead8c 97d0b9181d02696345e166b12d9ba09b18141c49 -- <path>` shows v12 (both are tree objects; the diff also carries the WS-C addendum's revision 8, gated separately).
Delta since v11 touching WS-J: (1) CODE AND TESTS: an index stored with no root is refused as Java's Index(proto) refuses it (pkg/recordlayer/metadata_proto.go, key_expression_proto.go; TestStoredIndexSubspaceKeyIsReadAsJavaReadsIt and the WSJIXK spec's fifth shape); legacyUnionHolder finds HeldBy by reachability from the union found (metadata_proto.go; the WSJTT spec's new shape "struct nested in a struct" and HeldBy's value asserted per shape; TestStoredMetaDataWithAnAmbiguousLegacyUnionIsRefused asserting HeldBy per shape, a nested-holder shape added). (2) DESIGN v12 (its Status paragraph lists the changes): the restore's carry compatibility (the evolution validator with the rebind options, no rename both ways, every index field EQUIVALENT at an equal last-modified version) and a fresh t's whole-prefix range (section 2); CreateTemplate refusing v′ <= latest and running the relational validator, the build-path-only write entry and RestoreTemplateVersion named (2, 3.2's table, 4, 8); the in-memory SaveSchema/RepairSchema holding the store mutex across template reads, with -race tests (2); the lane check over WIDENED only for a lane the widening removes, and (m) (3.2, 4, 9); the edited-file route: the as-framed stored union handed to the validator, the framing compared at (iii) and in the migration's output check, the direction for shape (b) and for a union both builds chose, R1 scoped to compared messages and refused beside a predicate or count key through the removed field, the reservation enforced on later saves, R2's rebuild admission scoped, the tests (4c, 8, 9 (p)); the carried route's (d) remedy as a template being stored (4); HeldBy by reachability (4c); stale validator citations re-taken. (3) DIVERGENCES.md (a PENDING entry for the reserved-number refusal), TODO.md (a v12 merge-unit block at the end), the umbrella's gate 8 wording, ws-j-oracle/evidence-v12.txt.
Evidence: rfcs/257-java-upgrade-audit/ws-j-oracle/evidence-v12.txt. Run records under /var/tmp/fdb-upgrade-recovery/: ev12/ (wsj-ev1.log, wsj-ev2.log, jvm-md.log, wsc-focus.log, justtest.log, justtest-executed.txt, justtest-tree*.txt, metadata-test-v.log, final-targets.log, src.sha, check.txt, run.out), ev12.sh, ev12-files.txt; wsc-v8/ (the WS-C mutations, jvm-wsjixk.log, jvm-wsjtt.log).
NOT WS-J, same tree, own gates: WS-C section 7 addendum (revision 8, gating now), WS-E v10 (NAK, being revised), WS-D v13 (NAK), WS-F v2 (NAK).
Java tag 4.14.2.0 at fdacd162a9c8acfadc49082b89185c823ab8ae4a in fdb-record-layer/ (read fdb-record-layer/.git/HEAD if rev-parse is denied); the pre-upgrade Go builder and loader are at commit e48f5b496.
Tree objects: run `git show TREE:path` or `git diff TREE1 TREE2 -- path` from the repo root (the read-only policy admits commands that START with the git subcommand, or `git -C /home/birdy/projects/fdb-go ...`; avoid shell variables and `cd ... &&` chains). Read tree objects, not the working tree, which may have moved on. Logs under /var/tmp/fdb-upgrade-recovery/ may be read with the Read tool.
Preserved boundaries: approved Go raw/nullable-array/scalar/sorting extensions; the Go IN-subquery read extension; LIMIT for queries; the materialized outer-join NLJ (RFC-152); C++ 7.3.77; format max 15 / default 14; no second query pipeline.
Actual reviewer gate run via `claude -p` (model opus, effort xhigh) under a read-only tool policy (Read/Grep/Glob plus read-only git/grep/sed Bash; no Edit/Write). The owner authorized this substitution for the exhausted Codex account. No tests/probes/mutations/edits/staging/commits/push/CI/PR/publication/operational changes. Return ACK or NAK, the exact tree and design hash, and actionable findings with source line references ordered by severity. Incomplete review is not ACK. Under 900 words unless needed.

Read ALL three v11 verdicts and check that EVERY finding is resolved, the FULL v12 design and its diff from v11, the landed code and tests, the oracle and its evidence, and check claims against actual Java, the pre-upgrade Go at e48f5b496, and current Go. Challenge sufficiency and interactions (the restore's carry compatibility against every way two histories can agree in keys and differ in meaning, CreateTemplate's checks against every writer, the lock order, the WIDENED lane rule, the as-framed union against every (a)-(d) shape and holder and both directions, R1's reach and the reservation's enforcement, R2's scoping, the remedy marking), not only whether the requested words appear. Distinguish measured facts from remaining source hypotheses.

Apply the virtual Goetz Graefe architecture lens: Java alignment of the DDL front end, IndexSpec and QuantifierValues on the translated graph, value-index expansion (section 3.5) and the non-integer match policy, metadata assembly order, lane resolution at construction, and the evolution/carry policy for stored templates.
